/* ============================================================================
   INTERVIEW 17.9 — Memory Management & the PVM
   ----------------------------------------------------------------------------
   Theory interview questions: the ones you answer out loud. Anything that
   asks for a program lives in the Coding Practice course instead.
   Generated by .build/import-interview.py — edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "17.9",
 "lede": "**32 interview questions on memory management & the pvm**, with the answers folded away. Say your answer out loud first — recognising an answer and being able to give one are different skills, and only the second survives a follow-up question.",
 "objectives": [
  "Answer 32 questions on memory management & the pvm without prompting",
  "State the trade-off behind each answer, not only the definition",
  "Recognise the follow-up each question is setting up",
  "Notice which answers you can recognise but not produce"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer out loud before revealing.** An answer you can only recognise is one you cannot give under pressure.",
      "**Say the trade-off, not just the definition.** Interviewers are listening for judgement, and the follow-up question is where it shows.",
      "Coding questions live in the **Coding Practice** course — these are the ones you answer in conversation."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "Is Python compiled or interpreted?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "Both. CPython first **compiles** source (`.py`) into **bytecode** (`.pyc`), then the **PVM (Python Virtual Machine) interprets** that bytecode instruction by instruction. So it is most accurately called \"compiled-then-interpreted.\" The compile step is hidden and produces bytecode — an intermediate representation, *not* native machine code — which is why Python is popularly described as \"interpreted.\""
    },
    {
     "t": "code",
     "lang": "text",
     "code": ".py  ──[COMPILER]──►  bytecode (.pyc)  ──[PVM / INTERPRETER]──►  output",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** There is a real compiler inside CPython; it just targets bytecode, not the CPU."
    }
   ],
   "terms": [
    "Answer",
    "compiles",
    "bytecode",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "2",
   "q": "Walk through the full lifecycle of running `python hello.py`.",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "ol",
     "items": [
      "OS launches the CPython interpreter",
      "CPython reads hello.py as text",
      "Lexer/tokenizer → stream of tokens",
      "Parser → Abstract Syntax Tree (AST)",
      "Compiler → bytecode (code objects)",
      "Bytecode cached → __pycache__/hello.cpython-3XX.pyc",
      "PVM loads the bytecode into a frame object",
      "PVM evaluation loop executes opcodes one by one",
      "Each opcode manipulates the value stack & namespaces",
      "PVM exits; the OS reclaims process memory"
     ]
    },
    {
     "t": "code",
     "lang": "python",
     "code": "import ast, dis\ntree = ast.parse(\"x = 1 + 2\")      # steps 3-4\ncode = compile(tree, \"<s>\", \"exec\") # step 5\ndis.dis(code)                        # inspect the bytecode",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Source → tokens → AST → bytecode → PVM execution."
    }
   ],
   "terms": [
    "Answer",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "3",
   "q": "What is the PVM and why is it \"stack-based\"?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "The Python Virtual Machine is CPython's runtime engine (implemented in C, in `ceval.c`). It is a **stack-based** interpreter: opcodes push operands onto a value stack and pop their results back. For example `a + b` compiles to `LOAD_FAST a; LOAD_FAST b; BINARY_OP +` — two pushes, then an op that pops two and pushes one. \"Stack-based\" (vs register-based) means operands live on an operand stack rather than in named registers."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** The PVM is a stack machine that walks bytecode and shuffles values on a value stack."
    }
   ],
   "terms": [
    "Answer",
    "stack-based",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "4",
   "q": "What is a `.pyc` file and when is it reused?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "It is the **compiled bytecode** cached under `__pycache__/`. Python writes it the first time a module is *imported*. On later imports it compares the `.pyc`'s stored timestamp and source size against the `.py`; if they match, it loads the `.pyc` directly and skips compilation, speeding up startup."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "import py_compile\npy_compile.compile(\"module.py\")   # → __pycache__/module.cpython-3XX.pyc",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** `.pyc` caches bytecode and is reused while the source is unchanged. The top-level script you run directly is not cached, only imported modules."
    }
   ],
   "terms": [
    "Answer",
    "compiled bytecode",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "5",
   "q": "What is a frame object?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "Every function call creates a **frame object** pushed onto the call stack. It holds that call's local variables, the current instruction pointer, the value stack, and `f_back` (a link to the caller's frame). When the call returns, its frame is popped."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "import sys\ndef inner():\n    f = sys._getframe()\n    print(f.f_code.co_name, f.f_back.f_code.co_name)  # 'inner' 'outer'\ndef outer():\n    inner()\nouter()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Frames are the runtime records that make the call stack and tracebacks possible."
    }
   ],
   "terms": [
    "Answer",
    "frame object",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "6",
   "q": "What is reference counting?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "CPython's primary memory-management mechanism. Every object tracks how many references point to it; when the count drops to **zero**, the object is freed **immediately and deterministically**. It is simple and prompt but **cannot reclaim reference cycles** on its own."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "import sys\na = [1, 2, 3]\nprint(sys.getrefcount(a))   # 2 — includes getrefcount's own temporary ref",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Refcounting is immediate and deterministic but blind to cycles."
    }
   ],
   "terms": [
    "Answer",
    "zero",
    "immediately and deterministically",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "7",
   "q": "Why is `sys.getrefcount(x)` always one higher than expected?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "Passing `x` into `getrefcount()` creates a temporary argument reference for the duration of the call, so the reported count includes that extra +1."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "import sys\nx = object()\nprint(sys.getrefcount(x))   # 2: the name 'x' plus the function's argument",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Subtract 1 to get the \"real\" count outside the call."
    }
   ],
   "terms": [
    "Answer",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "8",
   "q": "What is a circular reference and how does Python handle it?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "A cycle is when objects reference each other (`a.ref = b; b.ref = a`). Their refcounts never reach zero even after all external names are deleted, so refcounting alone leaks them. CPython's **generational garbage collector** periodically scans tracked container objects, detects groups reachable only from within the cycle, and reclaims them."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "import gc\nclass N:\n    def __init__(self): self.ref = None\na = N(); b = N(); a.ref = b; b.ref = a\ndel a, b\nprint(gc.collect())   # ≥ 2 — the cycle is collected",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** The cyclic GC is the safety net refcounting cannot provide."
    }
   ],
   "terms": [
    "Answer",
    "generational garbage collector",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "9",
   "q": "Explain the generations in Python's garbage collector.",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "There are three generations (0, 1, 2). New objects start in **Gen 0**, which is collected most frequently (default threshold ~700 allocations). Objects that survive a collection are **promoted** to the next generation, which is scanned less often. This rests on the **generational hypothesis**: most objects die young, so focusing effort on young objects catches most garbage cheaply."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "import gc\nprint(gc.get_threshold())   # (700, 10, 10)\nprint(gc.get_count())       # current per-generation counts",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Young objects are checked often; survivors graduate and are checked rarely."
    }
   ],
   "terms": [
    "Answer",
    "Gen 0",
    "promoted",
    "generational hypothesis",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "10",
   "q": "How does the GC's cycle-detection algorithm work?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "For the target generation it copies each object's refcount, then subtracts the references that come from *other objects within the set*. Any object whose adjusted count is zero is reachable only from inside the group (i.e. only via the cycle), so it is unreachable from the outside and can be collected. Survivors are promoted."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** It distinguishes \"referenced from outside\" from \"referenced only by the cycle\" using effective (subtracted) refcounts."
    }
   ],
   "terms": [
    "Answer",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "11",
   "q": "When would you disable the garbage collector?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "ul",
     "items": [
      "**Bulk loading with no cycles**refcounting suffices, and skipping GC scans is faster.",
      "**After `fork()`** in pre-fork servers — GC touching refcounts triggers copy-on-write page faults, bloating memory (Instagram's famous tweak).",
      "**Latency-critical paths** where GC pauses are unacceptable."
     ]
    },
    {
     "t": "p",
     "text": "Always re-enable and `gc.collect()` afterward."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "import gc\ngc.disable()\ntry:\n    data = list(range(10_000_000))   # no cycles\nfinally:\n    gc.enable(); gc.collect()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Disable only when you know there are no cycles or you need to avoid GC pauses/COW faults."
    }
   ],
   "terms": [
    "Answer",
    "Bulk loading with no cycles",
    "After fork()",
    "Latency-critical paths",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "12",
   "q": "What is the difference between `del x` and garbage collection?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "`del x` only **removes the name binding**, decrementing the object's refcount by 1. The object is destroyed *only if* that brings the count to zero. **Garbage collection** is the separate, automatic process that finds and frees unreachable objects — especially reference cycles that refcounting misses."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** `del` unbinds a name; GC reclaims unreachable objects."
    }
   ],
   "terms": [
    "Answer",
    "removes the name binding",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "13",
   "q": "What is `__del__` and why is it discouraged for cleanup?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "`__del__` is a finalizer called when an object is about to be destroyed. Avoid relying on it because: (1) timing is **non-deterministic** — you can't predict *when* it runs; (2) exceptions raised inside it are silently ignored; (3) before Python 3.4 it made cyclic objects uncollectable. Use **context managers** (`__enter__`/`__exit__`) or `contextlib`/`weakref.finalize` for deterministic cleanup."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "class GoodFile:\n    def __enter__(self): self.f = open(\"x\"); return self.f\n    def __exit__(self, *exc): self.f.close()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Prefer `with` blocks; `__del__` cleanup is unreliable."
    }
   ],
   "terms": [
    "Answer",
    "non-deterministic",
    "context managers",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "14",
   "q": "What are weak references and when do you use them?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "A weak reference (`weakref.ref`) points at an object **without** incrementing its refcount, so it doesn't keep the object alive; calling it returns the object or `None` once collected. Use cases: **caches** (`WeakValueDictionary` auto-evicts collected values), **observer patterns** (don't pin observers in memory), and **child→parent back-references** that shouldn't extend the parent's lifetime."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "import weakref\nclass C: pass\no = C(); w = weakref.ref(o)\ndel o\nprint(w())   # None",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Weakrefs let you reference objects without preventing their collection."
    }
   ],
   "terms": [
    "Answer",
    "without",
    "caches",
    "observer patterns",
    "child→parent back-references",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "15",
   "q": "What does `__slots__` do and what are its trade-offs?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "`__slots__` replaces the per-instance `__dict__` with a fixed C-level layout, cutting memory ~30–50% and slightly speeding attribute access. Trade-offs: you **cannot add attributes** not listed in slots, and subclasses must redeclare `__slots__` (with only *new* names) or they regain `__dict__` and lose the benefit."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "class P:\n    __slots__ = (\"x\", \"y\")\np = P(); p.x = 1\n# p.z = 2  # AttributeError\nprint(hasattr(p, \"__dict__\"))  # False",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** `__slots__` saves memory for many small fixed-shape objects, at the cost of dynamic attributes."
    }
   ],
   "terms": [
    "Answer",
    "cannot add attributes",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "16",
   "q": "Explain small-integer caching and string interning.",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "CPython pre-allocates the integers **−5..256** as singletons, so `256 is 256` is `True` but `257 is 257` may be `False`. Identifier-like string literals are **interned** at compile time (deduplicated to a single object); strings with spaces/punctuation usually aren't, but `sys.intern()` forces it."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "a = 256; b = 256; print(a is b)   # True\na = 257; b = 257; print(a is b)   # False (in a script)\nimport sys\nprint(sys.intern(\"a b\") is sys.intern(\"a b\"))  # True",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Caching/interning are optimizations — never use `is` to compare values; use `==`."
    }
   ],
   "terms": [
    "Answer",
    "−5..256",
    "interned",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "17",
   "q": "How does CPython's memory allocator (pymalloc) work?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "For objects ≤ 512 bytes CPython uses **pymalloc**, organized as **arenas (256 KB) → pools (4 KB pages, one size class) → blocks (the object slots)**. Larger objects go to the raw C allocator. This batches small allocations and reduces syscalls/fragmentation, but freed memory is often kept in pools for reuse rather than returned to the OS — so process RSS can stay high after a memory spike."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Small objects are pooled by pymalloc; \"memory not dropping after `del`\" is normal."
    }
   ],
   "terms": [
    "Answer",
    "pymalloc",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "18",
   "q": "Why doesn't Python compile straight to machine code like C?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "Python trades raw speed for flexibility: (1) **portability** — bytecode runs on any platform with CPython; (2) **dynamic typing** — types resolve at runtime, making ahead-of-time native compilation hard; (3) **introspection** — `eval`, `exec`, `getattr`, monkey-patching need a runtime interpreter. Tools like **PyPy** (JIT), **Cython**, and **Numba** do compile to native code for hotspots when needed."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Bytecode + interpreter buys portability and dynamism; native compilers exist for hotspots."
    }
   ],
   "terms": [
    "Answer",
    "portability",
    "dynamic typing",
    "introspection",
    "PyPy",
    "Cython"
   ]
  },
  {
   "t": "drill",
   "n": "19",
   "q": "What is the GIL and how does it relate to memory management?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "The **Global Interpreter Lock** ensures only one thread executes Python bytecode at a time. A major reason it exists is to make **reference-count updates thread-safe** without per-object locking. The side effect is that CPU-bound threads don't run in parallel — use `multiprocessing`, async, or native extensions that release the GIL."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** The GIL keeps refcounting safe and simple but serializes bytecode execution."
    }
   ],
   "terms": [
    "Answer",
    "Global Interpreter Lock",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "20",
   "q": "How do you measure the memory size of a Python object?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "`sys.getsizeof(obj)` returns the object's own size, but **not** what it references — a list's size doesn't include its elements. For deep size, recurse into containers; for live tracking use `tracemalloc`."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "import sys\nprint(sys.getsizeof([]))        # 56  (empty list overhead)\nprint(sys.getsizeof([1, 2, 3])) # 88  (overhead + 3 pointers, not the ints)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** `getsizeof` is shallow; walk containers (or use `tracemalloc`) for true totals."
    }
   ],
   "terms": [
    "Answer",
    "not",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "21",
   "q": "Tuple vs list — why is a tuple smaller and is it faster?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "A list **over-allocates** spare capacity so `append` is amortized O(1), and stores a separate growth bookkeeping field; a tuple is fixed-size and allocates exactly what it needs. So tuples use less memory and construct slightly faster, and being immutable they can be cached/interned and used as dict keys."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "import sys\nprint(sys.getsizeof((1, 2, 3)) < sys.getsizeof([1, 2, 3]))  # True",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Use tuples for fixed, hashable records to save memory; lists when you need mutation."
    }
   ],
   "terms": [
    "Answer",
    "over-allocates",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "22",
   "q": "Why is a generator more memory-efficient than a list comprehension?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "A list comprehension materializes **all** elements at once — O(n) memory. A generator expression yields items lazily, holding only its current state — O(1) memory regardless of length. For one-pass consumers like `sum()` the result is identical but the generator never builds the intermediate list."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "import sys\nprint(sys.getsizeof((x for x in range(10000))))   # ~200 bytes\nprint(sys.getsizeof([x for x in range(10000)]))   # tens of KB",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Generators stream; list comps store. Prefer generators for large or one-pass data."
    }
   ],
   "terms": [
    "Answer",
    "all",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "23",
   "q": "Why is `''.join(...)` preferred over `+=` for building strings?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "Strings are **immutable**, so `s += part` allocates a brand-new string and copies everything each iteration — O(n²) overall. `''.join(parts)` computes the final size once and copies each piece exactly once — O(n). `io.StringIO` is another O(n) option."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "s = \"\".join(str(i) for i in range(1000))   # O(n)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Accumulate into a list (or StringIO) and join once; never `+=` in a hot loop."
    }
   ],
   "terms": [
    "Answer",
    "immutable",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "24",
   "q": "Why is local variable access faster than global access?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "Locals are stored in a per-frame array and accessed by index via `LOAD_FAST` (O(1), no hashing). Globals and builtins are looked up by name in dictionaries via `LOAD_GLOBAL`. In tight loops, binding a global or builtin to a local (`_len = len`) is a real micro-optimization."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "import dis\ndef f():\n    x = 1\n    return x        # LOAD_FAST\ndis.dis(f)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** `LOAD_FAST` (array index) beats `LOAD_GLOBAL` (dict lookup)."
    }
   ],
   "terms": [
    "Answer",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "25",
   "q": "How do you find and debug a memory leak in Python?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "code",
     "lang": "python",
     "code": "# 1. tracemalloc — top allocators by line\nimport tracemalloc\ntracemalloc.start()\n# ... run code ...\nfor s in tracemalloc.take_snapshot().statistics(\"lineno\")[:10]:\n    print(s)\n\n# 2. gc — uncollectable cycles\nimport gc\ngc.set_debug(gc.DEBUG_LEAK)\ngc.collect()\nprint(gc.garbage)\n\n# 3. objgraph — what type keeps growing across iterations\nimport objgraph\nobjgraph.show_growth()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** `tracemalloc` localizes allocations, `gc.garbage` exposes uncollectable cycles, `objgraph` traces reference chains."
    }
   ],
   "terms": [
    "Answer",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "26",
   "q": "How do you profile CPU performance, and what's the golden rule?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "The rule: **profile first, optimize the measured hotspot second** — never guess. Use `timeit` for micro-benchmarks, `cProfile` for function-level CPU profiling, and `line_profiler` to drill into a specific function line by line."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "import timeit, cProfile, pstats\ntimeit.timeit(\"sum(range(1000))\", number=10_000)\ncProfile.run(\"work()\", \"out\")\npstats.Stats(\"out\").sort_stats(\"cumulative\").print_stats(10)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Measure with the right tool before changing anything."
    }
   ],
   "terms": [
    "Answer",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "27",
   "q": "What are `lru_cache` / memoization and when do they help?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "`functools.lru_cache` stores recent results keyed by arguments, so repeated calls with the same inputs are O(1) lookups. It helps for **pure** functions with expensive, repeated computations (recursion, parsing). Arguments must be hashable, and a bounded `maxsize` evicts the least-recently-used entries."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "from functools import lru_cache\n@lru_cache(maxsize=None)\ndef fib(n): return n if n < 2 else fib(n-1) + fib(n-2)\nfib(50); print(fib.cache_info())   # hits/misses/size",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Cache pure, repeatedly-called functions; mind hashability and cache size."
    }
   ],
   "terms": [
    "Answer",
    "pure",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "28",
   "q": "Compare CPython, PyPy, Jython, and IronPython.",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "table",
     "head": [
      "Implementation",
      "Host",
      "Memory strategy",
      "Notes"
     ],
     "rows": [
      [
       "**CPython**",
       "C",
       "Refcount + generational GC",
       "The reference impl"
      ],
      [
       "**PyPy**",
       "RPython",
       "Tracing GC (no refcount)",
       "JIT, 2–10× faster"
      ],
      [
       "**Jython**",
       "JVM",
       "JVM garbage collector",
       "Java interop"
      ],
      [
       "**IronPython**",
       ".NET",
       "CLR garbage collector",
       ".NET interop"
      ]
     ]
    },
    {
     "t": "p",
     "text": "PyPy drops refcounting for a tracing GC and adds a JIT, so identity timing and `__del__` behavior differ from CPython."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Only CPython uses refcounting; the others inherit their host runtime's GC."
    }
   ],
   "terms": [
    "Answer",
    "CPython",
    "PyPy",
    "Jython",
    "IronPython",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "29",
   "q": "After deleting a big list, why doesn't my process memory shrink?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "Two reasons: (1) pymalloc keeps freed pools around to satisfy future allocations rather than returning them to the OS immediately; and (2) the OS may not reclaim process RSS even after `free()`. The memory is available for reuse inside the process. If you truly need to release it, isolate the work in a **separate process** (e.g. `multiprocessing`) so its memory is reclaimed on exit."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Freed Python memory is recycled internally, not always returned to the OS; use a subprocess for hard reclamation."
    }
   ],
   "terms": [
    "Answer",
    "separate process",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "30",
   "q": "How does `weakref.WeakValueDictionary` differ from a normal dict cache?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "A normal dict holds a **strong** reference to each value, so cached objects live as long as the cache does — a common leak. A `WeakValueDictionary` holds **weak** references; once no strong references remain elsewhere, the value is collected and its entry disappears automatically."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "import weakref\nclass R: pass\ncache = weakref.WeakValueDictionary()\nr = R(); cache[\"k\"] = r\ndel r\nprint(list(cache))   # [] — auto-evicted",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Weak-value caches self-clean, preventing the \"cache keeps everything alive\" leak."
    }
   ],
   "terms": [
    "Answer",
    "strong",
    "weak",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "31",
   "q": "What is the difference between shallow and deep copy, memory-wise?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "`copy.copy` makes a **shallow** copy — a new outer container that **shares** the inner objects. `copy.deepcopy` recursively duplicates everything into a fully independent clone (more memory, handles cycles). Mutating a nested element of a shallow copy affects the original."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "import copy\norig = [[1, 2]]\nsh = copy.copy(orig); dp = copy.deepcopy(orig)\norig[0].append(99)\nprint(sh)  # [[1, 2, 99]]  shared\nprint(dp)  # [[1, 2]]      independent",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Shallow copies share inner objects (cheap); deep copies fully duplicate (costly, independent)."
    }
   ],
   "terms": [
    "Answer",
    "shallow",
    "shares",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "32",
   "q": "What practical techniques reduce a Python program's memory footprint?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "ol",
     "items": [
      "`__slots__` / `@dataclass(slots=True)` for many small fixed-shape objects.",
      "Generators and `range` instead of building large lists.",
      "`array.array` or NumPy for homogeneous numbers.",
      "`set`/`dict` for membership; `deque` for queues.",
      "Sparse storage for mostly-empty structures; flyweight/interning for duplicates.",
      "Process data in **chunks**; `del` large temporaries and call `gc.collect()` if needed.",
      "Offload heavy short-lived work to a subprocess so its memory is reclaimed on exit."
     ]
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Choose the right data structure, stream instead of materialize, and bound object overhead with slots/arrays."
    }
   ],
   "terms": [
    "Answer",
    "chunks",
    "Key takeaway"
   ]
  }
 ],
 "takeaways": []
});
