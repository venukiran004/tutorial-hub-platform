/* ============================================================================
   INTERVIEW 17.23 — Pydantic
   ----------------------------------------------------------------------------
   Theory interview questions: the ones you answer out loud. Anything that
   asks for a program lives in the Coding Practice course instead.
   Generated by .build/import-interview.py — edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "17.23",
 "lede": "**20 interview questions on pydantic**, with the answers folded away. Say your answer out loud first — recognising an answer and being able to give one are different skills, and only the second survives a follow-up question.",
 "objectives": [
  "Answer 20 questions on pydantic without prompting",
  "State the trade-off behind each answer, not only the definition",
  "Recognise the follow-up each question is setting up",
  "Notice which answers you can recognise but not produce"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer out loud before revealing.** An answer you can only recognise is one you cannot give under pressure.",
      "**Say the trade-off, not just the definition.** Interviewers are listening for judgement, and the follow-up question is where it shows.",
      "Coding questions live in the **Coding Practice** course — these are the ones you answer in conversation."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "Q1. What is Pydantic and what problem does it solve?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "Pydantic is a data validation and serialization library that uses Python type hints to define schemas. At construction time it validates and coerces input, raising a structured `ValidationError` for bad data; it also serializes models to dicts/JSON and generates JSON Schema."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "from pydantic import BaseModel, Field\n\nclass Item(BaseModel):\n    name: str\n    price: float = Field(gt=0)\n\nItem(name=\"x\", price=5)        # ok\n# Item(name=\"x\", price=-1)     # ValidationError: price must be > 0",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Pydantic turns type hints into enforced runtime contracts — validation, parsing, and serialization from one declaration."
    }
   ],
   "terms": [
    "Answer",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "2",
   "q": "Q2. What changed from Pydantic v1 to v2, and why?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "v2 rewrote the validation core in Rust (`pydantic-core`), giving ~5–50x speedups. Key API changes:"
    },
    {
     "t": "table",
     "head": [
      "v1",
      "v2"
     ],
     "rows": [
      [
       "`@validator`",
       "`@field_validator` (+ `@classmethod`)"
      ],
      [
       "`@root_validator`",
       "`@model_validator(mode=...)`"
      ],
      [
       "inner `class Config`",
       "`model_config = ConfigDict(...)`"
      ],
      [
       "`.dict()` / `.json()`",
       "`.model_dump()` / `.model_dump_json()`"
      ],
      [
       "`.parse_obj()` / `.parse_raw()`",
       "`.model_validate()` / `.model_validate_json()`"
      ],
      [
       "`orm_mode = True`",
       "`from_attributes=True`"
      ],
      [
       "`regex=`",
       "`pattern=`"
      ]
     ]
    },
    {
     "t": "p",
     "text": "v2 also added strict mode, `computed_field`, annotated validators, and `Field(discriminator=...)`."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** v2 is faster and stricter; the renames are mechanical but pervasive — know `model_dump`, `model_validate`, `field_validator`, `model_config`."
    }
   ],
   "terms": [
    "Answer",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "3",
   "q": "Q3. What is the difference between `field_validator` and `model_validator`?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "ul",
     "items": [
      "`@field_validator(\"x\")` validates/transforms a single field (or several named ones). It is a `@classmethod`.",
      "`@model_validator` validates the whole model — used for **cross-field** rules and reshaping the input."
     ]
    },
    {
     "t": "code",
     "lang": "python",
     "code": "@field_validator(\"batch_size\")\n@classmethod\ndef power_of_two(cls, v): ...\n\n@model_validator(mode=\"after\")\ndef warmup_lt_total(self):\n    if self.warmup_steps >= self.total_steps:\n        raise ValueError(\"warmup_steps must be < total_steps\")\n    return self",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Use `field_validator` for per-field rules, `model_validator` when a rule spans multiple fields."
    }
   ],
   "terms": [
    "Answer",
    "cross-field",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "4",
   "q": "Q4. What is the difference between validator `mode=\"before\"` and `mode=\"after\"`?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "ul",
     "items": [
      "**before** runs on the **raw input** before type coercion — use to normalize/reshape (e.g., parse a CSV string into a list, remap camelCase keys).",
      "**after** (default) runs on the **already-coerced, type-correct** value — use to enforce business rules."
     ]
    },
    {
     "t": "code",
     "lang": "python",
     "code": "@field_validator(\"gpus\", mode=\"before\")\n@classmethod\ndef parse(cls, v):\n    return [int(x) for x in v.split(\",\")] if isinstance(v, str) else v",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** \"before\" sees messy input, \"after\" sees clean typed data."
    }
   ],
   "terms": [
    "Answer",
    "before",
    "raw input",
    "after",
    "already-coerced, type-correct",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "5",
   "q": "Q5. What does `Field()` do? Give common constraints.",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "`Field()` attaches constraints, defaults, aliases, and documentation to a field."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "name: str = Field(min_length=1, max_length=100, pattern=r\"^[a-z_]+$\")\ndim: int = Field(ge=1, le=4096, alias=\"dimension\")\ntags: list[str] = Field(default_factory=list)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "Common constraints: `gt/ge/lt/le`, `multiple_of`, `min_length/max_length`, `pattern`, `default`/`default_factory`, `alias`, `frozen`, `exclude`."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Use `default_factory` for mutable defaults (lists/dicts) to avoid shared-state bugs across instances."
    }
   ],
   "terms": [
    "Answer",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "6",
   "q": "Q6. How do you serialize a Pydantic model?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "code",
     "lang": "python",
     "code": "m.model_dump()                    # dict\nm.model_dump(mode=\"json\")         # dict with JSON-safe values (datetime -> str)\nm.model_dump_json()               # JSON string\nm.model_dump(exclude_unset=True)  # only fields the user set\nm.model_dump(exclude_none=True)   # drop None\nm.model_dump(by_alias=True)       # use serialization aliases\nm.model_dump(include={\"a\"}, exclude={\"b\"})",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** `model_dump` (dict) and `model_dump_json` (string) replace v1's `.dict()`/`.json()`; `exclude_unset` is the key option for PATCH-style partial outputs."
    }
   ],
   "terms": [
    "Answer",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "7",
   "q": "Q7. How do you parse external data into a model?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "code",
     "lang": "python",
     "code": "M.model_validate(some_dict)                  # from a dict\nM.model_validate_json('{\"a\": 1}')            # from a JSON string\nM.model_validate(orm_obj, from_attributes=True)  # from an object's attributes",
     "numbered": false
    },
    {
     "t": "p",
     "text": "`model_validate` runs full validation; constructing `M(**data)` does too but won't help when data isn't already keyword args."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** `model_validate` / `model_validate_json` are the v2 parsing entry points (formerly `parse_obj`/`parse_raw`)."
    }
   ]
  },
  {
   "t": "drill",
   "n": "8",
   "q": "Q8. What is `ConfigDict` and what are common options?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "`model_config = ConfigDict(...)` configures model behavior (replaces v1's inner `class Config`)."
    },
    {
     "t": "table",
     "head": [
      "Option",
      "Effect"
     ],
     "rows": [
      [
       "`strict=True`",
       "disable type coercion"
      ],
      [
       "`frozen=True`",
       "immutable + hashable"
      ],
      [
       "`extra=\"forbid\"`",
       "reject unknown fields"
      ],
      [
       "`from_attributes=True`",
       "read from object attributes (ORM mode)"
      ],
      [
       "`populate_by_name=True`",
       "accept field name *and* alias"
      ],
      [
       "`use_enum_values=True`",
       "store enum value, not member"
      ],
      [
       "`str_strip_whitespace=True`",
       "trim strings"
      ]
     ]
    },
    {
     "t": "p",
     "text": "**Key takeaway:** `ConfigDict` is where model-wide policies live — strictness, immutability, ORM reading, alias handling."
    }
   ],
   "terms": [
    "Answer",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "9",
   "q": "Q9. What is strict mode and when would you use it?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "By default Pydantic coerces compatible types (`\"10\"` → `10`, `3.0` → `3`). With `strict=True` (per-model via ConfigDict or per-field via `Field(strict=True)`), no coercion happens — a string is rejected where an `int` is declared."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "class M(BaseModel):\n    model_config = ConfigDict(strict=True)\n    n: int\n\n# M(n=\"10\")  -> ValidationError",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Use strict mode when silent coercion could hide bugs (e.g., financial data, IDs) and you want exact-type inputs."
    }
   ],
   "terms": [
    "Answer",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "10",
   "q": "Q10. How do discriminated unions work and why use them?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "A discriminator field (a `Literal`) tells Pydantic which member of a `Union` to build, instead of trying each model until one validates."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "ModelConfig = Annotated[Union[CNNConfig, TransformerConfig], Field(discriminator=\"arch\")]",
     "numbered": false
    },
    {
     "t": "p",
     "text": "Benefits: **O(1)** dispatch instead of O(n), clearer error messages (it validates only the matching model), and correct `oneOf` OpenAPI schema."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Discriminated unions make polymorphic input fast, unambiguous, and well-documented."
    }
   ],
   "terms": [
    "Answer",
    "O(1)",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "11",
   "q": "Q11. What are computed fields?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "`@computed_field` exposes a derived `@property` in the serialized output, so calculated values appear in `model_dump()`/JSON and the schema."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "class Run(BaseModel):\n    train_loss: float\n    val_loss: float\n\n    @computed_field\n    @property\n    def gap(self) -> float:\n        return self.val_loss - self.train_loss",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Use computed fields for read-only derived values you want serialized without storing them."
    }
   ],
   "terms": [
    "Answer",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "12",
   "q": "Q12. How does `pydantic-settings` resolve configuration values?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "Priority, highest to lowest: 1. Constructor arguments (`Settings(debug=True)`) 2. Environment variables (optionally with `env_prefix`) 3. `.env` file values 4. Field defaults"
    },
    {
     "t": "p",
     "text": "It supports nested settings via `env_nested_delimiter` (e.g., `APP__DB__HOST`), secrets from files, and custom sources."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "class Settings(BaseSettings):\n    database_url: str\n    model_config = SettingsConfigDict(env_file=\".env\", env_prefix=\"APP_\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Explicit args beat env vars beat `.env` beat defaults — a predictable layering for 12-factor config."
    }
   ],
   "terms": [
    "Answer",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "13",
   "q": "Q13. How do you handle mutable defaults correctly?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "Never use `Field(default=[])` or `tags: list = []` directly — that mutable object would be shared across instances. Use `default_factory`."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "tags: list[str] = Field(default_factory=list)\ncreated_at: datetime = Field(default_factory=datetime.now)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** `default_factory` produces a fresh object per instance, avoiding the classic shared-mutable-default bug."
    }
   ],
   "terms": [
    "Answer",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "14",
   "q": "Q14. What is `from_attributes` (formerly `orm_mode`)?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "It lets a model populate from an object's **attributes** (`obj.name`) rather than dict keys (`obj[\"name\"]`), which is required to return SQLAlchemy/ORM instances as Pydantic response models."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "class ItemResponse(BaseModel):\n    id: int\n    name: str\n    model_config = ConfigDict(from_attributes=True)\n\nItemResponse.model_validate(orm_item)   # reads orm_item.id, orm_item.name",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** `from_attributes=True` bridges ORM objects and Pydantic models — essential for FastAPI + SQLAlchemy."
    }
   ],
   "terms": [
    "Answer",
    "attributes",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "15",
   "q": "Q15. How does Pydantic integrate with FastAPI?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "FastAPI uses Pydantic models as the validation/serialization layer: - A model-typed parameter becomes the **request body** (auto-validated → 422 on failure). - `response_model=Model` filters/serializes the response and documents it. - Models feed the auto-generated OpenAPI schema at `/openapi.json`."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "@app.post(\"/predict\", response_model=PredictResponse)\nasync def predict(req: PredictRequest):   # body validated by Pydantic\n    ...",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Pydantic is the engine; FastAPI wires models to requests, responses, and docs automatically."
    }
   ],
   "terms": [
    "Answer",
    "request body",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "16",
   "q": "Q16. When would you choose attrs or msgspec over Pydantic?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "ul",
     "items": [
      "**attrs**lean, fast classes for library internals with optional validators; no JSON-schema overhead. Pairs with `cattrs` for serialization. Choose when you don't need API schema generation.",
      "**msgspec**fastest validation + JSON/msgpack serde; ideal for high-throughput pipelines, message brokers, and real-time systems where FastAPI integration isn't required.",
      "**Pydantic**API layers (FastAPI), config management, anywhere you need JSON Schema/OpenAPI and rich coercion."
     ]
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Pydantic for APIs/configs, attrs for lean internals, msgspec for raw serialization speed."
    }
   ],
   "terms": [
    "Answer",
    "attrs",
    "msgspec",
    "Pydantic",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "17",
   "q": "Q17. How do dataclasses compare to Pydantic models?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "`dataclasses` are stdlib and fast but do **no validation or coercion** and have no built-in JSON serialization or schema. Pydantic validates, coerces, serializes, and generates schemas at some runtime cost. (Pydantic also offers `pydantic.dataclasses.dataclass` to add validation to a dataclass-style API.)"
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Use plain dataclasses for trivial internal DTOs; reach for Pydantic when input is untrusted or you need (de)serialization/schema."
    }
   ],
   "terms": [
    "Answer",
    "no validation or coercion",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "18",
   "q": "Q18. How do you create a reusable custom validated type?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "Two approaches: 1. **Annotated validators** — attach a validator to a type alias:"
    },
    {
     "t": "code",
     "lang": "python",
     "code": "ClampedFloat = Annotated[float, AfterValidator(lambda v: max(0.0, min(1.0, v)))]",
     "numbered": false
    },
    {
     "t": "ol",
     "items": [
      "**Custom type** implementing `__get_pydantic_core_schema__` for full control over validation and serialization."
     ]
    },
    {
     "t": "p",
     "text": "**Key takeaway:** `Annotated[...]` + `AfterValidator`/`BeforeValidator` is the lightweight way to share validation logic across many models."
    }
   ],
   "terms": [
    "Answer",
    "Annotated validators",
    "Custom type",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "19",
   "q": "Q19. How are validation errors structured, and why is that useful?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "A `ValidationError` aggregates **all** failures at once; `.errors()` returns a list of dicts with `type`, `loc` (field path), `msg`, and `input`."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "try:\n    Model.model_validate(bad_data)\nexcept ValidationError as e:\n    for err in e.errors():\n        print(err[\"loc\"], err[\"msg\"])",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Errors are machine-readable and collected together, so you fail fast at startup/request time with a full report instead of one-error-at-a-time."
    }
   ],
   "terms": [
    "Answer",
    "all",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "20",
   "q": "Q20. How would you validate a complex ML/training config at startup?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "ol",
     "items": [
      "Define sub-configs (`DataConfig`, `ModelConfig`, `TrainerConfig`) with typed fields + `Field` constraints.",
      "Compose them into a parent `ExperimentConfig`.",
      "Use `@model_validator(mode=\"after\")` for cross-field rules (e.g., `warmup_steps < total_steps`, paths exist).",
      "Use `model_post_init` for side effects (create output dirs).",
      "Load with `ExperimentConfig.model_validate_json(...)` / `model_validate(yaml.safe_load(...))`.",
      "Fail fast — a single `ValidationError` reports every problem before a long run begins."
     ]
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Hierarchical models + cross-field validators + fail-fast loading prevent silent misconfiguration during expensive jobs."
    }
   ],
   "terms": [
    "Answer",
    "Key takeaway"
   ]
  }
 ],
 "takeaways": []
});
