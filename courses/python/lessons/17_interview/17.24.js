/* ============================================================================
   INTERVIEW 17.24 — Database & ORM
   ----------------------------------------------------------------------------
   Theory interview questions: the ones you answer out loud. Anything that
   asks for a program lives in the Coding Practice course instead.
   Generated by .build/import-interview.py — edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "17.24",
 "lede": "**25 interview questions on database & orm**, with the answers folded away. Say your answer out loud first — recognising an answer and being able to give one are different skills, and only the second survives a follow-up question.",
 "objectives": [
  "Answer 25 questions on database & orm without prompting",
  "State the trade-off behind each answer, not only the definition",
  "Recognise the follow-up each question is setting up",
  "Notice which answers you can recognise but not produce"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer out loud before revealing.** An answer you can only recognise is one you cannot give under pressure.",
      "**Say the trade-off, not just the definition.** Interviewers are listening for judgement, and the follow-up question is where it shows.",
      "Coding questions live in the **Coding Practice** course — these are the ones you answer in conversation."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "Q1. What is an ORM and what are its trade-offs?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "An **ORM (Object-Relational Mapper)** maps database tables to classes and rows to objects, letting you query with Python instead of raw SQL."
    },
    {
     "t": "p",
     "text": "**Pros:** less boilerplate, database-agnostic code, automatic parameter escaping (SQL-injection safety), Python-native relationships, change tracking via the session."
    },
    {
     "t": "p",
     "text": "**Cons:** an abstraction that can **hide expensive queries** (e.g. the N+1 problem), a learning curve, and occasional need to drop to raw SQL for complex analytics."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** ORMs trade some control and performance transparency for productivity and safety — know when to bypass them with raw SQL."
    }
   ],
   "terms": [
    "Answer",
    "ORM (Object-Relational Mapper)",
    "Pros",
    "Cons",
    "hide expensive queries",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "2",
   "q": "Q2. What is the N+1 query problem and how do you fix it?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "It occurs when you run **1** query to fetch a list, then **N** more queries — one per item — to fetch related data. It silently turns one logical operation into hundreds of round-trips."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "users = db.query(User).all()           # 1 query\nfor u in users:\n    print(u.posts)                     # N queries (one per user)  ← N+1",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Fix with eager loading:**"
    },
    {
     "t": "code",
     "lang": "python",
     "code": "from sqlalchemy.orm import joinedload, selectinload\ndb.query(User).options(joinedload(User.posts)).all()    # single JOIN\ndb.query(User).options(selectinload(User.posts)).all()  # 2 queries, better for large sets",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** N+1 is the most common ORM performance bug — eager-load related data with `joinedload`/`selectinload` instead of lazy-loading in a loop."
    }
   ],
   "terms": [
    "Answer",
    "Fix with eager loading",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "3",
   "q": "Q3. What does ACID stand for?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "The guarantees a transaction provides in a relational database:"
    },
    {
     "t": "ul",
     "items": [
      "**Atomicity**all-or-nothing; partial work is rolled back.",
      "**Consistency**constraints/invariants hold before and after.",
      "**Isolation**concurrent transactions don't see each other's uncommitted state.",
      "**Durability**committed data survives a crash."
     ]
    },
    {
     "t": "p",
     "text": "**Key takeaway:** ACID is why relational databases are trusted for money/orders — a transaction is an indivisible, durable unit of work. See [[database-transaction-isolation]]."
    }
   ],
   "terms": [
    "Answer",
    "Atomicity",
    "Consistency",
    "Isolation",
    "Durability",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "4",
   "q": "Q4. When should you add an index, and what's the downside?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "**Add** indexes on columns used in `WHERE`, `JOIN`, `ORDER BY`, `GROUP BY`, foreign keys, and uniqueness checks — anything used to *find* rows."
    },
    {
     "t": "p",
     "text": "**Downsides:** - Every `INSERT`/`UPDATE`/`DELETE` must also update the index → slower writes. - Indexes consume disk and memory. - Over-indexing (an index per column) wastes space and rarely helps."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Indexes trade write speed and storage for read speed — index the columns you filter/sort/join on, not every column."
    }
   ],
   "terms": [
    "Answer",
    "Add",
    "Downsides",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "5",
   "q": "Q5. What is a database transaction isolation level?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "Isolation levels control how/when one transaction sees another's changes, trading consistency for concurrency:"
    },
    {
     "t": "table",
     "head": [
      "Level",
      "Prevents"
     ],
     "rows": [
      [
       "Read Uncommitted",
       "(nothing — allows dirty reads)"
      ],
      [
       "Read Committed",
       "Dirty reads"
      ],
      [
       "Repeatable Read",
       "Dirty + non-repeatable reads"
      ],
      [
       "Serializable",
       "Dirty + non-repeatable + phantom reads (fully isolated)"
      ]
     ]
    },
    {
     "t": "p",
     "text": "Higher isolation = fewer anomalies but more locking/contention. PostgreSQL defaults to Read Committed."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Pick the lowest isolation level that prevents the anomalies your use case can't tolerate; Serializable is safest but costs concurrency."
    }
   ],
   "terms": [
    "Answer",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "6",
   "q": "Q6. What is the difference between SQLAlchemy Core and the ORM?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "ul",
     "items": [
      "**Core** is a SQL expression toolkit — you build queries with Python expressions that map closely to SQL (`select(users).where(...)`), working with rows/tuples.",
      "**ORM** sits on top of Core and adds class↔table mapping, identity map, change tracking, and relationships — you work with **objects**."
     ]
    },
    {
     "t": "p",
     "text": "Use the ORM for application/business logic; drop to Core (or raw SQL) for bulk operations and complex analytical queries where object overhead hurts."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Core = Pythonic SQL builder; ORM = object mapping on top. They interoperate, so you can mix them."
    }
   ],
   "terms": [
    "Answer",
    "Core",
    "ORM",
    "objects",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "7",
   "q": "Q7. What is a connection pool and why does it matter?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "Opening a new database connection (TCP + auth handshake) is expensive (milliseconds). A **connection pool** keeps a set of open connections and lends them to requests, returning them when done. SQLAlchemy's `Engine` pools by default."
    },
    {
     "t": "p",
     "text": "Without pooling, a high-traffic API spends most of its time reconnecting and can exhaust the database's connection limit."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "engine = create_engine(url, pool_size=10, max_overflow=20)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Pools amortize the cost of connecting; tune `pool_size`/`max_overflow` to your concurrency and the DB's connection cap."
    }
   ],
   "terms": [
    "Answer",
    "connection pool",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "8",
   "q": "Q8. What is the Session in SQLAlchemy and how should its lifecycle be managed?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "The **Session** is the ORM's *unit of work*: it holds objects in an **identity map**, tracks their changes, and flushes them as SQL on `commit()`. Scope one Session to one logical operation — typically **one per web request** — and close it afterward."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "with Session() as db:      # opened per request\n    db.add(obj)\n    db.commit()            # flushes + commits; session closed on exit",
     "numbered": false
    },
    {
     "t": "p",
     "text": "Sharing a single long-lived Session across threads/requests causes stale data and concurrency bugs."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** A Session is short-lived and per-request; open it, do the work, commit/rollback, close it."
    }
   ],
   "terms": [
    "Answer",
    "Session",
    "identity map",
    "one per web request",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "9",
   "q": "Q9. How does an ORM protect against SQL injection?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "ORMs (and the DBAPI) use **parameterized queries** (bind parameters): the SQL structure and the data are sent separately, so user input is never interpreted as SQL."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "# Safe — value is bound, not interpolated\ndb.query(User).filter(User.email == user_input).all()\n\n# DANGEROUS — never build SQL with string formatting\ndb.execute(f\"SELECT * FROM users WHERE email = '{user_input}'\")  # injectable",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Always pass values as parameters, never via f-strings/concatenation — parameterization is what neutralizes SQL injection. See [[best-practices-security]]."
    }
   ],
   "terms": [
    "Answer",
    "parameterized queries",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "10",
   "q": "Q10. What is a database migration and why not just edit tables manually?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "A **migration** is a versioned, repeatable script describing a schema change (add column, create index, etc.). Tools like **Alembic** (for SQLAlchemy) generate and apply them in order."
    },
    {
     "t": "p",
     "text": "Manual edits don't replicate across dev/staging/prod, aren't version-controlled, and can't be reliably rolled back. Migrations give you a reproducible, reviewable, reversible schema history that travels with your code."
    },
    {
     "t": "code",
     "lang": "bash",
     "code": "alembic revision --autogenerate -m \"add users.email index\"\nalembic upgrade head",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Migrations make schema changes versioned, reviewable, and reproducible across environments — never alter production schema by hand."
    }
   ],
   "terms": [
    "Answer",
    "migration",
    "Alembic",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "11",
   "q": "Q11. What is the difference between `joinedload` and `selectinload`?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "Both prevent N+1, but differ in strategy:"
    },
    {
     "t": "ul",
     "items": [
      "**`joinedload`**emits **one** query with a `JOIN`. Great for one-to-one / small one-to-many; can multiply rows (and bytes) for large collections.",
      "**`selectinload`**emits **two** queries (parent, then children via `WHERE id IN (...)`). Better for large one-to-many because it avoids row duplication."
     ]
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Use `joinedload` for small/single related objects, `selectinload` for large collections to avoid JOIN row-explosion."
    }
   ],
   "terms": [
    "Answer",
    "joinedload",
    "one",
    "selectinload",
    "two",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "12",
   "q": "Q12. When would you choose NoSQL over a relational database?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "Choose **NoSQL** when: - The schema is fluid or highly variable (document stores like MongoDB). - You need massive horizontal scale / high write throughput (Cassandra). - The access pattern is simple key-value at low latency (Redis). - Data is naturally graph-shaped (Neo4j)."
    },
    {
     "t": "p",
     "text": "Choose **relational** when you need joins, multi-row transactions, and strong consistency/integrity (orders, payments, inventory)."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Relational for structured, transactional, integrity-critical data; NoSQL for scale, flexibility, or specialized access patterns — many systems use both (polyglot persistence)."
    }
   ],
   "terms": [
    "Answer",
    "NoSQL",
    "relational",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "13",
   "q": "Q13. What is normalization vs denormalization?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "ul",
     "items": [
      "**Normalization** organizes tables to eliminate redundancy (each fact stored once), reducing update anomalies — the default for transactional (OLTP) systems.",
      "**Denormalization** deliberately duplicates data to avoid joins and speed up reads — common in reporting/analytics (OLAP) and read-heavy systems."
     ]
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Normalize for write integrity, denormalize for read performance; it's a deliberate trade-off, not a default."
    }
   ],
   "terms": [
    "Answer",
    "Normalization",
    "Denormalization",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "14",
   "q": "Q14. What is a deadlock and how do you avoid it?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "A **deadlock** happens when two transactions each hold a lock the other needs, so neither can proceed. The database detects it and kills one transaction (which you must retry)."
    },
    {
     "t": "p",
     "text": "**Avoid it by:** - Acquiring locks in a **consistent order** across the codebase. - Keeping transactions **short**. - Using lower isolation levels where safe. - Adding retry logic for the victim transaction."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Deadlocks come from inconsistent lock ordering and long transactions — order access consistently, keep transactions short, and retry the loser."
    }
   ],
   "terms": [
    "Answer",
    "deadlock",
    "Avoid it by",
    "consistent order",
    "short",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "15",
   "q": "Q15. What is eager vs lazy loading?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "ul",
     "items": [
      "**Lazy loading** (default) — related objects are fetched only when first accessed. Convenient, but accessing them in a loop causes the N+1 problem.",
      "**Eager loading**related objects are fetched up front in the same operation (`joinedload`/`selectinload`), avoiding extra round-trips."
     ]
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Lazy is convenient for single objects; switch to eager loading the moment you iterate over a collection's relationships. See Q2."
    }
   ],
   "terms": [
    "Answer",
    "Lazy loading",
    "Eager loading",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "16",
   "q": "Q16. What is the difference between `flush` and `commit`?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "ul",
     "items": [
      "**`flush()`** sends pending SQL (INSERT/UPDATE/DELETE) to the database **within the current transaction**, so the DB can assign primary keys and enforce constraints — but nothing is made permanent and it can still be rolled back.",
      "**`commit()`** flushes *and* **commits** the transaction, making changes durable and visible to other connections. It also flushes automatically first."
     ]
    },
    {
     "t": "p",
     "text": "By default a Session **autoflushes** before each query, so your pending changes are visible to your own subsequent queries without an explicit flush."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "db.add(user)\ndb.flush()          # INSERT runs now → user.id is populated, still rollback-able\norder.user_id = user.id\ndb.commit()         # both rows committed atomically",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** `flush` = \"emit SQL now, still inside the transaction\" (e.g. to get a generated id); `commit` = \"make it permanent.\" Commit flushes for you."
    }
   ],
   "terms": [
    "Answer",
    "flush()",
    "commit()",
    "commits",
    "autoflushes",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "17",
   "q": "Q17. What is the identity map and the unit-of-work pattern?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "ul",
     "items": [
      "**Identity map**within one Session, each database row is represented by **exactly one** Python object, keyed by primary key. Querying the same row twice returns the *same* object, so changes are consistent and there's no duplicate state.",
      "**Unit of work**the Session **tracks all changes** to those objects and emits the minimal set of SQL in the correct order at flush/commit, instead of you writing each statement."
     ]
    },
    {
     "t": "code",
     "lang": "python",
     "code": "a = db.get(User, 1)\nb = db.get(User, 1)\nassert a is b        # identity map: same object, served from the session's cache",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** The Session is an identity map + unit of work — one object per row, automatic change tracking, batched SQL on commit. See [[database-session-lifecycle]]."
    }
   ],
   "terms": [
    "Answer",
    "Identity map",
    "exactly one",
    "Unit of work",
    "tracks all changes",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "18",
   "q": "Q18. Optimistic vs pessimistic locking — how do you prevent lost updates?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "A **lost update**: two transactions read the same row, both modify it, and the second overwrites the first. Two defenses:"
    },
    {
     "t": "ul",
     "items": [
      "**Optimistic** (low contention): add a `version` column; on write, check the version hasn't changed and bump it. A mismatch → 0 rows updated → raise a 409 and retry. SQLAlchemy automates this via `__mapper_args__ = {\"version_id_col\": version}`.",
      "**Pessimistic** (high contention, money): lock the row up front with `SELECT ... FOR UPDATE` (`with_for_update()`) so others block until you commit."
     ]
    },
    {
     "t": "code",
     "lang": "python",
     "code": "row = db.execute(select(Account).where(Account.id == aid).with_for_update()).scalar_one()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Optimistic locking (version column, 409 on conflict) scales for rare conflicts; pessimistic (`FOR UPDATE`) suits hot, high-stakes rows. See the worked scenarios in [[fastapi-threading-orm-scenarios]]."
    }
   ],
   "terms": [
    "Answer",
    "lost update",
    "Optimistic",
    "Pessimistic",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "19",
   "q": "Q19. What do relationship cascades control?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "Cascades define what happens to **related** objects when you act on a parent — most importantly on delete. Configure via `relationship(..., cascade=..., passive_deletes=...)`:"
    },
    {
     "t": "ul",
     "items": [
      "`save-update, merge` (default) — children added to the session follow the parent.",
      "`all, delete-orphan` — deleting the parent deletes its children; removing a child from the collection deletes it.",
      "`passive_deletes=True` — defer deletion to the DB's `ON DELETE CASCADE` foreign key instead of loading and deleting children in Python (much faster for large sets)."
     ]
    },
    {
     "t": "code",
     "lang": "python",
     "code": "class User(Base):\n    posts = relationship(\"Post\", cascade=\"all, delete-orphan\", passive_deletes=True)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Cascades decide whether deletes/saves propagate to children; use `delete-orphan` for owned children and `passive_deletes` + a DB-level `ON DELETE CASCADE` for performance."
    }
   ],
   "terms": [
    "Answer",
    "related",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "20",
   "q": "Q20. How do you efficiently insert/update thousands of rows?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "Adding objects one-by-one with a `commit()` per row is the classic slow path (a round-trip + the unit-of-work overhead each time). Faster options, in order:"
    },
    {
     "t": "code",
     "lang": "python",
     "code": "# Better: many adds, ONE commit\ndb.add_all([User(**d) for d in rows]); db.commit()\n\n# Faster: bulk operations (skip most ORM unit-of-work machinery)\ndb.bulk_insert_mappings(User, rows)         # 1.x style\ndb.execute(insert(User), rows)              # 2.0 style — executemany under the hood\ndb.commit()\n\n# Fastest for huge loads: the DB's native bulk loader (e.g. PostgreSQL COPY)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "Trade-off: bulk methods bypass identity map, relationship cascades, and events — use them for plain data loads, not when you need ORM features."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Batch the commit, then reach for bulk inserts / `executemany`, then native `COPY` — each step trades ORM convenience for throughput."
    }
   ]
  },
  {
   "t": "drill",
   "n": "21",
   "q": "Q21. How do you diagnose a slow query?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "ol",
     "items": [
      "**Find** it — log slow queries (SQLAlchemy `before/after_cursor_execute` events, or the DB's slow-query log).",
      "**Explain** it — run `EXPLAIN ANALYZE <query>` to see the plan: is it a **Seq Scan** (full table) where you expected an **Index Scan**? Are row estimates wildly off?",
      "**Fix** it — add/adjust an index, rewrite the query, eager-load to kill N+1, select only needed columns, or add `LIMIT`."
     ]
    },
    {
     "t": "code",
     "lang": "sql",
     "code": "EXPLAIN ANALYZE SELECT * FROM orders WHERE user_id = 42 AND status = 'paid';\n-- \"Seq Scan on orders ...\" → missing index on (user_id, status)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Measure, don't guess — `EXPLAIN ANALYZE` shows whether the planner uses your indexes; a sequential scan on a large filtered table is the usual smoking gun."
    }
   ],
   "terms": [
    "Answer",
    "Find",
    "Explain",
    "Seq Scan",
    "Index Scan",
    "Fix"
   ]
  },
  {
   "t": "drill",
   "n": "22",
   "q": "Q22. How does a composite index work, and what's the leftmost-prefix rule?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "A **composite index** covers multiple columns in order, e.g. `(user_id, status, created_at)`. The **leftmost-prefix rule**: the index can serve queries that filter on a *prefix* of those columns, left to right."
    },
    {
     "t": "table",
     "head": [
      "Query filter",
      "Uses the index?"
     ],
     "rows": [
      [
       "`user_id`",
       "✅ (prefix)"
      ],
      [
       "`user_id, status`",
       "✅ (prefix)"
      ],
      [
       "`user_id, status, created_at`",
       "✅ (full)"
      ],
      [
       "`status` alone",
       "❌ (skips the leftmost column)"
      ],
      [
       "`created_at` alone",
       "❌"
      ]
     ]
    },
    {
     "t": "p",
     "text": "A **covering index** additionally *includes* all columns the query needs, so the DB answers it from the index alone (\"index-only scan\") without touching the table."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Order composite-index columns by how you filter (most selective / always-present first); only queries using a left-anchored prefix benefit."
    }
   ],
   "terms": [
    "Answer",
    "composite index",
    "leftmost-prefix rule",
    "covering index",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "23",
   "q": "Q23. How do you scale a database for reads and for writes?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "ul",
     "items": [
      "**Read scaling → replicas.** A primary handles writes and streams changes to read **replicas**; route read-only queries there. Watch for **replication lag** (a replica may serve slightly stale data — read-after-write may need to hit the primary).",
      "**Write scaling → sharding / partitioning.** Split data across shards by a key (user_id, region) so each holds a subset. Powerful but adds complexity: cross-shard joins and transactions are hard.",
      "**Before either:** caching (Redis), connection pooling, indexes, and query tuning often remove the need to scale out at all."
     ]
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Scale reads with replicas (mind the lag), scale writes with sharding/partitioning (mind cross-shard complexity) — but exhaust caching and query tuning first."
    }
   ],
   "terms": [
    "Answer",
    "Read scaling → replicas",
    "replicas",
    "replication lag",
    "Before either",
    "Key takeaway"
   ]
  },
  {
   "t": "drill",
   "n": "24",
   "q": "Q24. What is a detached instance / `DetachedInstanceError`?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "After a Session **closes** (or `commit()` with the default `expire_on_commit=True`), its objects are **expired/detached**. Accessing a not-yet-loaded attribute or a lazy relationship then raises `DetachedInstanceError` — there's no live session to issue the SQL. Common when you return ORM objects from a function after its `with Session()` block ends."
    },
    {
     "t": "p",
     "text": "**Fixes:** - Set `expire_on_commit=False` so already-loaded attributes stay usable after commit. - **Eager-load** everything you'll need before the session closes. - Convert to a plain dict / Pydantic model **inside** the session scope (e.g. FastAPI `response_model` with `from_attributes=True`)."
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Don't touch lazy attributes on an object whose Session has closed — materialize what you need (eager-load or serialize) before the session ends, or keep attributes alive with `expire_on_commit=False`."
    }
   ],
   "terms": [
    "Answer",
    "closes",
    "expired/detached",
    "Fixes",
    "Eager-load",
    "inside"
   ]
  },
  {
   "t": "drill",
   "n": "25",
   "q": "Q25. What changes when you use async SQLAlchemy?",
   "body": [
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "p",
     "text": "With `AsyncSession` / `create_async_engine` (and an async driver like `asyncpg`), every database operation must be `await`ed, and you **cannot rely on implicit lazy loading** — touching an un-loaded relationship outside an awaited call raises `MissingGreenlet`. So you must **eager-load** (`selectinload`/`joinedload`) anything you'll access, including for serialization."
    },
    {
     "t": "code",
     "lang": "python",
     "code": "async with AsyncSession(engine) as db:\n    result = await db.execute(\n        select(User).where(User.id == uid).options(selectinload(User.posts))\n    )\n    user = result.scalar_one()      # user.posts already loaded — safe to serialize",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Key takeaway:** Async ORM removes the \"lazy load just works\" safety net — `await` everything and eager-load relationships explicitly. See the worked async pitfalls in [[fastapi-threading-orm-scenarios]]."
    }
   ],
   "terms": [
    "Answer",
    "eager-load",
    "Key takeaway"
   ]
  }
 ],
 "takeaways": []
});
