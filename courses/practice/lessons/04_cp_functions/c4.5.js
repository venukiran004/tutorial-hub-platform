/* ============================================================================
   PRACTICE C3.5 — Programs · 5
   ----------------------------------------------------------------------------
   Imported from the Coding Practice source set and rendered through the
   Tutorial Hub design system. Generated by .build/import-practice.py —
   edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "c4.5",
 "lede": "**20 problems** from the functions and functional set. Each answer is hidden until you ask for it — attempt it first, because reading a solution feels like learning and is not.",
 "objectives": [
  "Work through 20 problems on programs without looking anything up",
  "Predict each answer before revealing it",
  "Recognise the pattern each problem belongs to",
  "Explain your solution out loud, including its complexity"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer before you reveal.** Even a wrong attempt makes the answer stick; reading first does not.",
      "**Say the complexity out loud** where the problem has one. Getting it wrong is the useful part.",
      "Use the bar above to open or close everything at once, and **Ctrl K** to jump to another set."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "Program 120: *args and **kwargs, Packing and Unpacking",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def show(*args, **kwargs):\n    return args, dict(sorted(kwargs.items()))\n\nprint(show(1, 2, x=3))            # Output: ((1, 2), {'x': 3})\n\nvalues = [1, 2, 3]\noptions = {\"x\": 9, \"y\": 8}\nprint(show(*values, **options))   # Output: ((1, 2, 3), {'x': 9, 'y': 8})\n\ndef add(a, b, c):\n    return a + b + c\nprint(add(*values))               # Output: 6\n\ndef wrapper(*args, **kwargs):\n    return add(*args, **kwargs)   # forward everything, unchanged\nprint(wrapper(1, 2, c=10))        # Output: 13",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "2",
   "q": "Program 121: Default Arguments Are Evaluated Once",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import time\n\ndef stamp(t=time.time()):        # evaluated at DEFINITION time\n    return t\n\nfirst = stamp()\ntime.sleep(0.05)\nprint(stamp() == first)          # Output: True\n\ndef stamp_ok(t=None):\n    return time.time() if t is None else t\n\nfirst = stamp_ok()\ntime.sleep(0.05)\nprint(stamp_ok() == first)       # Output: False",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "3",
   "q": "Program 122: Closures Capture the Variable, Not the Value",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "funcs = [lambda: i for i in range(3)]\nprint([f() for f in funcs])          # Output: [2, 2, 2]\n\nbound = [lambda i=i: i for i in range(3)]\nprint([f() for f in bound])          # Output: [0, 1, 2]\n\nfrom functools import partial\npartials = [partial(lambda x: x, i) for i in range(3)]\nprint([f() for f in partials])       # Output: [0, 1, 2]\n\n# All three lambdas close over the SAME i, which is 2 when they finally run.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "4",
   "q": "Program 123: nonlocal Versus global",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "counter = 0\n\ndef outer():\n    count = 0\n    def inner():\n        nonlocal count\n        count += 1\n        global counter\n        counter += 10\n        return count\n    inner(); inner()\n    return count\n\nprint(outer())        # Output: 2\nprint(counter)        # Output: 20\n\ndef broken():\n    total = 0\n    def add():\n        try:\n            total += 1\n        except UnboundLocalError:\n            return \"needs nonlocal\"\n    return add()\nprint(broken())       # Output: needs nonlocal",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "5",
   "q": "Program 124: First-Class Functions",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def double(x): return x * 2\ndef square(x): return x * x\n\nops = {\"double\": double, \"square\": square}\nprint(ops[\"square\"](5))                  # Output: 25\nprint([f(3) for f in (double, square)])  # Output: [6, 9]\n\ndef apply_twice(fn, value):\n    return fn(fn(value))\nprint(apply_twice(double, 3))            # Output: 12\n\ndef make_multiplier(n):\n    def multiply(x):\n        return x * n\n    return multiply\ntriple = make_multiplier(3)\nprint(triple(7))                         # Output: 21\nprint(triple.__name__)                   # Output: multiply",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "6",
   "q": "Program 125: functools.partial Freezes Arguments",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from functools import partial\n\ndef power(base, exponent):\n    return base ** exponent\n\nsquare = partial(power, exponent=2)\ncube = partial(power, exponent=3)\nprint(square(5), cube(2))             # Output: 25 8\n\ntwo_to_the = partial(power, 2)        # freezes the FIRST positional argument\nprint(two_to_the(10))                 # Output: 1024\n\nprint(square.func.__name__, square.keywords)\n# Output: power {'exponent': 2}\n\nint_from_binary = partial(int, base=2)\nprint(int_from_binary(\"1011\"))        # Output: 11",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "7",
   "q": "Program 126: Recursion and the Depth Limit",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import sys\n\ndef depth(n=0):\n    try:\n        return depth(n + 1)\n    except RecursionError:\n        return n\n\nreached = depth()\nprint(reached > 500)                     # Output: True\nprint(sys.getrecursionlimit() >= 1000)   # Output: True\n\ndef factorial_iterative(n):\n    result = 1\n    for i in range(2, n + 1):\n        result *= i\n    return result\nprint(factorial_iterative(20))           # Output: 2432902008176640000\n\n# Python does not eliminate tail calls, so deep recursion is a real limit.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "8",
   "q": "Program 127: Functions Have Attributes",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def greet(name: str, punctuation: str = \"!\") -> str:\n    \"\"\"Say hello.\"\"\"\n    return f\"Hello, {name}{punctuation}\"\n\nprint(greet.__name__)              # Output: greet\nprint(greet.__doc__)               # Output: Say hello.\nprint(greet.__defaults__)          # Output: ('!',)\nprint(greet.__annotations__)\n# Output: {'name': <class 'str'>, 'punctuation': <class 'str'>, 'return': <class 'str'>}\n\ngreet.call_count = 0               # functions are objects: attach anything\ngreet.call_count += 1\nprint(greet.call_count)            # Output: 1\n\nimport inspect\nprint(list(inspect.signature(greet).parameters))     # Output: ['name', 'punctuation']",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "9",
   "q": "Program 128: Annotations Are Not Enforced",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def add(a: int, b: int) -> int:\n    return a + b\n\nprint(add(1, 2))              # Output: 3\nprint(add(\"x\", \"y\"))          # Output: xy\nprint(add([1], [2]))          # Output: [1, 2]\n\n# Python does not check annotations at runtime - they are metadata for readers,\n# mypy, and libraries like pydantic and dataclasses.\nprint(add.__annotations__[\"a\"])     # Output: <class 'int'>",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "10",
   "q": "Program 129: Higher-Order: map, filter, reduce",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from functools import reduce\n\nnums = [1, 2, 3, 4, 5]\nprint(list(map(lambda x: x * x, nums)))          # Output: [1, 4, 9, 16, 25]\nprint(list(filter(lambda x: x % 2, nums)))       # Output: [1, 3, 5]\nprint(reduce(lambda a, b: a + b, nums))          # Output: 15\nprint(reduce(lambda a, b: a * b, nums, 1))       # Output: 120\n\nprint([x * x for x in nums])                     # Output: [1, 4, 9, 16, 25]\nprint([x for x in nums if x % 2])                # Output: [1, 3, 5]\nprint(sum(nums))                                 # Output: 15\n\n# map and filter return lazy iterators, and are one-shot:\nm = map(str, nums)\nprint(list(m), list(m))                          # Output: ['1', '2', '3', '4', '5'] []",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "11",
   "q": "Program 130: A Pure Function Versus One With Side Effects",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def pure_add_item(items, item):\n    return items + [item]          # new list, caller's list untouched\n\ndef impure_add_item(items, item):\n    items.append(item)             # mutates the caller's list\n    return items\n\noriginal = [1, 2]\nresult = pure_add_item(original, 3)\nprint(original, result)            # Output: [1, 2] [1, 2, 3]\n\noriginal = [1, 2]\nresult = impure_add_item(original, 3)\nprint(original, result)            # Output: [1, 2, 3] [1, 2, 3]\nprint(original is result)          # Output: True\n\n# The impure version returned the SAME object it was given, which is how a\n# caller ends up sharing state it thought it owned.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "12",
   "q": "Program 131: Currying by Hand",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def curry_add(a):\n    def with_b(b):\n        def with_c(c):\n            return a + b + c\n        return with_c\n    return with_b\n\nprint(curry_add(1)(2)(3))         # Output: 6\n\nadd_10 = curry_add(10)\nadd_10_20 = add_10(20)\nprint(add_10_20(5))               # Output: 35\nprint(add_10_20(100))             # Output: 130\n\n# Each call returns a function that remembers the arguments so far - the same\n# mechanism as functools.partial, written out.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "13",
   "q": "Program 132: Function Composition",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from functools import reduce\n\ndef compose(*functions):\n    \"\"\"compose(f, g, h)(x) == f(g(h(x)))\"\"\"\n    return reduce(lambda f, g: lambda x: f(g(x)), functions)\n\nstrip = str.strip\nlower = str.lower\nexclaim = lambda s: s + \"!\"\n\nshout = compose(exclaim, lower, strip)\nprint(shout(\"  HeLLo  \"))         # Output: hello!\n\ndouble = lambda x: x * 2\ninc = lambda x: x + 1\nprint(compose(double, inc)(5))    # Output: 12\nprint(compose(inc, double)(5))    # Output: 11\n\n# Order matters: the RIGHTMOST function runs first.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "14",
   "q": "Program 133: Generators as a Pipeline",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def read_rows():\n    for line in [\"1,apple\", \"2,banana\", \"bad line\", \"3,cherry\"]:\n        yield line\n\ndef parse(rows):\n    for row in rows:\n        parts = row.split(\",\")\n        if len(parts) == 2:\n            yield int(parts[0]), parts[1]\n\ndef only_long(pairs):\n    for n, name in pairs:\n        if len(name) > 5:\n            yield n, name\n\nprint(list(only_long(parse(read_rows()))))\n# Output: [(2, 'banana'), (3, 'cherry')]\n\n# Nothing is materialised: one row is in memory at a time all the way through.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "15",
   "q": "Program 134: Unpacking in a Function Signature",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def stats(*values, precision=2, **labels):\n    mean = sum(values) / len(values)\n    return round(mean, precision), dict(sorted(labels.items()))\n\nprint(stats(1, 2, 3))                              # Output: (2.0, {})\nprint(stats(1, 2, 3, precision=4))                 # Output: (2.0, {})\nprint(stats(1, 2, unit=\"kg\", source=\"scale\"))\n# Output: (1.5, {'source': 'scale', 'unit': 'kg'})\n\ndata = [10, 20, 30]\nmeta = {\"unit\": \"cm\"}\nprint(stats(*data, **meta))                        # Output: (20.0, {'unit': 'cm'})",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "16",
   "q": "Program 135: EAFP Versus LBYL",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# LBYL - Look Before You Leap: check first, then act\ndef lbyl(d, key):\n    if key in d and isinstance(d[key], int) and d[key] != 0:\n        return 100 / d[key]\n    return None\n\n# EAFP - Easier to Ask Forgiveness than Permission: act, handle the failure\ndef eafp(d, key):\n    try:\n        return 100 / d[key]\n    except (KeyError, TypeError, ZeroDivisionError):\n        return None\n\ndata = {\"a\": 4, \"b\": 0, \"c\": \"x\"}\nprint([lbyl(data, k) for k in (\"a\", \"b\", \"c\", \"z\")])\n# Output: [25.0, None, None, None]\nprint([eafp(data, k) for k in (\"a\", \"b\", \"c\", \"z\")])\n# Output: [25.0, None, None, None]\n\n# Same answers - but EAFP is the Python idiom, for two concrete reasons.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "17",
   "q": "Program 136: Why EAFP Wins: the Check Can Go Stale",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import os\n\n# LBYL has a RACE: the file can vanish between the check and the open.\nopen(\"data.txt\", \"w\").write(\"content\")\n\ndef lbyl_read(path):\n    if os.path.exists(path):\n        # another process could delete the file right here\n        return open(path, encoding=\"utf-8\").read()\n    return \"missing\"\n\ndef eafp_read(path):\n    try:\n        return open(path, encoding=\"utf-8\").read()\n    except FileNotFoundError:\n        return \"missing\"\n\nprint(lbyl_read(\"data.txt\"), eafp_read(\"data.txt\"))     # Output: content content\nprint(lbyl_read(\"gone.txt\"), eafp_read(\"gone.txt\"))     # Output: missing missing\n\n# The EAFP version has no window: the open either succeeds or raises. This class\n# of bug is called TOCTOU - time of check to time of use.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "18",
   "q": "Program 137: Why EAFP Wins: It Is Faster When It Usually Succeeds",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import timeit\n\nd = {\"key\": 1}\n\nlbyl = timeit.timeit(lambda: d[\"key\"] if \"key\" in d else None, number=200_000)\n\ndef eafp():\n    try:\n        return d[\"key\"]\n    except KeyError:\n        return None\n\neafp_time = timeit.timeit(eafp, number=200_000)\n\nprint(\"both work:\", (d[\"key\"] if \"key\" in d else None) == eafp())\n# Output: both work: True\nprint(\"eafp avoids the second lookup:\", eafp_time < lbyl * 1.6)\n# Output: eafp avoids the second lookup: True\n\n# LBYL hashes the key TWICE - once for \"in\", once for the lookup. try/except\n# costs nothing until an exception is actually raised.\n# Use LBYL when the failure is common, or when the check is cheaper than the work.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "19",
   "q": "Program 138: EAFP and Duck Typing Go Together",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def total(items):\n    \"\"\"Works with anything summable - no isinstance ladder.\"\"\"\n    try:\n        return sum(items)\n    except TypeError:\n        return \"not summable\"\n\nprint(total([1, 2, 3]))            # Output: 6\nprint(total((1.5, 2.5)))           # Output: 4.0\nprint(total({1, 2}))               # Output: 3\nprint(total([\"a\", \"b\"]))           # Output: not summable\n\n# The LBYL version would need to enumerate every acceptable type and would\n# reject a perfectly good type nobody thought of.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "20",
   "q": "Program 139: Positional-Only Parameters and Why They Exist",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def clamp(value, low, high, /, *, inclusive=True):\n    if inclusive:\n        return max(low, min(value, high))\n    return max(low + 1, min(value, high - 1))\n\nprint(clamp(15, 0, 10))                        # Output: 10\nprint(clamp(15, 0, 10, inclusive=False))       # Output: 9\n\ntry:\n    clamp(value=15, low=0, high=10)\nexcept TypeError as e:\n    print(\"positional-only:\", \"positional\" in str(e))\n    # Output: positional-only: True\n\n# Names before / are NOT part of the API, so you can rename them later without\n# breaking a caller. That is why the built-ins use them: len(obj=...) is an error.\ntry:\n    len(obj=[1, 2])\nexcept TypeError:\n    print(\"even len() is positional-only\")     # Output: even len() is positional-only",
     "numbered": false
    }
   ]
  }
 ],
 "takeaways": [],
 "_drills": 20,
 "_code": 20
});
