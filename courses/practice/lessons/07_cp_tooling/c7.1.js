/* ============================================================================
   PRACTICE C7.1 — Programs · 1
   ----------------------------------------------------------------------------
   Imported from the Coding Practice source set and rendered through the
   Tutorial Hub design system. Generated by .build/import-practice.py —
   edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "c7.1",
 "lede": "**30 problems** from the modules, regex, testing and tooling set. Each answer is hidden until you ask for it — attempt it first, because reading a solution feels like learning and is not.",
 "objectives": [
  "Work through 30 problems on programs without looking anything up",
  "Predict each answer before revealing it",
  "Recognise the pattern each problem belongs to",
  "Explain your solution out loud, including its complexity"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer before you reveal.** Even a wrong attempt makes the answer stick; reading first does not.",
      "**Say the complexity out loud** where the problem has one. Getting it wrong is the useful part.",
      "Use the bar above to open or close everything at once, and **Ctrl K** to jump to another set."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "Program 1: Unit Test with unittest",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import unittest\n\ndef calculator(a, b, op):\n    if op == '+': return a + b\n    if op == '-': return a - b\n    if op == '*': return a * b\n    if op == '/':\n        if b == 0:\n            raise ValueError(\"Cannot divide by zero\")\n        return a / b\n    raise ValueError(f\"Unknown operator: {op}\")\n\nclass TestCalculator(unittest.TestCase):\n    def test_add(self):\n        self.assertEqual(calculator(2, 3, '+'), 5)\n    \n    def test_subtract(self):\n        self.assertEqual(calculator(10, 4, '-'), 6)\n    \n    def test_multiply(self):\n        self.assertEqual(calculator(3, 7, '*'), 21)\n    \n    def test_divide(self):\n        self.assertAlmostEqual(calculator(10, 3, '/'), 3.333, places=2)\n    \n    def test_divide_by_zero(self):\n        with self.assertRaises(ValueError):\n            calculator(5, 0, '/')\n    \n    def test_invalid_operator(self):\n        with self.assertRaises(ValueError):\n            calculator(1, 2, '%')\n\nif __name__ == \"__main__\":\n    unittest.main()\n# All 6 tests pass",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "2",
   "q": "Program 2: Pytest Basics",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import pytest\n\nclass Stack:\n    def __init__(self):\n        self._items = []\n    def push(self, item):\n        self._items.append(item)\n    def pop(self):\n        if not self._items:\n            raise IndexError(\"Pop from empty stack\")\n        return self._items.pop()\n    def peek(self):\n        if not self._items:\n            raise IndexError(\"Peek at empty stack\")\n        return self._items[-1]\n    def is_empty(self):\n        return len(self._items) == 0\n    def size(self):\n        return len(self._items)\n\n# Tests\ndef test_push_and_pop():\n    s = Stack()\n    s.push(1)\n    s.push(2)\n    assert s.pop() == 2\n    assert s.pop() == 1\n\ndef test_empty_stack():\n    s = Stack()\n    assert s.is_empty()\n    assert s.size() == 0\n\ndef test_pop_empty_raises():\n    s = Stack()\n    with pytest.raises(IndexError):\n        s.pop()\n\ndef test_peek():\n    s = Stack()\n    s.push(42)\n    assert s.peek() == 42\n    assert s.size() == 1  # peek doesn't remove\n\n# Run: pytest test_file.py -v",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "3",
   "q": "Program 3: Parametrized Tests",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import pytest\n\ndef is_palindrome(s):\n    cleaned = ''.join(c.lower() for c in s if c.isalnum())\n    return cleaned == cleaned[::-1]\n\n@pytest.mark.parametrize(\"input_str, expected\", [\n    (\"racecar\", True),\n    (\"hello\", False),\n    (\"A man a plan a canal Panama\", True),\n    (\"\", True),\n    (\"ab\", False),\n    (\"Was it a car or a cat I saw?\", True),\n])\ndef test_palindrome(input_str, expected):\n    assert is_palindrome(input_str) == expected\n\n# All 6 parametrized tests pass",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "4",
   "q": "Program 4: Fixtures in Pytest",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import pytest\nimport sqlite3\n\n@pytest.fixture\ndef db_connection():\n    \"\"\"Create in-memory database with test data.\"\"\"\n    conn = sqlite3.connect(\":memory:\")\n    cursor = conn.cursor()\n    cursor.execute(\"CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT, age INTEGER)\")\n    cursor.executemany(\"INSERT INTO users (name, age) VALUES (?, ?)\",\n                       [(\"Alice\", 30), (\"Bob\", 25), (\"Charlie\", 35)])\n    conn.commit()\n    yield conn\n    conn.close()\n\n@pytest.fixture\ndef user_repo(db_connection):\n    class UserRepo:\n        def __init__(self, conn):\n            self.conn = conn\n        def get_all(self):\n            return self.conn.execute(\"SELECT * FROM users\").fetchall()\n        def get_by_name(self, name):\n            return self.conn.execute(\"SELECT * FROM users WHERE name=?\", (name,)).fetchone()\n    return UserRepo(db_connection)\n\ndef test_get_all(user_repo):\n    users = user_repo.get_all()\n    assert len(users) == 3\n\ndef test_get_by_name(user_repo):\n    user = user_repo.get_by_name(\"Alice\")\n    assert user[1] == \"Alice\"\n    assert user[2] == 30",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "5",
   "q": "Program 5: Mocking with unittest.mock",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from unittest.mock import Mock, patch, MagicMock\nimport json\n\nclass WeatherService:\n    def __init__(self, api_key):\n        self.api_key = api_key\n    \n    def fetch_weather(self, city):\n        \"\"\"In real code, this calls an external API.\"\"\"\n        import requests\n        response = requests.get(f\"https://api.weather.com/{city}\")\n        return response.json()\n    \n    def get_temperature(self, city):\n        data = self.fetch_weather(city)\n        return data[\"main\"][\"temp\"]\n\n# Test with mocking\ndef test_get_temperature():\n    service = WeatherService(\"fake-key\")\n    \n    mock_response = {\"main\": {\"temp\": 25.5}, \"city\": \"London\"}\n    \n    with patch.object(service, 'fetch_weather', return_value=mock_response):\n        temp = service.get_temperature(\"London\")\n        assert temp == 25.5\n        service.fetch_weather.assert_called_once_with(\"London\")\n\ntest_get_temperature()\nprint(\"Mock test passed!\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "6",
   "q": "Program 6: Property-Based Testing",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# pip install hypothesis\nfrom hypothesis import given, strategies as st\n\ndef sort_list(lst):\n    return sorted(lst)\n\n@given(st.lists(st.integers()))\ndef test_sort_preserves_length(lst):\n    result = sort_list(lst)\n    assert len(result) == len(lst)\n\n@given(st.lists(st.integers(), min_size=1))\ndef test_sort_is_ordered(lst):\n    result = sort_list(lst)\n    for i in range(len(result) - 1):\n        assert result[i] <= result[i + 1]\n\n@given(st.lists(st.integers()))\ndef test_sort_preserves_elements(lst):\n    result = sort_list(lst)\n    assert sorted(result) == sorted(lst)\n\n# Run tests\ntest_sort_preserves_length()\ntest_sort_is_ordered()\ntest_sort_preserves_elements()\nprint(\"All property-based tests passed!\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "7",
   "q": "Program 7: Test Coverage Report",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# Example module: math_utils.py\ndef fibonacci(n):\n    if n < 0:\n        raise ValueError(\"Negative input\")\n    if n <= 1:\n        return n\n    a, b = 0, 1\n    for _ in range(2, n + 1):\n        a, b = b, a + b\n    return b\n\ndef factorial(n):\n    if n < 0:\n        raise ValueError(\"Negative input\")\n    result = 1\n    for i in range(2, n + 1):\n        result *= i\n    return result\n\n# Tests for coverage\nimport unittest\n\nclass TestMathUtils(unittest.TestCase):\n    def test_fibonacci_base(self):\n        self.assertEqual(fibonacci(0), 0)\n        self.assertEqual(fibonacci(1), 1)\n    \n    def test_fibonacci_normal(self):\n        self.assertEqual(fibonacci(10), 55)\n    \n    def test_fibonacci_negative(self):\n        with self.assertRaises(ValueError):\n            fibonacci(-1)\n    \n    def test_factorial_base(self):\n        self.assertEqual(factorial(0), 1)\n        self.assertEqual(factorial(1), 1)\n    \n    def test_factorial_normal(self):\n        self.assertEqual(factorial(5), 120)\n    \n    def test_factorial_negative(self):\n        with self.assertRaises(ValueError):\n            factorial(-1)\n\n# Run: pytest --cov=math_utils --cov-report=term-missing",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "8",
   "q": "Program 8: Integration Test Pattern",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import sqlite3\nimport json\n\nclass UserService:\n    def __init__(self, db_path=\":memory:\"):\n        self.conn = sqlite3.connect(db_path)\n        self.conn.execute(\"\"\"\n            CREATE TABLE IF NOT EXISTS users (\n                id INTEGER PRIMARY KEY AUTOINCREMENT,\n                name TEXT, email TEXT UNIQUE\n            )\n        \"\"\")\n    \n    def create_user(self, name, email):\n        try:\n            self.conn.execute(\"INSERT INTO users (name, email) VALUES (?, ?)\", (name, email))\n            self.conn.commit()\n            return True\n        except sqlite3.IntegrityError:\n            return False\n    \n    def get_user(self, email):\n        row = self.conn.execute(\"SELECT * FROM users WHERE email=?\", (email,)).fetchone()\n        return {\"id\": row[0], \"name\": row[1], \"email\": row[2]} if row else None\n    \n    def close(self):\n        self.conn.close()\n\n# Integration test\ndef test_user_lifecycle():\n    service = UserService()\n    \n    # Create\n    assert service.create_user(\"Alice\", \"alice@test.com\") == True\n    \n    # Read\n    user = service.get_user(\"alice@test.com\")\n    assert user[\"name\"] == \"Alice\"\n    \n    # Duplicate\n    assert service.create_user(\"Alice2\", \"alice@test.com\") == False\n    \n    # Not found\n    assert service.get_user(\"nobody@test.com\") is None\n    \n    service.close()\n    print(\"Integration test passed!\")\n\ntest_user_lifecycle()",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "9",
   "q": "Program 9: Custom Test Runner",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import time\nimport traceback\n\nclass TestRunner:\n    def __init__(self):\n        self.passed = 0\n        self.failed = 0\n        self.errors = []\n    \n    def run_test(self, name, func):\n        try:\n            start = time.time()\n            func()\n            elapsed = time.time() - start\n            print(f\"  ✓ {name} ({elapsed*1000:.1f}ms)\")\n            self.passed += 1\n        except AssertionError as e:\n            print(f\"  ✗ {name}: {e}\")\n            self.failed += 1\n            self.errors.append((name, str(e)))\n        except Exception as e:\n            print(f\"  ✗ {name}: ERROR - {e}\")\n            self.failed += 1\n            self.errors.append((name, traceback.format_exc()))\n    \n    def summary(self):\n        total = self.passed + self.failed\n        print(f\"\\n{'='*40}\")\n        print(f\"Results: {self.passed}/{total} passed, {self.failed} failed\")\n        if self.errors:\n            print(\"\\nFailures:\")\n            for name, err in self.errors:\n                print(f\"  - {name}: {err}\")\n\nrunner = TestRunner()\nrunner.run_test(\"1+1=2\", lambda: None if 1+1==2 else (_ for _ in ()).throw(AssertionError(\"wrong\")))\nrunner.run_test(\"list sort\", lambda: exec(\"assert sorted([3,1,2]) == [1,2,3]\"))\nrunner.run_test(\"should fail\", lambda: exec(\"assert 1 == 2, 'not equal'\"))\nrunner.summary()",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "10",
   "q": "Program 10: Benchmark Decorator",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import time\nimport functools\nimport statistics\n\ndef benchmark(iterations=100):\n    def decorator(func):\n        @functools.wraps(func)\n        def wrapper(*args, **kwargs):\n            times = []\n            result = None\n            for _ in range(iterations):\n                start = time.perf_counter()\n                result = func(*args, **kwargs)\n                elapsed = time.perf_counter() - start\n                times.append(elapsed)\n            \n            print(f\"Benchmark: {func.__name__}\")\n            print(f\"  Iterations: {iterations}\")\n            print(f\"  Mean: {statistics.mean(times)*1000:.3f}ms\")\n            print(f\"  Median: {statistics.median(times)*1000:.3f}ms\")\n            print(f\"  Std Dev: {statistics.stdev(times)*1000:.3f}ms\")\n            print(f\"  Min: {min(times)*1000:.3f}ms\")\n            print(f\"  Max: {max(times)*1000:.3f}ms\")\n            return result\n        return wrapper\n    return decorator\n\n@benchmark(iterations=1000)\ndef bubble_sort(arr):\n    a = arr[:]\n    n = len(a)\n    for i in range(n):\n        for j in range(0, n-i-1):\n            if a[j] > a[j+1]:\n                a[j], a[j+1] = a[j+1], a[j]\n    return a\n\nimport random\ndata = [random.randint(0, 100) for _ in range(100)]\nbubble_sort(data)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "11",
   "q": "Program 11: Memory Profiler",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import sys\nimport gc\n\ndef get_size(obj, seen=None):\n    \"\"\"Recursively calculate object size in bytes.\"\"\"\n    size = sys.getsizeof(obj)\n    if seen is None:\n        seen = set()\n    obj_id = id(obj)\n    if obj_id in seen:\n        return 0\n    seen.add(obj_id)\n    \n    if isinstance(obj, dict):\n        size += sum(get_size(k, seen) + get_size(v, seen) for k, v in obj.items())\n    elif isinstance(obj, (list, tuple, set, frozenset)):\n        size += sum(get_size(i, seen) for i in obj)\n    return size\n\n# Compare memory usage\nlist_data = list(range(10000))\ntuple_data = tuple(range(10000))\nset_data = set(range(10000))\ndict_data = {i: i*2 for i in range(10000)}\n\nfor name, data in [(\"list\", list_data), (\"tuple\", tuple_data), \n                    (\"set\", set_data), (\"dict\", dict_data)]:\n    size = get_size(data)\n    print(f\"{name:6s}: {size:>10,} bytes ({size/1024:.1f} KB)\")\n\n# Output:\n# list  :    400,056 bytes (390.7 KB)\n# tuple :    400,040 bytes (390.7 KB)\n# set   :    524,504 bytes (512.2 KB)\n# dict  :    920,792 bytes (899.2 KB)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "12",
   "q": "Program 12: Debug Context Manager",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import time\nimport traceback\nimport functools\n\nclass DebugContext:\n    def __init__(self, name=\"block\"):\n        self.name = name\n    \n    def __enter__(self):\n        self.start = time.perf_counter()\n        print(f\"[DEBUG] Entering {self.name}\")\n        return self\n    \n    def __exit__(self, exc_type, exc_val, exc_tb):\n        elapsed = time.perf_counter() - self.start\n        if exc_type:\n            print(f\"[DEBUG] {self.name} FAILED after {elapsed*1000:.1f}ms\")\n            print(f\"[DEBUG] Error: {exc_type.__name__}: {exc_val}\")\n            return False  # Don't suppress\n        print(f\"[DEBUG] {self.name} completed in {elapsed*1000:.1f}ms\")\n        return False\n\n# Usage\nwith DebugContext(\"data processing\"):\n    data = [i**2 for i in range(100000)]\n    total = sum(data)\n    print(f\"Sum: {total}\")\n\n# Output:\n# [DEBUG] Entering data processing\n# Sum: 333328333350000\n# [DEBUG] data processing completed in X.Xms",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "13",
   "q": "Program 13: Assertion Helpers",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class Assert:\n    @staticmethod\n    def equal(actual, expected, msg=\"\"):\n        if actual != expected:\n            raise AssertionError(f\"Expected {expected}, got {actual}. {msg}\")\n    \n    @staticmethod\n    def not_equal(actual, expected, msg=\"\"):\n        if actual == expected:\n            raise AssertionError(f\"Expected not {expected}. {msg}\")\n    \n    @staticmethod\n    def almost_equal(actual, expected, places=7, msg=\"\"):\n        if round(abs(actual - expected), places) != 0:\n            raise AssertionError(f\"{actual} != {expected} within {places} places. {msg}\")\n    \n    @staticmethod\n    def contains(container, item, msg=\"\"):\n        if item not in container:\n            raise AssertionError(f\"{item} not found in {container}. {msg}\")\n    \n    @staticmethod\n    def raises(exc_type, func, *args, **kwargs):\n        try:\n            func(*args, **kwargs)\n            raise AssertionError(f\"Expected {exc_type.__name__} but none raised\")\n        except exc_type:\n            pass\n    \n    @staticmethod\n    def is_type(obj, expected_type, msg=\"\"):\n        if not isinstance(obj, expected_type):\n            raise AssertionError(f\"Expected {expected_type}, got {type(obj)}. {msg}\")\n\n# Usage\nAssert.equal(1 + 1, 2)\nAssert.almost_equal(0.1 + 0.2, 0.3, places=1)\nAssert.contains([1, 2, 3], 2)\nAssert.raises(ZeroDivisionError, lambda: 1/0)\nAssert.is_type(\"hello\", str)\nprint(\"All assertions passed!\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "14",
   "q": "Program 14: Snapshot Testing",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import json\nimport os\nimport hashlib\n\nclass SnapshotTester:\n    def __init__(self, snapshot_dir=\"__snapshots__\"):\n        self.snapshot_dir = snapshot_dir\n        os.makedirs(snapshot_dir, exist_ok=True)\n    \n    def _snapshot_path(self, name):\n        return os.path.join(self.snapshot_dir, f\"{name}.json\")\n    \n    def assert_match(self, name, actual, update=False):\n        path = self._snapshot_path(name)\n        actual_json = json.dumps(actual, indent=2, sort_keys=True)\n        \n        if not os.path.exists(path) or update:\n            with open(path, 'w') as f:\n                f.write(actual_json)\n            print(f\"Snapshot '{name}' {'updated' if update else 'created'}\")\n            return\n        \n        with open(path, 'r') as f:\n            expected_json = f.read()\n        \n        if actual_json != expected_json:\n            raise AssertionError(\n                f\"Snapshot mismatch for '{name}':\\n\"\n                f\"Expected:\\n{expected_json}\\n\"\n                f\"Actual:\\n{actual_json}\"\n            )\n        print(f\"Snapshot '{name}' matches ✓\")\n\nsnap = SnapshotTester()\ndata = {\"users\": [{\"name\": \"Alice\", \"age\": 30}], \"count\": 1}\nsnap.assert_match(\"user_list\", data)\n# First run: creates snapshot\n# Second run: validates against snapshot",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "15",
   "q": "Program 15: Test Data Factory",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import random\nimport string\nfrom datetime import datetime, timedelta\n\nclass Factory:\n    _counter = 0\n    \n    @classmethod\n    def _next_id(cls):\n        cls._counter += 1\n        return cls._counter\n    \n    @classmethod\n    def user(cls, **overrides):\n        defaults = {\n            \"id\": cls._next_id(),\n            \"name\": f\"User_{random.randint(100,999)}\",\n            \"email\": f\"user{cls._counter}@test.com\",\n            \"age\": random.randint(18, 65),\n            \"active\": True,\n            \"created_at\": datetime.now().isoformat()\n        }\n        defaults.update(overrides)\n        return defaults\n    \n    @classmethod\n    def product(cls, **overrides):\n        defaults = {\n            \"id\": cls._next_id(),\n            \"name\": f\"Product-{cls._counter}\",\n            \"price\": round(random.uniform(9.99, 99.99), 2),\n            \"category\": random.choice([\"Electronics\", \"Books\", \"Clothing\"]),\n            \"in_stock\": True\n        }\n        defaults.update(overrides)\n        return defaults\n    \n    @classmethod\n    def batch(cls, factory_method, count, **overrides):\n        return [factory_method(**overrides) for _ in range(count)]\n\n# Usage\nusers = Factory.batch(Factory.user, 3, active=True)\nfor u in users:\n    print(f\"{u['name']} ({u['email']})\")\n\nproducts = Factory.batch(Factory.product, 2, category=\"Books\")\nfor p in products:\n    print(f\"{p['name']}: ${p['price']}\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "16",
   "q": "Program 16: Doctest Examples",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def reverse_words(sentence):\n    \"\"\"Reverse the order of words in a sentence.\n    \n    >>> reverse_words(\"hello world\")\n    'world hello'\n    \n    >>> reverse_words(\"Python is great\")\n    'great is Python'\n    \n    >>> reverse_words(\"\")\n    ''\n    \n    >>> reverse_words(\"single\")\n    'single'\n    \"\"\"\n    return ' '.join(sentence.split()[::-1])\n\ndef caesar_cipher(text, shift):\n    \"\"\"Apply Caesar cipher to text.\n    \n    >>> caesar_cipher(\"abc\", 1)\n    'bcd'\n    \n    >>> caesar_cipher(\"xyz\", 3)\n    'abc'\n    \n    >>> caesar_cipher(\"Hello\", 0)\n    'Hello'\n    \"\"\"\n    result = []\n    for char in text:\n        if char.isalpha():\n            base = ord('A') if char.isupper() else ord('a')\n            result.append(chr((ord(char) - base + shift) % 26 + base))\n        else:\n            result.append(char)\n    return ''.join(result)\n\nif __name__ == \"__main__\":\n    import doctest\n    results = doctest.testmod(verbose=True)\n    print(f\"\\n{results.attempted} tests, {results.failed} failures\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "17",
   "q": "Program 17: Logging Configuration",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import logging\nimport sys\n\ndef setup_logging(level=logging.DEBUG, log_file=None):\n    formatter = logging.Formatter(\n        '%(asctime)s | %(name)s | %(levelname)-8s | %(message)s',\n        datefmt='%Y-%m-%d %H:%M:%S'\n    )\n    \n    root_logger = logging.getLogger()\n    root_logger.setLevel(level)\n    \n    # Console handler\n    console = logging.StreamHandler(sys.stdout)\n    console.setLevel(level)\n    console.setFormatter(formatter)\n    root_logger.addHandler(console)\n    \n    # File handler\n    if log_file:\n        file_handler = logging.FileHandler(log_file)\n        file_handler.setLevel(level)\n        file_handler.setFormatter(formatter)\n        root_logger.addHandler(file_handler)\n    \n    return root_logger\n\nlogger = setup_logging(logging.INFO)\nlogger.info(\"Application started\")\nlogger.warning(\"Low memory: 85%\")\nlogger.error(\"Database connection failed\")\n\n# Custom logger for module\nml_logger = logging.getLogger(\"ml.training\")\nml_logger.info(\"Training started with lr=0.001\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "18",
   "q": "Program 18: Error Tracking System",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import traceback\nimport datetime\nfrom collections import defaultdict\n\nclass ErrorTracker:\n    def __init__(self):\n        self.errors = []\n        self.error_counts = defaultdict(int)\n    \n    def capture(self, error, context=None):\n        entry = {\n            \"type\": type(error).__name__,\n            \"message\": str(error),\n            \"traceback\": traceback.format_exc(),\n            \"timestamp\": datetime.datetime.now().isoformat(),\n            \"context\": context or {}\n        }\n        self.errors.append(entry)\n        self.error_counts[type(error).__name__] += 1\n    \n    def get_summary(self):\n        return {\n            \"total_errors\": len(self.errors),\n            \"by_type\": dict(self.error_counts),\n            \"recent\": self.errors[-5:] if self.errors else []\n        }\n    \n    def __call__(self, func):\n        def wrapper(*args, **kwargs):\n            try:\n                return func(*args, **kwargs)\n            except Exception as e:\n                self.capture(e, {\"function\": func.__name__, \"args\": str(args)})\n                raise\n        return wrapper\n\ntracker = ErrorTracker()\n\n@tracker\ndef risky_divide(a, b):\n    return a / b\n\ntry:\n    risky_divide(10, 0)\nexcept:\n    pass\n\ntry:\n    risky_divide(10, \"abc\")\nexcept:\n    pass\n\nprint(tracker.get_summary()[\"by_type\"])\n# Output: {'ZeroDivisionError': 1, 'TypeError': 1}",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "19",
   "q": "Program 19: Performance Profiler",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import cProfile\nimport pstats\nimport io\n\ndef profile_function(func, *args, **kwargs):\n    profiler = cProfile.Profile()\n    profiler.enable()\n    result = func(*args, **kwargs)\n    profiler.disable()\n    \n    stream = io.StringIO()\n    stats = pstats.Stats(profiler, stream=stream)\n    stats.sort_stats('cumulative')\n    stats.print_stats(10)\n    \n    print(stream.getvalue())\n    return result\n\n# Function to profile\ndef matrix_multiply(n):\n    A = [[i+j for j in range(n)] for i in range(n)]\n    B = [[i*j for j in range(n)] for i in range(n)]\n    C = [[0]*n for _ in range(n)]\n    for i in range(n):\n        for j in range(n):\n            for k in range(n):\n                C[i][j] += A[i][k] * B[k][j]\n    return C\n\nprofile_function(matrix_multiply, 50)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "20",
   "q": "Program 20: Retry Decorator with Logging",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import time\nimport logging\nimport functools\n\nlogging.basicConfig(level=logging.INFO)\nlogger = logging.getLogger(__name__)\n\ndef retry(max_attempts=3, delay=1, backoff=2, exceptions=(Exception,)):\n    def decorator(func):\n        @functools.wraps(func)\n        def wrapper(*args, **kwargs):\n            current_delay = delay\n            for attempt in range(1, max_attempts + 1):\n                try:\n                    result = func(*args, **kwargs)\n                    if attempt > 1:\n                        logger.info(f\"{func.__name__} succeeded on attempt {attempt}\")\n                    return result\n                except exceptions as e:\n                    if attempt == max_attempts:\n                        logger.error(f\"{func.__name__} failed after {max_attempts} attempts: {e}\")\n                        raise\n                    logger.warning(f\"{func.__name__} attempt {attempt} failed: {e}. \"\n                                   f\"Retrying in {current_delay}s...\")\n                    time.sleep(current_delay)\n                    current_delay *= backoff\n        return wrapper\n    return decorator\n\ncall_count = 0\n\n@retry(max_attempts=3, delay=0.1, exceptions=(ConnectionError,))\ndef flaky_api_call():\n    global call_count\n    call_count += 1\n    if call_count < 3:\n        raise ConnectionError(\"Server unavailable\")\n    return {\"status\": \"ok\"}\n\nresult = flaky_api_call()\nprint(f\"Result: {result}\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "21",
   "q": "Program 21: Contract/Precondition Checker",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import functools\n\ndef contract(pre=None, post=None):\n    def decorator(func):\n        @functools.wraps(func)\n        def wrapper(*args, **kwargs):\n            if pre:\n                assert pre(*args, **kwargs), f\"Precondition failed for {func.__name__}\"\n            result = func(*args, **kwargs)\n            if post:\n                assert post(result), f\"Postcondition failed for {func.__name__}\"\n            return result\n        return wrapper\n    return decorator\n\n@contract(\n    pre=lambda x, y: x >= 0 and y >= 0,\n    post=lambda result: result >= 0\n)\ndef safe_divide(x, y):\n    if y == 0:\n        return 0\n    return x / y\n\nprint(safe_divide(10, 3))  # Output: 3.333...\nprint(safe_divide(0, 5))   # Output: 0.0\n\ntry:\n    safe_divide(-1, 2)\nexcept AssertionError as e:\n    print(f\"Caught: {e}\")  # Precondition failed",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "22",
   "q": "Program 22: Fuzzy Test Generator",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import random\nimport string\n\nclass FuzzyGenerator:\n    @staticmethod\n    def string(min_len=0, max_len=50):\n        length = random.randint(min_len, max_len)\n        return ''.join(random.choices(string.printable, k=length))\n    \n    @staticmethod\n    def integer(min_val=-1000, max_val=1000):\n        return random.randint(min_val, max_val)\n    \n    @staticmethod\n    def float_val(min_val=-1000, max_val=1000):\n        return random.uniform(min_val, max_val)\n    \n    @staticmethod\n    def list_of(generator, min_len=0, max_len=20):\n        length = random.randint(min_len, max_len)\n        return [generator() for _ in range(length)]\n    \n    @staticmethod\n    def dict_of(key_gen, val_gen, min_len=0, max_len=10):\n        length = random.randint(min_len, max_len)\n        return {key_gen(): val_gen() for _ in range(length)}\n\ndef fuzz_test(func, arg_generators, iterations=100):\n    failures = []\n    for i in range(iterations):\n        args = [gen() for gen in arg_generators]\n        try:\n            func(*args)\n        except Exception as e:\n            failures.append({\"args\": args, \"error\": str(e)})\n    return failures\n\n# Fuzz test a sort function\nfailures = fuzz_test(\n    sorted,\n    [lambda: FuzzyGenerator.list_of(FuzzyGenerator.integer, 0, 50)],\n    iterations=100\n)\nprint(f\"Fuzz test: {100 - len(failures)}/100 passed\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "23",
   "q": "Program 23: Code Timer Comparison",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import time\n\nclass Timer:\n    def __init__(self):\n        self.results = {}\n    \n    def time_it(self, name, func, *args, iterations=1000):\n        start = time.perf_counter()\n        for _ in range(iterations):\n            func(*args)\n        elapsed = time.perf_counter() - start\n        self.results[name] = elapsed / iterations\n        return elapsed / iterations\n    \n    def compare(self):\n        if not self.results:\n            return\n        sorted_results = sorted(self.results.items(), key=lambda x: x[1])\n        baseline = sorted_results[0][1]\n        print(f\"\\n{'Method':<30} {'Time':>12} {'Relative':>10}\")\n        print(\"-\" * 55)\n        for name, t in sorted_results:\n            ratio = t / baseline\n            print(f\"{name:<30} {t*1000:>10.4f}ms {ratio:>9.1f}x\")\n\ntimer = Timer()\n\ndata = list(range(1000))\ntimer.time_it(\"list comprehension\", lambda d: [x**2 for x in d], data)\ntimer.time_it(\"map function\", lambda d: list(map(lambda x: x**2, d)), data)\ntimer.time_it(\"for loop\", lambda d: [None for x in d for _ in [None] if not None], data)\n\ntimer.compare()",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "24",
   "q": "Program 24: Invariant Checker",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class InvariantError(Exception):\n    pass\n\ndef check_invariants(cls):\n    \"\"\"Class decorator that checks invariants after each method call.\"\"\"\n    original_init = cls.__init__\n    \n    def new_init(self, *args, **kwargs):\n        original_init(self, *args, **kwargs)\n        self._check_invariants()\n    \n    cls.__init__ = new_init\n    \n    for name in list(vars(cls)):\n        if name.startswith('_'):\n            continue\n        method = getattr(cls, name)\n        if callable(method):\n            def make_wrapper(m):\n                def wrapper(self, *args, **kwargs):\n                    result = m(self, *args, **kwargs)\n                    self._check_invariants()\n                    return result\n                return wrapper\n            setattr(cls, name, make_wrapper(method))\n    \n    return cls\n\n@check_invariants\nclass BankAccount:\n    def __init__(self, balance=0):\n        self.balance = balance\n    \n    def _check_invariants(self):\n        if self.balance < 0:\n            raise InvariantError(f\"Balance cannot be negative: {self.balance}\")\n    \n    def deposit(self, amount):\n        self.balance += amount\n    \n    def withdraw(self, amount):\n        self.balance -= amount\n\nacc = BankAccount(100)\nacc.deposit(50)\nprint(f\"Balance: {acc.balance}\")  # 150\n\ntry:\n    acc.withdraw(200)  # Would make balance -50\nexcept InvariantError as e:\n    print(f\"Invariant violated: {e}\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "25",
   "q": "Program 25: Test Report Generator",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import time\nimport json\n\nclass TestReport:\n    def __init__(self, suite_name):\n        self.suite_name = suite_name\n        self.tests = []\n        self.start_time = time.time()\n    \n    def add_result(self, name, status, duration, error=None):\n        self.tests.append({\n            \"name\": name,\n            \"status\": status,\n            \"duration_ms\": round(duration * 1000, 2),\n            \"error\": error\n        })\n    \n    def run_test(self, name, func):\n        start = time.perf_counter()\n        try:\n            func()\n            duration = time.perf_counter() - start\n            self.add_result(name, \"PASSED\", duration)\n        except AssertionError as e:\n            duration = time.perf_counter() - start\n            self.add_result(name, \"FAILED\", duration, str(e))\n        except Exception as e:\n            duration = time.perf_counter() - start\n            self.add_result(name, \"ERROR\", duration, str(e))\n    \n    def generate(self):\n        passed = sum(1 for t in self.tests if t[\"status\"] == \"PASSED\")\n        failed = sum(1 for t in self.tests if t[\"status\"] == \"FAILED\")\n        errors = sum(1 for t in self.tests if t[\"status\"] == \"ERROR\")\n        total_time = sum(t[\"duration_ms\"] for t in self.tests)\n        \n        report = {\n            \"suite\": self.suite_name,\n            \"total\": len(self.tests),\n            \"passed\": passed, \"failed\": failed, \"errors\": errors,\n            \"total_time_ms\": round(total_time, 2),\n            \"pass_rate\": f\"{passed/len(self.tests)*100:.1f}%\",\n            \"tests\": self.tests\n        }\n        return report\n\nreport = TestReport(\"Math Tests\")\nreport.run_test(\"addition\", lambda: exec(\"assert 1+1 == 2\"))\nreport.run_test(\"string\", lambda: exec(\"assert 'hello'.upper() == 'HELLO'\"))\nreport.run_test(\"failing\", lambda: exec(\"assert 1 == 2, 'math broken'\"))\nreport.run_test(\"error\", lambda: exec(\"1/0\"))\n\nresult = report.generate()\nprint(json.dumps(result, indent=2))",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "26",
   "q": "Program 26: Basic Unit Test",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import unittest\n\ndef add(a, b):\n    return a + b\n\ndef multiply(a, b):\n    return a * b\n\nclass TestMath(unittest.TestCase):\n    def test_add(self):\n        self.assertEqual(add(2, 3), 5)\n        self.assertEqual(add(-1, 1), 0)\n    \n    def test_multiply(self):\n        self.assertEqual(multiply(3, 4), 12)\n        self.assertEqual(multiply(0, 5), 0)\n    \n    def test_add_floats(self):\n        self.assertAlmostEqual(add(0.1, 0.2), 0.3, places=7)\n\nif __name__ == '__main__':\n    unittest.main()",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "27",
   "q": "Program 27: Test Fixtures (setUp / tearDown)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import unittest\n\nclass Stack:\n    def __init__(self):\n        self._items = []\n    def push(self, item): self._items.append(item)\n    def pop(self): return self._items.pop()\n    def peek(self): return self._items[-1]\n    def is_empty(self): return len(self._items) == 0\n    def size(self): return len(self._items)\n\nclass TestStack(unittest.TestCase):\n    def setUp(self):\n        self.stack = Stack()\n        self.stack.push(1)\n        self.stack.push(2)\n    \n    def tearDown(self):\n        self.stack = None\n    \n    def test_push(self):\n        self.stack.push(3)\n        self.assertEqual(self.stack.size(), 3)\n    \n    def test_pop(self):\n        self.assertEqual(self.stack.pop(), 2)\n        self.assertEqual(self.stack.size(), 1)\n    \n    def test_peek(self):\n        self.assertEqual(self.stack.peek(), 2)\n        self.assertEqual(self.stack.size(), 2)\n    \n    def test_is_empty(self):\n        self.assertFalse(self.stack.is_empty())\n\nif __name__ == '__main__':\n    unittest.main()",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "28",
   "q": "Program 28: Parameterized Tests",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import unittest\n\ndef is_palindrome(s):\n    s = s.lower().replace(\" \", \"\")\n    return s == s[::-1]\n\nclass TestPalindrome(unittest.TestCase):\n    test_cases = [\n        (\"racecar\", True),\n        (\"hello\", False),\n        (\"A man a plan a canal Panama\", True),\n        (\"\", True),\n        (\"ab\", False),\n    ]\n    \n    def test_palindromes(self):\n        for text, expected in self.test_cases:\n            with self.subTest(text=text):\n                self.assertEqual(is_palindrome(text), expected)\n\nif __name__ == '__main__':\n    unittest.main()",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "29",
   "q": "Program 29: Mocking External Dependencies",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from unittest.mock import Mock, patch, MagicMock\nimport unittest\n\nclass WeatherService:\n    def get_temperature(self, city):\n        # In real code, this calls an API\n        raise NotImplementedError\n\nclass WeatherApp:\n    def __init__(self, service):\n        self.service = service\n    \n    def get_forecast(self, city):\n        temp = self.service.get_temperature(city)\n        if temp > 30:\n            return \"Hot\"\n        elif temp > 15:\n            return \"Warm\"\n        return \"Cold\"\n\nclass TestWeatherApp(unittest.TestCase):\n    def test_hot_weather(self):\n        mock_service = Mock()\n        mock_service.get_temperature.return_value = 35\n        app = WeatherApp(mock_service)\n        self.assertEqual(app.get_forecast(\"NYC\"), \"Hot\")\n    \n    def test_cold_weather(self):\n        mock_service = Mock()\n        mock_service.get_temperature.return_value = 5\n        app = WeatherApp(mock_service)\n        self.assertEqual(app.get_forecast(\"NYC\"), \"Cold\")\n    \n    def test_service_called_with_city(self):\n        mock_service = Mock()\n        mock_service.get_temperature.return_value = 20\n        app = WeatherApp(mock_service)\n        app.get_forecast(\"London\")\n        mock_service.get_temperature.assert_called_once_with(\"London\")\n\nif __name__ == '__main__':\n    unittest.main()",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "30",
   "q": "Program 30: Testing Exceptions",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import unittest\n\nclass BankAccount:\n    def __init__(self, balance=0):\n        self.balance = balance\n    \n    def deposit(self, amount):\n        if amount <= 0:\n            raise ValueError(\"Deposit must be positive\")\n        self.balance += amount\n    \n    def withdraw(self, amount):\n        if amount > self.balance:\n            raise InsufficientFundsError(f\"Need {amount}, have {self.balance}\")\n        self.balance -= amount\n\nclass InsufficientFundsError(Exception):\n    pass\n\nclass TestBankAccount(unittest.TestCase):\n    def test_negative_deposit(self):\n        acc = BankAccount(100)\n        with self.assertRaises(ValueError):\n            acc.deposit(-50)\n    \n    def test_overdraft(self):\n        acc = BankAccount(100)\n        with self.assertRaises(InsufficientFundsError) as ctx:\n            acc.withdraw(200)\n        self.assertIn(\"Need 200\", str(ctx.exception))\n    \n    def test_valid_operations(self):\n        acc = BankAccount(100)\n        acc.deposit(50)\n        acc.withdraw(30)\n        self.assertEqual(acc.balance, 120)\n\nif __name__ == '__main__':\n    unittest.main()",
     "numbered": false
    }
   ]
  }
 ],
 "takeaways": [],
 "_drills": 30,
 "_code": 30
});
