/* ============================================================================
   PRACTICE C7.5 — Programs · 5
   ----------------------------------------------------------------------------
   Imported from the Coding Practice source set and rendered through the
   Tutorial Hub design system. Generated by .build/import-practice.py —
   edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "c7.5",
 "lede": "**30 problems** from the modules, regex, testing and tooling set. Each answer is hidden until you ask for it — attempt it first, because reading a solution feels like learning and is not.",
 "objectives": [
  "Work through 30 problems on programs without looking anything up",
  "Predict each answer before revealing it",
  "Recognise the pattern each problem belongs to",
  "Explain your solution out loud, including its complexity"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer before you reveal.** Even a wrong attempt makes the answer stick; reading first does not.",
      "**Say the complexity out loud** where the problem has one. Getting it wrong is the useful part.",
      "Use the bar above to open or close everything at once, and **Ctrl K** to jump to another set."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "An itertools recipe — output of `f\"{key}: {list(group)}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from itertools import groupby\n\ndata = [('A', 1), ('A', 2), ('B', 3), ('B', 4), ('A', 5)]\nfor key, group in groupby(data, key=lambda x: x[0]):\n    print(f\"{key}: {list(group)}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "A: [('A', 1), ('A', 2)]\nB: [('B', 3), ('B', 4)]\nA: [('A', 5)]",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Explanation:** `groupby` groups consecutive elements. Data must be sorted for full grouping."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "2",
   "q": "An itertools recipe — output of `list(product('AB', '12'))`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from itertools import product, combinations, permutations\n\nprint(list(product('AB', '12')))\nprint(list(combinations('ABC', 2)))\nprint(list(permutations('AB')))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `[('A', '1'), ('A', '2'), ('B', '1'), ('B', '2')]`, `[('A', 'B'), ('A', 'C'), ('B', 'C')]`, `[('A', 'B'), ('B', 'A')]` **Explanation:** Cartesian product, combinations (order-insensitive), permutations (order-sensitive)."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "3",
   "q": "An lru_cache-memoised function — output of `fib(10)`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from functools import lru_cache\n\ncall_count = 0\n\n@lru_cache(maxsize=32)\ndef fib(n):\n    global call_count\n    call_count += 1\n    if n < 2:\n        return n\n    return fib(n-1) + fib(n-2)\n\nprint(fib(10))\nprint(f\"Calls: {call_count}\")\nprint(fib.cache_info())",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `55`, `Calls: 11`, `CacheInfo(hits=8, misses=11, maxsize=32, currsize=11)` **Explanation:** `lru_cache` memoizes results. Without it, fib(10) would require 177 calls."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "4",
   "q": "An abstract base class — output of `c.describe()`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from abc import ABC, abstractmethod\n\nclass Shape(ABC):\n    @abstractmethod\n    def area(self):\n        pass\n    \n    def describe(self):\n        return f\"{type(self).__name__}: area={self.area():.2f}\"\n\nclass Circle(Shape):\n    def __init__(self, r):\n        self.r = r\n    \n    def area(self):\n        return 3.14159 * self.r ** 2\n\nc = Circle(5)\nprint(c.describe())\n\ntry:\n    s = Shape()\nexcept TypeError as e:\n    print(\"Can't instantiate abstract class\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Circle: area=78.54`, `Can't instantiate abstract class` **Explanation:** Abstract classes can't be instantiated. Subclasses must implement abstract methods."
    }
   ]
  },
  {
   "t": "drill",
   "n": "5",
   "q": "Inheritance — output of `len(s)`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from typing import TypeVar, Generic\n\nT = TypeVar('T')\n\nclass Stack(Generic[T]):\n    def __init__(self):\n        self.items: list[T] = []\n    \n    def push(self, item: T):\n        self.items.append(item)\n    \n    def pop(self) -> T:\n        return self.items.pop()\n    \n    def __len__(self):\n        return len(self.items)\n\ns = Stack()\ns.push(1)\ns.push(2)\ns.push(3)\nprint(len(s))\nprint(s.pop())\nprint(len(s))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `3`, `3`, `2` **Explanation:** Generic typed stack. Type hints don't enforce at runtime but help static analysis."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "6",
   "q": "str.split — output of `calculate(\"10 + 20\")`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import operator\n\nops = {\n    '+': operator.add,\n    '-': operator.sub,\n    '*': operator.mul,\n    '/': operator.truediv,\n}\n\ndef calculate(expr):\n    a, op, b = expr.split()\n    return ops[op](float(a), float(b))\n\nprint(calculate(\"10 + 20\"))\nprint(calculate(\"50 / 4\"))\nprint(calculate(\"3 * 7\"))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `30.0`, `12.5`, `21.0` **Explanation:** `operator` module provides function equivalents of operators."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "7",
   "q": "A for loop — output of `from bisect import insort, bisect_left`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from bisect import insort, bisect_left\n\nsorted_list = []\nfor x in [5, 2, 8, 1, 9, 3]:\n    insort(sorted_list, x)\n\nprint(sorted_list)\nprint(bisect_left(sorted_list, 5))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `[1, 2, 3, 5, 8, 9]`, `3` **Explanation:** `insort` maintains sorted order. `bisect_left` finds insertion point."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "8",
   "q": "A list comprehension — output of `[heappop(heap) for _ in range(3)]`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from heapq import heappush, heappop, nlargest, nsmallest\n\nheap = []\nfor x in [5, 2, 8, 1, 9, 3]:\n    heappush(heap, x)\n\nprint([heappop(heap) for _ in range(3)])\n\ndata = [5, 2, 8, 1, 9, 3]\nprint(nlargest(3, data))\nprint(nsmallest(3, data))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `[1, 2, 3]`, `[9, 8, 5]`, `[1, 2, 3]` **Explanation:** `heapq` implements min-heap. `heappop` returns smallest."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "9",
   "q": "What is the output of `t.substitute(name=\"Alice\", count=5)`?",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from string import Template\n\nt = Template(\"Hello, $name! You have $count messages.\")\nprint(t.substitute(name=\"Alice\", count=5))\n\nt2 = Template(\"${noun}ification\")\nprint(t2.substitute(noun=\"class\"))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Hello, Alice! You have 5 messages.`, `classification` **Explanation:** `Template` uses `$variable` syntax. `${var}` for adjacent text."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "10",
   "q": "What is the output of `type(formatted)`?",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from pprint import pformat\n\ndata = {\n    'users': [\n        {'name': 'Alice', 'age': 30},\n        {'name': 'Bob', 'age': 25},\n    ],\n    'count': 2\n}\n\nformatted = pformat(data, width=40)\nprint(type(formatted))\nprint('users' in formatted)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `<class 'str'>`, `True` **Explanation:** `pformat` returns formatted string (vs `pprint` which prints directly)."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "11",
   "q": "A class definition — output of `weak().name`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from weakref import ref\n\nclass MyObj:\n    def __init__(self, name):\n        self.name = name\n\nobj = MyObj(\"test\")\nweak = ref(obj)\nprint(weak().name)\n\ndel obj\nprint(weak())",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `test`, `None` **Explanation:** Weak reference doesn't prevent garbage collection. After `del`, `weak()` returns `None`."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "12",
   "q": "What is the output of `f\"Mean: {mean(data)}\"`?",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from statistics import mean, median, mode, stdev\n\ndata = [2, 4, 4, 4, 5, 5, 7, 9]\nprint(f\"Mean: {mean(data)}\")\nprint(f\"Median: {median(data)}\")\nprint(f\"Mode: {mode(data)}\")\nprint(f\"Stdev: {stdev(data):.2f}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Mean: 5`, `Median: 4.5`, `Mode: 4`, `Stdev: 2.14` **Explanation:** Built-in statistics module for basic statistical calculations."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "13",
   "q": "What is the output of `randint(1, 10)`?",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from random import seed, randint, choice, shuffle, sample\n\nseed(42)\nprint(randint(1, 10))\nprint(choice(['a', 'b', 'c', 'd']))\n\ndata = [1, 2, 3, 4, 5]\nprint(sample(data, 3))\nprint(data)  # unchanged",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `2` (or similar deterministic), `d` (or similar), `[5, 4, 2]` (or similar), `[1, 2, 3, 4, 5]` **Explanation:** `seed(42)` makes results reproducible. `sample` doesn't modify original."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "14",
   "q": "Slicing — output of `string.ascii_lowercase`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import string\n\nprint(string.ascii_lowercase)\nprint(string.digits)\nprint(string.punctuation[:10])\n\ntable = str.maketrans('aeiou', '12345')\nprint(\"hello world\".translate(table))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `abcdefghijklmnopqrstuvwxyz`, `0123456789`, `!\"#$%&'()*`, `h2ll4 w4rld` **Explanation:** `string` module constants. `maketrans`/`translate` for character mapping."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "15",
   "q": "An itertools recipe — output of `list(accumulate(data))`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from itertools import accumulate\nimport operator\n\ndata = [1, 2, 3, 4, 5]\nprint(list(accumulate(data)))\nprint(list(accumulate(data, operator.mul)))\nprint(list(accumulate(data, max)))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `[1, 3, 6, 10, 15]`, `[1, 2, 6, 24, 120]`, `[1, 2, 3, 4, 5]` **Explanation:** `accumulate` computes running totals. Custom functions for running product/max."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "16",
   "q": "What is the output of `\"won't reach here\"`?",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from contextlib import suppress\n\nd = {}\nwith suppress(KeyError):\n    value = d['missing']\n    print(\"won't reach here\")\n\nprint(\"Continuing after suppressed error\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Continuing after suppressed error` **Explanation:** `suppress` catches and ignores specified errors. Code after error line is skipped."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "17",
   "q": "Inheritance — output of `render(Circle())`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from typing import Protocol\n\nclass Drawable(Protocol):\n    def draw(self) -> str: ...\n\nclass Circle:\n    def draw(self) -> str:\n        return \"Drawing circle\"\n\nclass Square:\n    def draw(self) -> str:\n        return \"Drawing square\"\n\ndef render(shape: Drawable) -> str:\n    return shape.draw()\n\nprint(render(Circle()))\nprint(render(Square()))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Drawing circle`, `Drawing square` **Explanation:** Protocol enables structural subtyping (duck typing with type hints)."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "18",
   "q": "Slicing — output of `f\"MD5 length: {len(md5)}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import hashlib\n\ntext = \"Hello, World!\"\nmd5 = hashlib.md5(text.encode()).hexdigest()\nsha256 = hashlib.sha256(text.encode()).hexdigest()\nprint(f\"MD5 length: {len(md5)}\")\nprint(f\"SHA256 length: {len(sha256)}\")\nprint(md5[:8])",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `MD5 length: 32`, `SHA256 length: 64`, `65a8e27d` **Explanation:** MD5 produces 128-bit (32 hex chars), SHA-256 produces 256-bit (64 hex chars) hash."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "19",
   "q": "A dataclass — output of `asdict(p)`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from dataclasses import dataclass, asdict, astuple\n\n@dataclass(frozen=True)\nclass Point:\n    x: float\n    y: float\n\np = Point(3.0, 4.0)\nprint(asdict(p))\nprint(astuple(p))\n\ntry:\n    p.x = 5.0\nexcept AttributeError:\n    print(\"Can't modify frozen dataclass\")\n\nprint(hash(p))  # Works because frozen=True",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `{'x': 3.0, 'y': 4.0}`, `(3.0, 4.0)`, `Can't modify frozen dataclass`, `<hash value>` **Explanation:** `frozen=True` makes dataclass immutable and hashable."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "20",
   "q": "__eq__ and __hash__ — output of `v1 < v2`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from functools import total_ordering\n\n@total_ordering\nclass Version:\n    def __init__(self, major, minor):\n        self.major = major\n        self.minor = minor\n    \n    def __eq__(self, other):\n        return (self.major, self.minor) == (other.major, other.minor)\n    \n    def __lt__(self, other):\n        return (self.major, self.minor) < (other.major, other.minor)\n\nv1 = Version(1, 0)\nv2 = Version(2, 0)\nv3 = Version(1, 5)\n\nprint(v1 < v2)\nprint(v1 > v3)\nprint(v2 >= v3)\nprint(sorted([v2, v1, v3], key=lambda v: (v.major, v.minor)))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `True`, `False`, `True`, `[<Version 1.0>, <Version 1.5>, <Version 2.0>]` (repr may vary) **Explanation:** `total_ordering` generates missing comparison methods from `__eq__` and `__lt__`."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "21",
   "q": "A ChainMap — output of `config['theme']`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from collections import ChainMap\n\ndefaults = {'theme': 'dark', 'font': 'mono', 'size': 12}\nuser_prefs = {'theme': 'light', 'size': 14}\n\nconfig = ChainMap(user_prefs, defaults)\nprint(config['theme'])\nprint(config['font'])\nprint(config['size'])\nprint(list(config.keys()))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `light`, `mono`, `14`, `['theme', 'font', 'size']` **Explanation:** `ChainMap` searches maps in order. First map's values take priority."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "22",
   "q": "Stripping whitespace — output of `\"world\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from textwrap import dedent, indent\n\ncode = dedent(\"\"\"\n    def hello():\n        print(\"world\")\n    hello()\n\"\"\").strip()\n\nindented = indent(code, \"    \")\nprint(indented)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "    def hello():\n        print(\"world\")\n    hello()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Explanation:** `dedent` removes common leading whitespace. `indent` adds prefix to each line."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "23",
   "q": "An itertools recipe — output of `list(takewhile(lambda x: x < 5, data))`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from itertools import takewhile, dropwhile\n\ndata = [1, 3, 5, 2, 4, 6, 7]\nprint(list(takewhile(lambda x: x < 5, data)))\nprint(list(dropwhile(lambda x: x < 5, data)))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `[1, 3]`, `[5, 2, 4, 6, 7]` **Explanation:** `takewhile` takes while condition True. `dropwhile` drops while condition True."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "24",
   "q": "An Enum — output of `user_perms = Permission.READ | Permission.WRI…`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from enum import Enum, Flag, auto\n\nclass Permission(Flag):\n    READ = auto()\n    WRITE = auto()\n    EXECUTE = auto()\n\nuser_perms = Permission.READ | Permission.WRITE\nprint(user_perms)\nprint(Permission.READ in user_perms)\nprint(Permission.EXECUTE in user_perms)\nprint(user_perms.value)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Permission.READ|WRITE`, `True`, `False`, `3` **Explanation:** `Flag` enum supports bitwise operations. `|` combines flags."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "25",
   "q": "What is the output of `id1 == id2`?",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from uuid import uuid4, uuid5, NAMESPACE_DNS\n\nid1 = uuid4()\nid2 = uuid4()\nprint(id1 == id2)\nprint(len(str(id1)))\n\nname_based = uuid5(NAMESPACE_DNS, \"example.com\")\nname_based2 = uuid5(NAMESPACE_DNS, \"example.com\")\nprint(name_based == name_based2)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `False`, `36`, `True` **Explanation:** `uuid4` generates random UUIDs. `uuid5` generates deterministic UUIDs from names."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "26",
   "q": "An itertools recipe — output of `list(starmap(pow, pairs))`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from itertools import starmap, zip_longest\n\npairs = [(2, 3), (4, 5), (6, 7)]\nprint(list(starmap(pow, pairs)))\n\na = [1, 2, 3]\nb = [4, 5]\nprint(list(zip_longest(a, b, fillvalue=0)))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `[8, 1024, 279936]`, `[(1, 4), (2, 5), (3, 0)]` **Explanation:** `starmap` unpacks tuples as arguments. `zip_longest` pads shorter iterables."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "27",
   "q": "functools.singledispatch — output of `process(5)`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from functools import singledispatch\n\n@singledispatch\ndef process(data):\n    return f\"Unknown: {data}\"\n\n@process.register(int)\ndef _(data):\n    return f\"Integer: {data * 2}\"\n\n@process.register(str)\ndef _(data):\n    return f\"String: {data.upper()}\"\n\n@process.register(list)\ndef _(data):\n    return f\"List: {len(data)} items\"\n\nprint(process(5))\nprint(process(\"hello\"))\nprint(process([1, 2, 3]))\nprint(process(3.14))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Integer: 10`, `String: HELLO`, `List: 3 items`, `Unknown: 3.14` **Explanation:** `singledispatch` dispatches to specialized functions based on first argument type."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "28",
   "q": "A namedtuple — output of `Point = namedtuple('Point', ['x', 'y'], defau…`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from collections import namedtuple\n\nPoint = namedtuple('Point', ['x', 'y'], defaults=[0])\np1 = Point(1)\np2 = Point(3, 4)\np3 = p2._replace(x=10)\n\nprint(p1)\nprint(p2)\nprint(p3)\nprint(p2._asdict())",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Point(x=1, y=0)`, `Point(x=3, y=4)`, `Point(x=10, y=4)`, `{'x': 3, 'y': 4}` **Explanation:** `defaults` applies to rightmost fields. `_replace` creates modified copy. `_asdict` converts to dict."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "29",
   "q": "A decorator using functools.wraps — output of `f\"{func.__name__}: {elapsed:.6f}s\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from time import perf_counter\nfrom functools import wraps\n\ndef timer(func):\n    @wraps(func)\n    def wrapper(*args, **kwargs):\n        start = perf_counter()\n        result = func(*args, **kwargs)\n        elapsed = perf_counter() - start\n        print(f\"{func.__name__}: {elapsed:.6f}s\")\n        return result\n    return wrapper\n\n@timer\ndef slow_sum(n):\n    \"\"\"Sum first n numbers\"\"\"\n    return sum(range(n))\n\nresult = slow_sum(1000000)\nprint(f\"Result: {result}\")\nprint(f\"Name: {slow_sum.__name__}\")\nprint(f\"Doc: {slow_sum.__doc__}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `slow_sum: <time>s`, `Result: 499999500000`, `Name: slow_sum`, `Doc: Sum first n numbers` **Explanation:** `@wraps` preserves original function's metadata (name, docstring)."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "30",
   "q": "A dataclass — output of `_tags: list = field(default_factory=list, rep…`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from dataclasses import dataclass, field\nfrom typing import ClassVar\n\n@dataclass\nclass Config:\n    name: str\n    debug: bool = False\n    _instance_count: ClassVar[int] = 0\n    _tags: list = field(default_factory=list, repr=False)\n    \n    def __post_init__(self):\n        Config._instance_count += 1\n\nc1 = Config(\"prod\")\nc2 = Config(\"dev\", debug=True)\nprint(c1)\nprint(c2)\nprint(Config._instance_count)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Config(name='prod', debug=False)`, `Config(name='dev', debug=True)`, `2` **Explanation:** `ClassVar` excluded from **init**. `repr=False` hides field. `__post_init__` runs after init."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  }
 ],
 "takeaways": [],
 "_drills": 30,
 "_code": 32
});
