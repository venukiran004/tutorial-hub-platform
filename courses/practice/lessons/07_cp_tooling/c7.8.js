/* ============================================================================
   PRACTICE C7.8 — Testing
   ----------------------------------------------------------------------------
   Imported from the Coding Practice source set and rendered through the
   Tutorial Hub design system. Generated by .build/import-practice.py —
   edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "c7.8",
 "lede": "**26 problems** from the modules, regex, testing and tooling set. Each answer is hidden until you ask for it — attempt it first, because reading a solution feels like learning and is not.",
 "objectives": [
  "Work through 26 problems on testing without looking anything up",
  "Predict each answer before revealing it",
  "Recognise the pattern each problem belongs to",
  "Explain your solution out loud, including its complexity"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer before you reveal.** Even a wrong attempt makes the answer stick; reading first does not.",
      "**Say the complexity out loud** where the problem has one. Getting it wrong is the useful part.",
      "Use the bar above to open or close everything at once, and **Ctrl K** to jump to another set."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "Program 104: Unit testing",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nUnit tests verify individual units (functions, methods, classes) in isolation.\nThey are fast, focused, and form the base of the testing pyramid.\n\nPython's unittest module provides TestCase base class with assertion methods.\npytest is the most popular alternative with simpler syntax.\n\nKey principles:\n- Test one thing per test\n- Tests should be independent\n- Use AAA pattern: Arrange, Act, Assert\n- Mock external dependencies\n\"\"\"\nimport unittest\n\ndef add(a, b):\n    return a + b\n\ndef divide(a, b):\n    if b == 0:\n        raise ValueError(\"Cannot divide by zero\")\n    return a / b\n\nclass TestMathFunctions(unittest.TestCase):\n    def test_add(self):\n        self.assertEqual(add(2, 3), 5)\n        self.assertEqual(add(-1, 1), 0)\n\n    def test_divide(self):\n        self.assertEqual(divide(10, 2), 5.0)\n\n    def test_divide_by_zero(self):\n        with self.assertRaises(ValueError):\n            divide(10, 0)\n\nprint(\"Q351: Unit testing\")\nsuite = unittest.TestLoader().loadTestsFromTestCase(TestMathFunctions)\nrunner = unittest.TextTestRunner(verbosity=0)\nresult = runner.run(suite)\nprint(f\"  Tests run: {result.testsRun}, Failures: {len(result.failures)}\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q351: Unit testing\n  Tests run: 3, Failures: 0",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "2",
   "q": "Program 105: Integration testing",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nIntegration tests verify that multiple components work together correctly.\nThey test the interactions between units, databases, APIs, etc.\n\nCompared to unit tests:\n- Slower (involve real components)\n- Test boundaries between systems\n- May need test databases, mock servers\n- Fewer in number (testing pyramid)\n\nExamples: testing API endpoints with a test database,\ntesting service-to-service communication.\n\"\"\"\n\nclass UserRepository:\n    def __init__(self):\n        self._db = {}\n        self._next_id = 1\n\n    def create(self, name):\n        uid = self._next_id\n        self._next_id += 1\n        self._db[uid] = {\"id\": uid, \"name\": name}\n        return self._db[uid]\n\n    def get(self, uid):\n        return self._db.get(uid)\n\nclass UserService:\n    def __init__(self, repo):\n        self.repo = repo\n\n    def register(self, name):\n        if not name:\n            raise ValueError(\"Name required\")\n        return self.repo.create(name)\n\nprint(\"Q352: Integration testing\")\n# Integration test: Service + Repository together\nrepo = UserRepository()\nservice = UserService(repo)\nuser = service.register(\"Alice\")\nfetched = repo.get(user[\"id\"])\nassert fetched[\"name\"] == \"Alice\", \"Integration test failed\"\nprint(f\"  Integration test passed: {fetched}\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q352: Integration testing\n  Integration test passed: {'id': 1, 'name': 'Alice'}",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "3",
   "q": "Program 106: Functional testing",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nFunctional tests verify that the system meets functional requirements from\nthe user's perspective. They test complete features/workflows without\nconcern for internal implementation.\n\nCharacteristics:\n- Black-box testing (test inputs/outputs)\n- Test complete user scenarios\n- Often use real or near-real environments\n- Tools: pytest, behave (BDD), robot framework\n\"\"\"\n\nclass Calculator:\n    def __init__(self):\n        self.history = []\n\n    def calculate(self, expression):\n        result = eval(expression)  # simplified\n        self.history.append((expression, result))\n        return result\n\n    def get_history(self):\n        return self.history\n\nprint(\"Q353: Functional testing\")\ncalc = Calculator()\n# Functional test: complete workflow\nassert calc.calculate(\"2 + 3\") == 5\nassert calc.calculate(\"10 * 2\") == 20\nassert len(calc.get_history()) == 2\nprint(\"  Functional test: Calculator workflow passed\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q353: Functional testing\n  Functional test: Calculator workflow passed",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "4",
   "q": "Program 107: End-to-end testing",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nEnd-to-end (E2E) tests verify the entire system from start to finish,\nsimulating real user scenarios through the full stack.\n\nFor web apps:\n- Selenium: browser automation\n- Playwright: modern browser automation\n- Cypress: JavaScript-based\n\nFor APIs:\n- requests + pytest\n- httpx for async testing\n- Postman/Newman for CI\n\nE2E tests are slowest but provide highest confidence.\n\"\"\"\n\nprint(\"Q354: End-to-end testing\")\nprint(\"  # API E2E test example:\")\nprint(\"  def test_user_workflow():\")\nprint(\"      # Create user\")\nprint(\"      resp = client.post('/users', json={'name': 'Alice'})\")\nprint(\"      assert resp.status_code == 201\")\nprint(\"      user_id = resp.json()['id']\")\nprint(\"      # Verify user exists\")\nprint(\"      resp = client.get(f'/users/{user_id}')\")\nprint(\"      assert resp.json()['name'] == 'Alice'\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q354: End-to-end testing\n  # API E2E test example:\n  def test_user_workflow():\n      # Create user\n      resp = client.post('/users', json={'name': 'Alice'})\n      assert resp.status_code == 201\n      user_id = resp.json()['id']\n      # Verify user exists\n      resp = client.get(f'/users/{user_id}')\n      assert resp.json()['name'] == 'Alice'",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "5",
   "q": "Program 108: Pytest fixtures",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nPytest fixtures provide reusable test setup/teardown. They replace\nunittest's setUp/tearDown with a more flexible, composable system.\n\nKey features:\n- @pytest.fixture decorator\n- Dependency injection (fixtures as parameters)\n- Scopes: function, class, module, session\n- yield fixtures for setup + teardown\n- conftest.py for shared fixtures\n- Parametrized fixtures\n\"\"\"\n\nprint(\"Q355: Pytest fixtures\")\nprint(\"  @pytest.fixture\")\nprint(\"  def db_session():\")\nprint(\"      session = create_session()\")\nprint(\"      yield session  # Test runs here\")\nprint(\"      session.rollback()\")\nprint(\"      session.close()\")\nprint(\"\")\nprint(\"  @pytest.fixture(scope='module')\")\nprint(\"  def app():\")\nprint(\"      return create_app(testing=True)\")\nprint(\"\")\nprint(\"  def test_create_user(db_session):\")\nprint(\"      user = User(name='Alice')\")\nprint(\"      db_session.add(user)\")\nprint(\"      assert user.name == 'Alice'\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q355: Pytest fixtures\n  @pytest.fixture\n  def db_session():\n      session = create_session()\n      yield session  # Test runs here\n      session.rollback()\n      session.close()\n\n  @pytest.fixture(scope='module')\n  def app():\n      return create_app(testing=True)\n\n  def test_create_user(db_session):\n      user = User(name='Alice')\n      db_session.add(user)\n      assert user.name == 'Alice'",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "6",
   "q": "Program 109: Parametrized tests",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nParametrized tests run the same test with different inputs, reducing code\nduplication. pytest.mark.parametrize decorates test functions with multiple\ninput/expected-output pairs.\n\nSyntax: @pytest.mark.parametrize(\"arg1,arg2,expected\", [(1,2,3), (4,5,9)])\n\"\"\"\n\n# Manual parametrize demo (without pytest runner)\ndef run_parametrized_tests():\n    test_cases = [\n        (2, 3, 5),\n        (-1, 1, 0),\n        (0, 0, 0),\n        (100, 200, 300),\n    ]\n    for a, b, expected in test_cases:\n        result = add(a, b)\n        status = \"PASS\" if result == expected else \"FAIL\"\n        print(f\"  add({a}, {b}) = {result}, expected {expected}: {status}\")\n\nprint(\"Q356: Parametrized tests\")\nrun_parametrized_tests()\nprint(\"  # pytest syntax: @pytest.mark.parametrize('a,b,expected', [(2,3,5)])\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q356: Parametrized tests\n  add(2, 3) = 5, expected 5: PASS\n  add(-1, 1) = 0, expected 0: PASS\n  add(0, 0) = 0, expected 0: PASS\n  add(100, 200) = 300, expected 300: PASS\n  # pytest syntax: @pytest.mark.parametrize('a,b,expected', [(2,3,5)])",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "7",
   "q": "Program 110: Mocking",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nMocking replaces real objects with fake ones during testing. This isolates\nthe unit under test from its dependencies.\n\nunittest.mock provides:\n- Mock(): general purpose mock object\n- MagicMock(): Mock with magic methods pre-configured\n- patch(): temporarily replace objects\n- PropertyMock: mock properties\n\nMocks record calls and can be configured to return specific values.\n\"\"\"\nfrom unittest.mock import Mock, MagicMock, patch\n\nprint(\"Q357: Mocking\")\n# Mock an API client\napi_client = Mock()\napi_client.get_user.return_value = {\"id\": 1, \"name\": \"Alice\"}\n\nresult = api_client.get_user(1)\nprint(f\"  Mock result: {result}\")\napi_client.get_user.assert_called_once_with(1)\nprint(f\"  Called with correct args: True\")\n\n# MagicMock supports magic methods\nmock_list = MagicMock()\nmock_list.__len__.return_value = 5\nprint(f\"  MagicMock len: {len(mock_list)}\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q357: Mocking\n  Mock result: {'id': 1, 'name': 'Alice'}\n  Called with correct args: True\n  MagicMock len: 5",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "8",
   "q": "Program 111: Monkey patching",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nMonkey patching is dynamically modifying code at runtime. In testing,\nit's used to replace functions/methods/attributes temporarily.\n\npytest provides monkeypatch fixture for safe monkey patching that\nautomatically reverts changes after the test.\n\nUse cases: mock environment variables, override functions, change\nclass attributes, modify dictionaries.\n\"\"\"\nimport os\n\nprint(\"Q358: Monkey patching\")\n# Manual monkey patching\noriginal_getcwd = os.getcwd\n\ndef fake_getcwd():\n    return \"/fake/path\"\n\nos.getcwd = fake_getcwd\nprint(f\"  Patched os.getcwd(): {os.getcwd()}\")\nos.getcwd = original_getcwd  # Restore\nprint(f\"  Restored os.getcwd(): {os.getcwd()}\")\nprint(\"  # pytest: monkeypatch.setattr(os, 'getcwd', fake_getcwd)\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q358: Monkey patching\n  Patched os.getcwd(): /fake/path\n  Restored os.getcwd(): C:\\Users\\venuk\\AppData\\Local\\Temp\\q_jlpmgzb5\n  # pytest: monkeypatch.setattr(os, 'getcwd', fake_getcwd)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "9",
   "q": "Program 112: Patch decorators",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nunittest.mock.patch() can be used as a decorator, context manager, or\nmanual start/stop. It temporarily replaces the target with a Mock.\n\nImportant: Patch where the object is USED, not where it's defined.\nExample: patch('mymodule.requests.get') not patch('requests.get')\n\nVariants:\n- @patch('module.Class')\n- @patch.object(Class, 'method')\n- @patch.dict(os.environ, {'KEY': 'value'})\n\"\"\"\n\ndef get_data():\n    \"\"\"Function that would normally call an API.\"\"\"\n    import json\n    return {\"data\": \"real\"}\n\nprint(\"Q359: Patch decorators\")\nwith patch.dict(os.environ, {\"API_KEY\": \"test-key-123\"}):\n    print(f\"  Patched env: API_KEY={os.environ['API_KEY']}\")\nprint(f\"  Restored env: API_KEY={os.environ.get('API_KEY', 'not set')}\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q359: Patch decorators\n  Patched env: API_KEY=test-key-123\n  Restored env: API_KEY=not set",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "10",
   "q": "Program 113: Test doubles",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nTest doubles are objects that stand in for real objects in tests.\nTypes (from Martin Fowler):\n\n1. Dummy: passed but never used (fill parameters)\n2. Stub: provides canned responses\n3. Spy: records calls for later verification\n4. Mock: pre-programmed with expectations\n5. Fake: working implementation (e.g., in-memory DB)\n\nChoose the right type based on what you're testing.\n\"\"\"\n\nprint(\"Q360: Test doubles\")\n\n# Stub\nclass StubEmailService:\n    def send(self, to, subject, body):\n        return True  # Always succeeds\n\n# Fake\nclass FakeDatabase:\n    def __init__(self):\n        self._data = {}\n    def save(self, key, value):\n        self._data[key] = value\n    def get(self, key):\n        return self._data.get(key)\n\n# Spy\nclass SpyLogger:\n    def __init__(self):\n        self.logs = []\n    def log(self, message):\n        self.logs.append(message)\n\nspy = SpyLogger()\nspy.log(\"test message\")\nprint(f\"  Spy recorded: {spy.logs}\")\n\nfake_db = FakeDatabase()\nfake_db.save(\"user:1\", {\"name\": \"Alice\"})\nprint(f\"  Fake DB: {fake_db.get('user:1')}\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q360: Test doubles\n  Spy recorded: ['test message']\n  Fake DB: {'name': 'Alice'}",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "11",
   "q": "Program 114: Stubs",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nStubs provide predetermined responses to calls made during tests. They don't\nrespond to anything outside what's programmed for the test.\n\nStubs are simpler than mocks - they just return values without verifying\nhow they were called.\n\nUse stubs when you need to control indirect inputs to the system under test.\n\"\"\"\n\nclass PaymentGatewayStub:\n    \"\"\"Stub that always returns success.\"\"\"\n    def charge(self, amount, card_token):\n        return {\"status\": \"success\", \"transaction_id\": \"stub-txn-001\"}\n\n    def refund(self, transaction_id):\n        return {\"status\": \"refunded\"}\n\nclass OrderService:\n    def __init__(self, payment_gateway):\n        self.gateway = payment_gateway\n\n    def place_order(self, amount, card):\n        result = self.gateway.charge(amount, card)\n        return result[\"status\"] == \"success\"\n\nprint(\"Q361: Stubs\")\nstub_gateway = PaymentGatewayStub()\norder_service = OrderService(stub_gateway)\nsuccess = order_service.place_order(99.99, \"tok_test\")\nprint(f\"  Order placed with stub gateway: {success}\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q361: Stubs\n  Order placed with stub gateway: True",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "12",
   "q": "Program 115: Fakes",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nFakes are working implementations with shortcuts, not suitable for production.\nCommon examples:\n- In-memory database instead of real DB\n- Fake file system\n- Fake HTTP server\n- In-memory message queue\n\nFakes are useful when you need realistic behavior without infrastructure.\n\"\"\"\n\nclass FakeCache:\n    \"\"\"Fake cache implementation (in-memory instead of Redis).\"\"\"\n    def __init__(self):\n        self._store = {}\n        self._ttls = {}\n\n    def set(self, key, value, ttl=None):\n        self._store[key] = value\n        if ttl:\n            self._ttls[key] = ttl\n\n    def get(self, key):\n        return self._store.get(key)\n\n    def delete(self, key):\n        self._store.pop(key, None)\n\n    def exists(self, key):\n        return key in self._store\n\nprint(\"Q362: Fakes\")\ncache = FakeCache()\ncache.set(\"session:123\", {\"user\": \"Alice\"}, ttl=3600)\nprint(f\"  Fake cache get: {cache.get('session:123')}\")\nprint(f\"  Fake cache exists: {cache.exists('session:123')}\")\ncache.delete(\"session:123\")\nprint(f\"  After delete: {cache.get('session:123')}\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q362: Fakes\n  Fake cache get: {'user': 'Alice'}\n  Fake cache exists: True\n  After delete: None",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "13",
   "q": "Program 116: Dependency injection for testing",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nDependency injection (DI) makes code testable by injecting dependencies\nrather than creating them internally. This allows swapping real\nimplementations with test doubles.\n\nPatterns:\n1. Constructor injection: pass dependencies in __init__\n2. Method injection: pass dependencies as parameters\n3. Property injection: set after construction\n\nDI frameworks: dependency-injector, inject, python-inject\n\"\"\"\n\nclass EmailService:\n    def send(self, to, message):\n        raise NotImplementedError(\"Real email service\")\n\nclass FakeEmailService:\n    def __init__(self):\n        self.sent = []\n    def send(self, to, message):\n        self.sent.append((to, message))\n        return True\n\nclass NotificationService:\n    def __init__(self, email_service):  # Constructor injection\n        self.email = email_service\n\n    def notify_user(self, user_email, message):\n        return self.email.send(user_email, message)\n\nprint(\"Q363: Dependency injection for testing\")\nfake_email = FakeEmailService()\nnotifier = NotificationService(fake_email)  # Inject fake\nnotifier.notify_user(\"alice@test.com\", \"Hello!\")\nprint(f\"  Emails sent via fake: {fake_email.sent}\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q363: Dependency injection for testing\n  Emails sent via fake: [('alice@test.com', 'Hello!')]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "14",
   "q": "Program 117: Coverage reports",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nCode coverage measures what percentage of code is executed during tests.\n\nTools:\n- coverage.py: standard Python coverage tool\n- pytest-cov: pytest plugin for coverage.py\n\nCommands:\n- pytest --cov=mypackage --cov-report=html\n- coverage run -m pytest\n- coverage report\n- coverage html\n\nCoverage types: line, branch, condition, path\nTarget: 80-90% (100% is often impractical)\n\"\"\"\n\nprint(\"Q364: Coverage reports\")\nprint(\"  # Run with coverage:\")\nprint(\"  pytest --cov=src --cov-report=html --cov-report=term\")\nprint(\"  # Or:\")\nprint(\"  coverage run -m pytest\")\nprint(\"  coverage report --show-missing\")\nprint(\"  coverage html  # generates htmlcov/index.html\")\nprint(\"  # .coveragerc config:\")\nprint(\"  [run]\")\nprint(\"  source = src\")\nprint(\"  omit = tests/*, */migrations/*\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q364: Coverage reports\n  # Run with coverage:\n  pytest --cov=src --cov-report=html --cov-report=term\n  # Or:\n  coverage run -m pytest\n  coverage report --show-missing\n  coverage html  # generates htmlcov/index.html\n  # .coveragerc config:\n  [run]\n  source = src\n  omit = tests/*, */migrations/*",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "15",
   "q": "Program 118: Property-based testing",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nProperty-based testing generates random inputs to verify properties that\nshould always hold true. Instead of specific examples, you define properties.\n\nExample properties:\n- reverse(reverse(list)) == list\n- sorted list first element <= last element\n- encode(decode(x)) == x\n\nThis finds edge cases you wouldn't think of manually.\nHypothesis is the main Python library for this.\n\"\"\"\n\nimport random\n\ndef property_test_reverse():\n    \"\"\"Property: reversing twice gives original.\"\"\"\n    for _ in range(100):\n        lst = [random.randint(-100, 100) for _ in range(random.randint(0, 20))]\n        assert list(reversed(list(reversed(lst)))) == lst\n    return True\n\ndef property_test_sort():\n    \"\"\"Property: sorted list is ordered.\"\"\"\n    for _ in range(100):\n        lst = [random.randint(-100, 100) for _ in range(random.randint(0, 20))]\n        s = sorted(lst)\n        for i in range(len(s) - 1):\n            assert s[i] <= s[i + 1]\n    return True\n\nprint(\"Q365: Property-based testing\")\nprint(f\"  Reverse property: {property_test_reverse()}\")\nprint(f\"  Sort property: {property_test_sort()}\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q365: Property-based testing\n  Reverse property: True\n  Sort property: True",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "16",
   "q": "Program 119: Hypothesis library",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nHypothesis is a property-based testing library that generates test cases\nautomatically. It finds edge cases and shrinks failing examples to minimal\nreproductions.\n\nKey concepts:\n- @given(st.integers(), st.text()): generate random inputs\n- Strategies: integers, text, lists, dictionaries, composite\n- Shrinking: finds smallest failing input\n- Database: remembers failing cases\n- Settings: max_examples, deadline, database\n\"\"\"\n\nprint(\"Q366: Hypothesis library\")\nprint(\"  from hypothesis import given, strategies as st\")\nprint(\"\")\nprint(\"  @given(st.lists(st.integers()))\")\nprint(\"  def test_sort_idempotent(lst):\")\nprint(\"      assert sorted(sorted(lst)) == sorted(lst)\")\nprint(\"\")\nprint(\"  @given(st.text(), st.text())\")\nprint(\"  def test_concat_length(a, b):\")\nprint(\"      assert len(a + b) == len(a) + len(b)\")\nprint(\"\")\nprint(\"  # Composite strategies:\")\nprint(\"  @st.composite\")\nprint(\"  def user_strategy(draw):\")\nprint(\"      name = draw(st.text(min_size=1, max_size=50))\")\nprint(\"      age = draw(st.integers(min_value=0, max_value=150))\")\nprint(\"      return {'name': name, 'age': age}\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q366: Hypothesis library\n  from hypothesis import given, strategies as st\n\n  @given(st.lists(st.integers()))\n  def test_sort_idempotent(lst):\n      assert sorted(sorted(lst)) == sorted(lst)\n\n  @given(st.text(), st.text())\n  def test_concat_length(a, b):\n      assert len(a + b) == len(a) + len(b)\n\n  # Composite strategies:\n  @st.composite\n  def user_strategy(draw):\n      name = draw(st.text(min_size=1, max_size=50))\n      age = draw(st.integers(min_value=0, max_value=150))\n      return {'name': name, 'age': age}",
     "numbered": false
    }
   ]
  },
  {
   "t": "h2",
   "n": "",
   "text": "Logging"
  },
  {
   "t": "drill",
   "n": "17",
   "q": "Program 120: Logging module",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nPython's logging module provides a flexible framework for emitting log messages.\n\nLog levels (increasing severity):\n- DEBUG (10): detailed diagnostic info\n- INFO (20): confirmation things work\n- WARNING (30): something unexpected (default level)\n- ERROR (40): serious problem\n- CRITICAL (50): program may not continue\n\nComponents: Logger, Handler, Formatter, Filter\n\"\"\"\nimport logging\n\n# Create logger\nlogger = logging.getLogger(\"Q367\")\nlogger.setLevel(logging.DEBUG)\nhandler = logging.StreamHandler()\nhandler.setFormatter(logging.Formatter(\"  %(levelname)s - %(name)s - %(message)s\"))\nlogger.addHandler(handler)\n\nprint(\"Q367: Logging module\")\nlogger.debug(\"Debug message\")\nlogger.info(\"Info message\")\nlogger.warning(\"Warning message\")\nlogger.error(\"Error message\")\nlogger.handlers.clear()\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q367: Logging module",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "18",
   "q": "Program 121: Logging handlers",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nHandlers send log records to appropriate destinations:\n\n- StreamHandler: console output (stdout/stderr)\n- FileHandler: write to file\n- RotatingFileHandler: rotate by file size\n- TimedRotatingFileHandler: rotate by time\n- SocketHandler: send to network socket\n- SMTPHandler: send via email\n- SysLogHandler: send to syslog\n- QueueHandler: send to queue (thread-safe)\n- NullHandler: discard (for library authors)\n\nMultiple handlers can be attached to one logger.\n\"\"\"\n\nprint(\"Q368: Logging handlers\")\nprint(\"  # Multiple handlers example:\")\nprint(\"  logger = logging.getLogger('myapp')\")\nprint(\"  logger.addHandler(logging.StreamHandler())  # Console\")\nprint(\"  logger.addHandler(logging.FileHandler('app.log'))  # File\")\nprint(\"  logger.addHandler(RotatingFileHandler('app.log', maxBytes=1MB))\")\nprint(\"  logger.addHandler(SMTPHandler(mailhost, fromaddr, toaddrs, subject))\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q368: Logging handlers\n  # Multiple handlers example:\n  logger = logging.getLogger('myapp')\n  logger.addHandler(logging.StreamHandler())  # Console\n  logger.addHandler(logging.FileHandler('app.log'))  # File\n  logger.addHandler(RotatingFileHandler('app.log', maxBytes=1MB))\n  logger.addHandler(SMTPHandler(mailhost, fromaddr, toaddrs, subject))",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "19",
   "q": "Program 122: Logging formatters",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nFormatters define the layout of log records. Common format attributes:\n\n%(asctime)s       - Human-readable time\n%(created)f       - Time as float\n%(levelname)s     - Level name (DEBUG, INFO, etc.)\n%(name)s          - Logger name\n%(module)s        - Module name\n%(funcName)s      - Function name\n%(lineno)d        - Line number\n%(message)s       - Log message\n%(process)d       - Process ID\n%(thread)d        - Thread ID\n%(threadName)s    - Thread name\n\"\"\"\n\nprint(\"Q369: Logging formatters\")\nfmt = logging.Formatter(\n    \"  %(asctime)s [%(levelname)s] %(name)s:%(lineno)d - %(message)s\",\n    datefmt=\"%Y-%m-%d %H:%M:%S\"\n)\ntemp_handler = logging.StreamHandler()\ntemp_handler.setFormatter(fmt)\ntemp_logger = logging.getLogger(\"Q369_demo\")\ntemp_logger.addHandler(temp_handler)\ntemp_logger.setLevel(logging.INFO)\ntemp_logger.info(\"Formatted log message\")\ntemp_logger.handlers.clear()\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q369: Logging formatters",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "20",
   "q": "Program 123: Rotating logs",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nRotating logs prevent log files from growing indefinitely.\n\nRotatingFileHandler: rotates when file reaches a size limit\n- maxBytes: maximum file size before rotation\n- backupCount: number of backup files to keep\n\nTimedRotatingFileHandler: rotates based on time intervals\n- when: 'S' (seconds), 'M' (minutes), 'H' (hours), 'D' (days),\n        'midnight', 'W0'-'W6' (weekday)\n- interval: rotation interval\n- backupCount: number of backups\n\"\"\"\n\nprint(\"Q370: Rotating logs\")\nprint(\"  from logging.handlers import RotatingFileHandler, TimedRotatingFileHandler\")\nprint(\"\")\nprint(\"  # Size-based rotation:\")\nprint(\"  handler = RotatingFileHandler('app.log', maxBytes=5*1024*1024, backupCount=5)\")\nprint(\"  # Creates: app.log, app.log.1, app.log.2, ... app.log.5\")\nprint(\"\")\nprint(\"  # Time-based rotation:\")\nprint(\"  handler = TimedRotatingFileHandler('app.log', when='midnight', backupCount=30)\")\nprint(\"  # Rotates daily, keeps 30 days\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q370: Rotating logs\n  from logging.handlers import RotatingFileHandler, TimedRotatingFileHandler\n\n  # Size-based rotation:\n  handler = RotatingFileHandler('app.log', maxBytes=5*1024*1024, backupCount=5)\n  # Creates: app.log, app.log.1, app.log.2, ... app.log.5\n\n  # Time-based rotation:\n  handler = TimedRotatingFileHandler('app.log', when='midnight', backupCount=30)\n  # Rotates daily, keeps 30 days",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "21",
   "q": "Program 124: Structured logging",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nStructured logging outputs logs in a machine-parseable format (JSON) instead\nof plain text. This makes logs easier to search, filter, and analyze in\nlog aggregation systems (ELK, Splunk, Datadog).\n\nBenefits:\n- Machine-parseable\n- Consistent format\n- Easy to add context (user_id, request_id)\n- Better for log aggregation\n\nLibraries: structlog, python-json-logger\n\"\"\"\nimport json\n\nclass StructuredLogger:\n    def __init__(self, name):\n        self.name = name\n\n    def log(self, level, message, **kwargs):\n        import time\n        record = {\n            \"timestamp\": time.strftime(\"%Y-%m-%dT%H:%M:%S\"),\n            \"level\": level,\n            \"logger\": self.name,\n            \"message\": message,\n            **kwargs\n        }\n        print(f\"  {json.dumps(record)}\")\n\nprint(\"Q371: Structured logging\")\nslog = StructuredLogger(\"myapp\")\nslog.log(\"INFO\", \"User logged in\", user_id=123, ip=\"192.168.1.1\")\nslog.log(\"ERROR\", \"Payment failed\", order_id=456, amount=99.99)\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q371: Structured logging\n  {\"timestamp\": \"2026-08-22T08:21:40\", \"level\": \"INFO\", \"logger\": \"myapp\", \"message\": \"User logged in\", \"user_id\": 123, \"ip\": \"192.168.1.1\"}\n  {\"timestamp\": \"2026-08-22T08:21:40\", \"level\": \"ERROR\", \"logger\": \"myapp\", \"message\": \"Payment failed\", \"order_id\": 456, \"amount\": 99.99}",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "22",
   "q": "Program 125: JSON logging",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nJSON logging formats each log entry as a JSON object. This integrates\nwell with log aggregation tools.\n\nUsing python-json-logger:\n  pip install python-json-logger\n\nOr build custom JSON formatter extending logging.Formatter.\n\"\"\"\n\nclass JsonFormatter(logging.Formatter):\n    def format(self, record):\n        log_data = {\n            \"timestamp\": self.formatTime(record),\n            \"level\": record.levelname,\n            \"logger\": record.name,\n            \"message\": record.getMessage(),\n        }\n        if record.exc_info:\n            log_data[\"exception\"] = self.formatException(record.exc_info)\n        return json.dumps(log_data)\n\nprint(\"Q372: JSON logging\")\njson_handler = logging.StreamHandler()\njson_handler.setFormatter(JsonFormatter())\njson_logger = logging.getLogger(\"Q372_json\")\njson_logger.addHandler(json_handler)\njson_logger.setLevel(logging.INFO)\njson_logger.info(\"Application started\")\njson_logger.handlers.clear()\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q372: JSON logging",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "23",
   "q": "Program 126: Correlation IDs",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nCorrelation IDs (request IDs) track a request across multiple services in\ndistributed systems. Each request gets a unique ID that's passed through\nall service calls and included in logs.\n\nImplementation:\n1. Generate UUID at entry point (API gateway/first service)\n2. Pass via HTTP headers (X-Request-ID, X-Correlation-ID)\n3. Store in context (contextvars, threading.local)\n4. Include in all log messages\n\"\"\"\nimport uuid\nimport contextvars\n\nrequest_id_var = contextvars.ContextVar('request_id', default='no-request')\n\nclass CorrelatedLogger:\n    def __init__(self, name):\n        self.name = name\n\n    def info(self, message, **kwargs):\n        rid = request_id_var.get()\n        print(f\"  [{rid}] INFO {self.name}: {message}\")\n\nprint(\"Q373: Correlation IDs\")\nclog = CorrelatedLogger(\"api\")\ntoken = request_id_var.set(str(uuid.uuid4())[:8])\nclog.info(\"Processing request\")\nclog.info(\"Fetching user data\")\nclog.info(\"Request completed\")\nrequest_id_var.reset(token)\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q373: Correlation IDs\n  [be35e14a] INFO api: Processing request\n  [be35e14a] INFO api: Fetching user data\n  [be35e14a] INFO api: Request completed",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "24",
   "q": "Program 127: Log aggregation",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nLog aggregation collects logs from multiple sources into a central system\nfor searching, analysis, and alerting.\n\nPopular stacks:\n- ELK: Elasticsearch + Logstash + Kibana\n- EFK: Elasticsearch + Fluentd + Kibana\n- Loki + Grafana: lightweight, label-based\n- Datadog: commercial SaaS\n- Splunk: enterprise\n- CloudWatch: AWS native\n\nPipeline: App -> Shipper -> Aggregator -> Storage -> UI\n\"\"\"\n\nprint(\"Q374: Log aggregation\")\nprint(\"  Common pipeline:\")\nprint(\"  App (JSON logs) -> Filebeat/Fluentd -> Elasticsearch -> Kibana\")\nprint(\"\")\nprint(\"  Best practices:\")\nprint(\"  1. Use structured/JSON logging\")\nprint(\"  2. Include correlation IDs\")\nprint(\"  3. Add context (service, version, environment)\")\nprint(\"  4. Set appropriate retention policies\")\nprint(\"  5. Create dashboards and alerts\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q374: Log aggregation\n  Common pipeline:\n  App (JSON logs) -> Filebeat/Fluentd -> Elasticsearch -> Kibana\n\n  Best practices:\n  1. Use structured/JSON logging\n  2. Include correlation IDs\n  3. Add context (service, version, environment)\n  4. Set appropriate retention policies\n  5. Create dashboards and alerts",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "25",
   "q": "Program 128: Exception logging",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nProperly logging exceptions preserves the full traceback for debugging.\n\nKey methods:\n- logger.exception(\"msg\"): logs ERROR with traceback (use in except block)\n- logger.error(\"msg\", exc_info=True): same as above\n- logger.error(\"msg\", exc_info=sys.exc_info()): explicit exc_info\n\nAlways log the full traceback, not just the error message.\n\"\"\"\n\nprint(\"Q375: Exception logging\")\nexc_logger = logging.getLogger(\"Q375\")\nexc_handler = logging.StreamHandler()\nexc_handler.setFormatter(logging.Formatter(\"  %(levelname)s: %(message)s\"))\nexc_logger.addHandler(exc_handler)\nexc_logger.setLevel(logging.ERROR)\n\ntry:\n    result = 1 / 0\nexcept ZeroDivisionError:\n    exc_logger.exception(\"Division failed\")  # Includes traceback\n\nexc_logger.handlers.clear()\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q375: Exception logging",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "26",
   "q": "Program 129: Custom log handlers",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nCustom handlers extend logging.Handler to send logs to custom destinations.\n\nOverride:\n- emit(record): called for each log record\n- flush(): flush any buffered output\n- close(): cleanup resources\n\nExamples: send to database, webhook, message queue, monitoring service.\n\"\"\"\n\nclass WebhookHandler(logging.Handler):\n    \"\"\"Custom handler that would send logs to a webhook.\"\"\"\n    def __init__(self, url):\n        super().__init__()\n        self.url = url\n        self.records = []  # Simulate sending\n\n    def emit(self, record):\n        log_entry = self.format(record)\n        self.records.append(log_entry)\n        # In production: requests.post(self.url, json={\"log\": log_entry})\n\nprint(\"Q376: Custom log handlers\")\nwh = WebhookHandler(\"https://hooks.example.com/logs\")\nwh.setFormatter(logging.Formatter(\"%(levelname)s: %(message)s\"))\ncustom_logger = logging.getLogger(\"Q376\")\ncustom_logger.addHandler(wh)\ncustom_logger.setLevel(logging.INFO)\ncustom_logger.info(\"Custom handler test\")\nprint(f\"  Webhook handler captured: {wh.records}\")\ncustom_logger.handlers.clear()\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q376: Custom log handlers\n  Webhook handler captured: ['INFO: Custom handler test']",
     "numbered": false
    }
   ]
  }
 ],
 "takeaways": [],
 "_drills": 26,
 "_code": 52
});
