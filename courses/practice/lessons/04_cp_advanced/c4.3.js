/* ============================================================================
   PRACTICE C4.3 — Programs · 3
   ----------------------------------------------------------------------------
   Imported from the Coding Practice source set and rendered through the
   Tutorial Hub design system. Generated by .build/import-practice.py —
   edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "c4.3",
 "lede": "**30 problems** from the decorators, generators and internals set. Each answer is hidden until you ask for it — attempt it first, because reading a solution feels like learning and is not.",
 "objectives": [
  "Work through 30 problems on programs without looking anything up",
  "Predict each answer before revealing it",
  "Recognise the pattern each problem belongs to",
  "Explain your solution out loud, including its complexity"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer before you reveal.** Even a wrong attempt makes the answer stick; reading first does not.",
      "**Say the complexity out loud** where the problem has one. Getting it wrong is the useful part.",
      "Use the bar above to open or close everything at once, and **Ctrl K** to jump to another set."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "Program 61: LRU Cache (OrderedDict)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from collections import OrderedDict\n\nclass LRUCache:\n    def __init__(self, capacity):\n        self.cache = OrderedDict()\n        self.cap = capacity\n    \n    def get(self, key):\n        if key in self.cache:\n            self.cache.move_to_end(key)\n            return self.cache[key]\n        return -1\n    \n    def put(self, key, val):\n        if key in self.cache:\n            self.cache.move_to_end(key)\n        self.cache[key] = val\n        if len(self.cache) > self.cap:\n            self.cache.popitem(last=False)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "2",
   "q": "Program 62: Min Stack",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class MinStack:\n    def __init__(self):\n        self.stack = []\n        self.min_stack = []\n    \n    def push(self, val):\n        self.stack.append(val)\n        if not self.min_stack or val <= self.min_stack[-1]:\n            self.min_stack.append(val)\n    \n    def pop(self):\n        val = self.stack.pop()\n        if val == self.min_stack[-1]:\n            self.min_stack.pop()\n        return val\n    \n    def get_min(self):\n        return self.min_stack[-1]\n\nms = MinStack()\nms.push(3); ms.push(1); ms.push(2)\nprint(ms.get_min())  # Output: 1\nms.pop()\nprint(ms.get_min())  # Output: 1",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "3",
   "q": "Program 63: Implement Queue Using Stacks",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class QueueUsingStacks:\n    def __init__(self):\n        self.in_stack = []\n        self.out_stack = []\n    \n    def enqueue(self, val):\n        self.in_stack.append(val)\n    \n    def dequeue(self):\n        if not self.out_stack:\n            while self.in_stack:\n                self.out_stack.append(self.in_stack.pop())\n        return self.out_stack.pop()\n\nq = QueueUsingStacks()\nq.enqueue(1); q.enqueue(2); q.enqueue(3)\nprint(q.dequeue())  # Output: 1\nprint(q.dequeue())  # Output: 2",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "4",
   "q": "Program 64: Design Twitter (Simplified)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from collections import defaultdict\nimport heapq\n\nclass Twitter:\n    def __init__(self):\n        self.tweets = defaultdict(list)\n        self.following = defaultdict(set)\n        self.time = 0\n    \n    def post(self, user, tweet_id):\n        self.tweets[user].append((self.time, tweet_id))\n        self.time += 1\n    \n    def get_feed(self, user, n=10):\n        candidates = list(self.tweets[user])\n        for followee in self.following[user]:\n            candidates.extend(self.tweets[followee])\n        candidates.sort(reverse=True)\n        return [tweet_id for _, tweet_id in candidates[:n]]\n    \n    def follow(self, follower, followee):\n        self.following[follower].add(followee)\n    \n    def unfollow(self, follower, followee):\n        self.following[follower].discard(followee)\n\ntw = Twitter()\ntw.post(1, 101); tw.post(2, 201); tw.follow(1, 2)\nprint(tw.get_feed(1))  # Output: [201, 101]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "5",
   "q": "Program 65: Evaluate Reverse Polish Notation",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def eval_rpn(tokens):\n    stack = []\n    ops = {'+': lambda a, b: a + b, '-': lambda a, b: a - b,\n           '*': lambda a, b: a * b, '/': lambda a, b: int(a / b)}\n    for token in tokens:\n        if token in ops:\n            b, a = stack.pop(), stack.pop()\n            stack.append(ops[token](a, b))\n        else:\n            stack.append(int(token))\n    return stack[0]\n\nprint(eval_rpn([\"2\",\"1\",\"+\",\"3\",\"*\"]))  # Output: 9",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "6",
   "q": "Program 66: Basic Calculator",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def calculate(s):\n    stack = []\n    num = 0\n    sign = 1\n    result = 0\n    for char in s:\n        if char.isdigit():\n            num = num * 10 + int(char)\n        elif char in '+-':\n            result += sign * num\n            num = 0\n            sign = 1 if char == '+' else -1\n        elif char == '(':\n            stack.append(result)\n            stack.append(sign)\n            result = 0\n            sign = 1\n        elif char == ')':\n            result += sign * num\n            num = 0\n            result *= stack.pop()\n            result += stack.pop()\n    return result + sign * num\n\nprint(calculate(\"(1+(4+5+2)-3)+(6+8)\"))  # Output: 23",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "7",
   "q": "Program 67: Implement Trie with Autocomplete",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class TrieNode:\n    def __init__(self):\n        self.children = {}\n        self.is_word = False\n        self.count = 0\n\nclass AutocompleteTrie:\n    def __init__(self):\n        self.root = TrieNode()\n    \n    def insert(self, word, count=1):\n        node = self.root\n        for char in word:\n            if char not in node.children:\n                node.children[char] = TrieNode()\n            node = node.children[char]\n        node.is_word = True\n        node.count += count\n    \n    def autocomplete(self, prefix, limit=5):\n        node = self.root\n        for char in prefix:\n            if char not in node.children:\n                return []\n            node = node.children[char]\n        results = []\n        self._dfs(node, prefix, results)\n        results.sort(key=lambda x: -x[1])\n        return [word for word, _ in results[:limit]]\n    \n    def _dfs(self, node, prefix, results):\n        if node.is_word:\n            results.append((prefix, node.count))\n        for char, child in node.children.items():\n            self._dfs(child, prefix + char, results)\n\ntrie = AutocompleteTrie()\nfor word in [\"apple\", \"app\", \"application\", \"apply\", \"apt\", \"banana\"]:\n    trie.insert(word)\nprint(trie.autocomplete(\"app\"))\n# Output: ['app', 'apple', 'application', 'apply']",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "8",
   "q": "Program 68: Largest Rectangle in Histogram",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def largest_rectangle(heights):\n    stack = [-1]\n    max_area = 0\n    for i, h in enumerate(heights):\n        while stack[-1] != -1 and heights[stack[-1]] >= h:\n            height = heights[stack.pop()]\n            width = i - stack[-1] - 1\n            max_area = max(max_area, height * width)\n        stack.append(i)\n    while stack[-1] != -1:\n        height = heights[stack.pop()]\n        width = len(heights) - stack[-1] - 1\n        max_area = max(max_area, height * width)\n    return max_area\n\nprint(largest_rectangle([2,1,5,6,2,3]))  # Output: 10",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "9",
   "q": "Program 69: Number of Islands",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def num_islands(grid):\n    if not grid:\n        return 0\n    count = 0\n    rows, cols = len(grid), len(grid[0])\n    \n    def dfs(r, c):\n        if r < 0 or r >= rows or c < 0 or c >= cols or grid[r][c] != '1':\n            return\n        grid[r][c] = '0'\n        dfs(r+1, c); dfs(r-1, c); dfs(r, c+1); dfs(r, c-1)\n    \n    for r in range(rows):\n        for c in range(cols):\n            if grid[r][c] == '1':\n                dfs(r, c)\n                count += 1\n    return count\n\ngrid = [[\"1\",\"1\",\"0\"],[\"1\",\"0\",\"0\"],[\"0\",\"0\",\"1\"]]\nprint(num_islands(grid))  # Output: 2",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "10",
   "q": "Program 70: Course Schedule (Cycle Detection)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from collections import defaultdict\n\ndef can_finish(n, prerequisites):\n    graph = defaultdict(list)\n    for course, prereq in prerequisites:\n        graph[prereq].append(course)\n    \n    UNVISITED, VISITING, VISITED = 0, 1, 2\n    states = [UNVISITED] * n\n    \n    def has_cycle(node):\n        if states[node] == VISITING:\n            return True\n        if states[node] == VISITED:\n            return False\n        states[node] = VISITING\n        for neighbor in graph[node]:\n            if has_cycle(neighbor):\n                return True\n        states[node] = VISITED\n        return False\n    \n    return not any(has_cycle(i) for i in range(n))\n\nprint(can_finish(2, [[1,0]]))    # Output: True\nprint(can_finish(2, [[1,0],[0,1]]))  # Output: False (cycle)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "11",
   "q": "Program 71: Clone Graph",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def clone_graph(node):\n    if not node:\n        return None\n    visited = {}\n    \n    def dfs(n):\n        if n in visited:\n            return visited[n]\n        clone = Node(n.val)\n        visited[n] = clone\n        for neighbor in n.neighbors:\n            clone.neighbors.append(dfs(neighbor))\n        return clone\n    \n    return dfs(node)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "12",
   "q": "Program 72: Longest Substring Without Repeating",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def length_of_longest_substring(s):\n    char_index = {}\n    max_len = start = 0\n    for i, char in enumerate(s):\n        if char in char_index and char_index[char] >= start:\n            start = char_index[char] + 1\n        char_index[char] = i\n        max_len = max(max_len, i - start + 1)\n    return max_len\n\nprint(length_of_longest_substring(\"abcabcbb\"))  # Output: 3\nprint(length_of_longest_substring(\"bbbbb\"))     # Output: 1",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "13",
   "q": "Program 73: Generate Parentheses",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def generate_parentheses(n):\n    result = []\n    def backtrack(s, open_count, close_count):\n        if len(s) == 2 * n:\n            result.append(s)\n            return\n        if open_count < n:\n            backtrack(s + '(', open_count + 1, close_count)\n        if close_count < open_count:\n            backtrack(s + ')', open_count, close_count + 1)\n    backtrack('', 0, 0)\n    return result\n\nprint(generate_parentheses(3))\n# Output: ['((()))', '(()())', '(())()', '()(())', '()()()']",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "14",
   "q": "Program 74: Combination Sum",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def combination_sum(candidates, target):\n    result = []\n    def backtrack(start, path, remaining):\n        if remaining == 0:\n            result.append(path[:])\n            return\n        for i in range(start, len(candidates)):\n            if candidates[i] > remaining:\n                break\n            path.append(candidates[i])\n            backtrack(i, path, remaining - candidates[i])\n            path.pop()\n    candidates.sort()\n    backtrack(0, [], target)\n    return result\n\nprint(combination_sum([2,3,6,7], 7))\n# Output: [[2,2,3], [7]]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "15",
   "q": "Program 75: Palindrome Partitioning",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def partition(s):\n    result = []\n    def backtrack(start, path):\n        if start == len(s):\n            result.append(path[:])\n            return\n        for end in range(start + 1, len(s) + 1):\n            sub = s[start:end]\n            if sub == sub[::-1]:\n                path.append(sub)\n                backtrack(end, path)\n                path.pop()\n    backtrack(0, [])\n    return result\n\nprint(partition(\"aab\"))\n# Output: [['a','a','b'], ['aa','b']]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "16",
   "q": "Program 76: Regular Expression Matching (DP)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def is_match(s, p):\n    m, n = len(s), len(p)\n    dp = [[False] * (n + 1) for _ in range(m + 1)]\n    dp[0][0] = True\n    for j in range(1, n + 1):\n        if p[j-1] == '*':\n            dp[0][j] = dp[0][j-2]\n    for i in range(1, m + 1):\n        for j in range(1, n + 1):\n            if p[j-1] == '.' or p[j-1] == s[i-1]:\n                dp[i][j] = dp[i-1][j-1]\n            elif p[j-1] == '*':\n                dp[i][j] = dp[i][j-2]\n                if p[j-2] == '.' or p[j-2] == s[i-1]:\n                    dp[i][j] = dp[i][j] or dp[i-1][j]\n    return dp[m][n]\n\nprint(is_match(\"aab\", \"c*a*b\"))  # Output: True",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "17",
   "q": "Program 77: Alien Dictionary Order Check",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def is_alien_sorted(words, order):\n    index = {c: i for i, c in enumerate(order)}\n    for i in range(len(words) - 1):\n        w1, w2 = words[i], words[i+1]\n        for j in range(min(len(w1), len(w2))):\n            if w1[j] != w2[j]:\n                if index[w1[j]] > index[w2[j]]:\n                    return False\n                break\n        else:\n            if len(w1) > len(w2):\n                return False\n    return True\n\nprint(is_alien_sorted([\"hello\",\"leetcode\"], \"hlabcdefgijkmnopqrstuvwxyz\"))\n# Output: True",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "18",
   "q": "Program 78: Top K Frequent Elements",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from collections import Counter\n\ndef top_k_frequent(nums, k):\n    return [x for x, _ in Counter(nums).most_common(k)]\n\nprint(top_k_frequent([1,1,1,2,2,3], 2))  # Output: [1, 2]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "19",
   "q": "Program 79: Kth Smallest in BST",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def kth_smallest(root, k):\n    stack = []\n    current = root\n    count = 0\n    while stack or current:\n        while current:\n            stack.append(current)\n            current = current.left\n        current = stack.pop()\n        count += 1\n        if count == k:\n            return current.val\n        current = current.right",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "20",
   "q": "Program 80: Binary Tree Right Side View",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from collections import deque\n\ndef right_side_view(root):\n    if not root:\n        return []\n    result = []\n    queue = deque([root])\n    while queue:\n        level_size = len(queue)\n        for i in range(level_size):\n            node = queue.popleft()\n            if i == level_size - 1:\n                result.append(node.val)\n            if node.left: queue.append(node.left)\n            if node.right: queue.append(node.right)\n    return result",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "21",
   "q": "Program 81: Flatten Binary Tree to Linked List",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def flatten(root):\n    if not root:\n        return\n    flatten(root.left)\n    flatten(root.right)\n    left = root.left\n    right = root.right\n    root.left = None\n    root.right = left\n    current = root\n    while current.right:\n        current = current.right\n    current.right = right",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "22",
   "q": "Program 82: Construct Tree from Preorder and Inorder",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def build_tree(preorder, inorder):\n    if not preorder or not inorder:\n        return None\n    root = TreeNode(preorder[0])\n    mid = inorder.index(preorder[0])\n    root.left = build_tree(preorder[1:mid+1], inorder[:mid])\n    root.right = build_tree(preorder[mid+1:], inorder[mid+1:])\n    return root",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "23",
   "q": "Program 83: Path Sum (All Paths)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def path_sum(root, target):\n    result = []\n    def dfs(node, remaining, path):\n        if not node:\n            return\n        path.append(node.val)\n        if not node.left and not node.right and remaining == node.val:\n            result.append(path[:])\n        dfs(node.left, remaining - node.val, path)\n        dfs(node.right, remaining - node.val, path)\n        path.pop()\n    dfs(root, target, [])\n    return result",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "24",
   "q": "Program 84: Symmetric Tree Check",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def is_symmetric(root):\n    def mirror(left, right):\n        if not left and not right:\n            return True\n        if not left or not right:\n            return False\n        return (left.val == right.val and \n                mirror(left.left, right.right) and \n                mirror(left.right, right.left))\n    return mirror(root.left, root.right) if root else True",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "25",
   "q": "Program 85: Sum Root to Leaf Numbers",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def sum_numbers(root):\n    def dfs(node, current_num):\n        if not node:\n            return 0\n        current_num = current_num * 10 + node.val\n        if not node.left and not node.right:\n            return current_num\n        return dfs(node.left, current_num) + dfs(node.right, current_num)\n    return dfs(root, 0)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "26",
   "q": "Program 86: Maximum Path Sum (Binary Tree)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def max_path_sum(root):\n    max_sum = [float('-inf')]\n    def dfs(node):\n        if not node:\n            return 0\n        left = max(0, dfs(node.left))\n        right = max(0, dfs(node.right))\n        max_sum[0] = max(max_sum[0], left + right + node.val)\n        return max(left, right) + node.val\n    dfs(root)\n    return max_sum[0]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "27",
   "q": "Program 87: Implement Heap Sort",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def heap_sort(arr):\n    def heapify(arr, n, i):\n        largest = i\n        left = 2 * i + 1\n        right = 2 * i + 2\n        if left < n and arr[left] > arr[largest]:\n            largest = left\n        if right < n and arr[right] > arr[largest]:\n            largest = right\n        if largest != i:\n            arr[i], arr[largest] = arr[largest], arr[i]\n            heapify(arr, n, largest)\n    \n    n = len(arr)\n    for i in range(n // 2 - 1, -1, -1):\n        heapify(arr, n, i)\n    for i in range(n - 1, 0, -1):\n        arr[0], arr[i] = arr[i], arr[0]\n        heapify(arr, i, 0)\n    return arr\n\nprint(heap_sort([12, 11, 13, 5, 6, 7]))\n# Output: [5, 6, 7, 11, 12, 13]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "28",
   "q": "Program 88: Radix Sort",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def radix_sort(arr):\n    max_val = max(arr)\n    exp = 1\n    while max_val // exp > 0:\n        counting_sort_by_digit(arr, exp)\n        exp *= 10\n    return arr\n\ndef counting_sort_by_digit(arr, exp):\n    n = len(arr)\n    output = [0] * n\n    count = [0] * 10\n    for num in arr:\n        idx = (num // exp) % 10\n        count[idx] += 1\n    for i in range(1, 10):\n        count[i] += count[i-1]\n    for i in range(n-1, -1, -1):\n        idx = (arr[i] // exp) % 10\n        output[count[idx]-1] = arr[i]\n        count[idx] -= 1\n    arr[:] = output\n\nprint(radix_sort([170, 45, 75, 90, 802, 24, 2, 66]))\n# Output: [2, 24, 45, 66, 75, 90, 170, 802]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "29",
   "q": "Program 89: Bucket Sort",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def bucket_sort(arr):\n    if not arr:\n        return arr\n    min_val, max_val = min(arr), max(arr)\n    bucket_count = len(arr)\n    bucket_range = (max_val - min_val) / bucket_count + 1\n    buckets = [[] for _ in range(bucket_count)]\n    for num in arr:\n        idx = int((num - min_val) / bucket_range)\n        buckets[idx].append(num)\n    result = []\n    for bucket in buckets:\n        result.extend(sorted(bucket))\n    return result\n\nprint(bucket_sort([0.42, 0.32, 0.23, 0.52, 0.25, 0.47, 0.51]))\n# Output: [0.23, 0.25, 0.32, 0.42, 0.47, 0.51, 0.52]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "30",
   "q": "Program 90: Find Duplicate (Floyd's Cycle)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def find_duplicate(nums):\n    slow = fast = nums[0]\n    while True:\n        slow = nums[slow]\n        fast = nums[nums[fast]]\n        if slow == fast:\n            break\n    slow = nums[0]\n    while slow != fast:\n        slow = nums[slow]\n        fast = nums[fast]\n    return slow\n\nprint(find_duplicate([1, 3, 4, 2, 2]))  # Output: 2",
     "numbered": false
    }
   ]
  }
 ],
 "takeaways": [],
 "_drills": 30,
 "_code": 30
});
