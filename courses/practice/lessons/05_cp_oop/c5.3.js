/* ============================================================================
   PRACTICE C5.3 — Programs · 3
   ----------------------------------------------------------------------------
   Imported from the Coding Practice source set and rendered through the
   Tutorial Hub design system. Generated by .build/import-practice.py —
   edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "c5.3",
 "lede": "**30 problems** from the oop and design patterns set. Each answer is hidden until you ask for it — attempt it first, because reading a solution feels like learning and is not.",
 "objectives": [
  "Work through 30 problems on programs without looking anything up",
  "Predict each answer before revealing it",
  "Recognise the pattern each problem belongs to",
  "Explain your solution out loud, including its complexity"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer before you reveal.** Even a wrong attempt makes the answer stick; reading first does not.",
      "**Say the complexity out loud** where the problem has one. Getting it wrong is the useful part.",
      "Use the bar above to open or close everything at once, and **Ctrl K** to jump to another set."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "Program 61: Structural Pattern Matching (Python 3.10+)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def process_command(command):\n    match command.split():\n        case [\"quit\" | \"exit\"]:\n            return \"Goodbye!\"\n        case [\"hello\", name]:\n            return f\"Hello, {name}!\"\n        case [\"add\", *numbers]:\n            return sum(int(n) for n in numbers)\n        case [\"search\", keyword, (\"--verbose\" | \"-v\")]:\n            return f\"Verbose search: {keyword}\"\n        case _:\n            return \"Unknown command\"\n\nprint(process_command(\"hello Alice\"))      # Hello, Alice!\nprint(process_command(\"add 1 2 3 4 5\"))    # 15\nprint(process_command(\"quit\"))             # Goodbye!\n\n# Match with guard\ndef classify_point(point):\n    match point:\n        case (0, 0):\n            return \"Origin\"\n        case (x, 0):\n            return f\"X-axis at {x}\"\n        case (0, y):\n            return f\"Y-axis at {y}\"\n        case (x, y) if x == y:\n            return f\"Diagonal at {x}\"\n        case (x, y):\n            return f\"Point({x}, {y})\"\n\nprint(classify_point((0, 0)))   # Origin\nprint(classify_point((5, 5)))   # Diagonal at 5",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "2",
   "q": "Program 62: Walrus Operator (:=)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# Filter and transform in one pass\ndata = [1, -2, 3, -4, 5, -6, 7]\npositives = [y for x in data if (y := x * 2) > 0]\nprint(positives)  # Output: [2, 6, 10, 14]\n\n# Read until empty\nimport io\ncontent = io.StringIO(\"line1\\nline2\\nline3\\n\")\nlines = []\nwhile (line := content.readline()):\n    lines.append(line.strip())\nprint(lines)  # Output: ['line1', 'line2', 'line3']\n\n# Regex match and use\nimport re\ntext = \"Contact: user@example.com\"\nif (match := re.search(r'[\\w.]+@[\\w.]+', text)):\n    print(f\"Found email: {match.group()}\")\n# Output: Found email: user@example.com",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "3",
   "q": "Program 63: Type Guards",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from typing import Union, TypeGuard\n\ndef is_str_list(val: list) -> TypeGuard[list[str]]:\n    return all(isinstance(x, str) for x in val)\n\ndef process(data: Union[list[str], list[int]]):\n    if is_str_list(data):\n        # Type checker knows data is list[str]\n        return [s.upper() for s in data]\n    return [x * 2 for x in data]\n\nprint(process([\"hello\", \"world\"]))  # ['HELLO', 'WORLD']\nprint(process([1, 2, 3]))           # [2, 4, 6]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "4",
   "q": "Program 64: Dataclass with Post-Init",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from dataclasses import dataclass, field\nfrom typing import List\nimport math\n\n@dataclass\nclass Triangle:\n    a: float\n    b: float\n    c: float\n    _area: float = field(init=False, repr=False)\n    _perimeter: float = field(init=False, repr=False)\n    \n    def __post_init__(self):\n        sides = sorted([self.a, self.b, self.c])\n        if sides[0] + sides[1] <= sides[2]:\n            raise ValueError(\"Invalid triangle\")\n        self._perimeter = self.a + self.b + self.c\n        s = self._perimeter / 2\n        self._area = math.sqrt(s * (s - self.a) * (s - self.b) * (s - self.c))\n    \n    @property\n    def area(self):\n        return round(self._area, 2)\n    \n    @property\n    def perimeter(self):\n        return self._perimeter\n\nt = Triangle(3, 4, 5)\nprint(f\"Area: {t.area}, Perimeter: {t.perimeter}\")\n# Output: Area: 6.0, Perimeter: 12",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "5",
   "q": "Program 65: Async Context Manager",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import asyncio\n\nclass AsyncTimer:\n    def __init__(self, name):\n        self.name = name\n    \n    async def __aenter__(self):\n        self.start = asyncio.get_event_loop().time()\n        print(f\"[{self.name}] Starting...\")\n        return self\n    \n    async def __aexit__(self, *args):\n        elapsed = asyncio.get_event_loop().time() - self.start\n        print(f\"[{self.name}] Finished in {elapsed:.3f}s\")\n\nasync def main():\n    async with AsyncTimer(\"task\"):\n        await asyncio.sleep(0.5)\n\nasyncio.run(main())\n# [task] Starting...\n# [task] Finished in 0.500s",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "6",
   "q": "Program 66: Async Iterator",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import asyncio\n\nclass AsyncRange:\n    def __init__(self, start, end):\n        self.start = start\n        self.end = end\n    \n    def __aiter__(self):\n        self.current = self.start\n        return self\n    \n    async def __anext__(self):\n        if self.current >= self.end:\n            raise StopAsyncIteration\n        value = self.current\n        self.current += 1\n        await asyncio.sleep(0.1)  # Simulate async work\n        return value\n\nasync def main():\n    results = []\n    async for num in AsyncRange(0, 5):\n        results.append(num)\n    print(results)  # [0, 1, 2, 3, 4]\n\nasyncio.run(main())",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "7",
   "q": "Program 67: Async Generator",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import asyncio\n\nasync def async_range(start, end, delay=0.1):\n    for i in range(start, end):\n        await asyncio.sleep(delay)\n        yield i\n\nasync def async_filter(aiterable, predicate):\n    async for item in aiterable:\n        if predicate(item):\n            yield item\n\nasync def main():\n    # Async generator with filtering\n    evens = []\n    async for num in async_filter(async_range(0, 10), lambda x: x % 2 == 0):\n        evens.append(num)\n    print(evens)  # [0, 2, 4, 6, 8]\n\nasyncio.run(main())",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "8",
   "q": "Program 68: Semaphore for Rate Limiting",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import asyncio\n\nasync def limited_request(sem, url, delay):\n    async with sem:\n        print(f\"Requesting {url}...\")\n        await asyncio.sleep(delay)\n        print(f\"Done: {url}\")\n        return f\"Response from {url}\"\n\nasync def main():\n    sem = asyncio.Semaphore(3)  # Max 3 concurrent\n    tasks = [\n        limited_request(sem, f\"url_{i}\", 0.5)\n        for i in range(8)\n    ]\n    results = await asyncio.gather(*tasks)\n    print(f\"Total results: {len(results)}\")\n\nasyncio.run(main())",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "9",
   "q": "Program 69: Generic Class",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from typing import TypeVar, Generic, List, Optional\n\nT = TypeVar('T')\n\nclass TypedStack(Generic[T]):\n    def __init__(self):\n        self._items: List[T] = []\n    \n    def push(self, item: T) -> None:\n        self._items.append(item)\n    \n    def pop(self) -> Optional[T]:\n        return self._items.pop() if self._items else None\n    \n    def peek(self) -> Optional[T]:\n        return self._items[-1] if self._items else None\n    \n    def __len__(self) -> int:\n        return len(self._items)\n    \n    def __repr__(self) -> str:\n        return f\"TypedStack({self._items})\"\n\n# Type-safe usage\nint_stack: TypedStack[int] = TypedStack()\nint_stack.push(1)\nint_stack.push(2)\nprint(int_stack)      # TypedStack([1, 2])\nprint(int_stack.pop()) # 2",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "10",
   "q": "Program 70: Method Chaining (Fluent Interface)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class DataFrame:\n    def __init__(self, data):\n        self.data = data\n        self._columns = list(data[0].keys()) if data else []\n    \n    def filter(self, predicate):\n        self.data = [row for row in self.data if predicate(row)]\n        return self\n    \n    def select(self, *columns):\n        self.data = [{k: row[k] for k in columns} for row in self.data]\n        self._columns = list(columns)\n        return self\n    \n    def sort_by(self, key, reverse=False):\n        self.data.sort(key=lambda row: row[key], reverse=reverse)\n        return self\n    \n    def limit(self, n):\n        self.data = self.data[:n]\n        return self\n    \n    def show(self):\n        for row in self.data:\n            print(row)\n        return self\n\ndata = [\n    {\"name\": \"Alice\", \"age\": 30, \"city\": \"NYC\"},\n    {\"name\": \"Bob\", \"age\": 25, \"city\": \"LA\"},\n    {\"name\": \"Charlie\", \"age\": 35, \"city\": \"NYC\"},\n    {\"name\": \"Diana\", \"age\": 28, \"city\": \"Chicago\"},\n]\n\n(DataFrame(data)\n    .filter(lambda r: r[\"age\"] > 26)\n    .select(\"name\", \"age\")\n    .sort_by(\"age\", reverse=True)\n    .limit(2)\n    .show())\n# {'name': 'Charlie', 'age': 35}\n# {'name': 'Alice', 'age': 30}",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "11",
   "q": "Program 71: Memory Profiling",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import sys\nfrom collections import OrderedDict\n\n# Object sizes\nprint(f\"int: {sys.getsizeof(0)} bytes\")\nprint(f\"float: {sys.getsizeof(0.0)} bytes\")\nprint(f\"str: {sys.getsizeof('')} bytes\")\nprint(f\"list: {sys.getsizeof([])} bytes\")\nprint(f\"dict: {sys.getsizeof({})} bytes\")\nprint(f\"set: {sys.getsizeof(set())} bytes\")\n\n# Compare data structures\nn = 1000\nlist_mem = sys.getsizeof(list(range(n)))\ntuple_mem = sys.getsizeof(tuple(range(n)))\nset_mem = sys.getsizeof(set(range(n)))\ndict_mem = sys.getsizeof({i: i for i in range(n)})\n\nprint(f\"\\nFor {n} elements:\")\nprint(f\"list:  {list_mem:,} bytes\")\nprint(f\"tuple: {tuple_mem:,} bytes\")\nprint(f\"set:   {set_mem:,} bytes\")\nprint(f\"dict:  {dict_mem:,} bytes\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "12",
   "q": "Program 72: Custom Exception Hierarchy",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class AppError(Exception):\n    \"\"\"Base application error\"\"\"\n    def __init__(self, message, code=None):\n        self.message = message\n        self.code = code\n        super().__init__(message)\n\nclass ValidationError(AppError):\n    def __init__(self, field, message):\n        self.field = field\n        super().__init__(f\"Validation error on '{field}': {message}\", code=400)\n\nclass NotFoundError(AppError):\n    def __init__(self, resource, id):\n        super().__init__(f\"{resource} with id={id} not found\", code=404)\n\nclass AuthenticationError(AppError):\n    def __init__(self, message=\"Invalid credentials\"):\n        super().__init__(message, code=401)\n\n# Usage\ndef find_user(user_id):\n    if user_id < 0:\n        raise ValidationError(\"user_id\", \"must be positive\")\n    if user_id > 100:\n        raise NotFoundError(\"User\", user_id)\n    return {\"id\": user_id, \"name\": \"Alice\"}\n\ntry:\n    find_user(999)\nexcept NotFoundError as e:\n    print(f\"[{e.code}] {e.message}\")  # [404] User with id=999 not found\nexcept AppError as e:\n    print(f\"[{e.code}] {e.message}\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "13",
   "q": "Program 73: Bisect — Sorted List Operations",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import bisect\n\n# Maintain sorted list\nscores = []\nfor score in [85, 92, 73, 95, 88, 79]:\n    bisect.insort(scores, score)\nprint(scores)  # [73, 79, 85, 88, 92, 95]\n\n# Find insertion point\nidx = bisect.bisect_left(scores, 87)\nprint(f\"87 would go at index {idx}\")  # 3\n\n# Grade boundaries\ndef grade(score, breakpoints=[60, 70, 80, 90], grades='FDCBA'):\n    idx = bisect.bisect(breakpoints, score)\n    return grades[idx]\n\nprint([grade(s) for s in [33, 65, 77, 85, 95]])\n# Output: ['F', 'D', 'C', 'B', 'A']",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "14",
   "q": "Program 74: Counter Advanced Usage",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from collections import Counter\n\n# Text analysis\ntext = \"the quick brown fox jumps over the lazy dog the fox\"\nwords = Counter(text.split())\n\nprint(words.most_common(3))  # [('the', 3), ('fox', 2), ('quick', 1)]\n\n# Arithmetic\nc1 = Counter(a=3, b=1, c=2)\nc2 = Counter(a=1, b=2, d=1)\nprint(c1 + c2)  # Counter({'a': 4, 'b': 3, 'c': 2, 'd': 1})\nprint(c1 - c2)  # Counter({'c': 2, 'a': 2})  (only positive)\nprint(c1 & c2)  # Counter({'a': 1, 'b': 1})  (min)\nprint(c1 | c2)  # Counter({'a': 3, 'c': 2, 'b': 2, 'd': 1})  (max)\n\n# Elements\nc = Counter(a=2, b=3)\nprint(list(c.elements()))  # ['a', 'a', 'b', 'b', 'b']",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "15",
   "q": "Program 75: DefaultDict for Grouping",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from collections import defaultdict\n\n# Group by category\nitems = [\n    (\"fruit\", \"apple\"), (\"vegetable\", \"carrot\"),\n    (\"fruit\", \"banana\"), (\"vegetable\", \"broccoli\"),\n    (\"fruit\", \"cherry\"), (\"grain\", \"rice\")\n]\n\ngrouped = defaultdict(list)\nfor category, item in items:\n    grouped[category].append(item)\n\nprint(dict(grouped))\n# {'fruit': ['apple', 'banana', 'cherry'], 'vegetable': ['carrot', 'broccoli'], 'grain': ['rice']}\n\n# Nested defaultdict\ntree = lambda: defaultdict(tree)\ntaxonomy = tree()\ntaxonomy['animal']['mammal']['dog'] = 'bark'\ntaxonomy['animal']['mammal']['cat'] = 'meow'\ntaxonomy['animal']['bird']['eagle'] = 'screech'\nprint(taxonomy['animal']['mammal']['dog'])  # bark",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "16",
   "q": "Program 76: ChainMap",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from collections import ChainMap\n\ndefaults = {'color': 'red', 'size': 'medium', 'font': 'Arial'}\nuser_prefs = {'color': 'blue', 'font': 'Helvetica'}\ncli_args = {'color': 'green'}\n\n# Later maps override earlier ones\nconfig = ChainMap(cli_args, user_prefs, defaults)\nprint(config['color'])  # green (from cli_args)\nprint(config['font'])   # Helvetica (from user_prefs)\nprint(config['size'])   # medium (from defaults)\nprint(list(config))     # All unique keys",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "17",
   "q": "Program 77: Deque as Circular Buffer",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from collections import deque\n\nclass CircularBuffer:\n    def __init__(self, max_size):\n        self.buffer = deque(maxlen=max_size)\n    \n    def add(self, item):\n        self.buffer.append(item)\n    \n    def get_all(self):\n        return list(self.buffer)\n    \n    def average(self):\n        return sum(self.buffer) / len(self.buffer) if self.buffer else 0\n\n# Rolling average of last 5 values\nbuf = CircularBuffer(5)\nfor i in range(10):\n    buf.add(i)\n    print(f\"Added {i}: buffer={buf.get_all()}, avg={buf.average():.1f}\")\n# Last output: Added 9: buffer=[5, 6, 7, 8, 9], avg=7.0",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "18",
   "q": "Program 78: Struct Packing/Unpacking",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import struct\n\n# Pack data into bytes\npacked = struct.pack('!I2sf', 42, b'AB', 3.14)\nprint(f\"Packed: {packed.hex()}\")\nprint(f\"Size: {len(packed)} bytes\")\n\n# Unpack bytes back\nnum, text, pi = struct.unpack('!I2sf', packed)\nprint(f\"Unpacked: num={num}, text={text}, pi={pi:.2f}\")\n# Output: num=42, text=b'AB', pi=3.14\n\n# Format characters: ! = network byte order, I = uint32, \n# 2s = 2-char string, f = float",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "19",
   "q": "Program 79: ABC Registry",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from abc import ABCMeta\n\nclass PluginMeta(ABCMeta):\n    registry = {}\n    \n    def __new__(mcs, name, bases, namespace):\n        cls = super().__new__(mcs, name, bases, namespace)\n        if bases:  # Don't register the base class\n            PluginMeta.registry[name] = cls\n        return cls\n\nclass Plugin(metaclass=PluginMeta):\n    def run(self):\n        raise NotImplementedError\n\nclass CSVPlugin(Plugin):\n    def run(self):\n        return \"Processing CSV\"\n\nclass JSONPlugin(Plugin):\n    def run(self):\n        return \"Processing JSON\"\n\nprint(PluginMeta.registry)\n# {'CSVPlugin': <class 'CSVPlugin'>, 'JSONPlugin': <class 'JSONPlugin'>}\n\nfor name, cls in PluginMeta.registry.items():\n    print(f\"{name}: {cls().run()}\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "20",
   "q": "Program 80: Contextvar for Thread-Local",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from contextvars import ContextVar, copy_context\n\nrequest_id: ContextVar[str] = ContextVar('request_id', default='none')\nuser: ContextVar[str] = ContextVar('user', default='anonymous')\n\ndef process_request():\n    print(f\"Processing request {request_id.get()} for {user.get()}\")\n\ndef handle_request(req_id, username):\n    request_id.set(req_id)\n    user.set(username)\n    process_request()\n\nhandle_request(\"REQ-001\", \"Alice\")\n# Output: Processing request REQ-001 for Alice\n\nhandle_request(\"REQ-002\", \"Bob\")\n# Output: Processing request REQ-002 for Bob",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "21",
   "q": "Program 81: Profiling with cProfile",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import cProfile\nimport pstats\nfrom io import StringIO\n\ndef fibonacci(n):\n    if n < 2:\n        return n\n    return fibonacci(n-1) + fibonacci(n-2)\n\ndef profile_code():\n    profiler = cProfile.Profile()\n    profiler.enable()\n    \n    result = fibonacci(20)\n    \n    profiler.disable()\n    \n    stream = StringIO()\n    stats = pstats.Stats(profiler, stream=stream)\n    stats.sort_stats('cumulative')\n    stats.print_stats(5)\n    print(stream.getvalue())\n    print(f\"Result: {result}\")\n\nprofile_code()",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "22",
   "q": "Program 82: Custom Collection",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class SortedList:\n    def __init__(self, iterable=None):\n        self._data = sorted(iterable) if iterable else []\n    \n    def add(self, item):\n        from bisect import insort\n        insort(self._data, item)\n    \n    def remove(self, item):\n        self._data.remove(item)\n    \n    def __contains__(self, item):\n        from bisect import bisect_left\n        idx = bisect_left(self._data, item)\n        return idx < len(self._data) and self._data[idx] == item\n    \n    def __getitem__(self, index):\n        return self._data[index]\n    \n    def __len__(self):\n        return len(self._data)\n    \n    def __repr__(self):\n        return f\"SortedList({self._data})\"\n\nsl = SortedList([5, 2, 8, 1, 9])\nprint(sl)        # SortedList([1, 2, 5, 8, 9])\nsl.add(3)\nprint(sl)        # SortedList([1, 2, 3, 5, 8, 9])\nprint(3 in sl)   # True\nprint(sl[2])     # 3",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "23",
   "q": "Program 83: Async HTTP Requests (aiohttp simulation)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import asyncio\n\nasync def fetch(url, delay=0.5):\n    \"\"\"Simulate async HTTP request\"\"\"\n    await asyncio.sleep(delay)\n    return {\"url\": url, \"status\": 200, \"data\": f\"Response from {url}\"}\n\nasync def fetch_all(urls):\n    tasks = [fetch(url) for url in urls]\n    return await asyncio.gather(*tasks)\n\nasync def main():\n    urls = [f\"https://api.example.com/data/{i}\" for i in range(5)]\n    \n    import time\n    start = time.perf_counter()\n    results = await fetch_all(urls)\n    elapsed = time.perf_counter() - start\n    \n    for r in results:\n        print(f\"{r['url']}: {r['status']}\")\n    print(f\"\\nTotal time: {elapsed:.2f}s (concurrent, not {0.5*len(urls)}s)\")\n\nasyncio.run(main())",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "24",
   "q": "Program 84: Token Bucket Rate Limiter (Async)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import asyncio\nimport time\n\nclass AsyncRateLimiter:\n    def __init__(self, rate, burst):\n        self.rate = rate      # tokens per second\n        self.burst = burst    # max tokens\n        self.tokens = burst\n        self.last = time.monotonic()\n        self.lock = asyncio.Lock()\n    \n    async def acquire(self):\n        async with self.lock:\n            now = time.monotonic()\n            elapsed = now - self.last\n            self.tokens = min(self.burst, self.tokens + elapsed * self.rate)\n            self.last = now\n            \n            if self.tokens >= 1:\n                self.tokens -= 1\n                return True\n            return False\n\nasync def main():\n    limiter = AsyncRateLimiter(rate=2, burst=3)\n    \n    for i in range(8):\n        allowed = await limiter.acquire()\n        status = \"ALLOWED\" if allowed else \"DENIED\"\n        print(f\"Request {i+1}: {status}\")\n        await asyncio.sleep(0.3)\n\nasyncio.run(main())",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "25",
   "q": "Program 85: Retry with Exponential Backoff",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import asyncio\nimport random\n\nasync def retry_async(func, max_retries=5, base_delay=0.1, max_delay=5.0):\n    for attempt in range(max_retries):\n        try:\n            return await func()\n        except Exception as e:\n            if attempt == max_retries - 1:\n                raise\n            delay = min(base_delay * (2 ** attempt) + random.uniform(0, 0.1), max_delay)\n            print(f\"Attempt {attempt + 1} failed: {e}. Retrying in {delay:.2f}s\")\n            await asyncio.sleep(delay)\n\nasync def unreliable_api():\n    if random.random() < 0.6:\n        raise ConnectionError(\"Server unavailable\")\n    return \"Success!\"\n\nasync def main():\n    try:\n        result = await retry_async(unreliable_api)\n        print(f\"Result: {result}\")\n    except ConnectionError:\n        print(\"All retries failed\")\n\nasyncio.run(main())",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "26",
   "q": "Program 86: Finite State Machine (Enum-based)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from enum import Enum, auto\n\nclass OrderState(Enum):\n    CREATED = auto()\n    PAID = auto()\n    SHIPPED = auto()\n    DELIVERED = auto()\n    CANCELLED = auto()\n\nclass Order:\n    TRANSITIONS = {\n        OrderState.CREATED: {OrderState.PAID, OrderState.CANCELLED},\n        OrderState.PAID: {OrderState.SHIPPED, OrderState.CANCELLED},\n        OrderState.SHIPPED: {OrderState.DELIVERED},\n        OrderState.DELIVERED: set(),\n        OrderState.CANCELLED: set(),\n    }\n    \n    def __init__(self, order_id):\n        self.id = order_id\n        self.state = OrderState.CREATED\n        self.history = [(self.state, \"Order created\")]\n    \n    def transition(self, new_state, note=\"\"):\n        if new_state not in self.TRANSITIONS[self.state]:\n            raise ValueError(f\"Cannot go from {self.state.name} to {new_state.name}\")\n        self.state = new_state\n        self.history.append((self.state, note))\n    \n    def __repr__(self):\n        return f\"Order({self.id}, state={self.state.name})\"\n\norder = Order(\"ORD-001\")\norder.transition(OrderState.PAID, \"Payment received\")\norder.transition(OrderState.SHIPPED, \"Dispatched\")\nprint(order)  # Order(ORD-001, state=SHIPPED)\n\nfor state, note in order.history:\n    print(f\"  {state.name}: {note}\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "27",
   "q": "Program 87: Abstract Factory with Registration",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class SerializerFactory:\n    _serializers = {}\n    \n    @classmethod\n    def register(cls, format_name):\n        def wrapper(serializer_cls):\n            cls._serializers[format_name] = serializer_cls\n            return serializer_cls\n        return wrapper\n    \n    @classmethod\n    def create(cls, format_name):\n        if format_name not in cls._serializers:\n            raise ValueError(f\"Unknown format: {format_name}\")\n        return cls._serializers[format_name]()\n\n@SerializerFactory.register(\"json\")\nclass JSONSerializer:\n    def serialize(self, data):\n        import json\n        return json.dumps(data)\n\n@SerializerFactory.register(\"csv\")\nclass CSVSerializer:\n    def serialize(self, data):\n        if isinstance(data, list) and data:\n            header = \",\".join(data[0].keys())\n            rows = [\",\".join(str(v) for v in d.values()) for d in data]\n            return header + \"\\n\" + \"\\n\".join(rows)\n        return str(data)\n\n# Usage\ndata = [{\"name\": \"Alice\", \"age\": 30}, {\"name\": \"Bob\", \"age\": 25}]\njson_out = SerializerFactory.create(\"json\").serialize(data)\ncsv_out = SerializerFactory.create(\"csv\").serialize(data)\nprint(json_out)\nprint(csv_out)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "28",
   "q": "Program 88: Event Loop Simulation",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import heapq\nimport time\n\nclass EventLoop:\n    def __init__(self):\n        self._queue = []\n        self._time = 0\n        self._counter = 0\n    \n    def schedule(self, delay, callback, *args):\n        self._counter += 1\n        heapq.heappush(self._queue, (self._time + delay, self._counter, callback, args))\n    \n    def run(self):\n        while self._queue:\n            event_time, _, callback, args = heapq.heappop(self._queue)\n            self._time = event_time\n            callback(*args)\n\nloop = EventLoop()\n\ndef task(name):\n    print(f\"[t={loop._time:.1f}] Task: {name}\")\n\nloop.schedule(0.5, task, \"A\")\nloop.schedule(0.2, task, \"B\")\nloop.schedule(1.0, task, \"C\")\nloop.schedule(0.2, task, \"D\")\n\nloop.run()\n# [t=0.2] Task: B\n# [t=0.2] Task: D\n# [t=0.5] Task: A\n# [t=1.0] Task: C",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "29",
   "q": "Program 89: Tree Traversals (All Types)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class TreeNode:\n    def __init__(self, val, left=None, right=None):\n        self.val = val\n        self.left = left\n        self.right = right\n\n# Build tree:     1\n#               /   \\\n#              2     3\n#             / \\   /\n#            4   5 6\n\nroot = TreeNode(1,\n    TreeNode(2, TreeNode(4), TreeNode(5)),\n    TreeNode(3, TreeNode(6)))\n\n# Inorder (Left, Root, Right)\ndef inorder(node):\n    if not node: return []\n    return inorder(node.left) + [node.val] + inorder(node.right)\n\n# Preorder (Root, Left, Right)\ndef preorder(node):\n    if not node: return []\n    return [node.val] + preorder(node.left) + preorder(node.right)\n\n# Postorder (Left, Right, Root)\ndef postorder(node):\n    if not node: return []\n    return postorder(node.left) + postorder(node.right) + [node.val]\n\n# Level Order (BFS)\ndef level_order(root):\n    if not root: return []\n    from collections import deque\n    queue = deque([root])\n    result = []\n    while queue:\n        node = queue.popleft()\n        result.append(node.val)\n        if node.left: queue.append(node.left)\n        if node.right: queue.append(node.right)\n    return result\n\nprint(f\"Inorder:     {inorder(root)}\")      # [4, 2, 5, 1, 6, 3]\nprint(f\"Preorder:    {preorder(root)}\")      # [1, 2, 4, 5, 3, 6]\nprint(f\"Postorder:   {postorder(root)}\")     # [4, 5, 2, 6, 3, 1]\nprint(f\"Level order: {level_order(root)}\")   # [1, 2, 3, 4, 5, 6]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "30",
   "q": "Program 90: Union-Find (Disjoint Set)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class UnionFind:\n    def __init__(self, n):\n        self.parent = list(range(n))\n        self.rank = [0] * n\n        self.components = n\n    \n    def find(self, x):\n        if self.parent[x] != x:\n            self.parent[x] = self.find(self.parent[x])  # Path compression\n        return self.parent[x]\n    \n    def union(self, x, y):\n        px, py = self.find(x), self.find(y)\n        if px == py:\n            return False\n        # Union by rank\n        if self.rank[px] < self.rank[py]:\n            px, py = py, px\n        self.parent[py] = px\n        if self.rank[px] == self.rank[py]:\n            self.rank[px] += 1\n        self.components -= 1\n        return True\n    \n    def connected(self, x, y):\n        return self.find(x) == self.find(y)\n\nuf = UnionFind(6)\nuf.union(0, 1)\nuf.union(2, 3)\nuf.union(1, 3)\nprint(uf.connected(0, 3))  # True\nprint(uf.connected(0, 4))  # False\nprint(f\"Components: {uf.components}\")  # 3",
     "numbered": false
    }
   ]
  }
 ],
 "takeaways": [],
 "_drills": 30,
 "_code": 30
});
