/* ============================================================================
   PRACTICE C5.1 — Programs · 1
   ----------------------------------------------------------------------------
   Imported from the Coding Practice source set and rendered through the
   Tutorial Hub design system. Generated by .build/import-practice.py —
   edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "c5.1",
 "lede": "**30 problems** from the oop and design patterns set. Each answer is hidden until you ask for it — attempt it first, because reading a solution feels like learning and is not.",
 "objectives": [
  "Work through 30 problems on programs without looking anything up",
  "Predict each answer before revealing it",
  "Recognise the pattern each problem belongs to",
  "Explain your solution out loud, including its complexity"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer before you reveal.** Even a wrong attempt makes the answer stick; reading first does not.",
      "**Say the complexity out loud** where the problem has one. Getting it wrong is the useful part.",
      "Use the bar above to open or close everything at once, and **Ctrl K** to jump to another set."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "Program 1: Class Inheritance",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class Animal:\n    def __init__(self, name, sound):\n        self.name = name\n        self.sound = sound\n    \n    def speak(self):\n        return f\"{self.name} says {self.sound}\"\n\nclass Dog(Animal):\n    def __init__(self, name):\n        super().__init__(name, \"Woof\")\n    \n    def fetch(self, item):\n        return f\"{self.name} fetches {item}\"\n\nclass Cat(Animal):\n    def __init__(self, name):\n        super().__init__(name, \"Meow\")\n\ndog = Dog(\"Rex\")\nprint(dog.speak())        # Output: Rex says Woof\nprint(dog.fetch(\"ball\"))  # Output: Rex fetches ball",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "2",
   "q": "Program 2: Abstract Base Class",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from abc import ABC, abstractmethod\n\nclass Shape(ABC):\n    @abstractmethod\n    def area(self):\n        pass\n    \n    @abstractmethod\n    def perimeter(self):\n        pass\n\nclass Circle(Shape):\n    def __init__(self, radius):\n        self.radius = radius\n    \n    def area(self):\n        return 3.14159 * self.radius ** 2\n    \n    def perimeter(self):\n        return 2 * 3.14159 * self.radius\n\nclass Rectangle(Shape):\n    def __init__(self, width, height):\n        self.width = width\n        self.height = height\n    \n    def area(self):\n        return self.width * self.height\n    \n    def perimeter(self):\n        return 2 * (self.width + self.height)\n\nc = Circle(5)\nr = Rectangle(4, 6)\nprint(f\"Circle: area={c.area():.2f}, perimeter={c.perimeter():.2f}\")\nprint(f\"Rectangle: area={r.area()}, perimeter={r.perimeter()}\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "3",
   "q": "Program 3: Multiple Inheritance & MRO",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class A:\n    def greet(self):\n        return \"Hello from A\"\n\nclass B(A):\n    def greet(self):\n        return \"Hello from B\"\n\nclass C(A):\n    def greet(self):\n        return \"Hello from C\"\n\nclass D(B, C):\n    pass\n\nd = D()\nprint(d.greet())          # Output: Hello from B (MRO: D -> B -> C -> A)\nprint(D.__mro__)          # Shows method resolution order",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "4",
   "q": "Program 4: Property Decorator",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class Temperature:\n    def __init__(self, celsius=0):\n        self._celsius = celsius\n    \n    @property\n    def celsius(self):\n        return self._celsius\n    \n    @celsius.setter\n    def celsius(self, value):\n        if value < -273.15:\n            raise ValueError(\"Below absolute zero!\")\n        self._celsius = value\n    \n    @property\n    def fahrenheit(self):\n        return self._celsius * 9/5 + 32\n    \n    @fahrenheit.setter\n    def fahrenheit(self, value):\n        self.celsius = (value - 32) * 5/9\n\nt = Temperature(100)\nprint(t.fahrenheit)   # Output: 212.0\nt.fahrenheit = 32\nprint(t.celsius)      # Output: 0.0",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "5",
   "q": "Program 5: Dunder Methods (Magic Methods)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class Vector:\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y\n    \n    def __add__(self, other):\n        return Vector(self.x + other.x, self.y + other.y)\n    \n    def __sub__(self, other):\n        return Vector(self.x - other.x, self.y - other.y)\n    \n    def __mul__(self, scalar):\n        return Vector(self.x * scalar, self.y * scalar)\n    \n    def __abs__(self):\n        return (self.x**2 + self.y**2)**0.5\n    \n    def __repr__(self):\n        return f\"Vector({self.x}, {self.y})\"\n    \n    def __eq__(self, other):\n        return self.x == other.x and self.y == other.y\n\nv1 = Vector(3, 4)\nv2 = Vector(1, 2)\nprint(v1 + v2)    # Output: Vector(4, 6)\nprint(abs(v1))    # Output: 5.0\nprint(v1 * 3)     # Output: Vector(9, 12)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "6",
   "q": "Program 6: Descriptor Protocol",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class Validated:\n    def __init__(self, min_val=None, max_val=None):\n        self.min_val = min_val\n        self.max_val = max_val\n    \n    def __set_name__(self, owner, name):\n        self.name = name\n    \n    def __set__(self, obj, value):\n        if self.min_val is not None and value < self.min_val:\n            raise ValueError(f\"{self.name} must be >= {self.min_val}\")\n        if self.max_val is not None and value > self.max_val:\n            raise ValueError(f\"{self.name} must be <= {self.max_val}\")\n        obj.__dict__[self.name] = value\n    \n    def __get__(self, obj, objtype=None):\n        return obj.__dict__.get(self.name)\n\nclass Student:\n    age = Validated(min_val=0, max_val=150)\n    grade = Validated(min_val=0, max_val=100)\n\ns = Student()\ns.age = 20\ns.grade = 95\nprint(f\"Age: {s.age}, Grade: {s.grade}\")  # Output: Age: 20, Grade: 95",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "7",
   "q": "Program 7: Metaclass",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class SingletonMeta(type):\n    _instances = {}\n    \n    def __call__(cls, *args, **kwargs):\n        if cls not in cls._instances:\n            cls._instances[cls] = super().__call__(*args, **kwargs)\n        return cls._instances[cls]\n\nclass Database(metaclass=SingletonMeta):\n    def __init__(self):\n        self.connection = \"connected\"\n\ndb1 = Database()\ndb2 = Database()\nprint(db1 is db2)  # Output: True",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "8",
   "q": "Program 8: __slots__ for Memory Optimization",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import sys\n\nclass PointWithDict:\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y\n\nclass PointWithSlots:\n    __slots__ = ['x', 'y']\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y\n\np1 = PointWithDict(1, 2)\np2 = PointWithSlots(1, 2)\nprint(f\"With dict: {sys.getsizeof(p1.__dict__)} bytes\")\nprint(f\"With slots: no __dict__ (much smaller)\")\nprint(hasattr(p2, '__dict__'))  # Output: False",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "9",
   "q": "Program 9: Class Method & Static Method",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class Date:\n    def __init__(self, year, month, day):\n        self.year = year\n        self.month = month\n        self.day = day\n    \n    @classmethod\n    def from_string(cls, date_str):\n        year, month, day = map(int, date_str.split('-'))\n        return cls(year, month, day)\n    \n    @staticmethod\n    def is_valid(date_str):\n        try:\n            parts = date_str.split('-')\n            return len(parts) == 3 and all(p.isdigit() for p in parts)\n        except:\n            return False\n    \n    def __repr__(self):\n        return f\"{self.year}-{self.month:02d}-{self.day:02d}\"\n\nd = Date.from_string(\"2024-06-15\")\nprint(d)                           # Output: 2024-06-15\nprint(Date.is_valid(\"2024-06-15\")) # Output: True",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "10",
   "q": "Program 10: Mixin Classes",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class SerializeMixin:\n    def to_dict(self):\n        return {k: v for k, v in self.__dict__.items() if not k.startswith('_')}\n    \n    def to_json(self):\n        import json\n        return json.dumps(self.to_dict(), indent=2)\n\nclass LogMixin:\n    def log(self, message):\n        print(f\"[{self.__class__.__name__}] {message}\")\n\nclass User(SerializeMixin, LogMixin):\n    def __init__(self, name, email):\n        self.name = name\n        self.email = email\n\nu = User(\"Alice\", \"alice@example.com\")\nprint(u.to_dict())   # Output: {'name': 'Alice', 'email': 'alice@example.com'}\nu.log(\"User created\") # Output: [User] User created",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "11",
   "q": "Program 11: Iterator Protocol",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class Range:\n    def __init__(self, start, end, step=1):\n        self.start = start\n        self.end = end\n        self.step = step\n    \n    def __iter__(self):\n        self.current = self.start\n        return self\n    \n    def __next__(self):\n        if self.current >= self.end:\n            raise StopIteration\n        value = self.current\n        self.current += self.step\n        return value\n\nfor num in Range(0, 10, 2):\n    print(num, end=\" \")  # Output: 0 2 4 6 8\nprint()",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "12",
   "q": "Program 12: Generator Pipeline",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def read_lines(text):\n    for line in text.strip().split('\\n'):\n        yield line\n\ndef filter_comments(lines):\n    for line in lines:\n        if not line.strip().startswith('#'):\n            yield line\n\ndef strip_lines(lines):\n    for line in lines:\n        yield line.strip()\n\ntext = \"\"\"\n# This is a comment\nname = Alice\n# Another comment\nage = 25\ncity = NYC\n\"\"\"\n\npipeline = strip_lines(filter_comments(read_lines(text)))\nfor line in pipeline:\n    if line:\n        print(line)\n# Output: name = Alice\n#         age = 25\n#         city = NYC",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "13",
   "q": "Program 13: Coroutine (send/yield)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def running_average():\n    total = 0\n    count = 0\n    average = None\n    while True:\n        value = yield average\n        total += value\n        count += 1\n        average = total / count\n\navg = running_average()\nnext(avg)  # Prime the coroutine\nprint(avg.send(10))  # Output: 10.0\nprint(avg.send(20))  # Output: 15.0\nprint(avg.send(30))  # Output: 20.0",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "14",
   "q": "Program 14: Async/Await Basics",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import asyncio\n\nasync def fetch_data(name, delay):\n    print(f\"Fetching {name}...\")\n    await asyncio.sleep(delay)\n    return f\"{name}: data\"\n\nasync def main():\n    # Run concurrently\n    results = await asyncio.gather(\n        fetch_data(\"API1\", 2),\n        fetch_data(\"API2\", 1),\n        fetch_data(\"API3\", 3),\n    )\n    for r in results:\n        print(r)\n\nasyncio.run(main())\n# Output: Fetching API1... Fetching API2... Fetching API3...\n#         API1: data  API2: data  API3: data",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "15",
   "q": "Program 15: Async Queue Producer-Consumer",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import asyncio\n\nasync def producer(queue, n):\n    for i in range(n):\n        await asyncio.sleep(0.1)\n        await queue.put(i)\n        print(f\"Produced: {i}\")\n    await queue.put(None)  # Signal done\n\nasync def consumer(queue):\n    while True:\n        item = await queue.get()\n        if item is None:\n            break\n        await asyncio.sleep(0.2)\n        print(f\"Consumed: {item}\")\n\nasync def main():\n    queue = asyncio.Queue(maxsize=3)\n    await asyncio.gather(producer(queue, 5), consumer(queue))\n\nasyncio.run(main())",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "16",
   "q": "Program 16: Thread Pool Executor",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from concurrent.futures import ThreadPoolExecutor\nimport time\n\ndef download(url):\n    time.sleep(0.5)  # Simulate download\n    return f\"Downloaded {url}\"\n\nurls = [f\"http://example.com/page{i}\" for i in range(5)]\n\nwith ThreadPoolExecutor(max_workers=3) as executor:\n    results = list(executor.map(download, urls))\n\nfor r in results:\n    print(r)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "17",
   "q": "Program 17: Process Pool for CPU-bound Tasks",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from concurrent.futures import ProcessPoolExecutor\nimport math\n\ndef is_prime(n):\n    if n < 2:\n        return False\n    for i in range(2, int(math.sqrt(n)) + 1):\n        if n % i == 0:\n            return False\n    return True\n\nnumbers = [15485863, 15485867, 15485869, 15485881, 15485891]\n\n# Note: ProcessPoolExecutor must be in if __name__ == '__main__'\n# with ProcessPoolExecutor() as executor:\n#     results = list(executor.map(is_prime, numbers))\n# print(dict(zip(numbers, results)))\n\n# Single-threaded version for demonstration\nfor n in numbers:\n    print(f\"{n}: {'Prime' if is_prime(n) else 'Not prime'}\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "18",
   "q": "Program 18: Threading with Lock",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\n\nclass BankAccount:\n    def __init__(self, balance=0):\n        self.balance = balance\n        self.lock = threading.Lock()\n    \n    def deposit(self, amount):\n        with self.lock:\n            current = self.balance\n            self.balance = current + amount\n    \n    def withdraw(self, amount):\n        with self.lock:\n            if self.balance >= amount:\n                self.balance -= amount\n                return True\n            return False\n\naccount = BankAccount(1000)\n\ndef make_transactions():\n    for _ in range(100):\n        account.deposit(10)\n        account.withdraw(10)\n\nthreads = [threading.Thread(target=make_transactions) for _ in range(10)]\nfor t in threads:\n    t.start()\nfor t in threads:\n    t.join()\n\nprint(f\"Final balance: {account.balance}\")  # Output: 1000 (correct with lock)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "19",
   "q": "Program 19: Functools — partial, lru_cache, wraps",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from functools import partial, lru_cache, wraps\n\n# partial: fix some arguments\ndef multiply(x, y):\n    return x * y\n\ndouble = partial(multiply, 2)\ntriple = partial(multiply, 3)\nprint(double(5), triple(5))  # Output: 10 15\n\n# lru_cache: automatic memoization\n@lru_cache(maxsize=128)\ndef expensive_fib(n):\n    if n < 2:\n        return n\n    return expensive_fib(n-1) + expensive_fib(n-2)\n\nprint(expensive_fib(100))  # Output: 354224848179261915075\nprint(expensive_fib.cache_info())  # CacheInfo(hits=98, misses=101, ...)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "20",
   "q": "Program 20: Itertools — Combinatorics",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from itertools import combinations, permutations, product, chain, groupby\n\n# Combinations\nprint(list(combinations([1,2,3,4], 2)))\n# [(1,2), (1,3), (1,4), (2,3), (2,4), (3,4)]\n\n# Product (Cartesian)\nprint(list(product('AB', '12')))\n# [('A','1'), ('A','2'), ('B','1'), ('B','2')]\n\n# Chain: flatten iterables\nprint(list(chain([1,2], [3,4], [5,6])))\n# [1, 2, 3, 4, 5, 6]\n\n# Groupby\ndata = [('A', 1), ('A', 2), ('B', 3), ('B', 4)]\nfor key, group in groupby(data, key=lambda x: x[0]):\n    print(f\"{key}: {list(group)}\")\n# A: [('A', 1), ('A', 2)]\n# B: [('B', 3), ('B', 4)]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "21",
   "q": "Program 21: Type Hints & Protocol",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from typing import Protocol, List, Optional\n\nclass Drawable(Protocol):\n    def draw(self) -> str:\n        ...\n\nclass Circle:\n    def __init__(self, radius: float):\n        self.radius = radius\n    \n    def draw(self) -> str:\n        return f\"Circle(r={self.radius})\"\n\nclass Square:\n    def __init__(self, side: float):\n        self.side = side\n    \n    def draw(self) -> str:\n        return f\"Square(s={self.side})\"\n\ndef render_all(shapes: List[Drawable]) -> None:\n    for shape in shapes:\n        print(shape.draw())\n\nshapes = [Circle(5), Square(3), Circle(2)]\nrender_all(shapes)\n# Output: Circle(r=5)  Square(s=3)  Circle(r=2)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "22",
   "q": "Program 22: Pydantic Model Validation",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from pydantic import BaseModel, validator, Field\nfrom typing import List, Optional\n\nclass Address(BaseModel):\n    street: str\n    city: str\n    zip_code: str = Field(..., pattern=r'^\\d{5}$')\n\nclass User(BaseModel):\n    name: str = Field(..., min_length=1, max_length=100)\n    age: int = Field(..., ge=0, le=150)\n    email: str\n    address: Optional[Address] = None\n    tags: List[str] = []\n    \n    @validator('email')\n    def validate_email(cls, v):\n        if '@' not in v:\n            raise ValueError('Invalid email')\n        return v\n\nuser = User(name=\"Alice\", age=30, email=\"alice@example.com\",\n            address={\"street\": \"123 Main St\", \"city\": \"NYC\", \"zip_code\": \"10001\"})\nprint(user.model_dump())",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "23",
   "q": "Program 23: Enum Class",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from enum import Enum, auto\n\nclass Status(Enum):\n    PENDING = auto()\n    ACTIVE = auto()\n    INACTIVE = auto()\n    DELETED = auto()\n\nclass Priority(Enum):\n    LOW = 1\n    MEDIUM = 2\n    HIGH = 3\n    CRITICAL = 4\n    \n    def __lt__(self, other):\n        return self.value < other.value\n\nprint(Status.ACTIVE)           # Output: Status.ACTIVE\nprint(Status.ACTIVE.name)      # Output: ACTIVE\nprint(Status.ACTIVE.value)     # Output: 2\nprint(Priority.HIGH > Priority.LOW)  # Output: True",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "24",
   "q": "Program 24: Weakref — Avoid Memory Leaks",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import weakref\n\nclass ExpensiveObject:\n    def __init__(self, value):\n        self.value = value\n    def __repr__(self):\n        return f\"ExpensiveObject({self.value})\"\n\nclass Cache:\n    def __init__(self):\n        self._cache = weakref.WeakValueDictionary()\n    \n    def get(self, key):\n        return self._cache.get(key)\n    \n    def put(self, key, obj):\n        self._cache[key] = obj\n\ncache = Cache()\nobj = ExpensiveObject(42)\ncache.put(\"item\", obj)\nprint(cache.get(\"item\"))  # Output: ExpensiveObject(42)\ndel obj\nprint(cache.get(\"item\"))  # Output: None (garbage collected)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "25",
   "q": "Program 25: Pickle — Serialization",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import pickle\n\ndata = {\n    'name': 'Alice',\n    'scores': [90, 85, 92],\n    'metadata': {'level': 'advanced'}\n}\n\n# Serialize\nwith open('data.pkl', 'wb') as f:\n    pickle.dump(data, f)\n\n# Deserialize\nwith open('data.pkl', 'rb') as f:\n    loaded = pickle.load(f)\n\nprint(loaded == data)  # Output: True",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "26",
   "q": "Program 26: JSON Custom Serializer",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import json\nfrom datetime import datetime, date\nfrom decimal import Decimal\n\nclass CustomEncoder(json.JSONEncoder):\n    def default(self, obj):\n        if isinstance(obj, (datetime, date)):\n            return obj.isoformat()\n        if isinstance(obj, Decimal):\n            return float(obj)\n        if isinstance(obj, set):\n            return list(obj)\n        return super().default(obj)\n\ndata = {\n    'timestamp': datetime.now(),\n    'price': Decimal('19.99'),\n    'tags': {'python', 'coding'}\n}\n\njson_str = json.dumps(data, cls=CustomEncoder, indent=2)\nprint(json_str)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "27",
   "q": "Program 27: Logging Module",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import logging\n\n# Configure\nlogging.basicConfig(\n    level=logging.DEBUG,\n    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'\n)\nlogger = logging.getLogger(__name__)\n\ndef divide(a, b):\n    logger.debug(f\"Dividing {a} by {b}\")\n    try:\n        result = a / b\n        logger.info(f\"Result: {result}\")\n        return result\n    except ZeroDivisionError:\n        logger.error(\"Division by zero!\", exc_info=True)\n        return None\n\ndivide(10, 3)  # Logs debug + info\ndivide(10, 0)  # Logs debug + error",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "28",
   "q": "Program 28: Unit Testing with pytest",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# test_calculator.py\ndef add(a, b):\n    return a + b\n\ndef divide(a, b):\n    if b == 0:\n        raise ValueError(\"Cannot divide by zero\")\n    return a / b\n\n# Tests\ndef test_add():\n    assert add(2, 3) == 5\n    assert add(-1, 1) == 0\n    assert add(0, 0) == 0\n\ndef test_divide():\n    assert divide(10, 2) == 5\n    assert divide(9, 3) == 3\n\ndef test_divide_by_zero():\n    import pytest\n    with pytest.raises(ValueError):\n        divide(10, 0)\n\n# Run: pytest test_calculator.py -v\nprint(\"All tests defined\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "29",
   "q": "Program 29: Mock Objects",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from unittest.mock import Mock, patch, MagicMock\n\n# Mock an API call\nclass WeatherService:\n    def get_temperature(self, city):\n        # Real implementation would call an API\n        pass\n\ndef weather_report(service, city):\n    temp = service.get_temperature(city)\n    if temp > 30:\n        return \"Hot\"\n    elif temp > 20:\n        return \"Warm\"\n    return \"Cold\"\n\n# Test with mock\nmock_service = Mock()\nmock_service.get_temperature.return_value = 35\nprint(weather_report(mock_service, \"NYC\"))  # Output: Hot\n\nmock_service.get_temperature.return_value = 15\nprint(weather_report(mock_service, \"NYC\"))  # Output: Cold",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "30",
   "q": "Program 30: Parametrized Testing",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import pytest\n\ndef is_palindrome(s):\n    s = s.lower().replace(\" \", \"\")\n    return s == s[::-1]\n\n# Parametrized test\n# @pytest.mark.parametrize(\"input_str, expected\", [\n#     (\"racecar\", True),\n#     (\"hello\", False),\n#     (\"A man a plan a canal Panama\", True),\n#     (\"\", True),\n#     (\"a\", True),\n# ])\n# def test_palindrome(input_str, expected):\n#     assert is_palindrome(input_str) == expected\n\n# Example output format:\ntest_cases = [\n    (\"racecar\", True), (\"hello\", False),\n    (\"A man a plan a canal Panama\", True)\n]\nfor s, expected in test_cases:\n    result = is_palindrome(s)\n    status = \"PASS\" if result == expected else \"FAIL\"\n    print(f\"{status}: is_palindrome('{s}') = {result}\")",
     "numbered": false
    }
   ]
  }
 ],
 "takeaways": [],
 "_drills": 30,
 "_code": 30
});
