/* ============================================================================
   PRACTICE C4.10 — Predict the Output — Scenario Questions · 5
   ----------------------------------------------------------------------------
   Imported from the Coding Practice source set and rendered through the
   Tutorial Hub design system. Generated by .build/import-practice.py —
   edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "c6.10",
 "lede": "**30 problems** from the decorators, generators and internals set. Each answer is hidden until you ask for it — attempt it first, because reading a solution feels like learning and is not.",
 "objectives": [
  "Work through 30 problems on predict the output — scenario questions without looking anything up",
  "Predict each answer before revealing it",
  "Recognise the pattern each problem belongs to",
  "Explain your solution out loud, including its complexity"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer before you reveal.** Even a wrong attempt makes the answer stick; reading first does not.",
      "**Say the complexity out loud** where the problem has one. Getting it wrong is the useful part.",
      "Use the bar above to open or close everything at once, and **Ctrl K** to jump to another set."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "A generator — output of `data = [\" Hello World \", \" Python Rocks \", \"…`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# Memory-efficient string processing\ndef process_lines(lines):\n    for line in lines:\n        yield line.strip().upper()\n\ndef filter_lines(lines, keyword):\n    for line in lines:\n        if keyword in line:\n            yield line\n\ndata = [\"  Hello World  \", \"  Python Rocks  \", \"  Hello Python  \", \"  Java World  \"]\npipeline = filter_lines(process_lines(iter(data)), \"PYTHON\")\nresult = list(pipeline)\nprint(result)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `['PYTHON ROCKS', 'HELLO PYTHON']` **Explanation:** Generator pipeline — each item flows through without storing intermediate results."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "2",
   "q": "range() — output of `f\"List: {sys.getsizeof(as_list)} bytes\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import sys\n\n# Tuple vs list memory for same data\ndata = list(range(100))\nas_list = list(data)\nas_tuple = tuple(data)\n\nprint(f\"List: {sys.getsizeof(as_list)} bytes\")\nprint(f\"Tuple: {sys.getsizeof(as_tuple)} bytes\")\nprint(f\"Savings: {sys.getsizeof(as_list) - sys.getsizeof(as_tuple)} bytes\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** (Exact values vary) List is larger than Tuple. **Explanation:** Tuples are more memory efficient — no over-allocation for growth."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "3",
   "q": "__slots__ — output of `gc.x, gc.y, gc.z`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# __slots__ inheritance\nclass Base:\n    __slots__ = ['x']\n\nclass Child(Base):\n    __slots__ = ['y']\n\nclass GrandChild(Child):\n    __slots__ = ['z']\n\ngc = GrandChild()\ngc.x = 1\ngc.y = 2\ngc.z = 3\nprint(gc.x, gc.y, gc.z)\n\ntry:\n    gc.w = 4\nexcept AttributeError:\n    print(\"Cannot add 'w' — slots enforced\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `1 2 3`, `Cannot add 'w' — slots enforced` **Explanation:** Slots are inherited and combined. Each class adds its own slots."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "4",
   "q": "An lru_cache-memoised function — output of `f\"Computing {n}²\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from functools import lru_cache\n\n@lru_cache(maxsize=4)\ndef square(n):\n    print(f\"Computing {n}²\")\n    return n * n\n\n# Fill cache\nfor i in range(4):\n    square(i)\n\nprint(\"---\")\nsquare(0)  # cache hit\nsquare(4)  # cache miss, evicts oldest\n\nprint(\"---\")\nsquare(1)  # was it evicted?",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Computing 0²\nComputing 1²\nComputing 2²\nComputing 3²\n---\n---\nComputing 1²",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Explanation:** maxsize=4 cache. Adding `square(4)` evicts `square(0)` (LRU). Then `square(1)` evicts next LRU and needs recomputation since `square(0)` access moved it, making `1` the LRU candidate."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "5",
   "q": "Identity (is) versus equality (==) — output of `a is b`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# intern() for string deduplication\nimport sys\n\na = sys.intern(\"hello world test\")\nb = sys.intern(\"hello world test\")\nprint(a is b)\n\nc = \"hello world test\"\nd = \"hello world test\"\nprint(c is d)  # May or may not be interned automatically",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `True`, `True` (in most implementations for string literals) **Explanation:** `sys.intern` guarantees string deduplication. Literal strings often auto-interned."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "6",
   "q": "A class definition — output of `f\"Non-zero: {len(m.data)}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class MemoryEfficientMatrix:\n    def __init__(self, rows, cols, default=0):\n        self.rows = rows\n        self.cols = cols\n        self.default = default\n        self.data = {}  # sparse storage\n    \n    def __setitem__(self, key, value):\n        r, c = key\n        if value != self.default:\n            self.data[(r, c)] = value\n        elif (r, c) in self.data:\n            del self.data[(r, c)]\n    \n    def __getitem__(self, key):\n        return self.data.get(key, self.default)\n    \n    def density(self):\n        return len(self.data) / (self.rows * self.cols)\n\nm = MemoryEfficientMatrix(1000, 1000)\nm[0, 0] = 1\nm[500, 500] = 2\nm[999, 999] = 3\n\nprint(f\"Non-zero: {len(m.data)}\")\nprint(f\"Density: {m.density():.6f}\")\nprint(m[0, 0])\nprint(m[1, 1])",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Non-zero: 3`, `Density: 0.000003`, `1`, `0` **Explanation:** Sparse matrix stores only non-default values — huge memory savings for sparse data."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "7",
   "q": "A list comprehension — output of `f\"Memory allocated: {top[0].size / 1024:.…`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# Memory profiling with tracemalloc\nimport tracemalloc\n\ntracemalloc.start()\n\nx = [i ** 2 for i in range(10000)]\nsnapshot = tracemalloc.take_snapshot()\ntop = snapshot.statistics('lineno')\n\nprint(f\"Memory allocated: {top[0].size / 1024:.1f} KB\")\nprint(f\"Number of blocks: {top[0].count}\")\n\ntracemalloc.stop()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** (Varies) ~`Memory allocated: XX.X KB`, `Number of blocks: 1` **Explanation:** `tracemalloc` tracks memory allocations. Shows size and location of memory usage."
    }
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "8",
   "q": "__new__ versus __init__ — output of `a is b`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class Flyweight:\n    _instances = {}\n    \n    def __new__(cls, color):\n        if color not in cls._instances:\n            instance = super().__new__(cls)\n            instance.color = color\n            cls._instances[color] = instance\n        return cls._instances[color]\n\na = Flyweight(\"red\")\nb = Flyweight(\"red\")\nc = Flyweight(\"blue\")\n\nprint(a is b)\nprint(a is c)\nprint(len(Flyweight._instances))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `True`, `False`, `2` **Explanation:** Flyweight pattern shares instances for same value — reduces memory for many similar objects."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "9",
   "q": "A deque — output of `f\"List: {t_list:.4f}s\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# deque vs list for append/popleft performance\nfrom collections import deque\nfrom timeit import timeit\n\ndef list_operations():\n    lst = []\n    for i in range(10000):\n        lst.append(i)\n    for _ in range(10000):\n        lst.pop(0)\n\ndef deque_operations():\n    dq = deque()\n    for i in range(10000):\n        dq.append(i)\n    for _ in range(10000):\n        dq.popleft()\n\nt_list = timeit(list_operations, number=10)\nt_deque = timeit(deque_operations, number=10)\n\nprint(f\"List: {t_list:.4f}s\")\nprint(f\"Deque: {t_deque:.4f}s\")\nprint(f\"Deque faster: {t_deque < t_list}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** Times vary, `Deque faster: True` **Explanation:** List `pop(0)` is O(n) — shifts all elements. Deque `popleft()` is O(1)."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "10",
   "q": "A class definition — output of `f\"Created: {pool.created}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class ObjectPool:\n    def __init__(self, factory, max_size=10):\n        self.factory = factory\n        self.max_size = max_size\n        self.pool = []\n        self.created = 0\n    \n    def acquire(self):\n        if self.pool:\n            return self.pool.pop()\n        self.created += 1\n        return self.factory()\n    \n    def release(self, obj):\n        if len(self.pool) < self.max_size:\n            self.pool.append(obj)\n\npool = ObjectPool(lambda: [0] * 1000, max_size=5)\n\n# Simulate usage\nobjs = [pool.acquire() for _ in range(3)]\nprint(f\"Created: {pool.created}\")\n\nfor obj in objs:\n    pool.release(obj)\nprint(f\"Pooled: {len(pool.pool)}\")\n\n# Reuse pooled objects\nobj = pool.acquire()\nprint(f\"Created after reuse: {pool.created}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Created: 3`, `Pooled: 3`, `Created after reuse: 3` **Explanation:** Object pool reuses expensive objects instead of creating new ones."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "11",
   "q": "A generator expression — output of `f\"List: {t_list:.4f}s\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# Generator expression vs list comprehension performance\nfrom timeit import timeit\n\ndef with_list():\n    return sum([x * x for x in range(10000)])\n\ndef with_gen():\n    return sum(x * x for x in range(10000))\n\nt_list = timeit(with_list, number=1000)\nt_gen = timeit(with_gen, number=1000)\n\nprint(f\"List: {t_list:.4f}s\")\nprint(f\"Gen: {t_gen:.4f}s\")\nprint(f\"Same result: {with_list() == with_gen()}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** Times vary, `Same result: True` **Explanation:** Generator avoids creating intermediate list. For `sum()`, both give same result."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "12",
   "q": "A dict comprehension — output of `f\"Int keys: {sys.getsizeof(d1)} bytes\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import sys\n\n# Impact of dict key types on memory\nd1 = {i: i for i in range(100)}\nd2 = {str(i): i for i in range(100)}\nd3 = {(i, i): i for i in range(100)}\n\nprint(f\"Int keys: {sys.getsizeof(d1)} bytes\")\nprint(f\"Str keys: {sys.getsizeof(d2)} bytes\")\nprint(f\"Tuple keys: {sys.getsizeof(d3)} bytes\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** All similar sizes for the dict structure itself (but string/tuple keys use more total memory) **Explanation:** Dict itself has similar overhead. Key objects take additional memory."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "13",
   "q": "A class definition — output of `\"Computing stats...\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class CachedProperty:\n    def __init__(self, func):\n        self.func = func\n        self.cache = {}\n    \n    def __get__(self, obj, objtype=None):\n        if obj is None:\n            return self\n        obj_id = id(obj)\n        if obj_id not in self.cache:\n            self.cache[obj_id] = self.func(obj)\n        return self.cache[obj_id]\n\nclass Data:\n    def __init__(self, values):\n        self.values = values\n    \n    @CachedProperty\n    def stats(self):\n        print(\"Computing stats...\")\n        return {'sum': sum(self.values), 'len': len(self.values)}\n\nd = Data([1, 2, 3, 4, 5])\nprint(d.stats)\nprint(d.stats)  # cached",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Computing stats...`, `{'sum': 15, 'len': 5}`, `{'sum': 15, 'len': 5}` **Explanation:** Descriptor-based caching using object id. Computes once per instance."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "14",
   "q": "list.append — output of `f\"Plus: {timeit(lambda: build_with_plus(n…`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# Efficient string building\nimport io\nfrom timeit import timeit\n\ndef build_with_plus(n):\n    s = \"\"\n    for i in range(n):\n        s = s + str(i) + \",\"\n    return s\n\ndef build_with_list(n):\n    parts = []\n    for i in range(n):\n        parts.append(str(i))\n    return \",\".join(parts)\n\ndef build_with_io(n):\n    buf = io.StringIO()\n    for i in range(n):\n        buf.write(str(i))\n        buf.write(\",\")\n    return buf.getvalue()\n\nn = 1000\nprint(f\"Plus: {timeit(lambda: build_with_plus(n), number=100):.4f}s\")\nprint(f\"List join: {timeit(lambda: build_with_list(n), number=100):.4f}s\")\nprint(f\"StringIO: {timeit(lambda: build_with_io(n), number=100):.4f}s\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** List join and StringIO are faster than plus concatenation. **Explanation:** `+` creates new string each iteration. `join` and `StringIO` are O(n)."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "15",
   "q": "range() — output of `f\"Range: {sys.getsizeof(r)} bytes\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# Range vs list for iteration\nimport sys\n\nr = range(1000000)\nl = list(range(1000000))\n\nprint(f\"Range: {sys.getsizeof(r)} bytes\")\nprint(f\"List: {sys.getsizeof(l)} bytes\")\nprint(f\"500000 in range: True\")  # O(1)\nprint(500000 in r)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Range: 48 bytes`, `List: ~8000056 bytes`, `500000 in range: True`, `True` **Explanation:** `range` is O(1) memory and O(1) membership test. Lists are O(n) for both."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "16",
   "q": "__slots__ — output of `f\"Dict storage size: {sys.getsizeof(ds)}…`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from collections import defaultdict\nimport sys\n\n# Compare storage approaches\nclass DictStorage:\n    def __init__(self):\n        self.data = {}\n    \n    def add(self, key, value):\n        self.data[key] = value\n\nclass SlotStorage:\n    __slots__ = ['_items']\n    \n    def __init__(self):\n        self._items = {}\n    \n    def add(self, key, value):\n        self._items[key] = value\n\nds = DictStorage()\nss = SlotStorage()\n\nprint(f\"Dict storage size: {sys.getsizeof(ds)} bytes\")\nprint(f\"Slot storage size: {sys.getsizeof(ss)} bytes\")\nprint(f\"Has __dict__ (Dict): {hasattr(ds, '__dict__')}\")\nprint(f\"Has __dict__ (Slot): {hasattr(ss, '__dict__')}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Dict storage size: 48 bytes`, `Slot storage size: 48 bytes` (varies), `Has __dict__ (Dict): True`, `Has __dict__ (Slot): False` **Explanation:** Slots save memory by eliminating per-instance `__dict__`."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "17",
   "q": "A defaultdict — output of `f\"Manual: {t1:.4f}s\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# Efficient counting with Counter vs manual\nfrom collections import Counter\nfrom timeit import timeit\n\ndata = list(range(100)) * 100\n\ndef count_manual():\n    counts = {}\n    for item in data:\n        counts[item] = counts.get(item, 0) + 1\n    return counts\n\ndef count_counter():\n    return Counter(data)\n\ndef count_defaultdict():\n    from collections import defaultdict\n    counts = defaultdict(int)\n    for item in data:\n        counts[item] += 1\n    return dict(counts)\n\nt1 = timeit(count_manual, number=1000)\nt2 = timeit(count_counter, number=1000)\nt3 = timeit(count_defaultdict, number=1000)\n\nprint(f\"Manual: {t1:.4f}s\")\nprint(f\"Counter: {t2:.4f}s\")\nprint(f\"DefaultDict: {t3:.4f}s\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** Counter is typically fastest (C implementation) **Explanation:** `Counter` is implemented in C for CPython — faster than pure Python alternatives."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "18",
   "q": "A list comprehension — output of `f\"Small ints: {small_ints}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# Memory impact of small vs large integers\nimport sys\n\nsmall_ints = [sys.getsizeof(i) for i in [0, 1, 255, 256]]\nlarge_ints = [sys.getsizeof(i) for i in [257, 10000, 2**30, 2**60]]\n\nprint(f\"Small ints: {small_ints}\")\nprint(f\"Large ints: {large_ints}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Small ints: [28, 28, 28, 28]`, `Large ints: [28, 28, 32, 32]` (varies by magnitude) **Explanation:** Python ints grow in size with magnitude — arbitrary precision uses more memory for larger values."
    }
   ],
   "terms": [
    "30, 2",
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "19",
   "q": "Set operations — output of `f\"|s1| = {len(s1)}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# Efficient set operations\ns1 = set(range(0, 10000, 2))    # even numbers\ns2 = set(range(0, 10000, 3))    # multiples of 3\ns3 = set(range(0, 10000, 6))    # multiples of 6\n\nprint(f\"|s1| = {len(s1)}\")\nprint(f\"|s2| = {len(s2)}\")\nprint(f\"|s1 & s2| = {len(s1 & s2)}\")\nprint(f\"|s1 & s2| == |s3|: {s1 & s2 == s3}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `|s1| = 5000`, `|s2| = 3334`, `|s1 & s2| = 1667`, `|s1 & s2| == |s3|: True` **Explanation:** Intersection of even and multiples-of-3 equals multiples-of-6. Set ops are O(min(n,m))."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "20",
   "q": "A callable object via __call__ — output of `expensive.stats()`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class Memoize:\n    def __init__(self, func):\n        self.func = func\n        self.cache = {}\n        self.hits = 0\n        self.misses = 0\n    \n    def __call__(self, *args):\n        if args in self.cache:\n            self.hits += 1\n            return self.cache[args]\n        self.misses += 1\n        result = self.func(*args)\n        self.cache[args] = result\n        return result\n    \n    def stats(self):\n        total = self.hits + self.misses\n        return f\"Hit rate: {self.hits/total*100:.1f}%\" if total else \"No calls\"\n\n@Memoize\ndef expensive(n):\n    return sum(range(n))\n\nfor _ in range(5):\n    expensive(100)\n    expensive(200)\n    expensive(100)\n\nprint(expensive.stats())\nprint(f\"Cache size: {len(expensive.cache)}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Hit rate: 86.7%`, `Cache size: 2` **Explanation:** 15 calls total, 2 unique args. First call of each is miss (2), rest are hits (13). 13/15 = 86.7%."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "21",
   "q": "A list comprehension — output of `f\"Loop: {t1:.4f}s\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# Comparison: for loop vs map vs list comprehension\nfrom timeit import timeit\n\ndata = list(range(10000))\n\ndef with_loop():\n    result = []\n    for x in data:\n        result.append(x * 2)\n    return result\n\ndef with_map():\n    return list(map(lambda x: x * 2, data))\n\ndef with_comp():\n    return [x * 2 for x in data]\n\nt1 = timeit(with_loop, number=1000)\nt2 = timeit(with_map, number=1000)\nt3 = timeit(with_comp, number=1000)\n\nprint(f\"Loop: {t1:.4f}s\")\nprint(f\"Map: {t2:.4f}s\")\nprint(f\"Comprehension: {t3:.4f}s\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** Comprehension is typically fastest, followed by map, then loop. **Explanation:** List comprehension is optimized at bytecode level. Map has function call overhead."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "22",
   "q": "A generator — output of `records = list(process_large_file_sim(iter(li…`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# Memory-efficient data processing\ndef process_large_file_sim(lines):\n    \"\"\"Simulate processing large file line by line\"\"\"\n    for line in lines:\n        if line.startswith('#'):\n            continue\n        parts = line.strip().split(',')\n        yield tuple(parts)\n\nlines = [\n    \"# comment\",\n    \"Alice,30,NYC\",\n    \"Bob,25,LA\",\n    \"# another comment\",\n    \"Charlie,35,SF\"\n]\n\nrecords = list(process_large_file_sim(iter(lines)))\nprint(records)\nprint(f\"Records: {len(records)}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `[('Alice', '30', 'NYC'), ('Bob', '25', 'LA'), ('Charlie', '35', 'SF')]`, `Records: 3` **Explanation:** Generator processes one line at a time — constant memory for any file size."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "23",
   "q": "A ChainMap — output of `merged3 = dict(ChainMap(d3, d2, d1))`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# Efficient dict merging\nd1 = {'a': 1, 'b': 2}\nd2 = {'b': 3, 'c': 4}\nd3 = {'c': 5, 'd': 6}\n\n# Method 1: ** unpacking (Python 3.5+)\nmerged1 = {**d1, **d2, **d3}\n\n# Method 2: | operator (Python 3.9+)\nmerged2 = d1 | d2 | d3\n\n# Method 3: ChainMap (different semantics)\nfrom collections import ChainMap\nmerged3 = dict(ChainMap(d3, d2, d1))\n\nprint(merged1)\nprint(merged1 == merged2)\nprint(merged1 == merged3)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `{'a': 1, 'b': 3, 'c': 5, 'd': 6}`, `True`, `True` **Explanation:** Later dicts override earlier ones. All methods produce same result."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "24",
   "q": "A lambda — output of `f\"Lambda: {t1:.4f}s\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# Efficient sorting with key function\nfrom operator import itemgetter, attrgetter\nfrom timeit import timeit\n\ndata = [(3, 'c'), (1, 'a'), (2, 'b')] * 1000\n\ndef sort_lambda():\n    return sorted(data, key=lambda x: x[0])\n\ndef sort_itemgetter():\n    return sorted(data, key=itemgetter(0))\n\nt1 = timeit(sort_lambda, number=1000)\nt2 = timeit(sort_itemgetter, number=1000)\n\nprint(f\"Lambda: {t1:.4f}s\")\nprint(f\"Itemgetter: {t2:.4f}s\")\nprint(f\"Itemgetter faster: {t2 < t1}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Itemgetter faster: True` **Explanation:** `itemgetter` is implemented in C — avoids Python function call overhead per comparison."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "25",
   "q": "A class definition — output of `f\"Created: {count}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import gc\n\nclass LeakyCache:\n    def __init__(self):\n        self.cache = {}\n    \n    def add(self, key, value):\n        self.cache[key] = value\n\ndef simulate_leak():\n    caches = []\n    for i in range(100):\n        c = LeakyCache()\n        c.add(f\"key_{i}\", \"x\" * 1000)\n        caches.append(c)\n    return len(caches)\n\ncount = simulate_leak()\ngc.collect()\nprint(f\"Created: {count}\")\nprint(f\"After GC collected: {gc.collect()}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Created: 100`, `After GC collected: 0` **Explanation:** No circular references — reference counting handles cleanup. GC finds nothing to collect."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "26",
   "q": "An itertools recipe — output of `f\"Same result: {r1 == r2}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# Efficient iteration patterns\nfrom itertools import chain\n\n# Bad: creates intermediate list\ndef flatten_bad(lists):\n    result = []\n    for lst in lists:\n        result.extend(lst)\n    return result\n\n# Good: lazy iteration\ndef flatten_good(lists):\n    return list(chain.from_iterable(lists))\n\ndata = [[i] * 100 for i in range(100)]\nr1 = flatten_bad(data)\nr2 = flatten_good(data)\nprint(f\"Same result: {r1 == r2}\")\nprint(f\"Length: {len(r1)}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Same result: True`, `Length: 10000` **Explanation:** `chain.from_iterable` is lazier and more memory efficient for large inputs."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "27",
   "q": "range() — output of `f\"Global: {t1:.4f}s\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# Local vs global variable access speed\nfrom timeit import timeit\n\nx = 42\n\ndef access_global():\n    total = 0\n    for _ in range(10000):\n        total += x\n    return total\n\ndef access_local():\n    local_x = 42\n    total = 0\n    for _ in range(10000):\n        total += local_x\n    return total\n\nt1 = timeit(access_global, number=1000)\nt2 = timeit(access_local, number=1000)\n\nprint(f\"Global: {t1:.4f}s\")\nprint(f\"Local: {t2:.4f}s\")\nprint(f\"Local faster: {t2 < t1}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** Times vary, `Local faster: True` **Explanation:** Local variable lookup is faster than global — uses LOAD_FAST vs LOAD_GLOBAL bytecode."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "28",
   "q": "__slots__ — output of `records[0]`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# Memory-efficient data class with __slots__\nclass EfficientRecord:\n    __slots__ = ['id', 'name', 'value']\n    \n    def __init__(self, id, name, value):\n        self.id = id\n        self.name = name\n        self.value = value\n    \n    def __repr__(self):\n        return f\"Record({self.id}, {self.name}, {self.value})\"\n\nrecords = [EfficientRecord(i, f\"item_{i}\", i*10) for i in range(5)]\nprint(records[0])\nprint(records[-1])\n\n# Cannot add arbitrary attributes\ntry:\n    records[0].extra = \"test\"\nexcept AttributeError:\n    print(\"Cannot add extra attributes\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Record(0, item_0, 0)`, `Record(4, item_4, 40)`, `Cannot add extra attributes` **Explanation:** `__slots__` enforces fixed attributes — less memory, slightly faster access."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "29",
   "q": "A generator expression — output of `f\"Loop: {t1:.4f}s\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# Efficient boolean evaluation\nfrom timeit import timeit\n\ndef check_any_loop(data, target):\n    for item in data:\n        if item == target:\n            return True\n    return False\n\ndef check_any_builtin(data, target):\n    return any(item == target for item in data)\n\ndef check_any_in(data, target):\n    return target in data\n\ndata = list(range(10000))\n\nt1 = timeit(lambda: check_any_loop(data, 5000), number=10000)\nt2 = timeit(lambda: check_any_builtin(data, 5000), number=10000)\nt3 = timeit(lambda: check_any_in(data, 5000), number=10000)\n\nprint(f\"Loop: {t1:.4f}s\")\nprint(f\"Any: {t2:.4f}s\")\nprint(f\"In: {t3:.4f}s\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `in` is typically fastest **Explanation:** `in` operator is C-optimized for lists. `any()` has generator overhead. Loop is pure Python."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "30",
   "q": "range() — output of `f\"Items: {len(items)}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# Efficient batch operations\ndef batch_insert(items, batch_size=100):\n    \"\"\"Simulate batch database inserts\"\"\"\n    batches = 0\n    for i in range(0, len(items), batch_size):\n        batch = items[i:i+batch_size]\n        batches += 1\n    return batches\n\nitems = list(range(1050))\nn_batches = batch_insert(items, batch_size=100)\nprint(f\"Items: {len(items)}\")\nprint(f\"Batches: {n_batches}\")\nprint(f\"Last batch size: {len(items) % 100 or 100}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Items: 1050`, `Batches: 11`, `Last batch size: 50` **Explanation:** Batch processing reduces overhead. Last batch may be smaller than batch_size."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  }
 ],
 "takeaways": [],
 "_drills": 30,
 "_code": 31
});
