/* ============================================================================
   PRACTICE C4.17 — Performance & Profiling
   ----------------------------------------------------------------------------
   Imported from the Coding Practice source set and rendered through the
   Tutorial Hub design system. Generated by .build/import-practice.py —
   edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "c6.17",
 "lede": "**10 problems** from the decorators, generators and internals set. Each answer is hidden until you ask for it — attempt it first, because reading a solution feels like learning and is not.",
 "objectives": [
  "Work through 10 problems on performance & profiling without looking anything up",
  "Predict each answer before revealing it",
  "Recognise the pattern each problem belongs to",
  "Explain your solution out loud, including its complexity"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer before you reveal.** Even a wrong attempt makes the answer stick; reading first does not.",
      "**Say the complexity out loud** where the problem has one. Getting it wrong is the useful part.",
      "Use the bar above to open or close everything at once, and **Ctrl K** to jump to another set."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "Program 257: Profiling techniques",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q641: Profiling techniques\")\nprint(\"  cProfile: deterministic profiler (function-level)\")\nprint(\"  py-spy: sampling profiler (attach to running process)\")\nprint(\"  line_profiler: line-by-line execution time\")\nprint(\"  memory_profiler: line-by-line memory usage\")\nprint(\"  scalene: CPU + memory + GPU profiler\")\nprint(\"  timeit: micro-benchmark specific code\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q641: Profiling techniques\n  cProfile: deterministic profiler (function-level)\n  py-spy: sampling profiler (attach to running process)\n  line_profiler: line-by-line execution time\n  memory_profiler: line-by-line memory usage\n  scalene: CPU + memory + GPU profiler\n  timeit: micro-benchmark specific code",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "2",
   "q": "Program 258: cProfile",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q642: cProfile\")\n\ndef fibonacci(n):\n    if n <= 1:\n        return n\n    return fibonacci(n-1) + fibonacci(n-2)\n\nprofiler = cProfile.Profile()\nprofiler.enable()\nfibonacci(15)\nprofiler.disable()\ns = io.StringIO()\nps = pstats.Stats(profiler, stream=s).sort_stats('calls')\nps.print_stats(3)\nfor line in s.getvalue().strip().split('\\n')[:5]:\n    print(f\"  {line}\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q642: cProfile\n  1974 function calls (2 primitive calls) in 0.000 seconds\n  \n     Ordered by: call count\n  \n     ncalls  tottime  percall  cumtime  percall filename:lineno(function)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "3",
   "q": "Program 259: py-spy",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q643: py-spy\")\nprint(\"  Sampling profiler that works without code changes.\")\nprint(\"  py-spy top --pid <PID>         # Live view\")\nprint(\"  py-spy record -o profile.svg -- python app.py  # Flame graph\")\nprint(\"  py-spy dump --pid <PID>        # Thread dump\")\nprint(\"  Zero overhead, works in production, no restart needed\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q643: py-spy\n  Sampling profiler that works without code changes.\n  py-spy top --pid <PID>         # Live view\n  py-spy record -o profile.svg -- python app.py  # Flame graph\n  py-spy dump --pid <PID>        # Thread dump\n  Zero overhead, works in production, no restart needed",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "4",
   "q": "Program 260: memory_profiler",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q644: memory_profiler\")\nprint(\"  Line-by-line memory usage.\")\nprint(\"  pip install memory_profiler\")\nprint(\"  @profile\")\nprint(\"  def my_function():\")\nprint(\"      a = [1] * (10 ** 6)\")\nprint(\"      b = [2] * (2 * 10 ** 7)\")\nprint(\"      del b\")\nprint(\"  python -m memory_profiler script.py\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q644: memory_profiler\n  Line-by-line memory usage.\n  pip install memory_profiler\n  @profile\n  def my_function():\n      a = [1] * (10 ** 6)\n      b = [2] * (2 * 10 ** 7)\n      del b\n  python -m memory_profiler script.py",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "5",
   "q": "Program 261: Scalene profiler",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q645: Scalene profiler\")\nprint(\"  CPU + memory + GPU profiler for Python.\")\nprint(\"  scalene script.py\")\nprint(\"  Shows: CPU time (Python vs C), memory allocation/deallocation\")\nprint(\"  Web-based UI, low overhead, line-level granularity\")\nprint(\"  Distinguishes Python time from native (C/C++) time\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q645: Scalene profiler\n  CPU + memory + GPU profiler for Python.\n  scalene script.py\n  Shows: CPU time (Python vs C), memory allocation/deallocation\n  Web-based UI, low overhead, line-level granularity\n  Distinguishes Python time from native (C/C++) time",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "6",
   "q": "Program 262: GIL contention analysis",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q646: GIL contention analysis\")\nprint(\"  GIL prevents true parallelism in multi-threaded Python.\")\nprint(\"  Detect: py-spy with --gil flag shows GIL contention\")\nprint(\"  sys.getswitchinterval()  # Default: 5ms\")\nprint(f\"  Switch interval: {sys.getswitchinterval()}s\")\nprint(\"  Solutions: multiprocessing, C extensions releasing GIL, asyncio\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q646: GIL contention analysis\n  GIL prevents true parallelism in multi-threaded Python.\n  Detect: py-spy with --gil flag shows GIL contention\n  sys.getswitchinterval()  # Default: 5ms\n  Switch interval: 0.005s\n  Solutions: multiprocessing, C extensions releasing GIL, asyncio",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "7",
   "q": "Program 263: CPU optimization",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q647: CPU optimization\")\nprint(\"  1. Use built-in functions (map, filter, sum, min, max)\")\nprint(\"  2. List comprehensions over for loops\")\nprint(\"  3. Local variables faster than global\")\nprint(\"  4. Use collections (deque, Counter, defaultdict)\")\nprint(\"  5. NumPy for numerical computation\")\nprint(\"  6. Cython/C extensions for hot paths\")\nprint(\"  7. functools.lru_cache for memoization\")\n\nfrom functools import lru_cache\n\n@lru_cache(maxsize=128)\ndef fib_cached(n):\n    if n <= 1:\n        return n\n    return fib_cached(n-1) + fib_cached(n-2)\n\nstart = time.perf_counter()\nresult = fib_cached(30)\nelapsed = time.perf_counter() - start\nprint(f\"  fib(30) with cache: {result} in {elapsed:.6f}s\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q647: CPU optimization\n  1. Use built-in functions (map, filter, sum, min, max)\n  2. List comprehensions over for loops\n  3. Local variables faster than global\n  4. Use collections (deque, Counter, defaultdict)\n  5. NumPy for numerical computation\n  6. Cython/C extensions for hot paths\n  7. functools.lru_cache for memoization\n  fib(30) with cache: 832040 in 0.000025s",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "8",
   "q": "Program 264: Memory optimization",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q648: Memory optimization\")\nprint(\"  1. Use __slots__ to reduce instance memory\")\nprint(\"  2. Use generators instead of lists for iteration\")\nprint(\"  3. Use array.array instead of list for numeric data\")\nprint(\"  4. Use sys.getsizeof() to measure object size\")\nprint(\"  5. Use weakref to avoid preventing GC\")\nprint(\"  6. Delete unused large objects explicitly\")\n\nclass WithSlots:\n    __slots__ = ['x', 'y']\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y\n\nclass WithoutSlots:\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y\n\nprint(f\"  With __slots__: {sys.getsizeof(WithSlots(1,2))} bytes\")\nprint(f\"  Without __slots__: {sys.getsizeof(WithoutSlots(1,2))} bytes\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q648: Memory optimization\n  1. Use __slots__ to reduce instance memory\n  2. Use generators instead of lists for iteration\n  3. Use array.array instead of list for numeric data\n  4. Use sys.getsizeof() to measure object size\n  5. Use weakref to avoid preventing GC\n  6. Delete unused large objects explicitly\n  With __slots__: 48 bytes\n  Without __slots__: 56 bytes",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "9",
   "q": "Program 265: Object pooling",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q649: Object pooling\")\nprint(\"  Reuse objects instead of creating/destroying repeatedly.\")\n\nclass ObjectPool:\n    def __init__(self, factory, max_size=10):\n        self._factory = factory\n        self._pool = []\n        self._max_size = max_size\n\n    def acquire(self):\n        if self._pool:\n            return self._pool.pop()\n        return self._factory()\n\n    def release(self, obj):\n        if len(self._pool) < self._max_size:\n            self._pool.append(obj)\n\npool = ObjectPool(factory=lambda: {\"data\": None})\nobj1 = pool.acquire()\nobj1[\"data\"] = \"work\"\npool.release(obj1)\nobj2 = pool.acquire()\nprint(f\"  Reused object: {obj2}\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q649: Object pooling\n  Reuse objects instead of creating/destroying repeatedly.\n  Reused object: {'data': 'work'}",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "10",
   "q": "Program 266: Performance benchmarking",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q650: Performance benchmarking\")\nprint(\"  Use timeit for micro-benchmarks:\")\nlist_time = timeit.timeit('[i**2 for i in range(100)]', number=10000)\ngen_time = timeit.timeit('list(i**2 for i in range(100))', number=10000)\nprint(f\"  List comp: {list_time:.4f}s\")\nprint(f\"  Generator: {gen_time:.4f}s\")\nprint(\"  Use pytest-benchmark for systematic benchmarking\")\nprint(\"  Always benchmark with realistic data sizes\")\nprint()\n\nprint(\"=\" * 70)\nprint(\"FILE 13 COMPLETE: Questions 626-650\")\nprint(\"=\" * 70)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q650: Performance benchmarking\n  Use timeit for micro-benchmarks:\n  List comp: 0.0370s\n  Generator: 0.0544s\n  Use pytest-benchmark for systematic benchmarking\n  Always benchmark with realistic data sizes\n\n======================================================================\nFILE 13 COMPLETE: Questions 626-650\n======================================================================",
     "numbered": false
    }
   ]
  }
 ],
 "takeaways": [],
 "_drills": 10,
 "_code": 20
});
