/* ============================================================================
   PRACTICE C8.1 — Programs · 1
   ----------------------------------------------------------------------------
   Imported from the Coding Practice source set and rendered through the
   Tutorial Hub design system. Generated by .build/import-practice.py —
   edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "c8.1",
 "lede": "**30 problems** from the algorithms and data structures set. Each answer is hidden until you ask for it — attempt it first, because reading a solution feels like learning and is not.",
 "objectives": [
  "Work through 30 problems on programs without looking anything up",
  "Predict each answer before revealing it",
  "Recognise the pattern each problem belongs to",
  "Explain your solution out loud, including its complexity"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer before you reveal.** Even a wrong attempt makes the answer stick; reading first does not.",
      "**Say the complexity out loud** where the problem has one. Getting it wrong is the useful part.",
      "Use the bar above to open or close everything at once, and **Ctrl K** to jump to another set."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "Program 1: Binary Search Tree (Full Implementation)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class BSTNode:\n    def __init__(self, val):\n        self.val = val\n        self.left = None\n        self.right = None\n\nclass BST:\n    def __init__(self):\n        self.root = None\n    \n    def insert(self, val):\n        self.root = self._insert(self.root, val)\n    \n    def _insert(self, node, val):\n        if not node:\n            return BSTNode(val)\n        if val < node.val:\n            node.left = self._insert(node.left, val)\n        elif val > node.val:\n            node.right = self._insert(node.right, val)\n        return node\n    \n    def search(self, val):\n        return self._search(self.root, val)\n    \n    def _search(self, node, val):\n        if not node or node.val == val:\n            return node\n        if val < node.val:\n            return self._search(node.left, val)\n        return self._search(node.right, val)\n    \n    def delete(self, val):\n        self.root = self._delete(self.root, val)\n    \n    def _delete(self, node, val):\n        if not node:\n            return None\n        if val < node.val:\n            node.left = self._delete(node.left, val)\n        elif val > node.val:\n            node.right = self._delete(node.right, val)\n        else:\n            if not node.left:\n                return node.right\n            if not node.right:\n                return node.left\n            successor = self._min_node(node.right)\n            node.val = successor.val\n            node.right = self._delete(node.right, successor.val)\n        return node\n    \n    def _min_node(self, node):\n        while node.left:\n            node = node.left\n        return node\n    \n    def inorder(self):\n        result = []\n        self._inorder(self.root, result)\n        return result\n    \n    def _inorder(self, node, result):\n        if node:\n            self._inorder(node.left, result)\n            result.append(node.val)\n            self._inorder(node.right, result)\n\nbst = BST()\nfor val in [5, 3, 7, 1, 4, 6, 8]:\n    bst.insert(val)\nprint(bst.inorder())     # [1, 3, 4, 5, 6, 7, 8]\nbst.delete(3)\nprint(bst.inorder())     # [1, 4, 5, 6, 7, 8]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "2",
   "q": "Program 2: AVL Tree (Self-Balancing BST)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class AVLNode:\n    def __init__(self, val):\n        self.val = val\n        self.left = None\n        self.right = None\n        self.height = 1\n\nclass AVLTree:\n    def _height(self, node):\n        return node.height if node else 0\n    \n    def _balance(self, node):\n        return self._height(node.left) - self._height(node.right) if node else 0\n    \n    def _rotate_right(self, y):\n        x = y.left\n        t = x.right\n        x.right = y\n        y.left = t\n        y.height = max(self._height(y.left), self._height(y.right)) + 1\n        x.height = max(self._height(x.left), self._height(x.right)) + 1\n        return x\n    \n    def _rotate_left(self, x):\n        y = x.right\n        t = y.left\n        y.left = x\n        x.right = t\n        x.height = max(self._height(x.left), self._height(x.right)) + 1\n        y.height = max(self._height(y.left), self._height(y.right)) + 1\n        return y\n    \n    def insert(self, root, val):\n        if not root:\n            return AVLNode(val)\n        if val < root.val:\n            root.left = self.insert(root.left, val)\n        else:\n            root.right = self.insert(root.right, val)\n        \n        root.height = max(self._height(root.left), self._height(root.right)) + 1\n        balance = self._balance(root)\n        \n        if balance > 1 and val < root.left.val:\n            return self._rotate_right(root)\n        if balance < -1 and val > root.right.val:\n            return self._rotate_left(root)\n        if balance > 1 and val > root.left.val:\n            root.left = self._rotate_left(root.left)\n            return self._rotate_right(root)\n        if balance < -1 and val < root.right.val:\n            root.right = self._rotate_right(root.right)\n            return self._rotate_left(root)\n        \n        return root\n\navl = AVLTree()\nroot = None\nfor v in [10, 20, 30, 40, 50, 25]:\n    root = avl.insert(root, v)\nprint(f\"Root: {root.val}, Height: {root.height}\")\n# Root: 30, Height: 3 (balanced!)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "3",
   "q": "Program 3: Segment Tree (Range Sum Query)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class SegmentTree:\n    def __init__(self, arr):\n        self.n = len(arr)\n        self.tree = [0] * (4 * self.n)\n        self._build(arr, 1, 0, self.n - 1)\n    \n    def _build(self, arr, node, start, end):\n        if start == end:\n            self.tree[node] = arr[start]\n            return\n        mid = (start + end) // 2\n        self._build(arr, 2*node, start, mid)\n        self._build(arr, 2*node+1, mid+1, end)\n        self.tree[node] = self.tree[2*node] + self.tree[2*node+1]\n    \n    def query(self, l, r):\n        return self._query(1, 0, self.n-1, l, r)\n    \n    def _query(self, node, start, end, l, r):\n        if r < start or end < l:\n            return 0\n        if l <= start and end <= r:\n            return self.tree[node]\n        mid = (start + end) // 2\n        return (self._query(2*node, start, mid, l, r) + \n                self._query(2*node+1, mid+1, end, l, r))\n    \n    def update(self, idx, val):\n        self._update(1, 0, self.n-1, idx, val)\n    \n    def _update(self, node, start, end, idx, val):\n        if start == end:\n            self.tree[node] = val\n            return\n        mid = (start + end) // 2\n        if idx <= mid:\n            self._update(2*node, start, mid, idx, val)\n        else:\n            self._update(2*node+1, mid+1, end, idx, val)\n        self.tree[node] = self.tree[2*node] + self.tree[2*node+1]\n\narr = [1, 3, 5, 7, 9, 11]\nst = SegmentTree(arr)\nprint(f\"Sum [1,3]: {st.query(1, 3)}\")  # 3+5+7 = 15\nst.update(2, 10)  # Change 5 to 10\nprint(f\"Sum [1,3] after update: {st.query(1, 3)}\")  # 3+10+7 = 20",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "4",
   "q": "Program 4: Fenwick Tree (Binary Indexed Tree)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class FenwickTree:\n    def __init__(self, n):\n        self.n = n\n        self.tree = [0] * (n + 1)\n    \n    def update(self, i, delta):\n        i += 1  # 1-indexed\n        while i <= self.n:\n            self.tree[i] += delta\n            i += i & (-i)\n    \n    def prefix_sum(self, i):\n        i += 1\n        s = 0\n        while i > 0:\n            s += self.tree[i]\n            i -= i & (-i)\n        return s\n    \n    def range_sum(self, l, r):\n        return self.prefix_sum(r) - (self.prefix_sum(l-1) if l > 0 else 0)\n\nft = FenwickTree(6)\narr = [1, 3, 5, 7, 9, 11]\nfor i, v in enumerate(arr):\n    ft.update(i, v)\n\nprint(f\"Prefix sum [0..3]: {ft.prefix_sum(3)}\")  # 1+3+5+7 = 16\nprint(f\"Range sum [2..4]: {ft.range_sum(2, 4)}\")  # 5+7+9 = 21",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "5",
   "q": "Program 5: Graph — Kruskal's MST",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class KruskalMST:\n    def __init__(self, vertices):\n        self.vertices = vertices\n        self.parent = {v: v for v in vertices}\n        self.rank = {v: 0 for v in vertices}\n    \n    def find(self, x):\n        if self.parent[x] != x:\n            self.parent[x] = self.find(self.parent[x])\n        return self.parent[x]\n    \n    def union(self, x, y):\n        px, py = self.find(x), self.find(y)\n        if px == py:\n            return False\n        if self.rank[px] < self.rank[py]:\n            px, py = py, px\n        self.parent[py] = px\n        if self.rank[px] == self.rank[py]:\n            self.rank[px] += 1\n        return True\n    \n    def mst(self, edges):\n        edges.sort(key=lambda x: x[2])\n        result = []\n        total_weight = 0\n        for u, v, w in edges:\n            if self.union(u, v):\n                result.append((u, v, w))\n                total_weight += w\n        return result, total_weight\n\nedges = [('A','B',4), ('A','C',2), ('B','C',1), ('B','D',5), ('C','D',8), ('C','E',10), ('D','E',2)]\nk = KruskalMST(['A', 'B', 'C', 'D', 'E'])\nmst_edges, total = k.mst(edges)\nprint(f\"MST edges: {mst_edges}\")\nprint(f\"Total weight: {total}\")  # 9",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "6",
   "q": "Program 6: Prim's MST",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import heapq\n\ndef prim_mst(graph, start):\n    visited = set()\n    mst = []\n    edges = [(0, start, None)]  # (weight, node, from)\n    total = 0\n    \n    while edges and len(visited) < len(graph):\n        weight, node, from_node = heapq.heappop(edges)\n        if node in visited:\n            continue\n        visited.add(node)\n        if from_node is not None:\n            mst.append((from_node, node, weight))\n            total += weight\n        for neighbor, w in graph[node]:\n            if neighbor not in visited:\n                heapq.heappush(edges, (w, neighbor, node))\n    \n    return mst, total\n\ngraph = {\n    'A': [('B', 4), ('C', 2)],\n    'B': [('A', 4), ('C', 1), ('D', 5)],\n    'C': [('A', 2), ('B', 1), ('D', 8), ('E', 10)],\n    'D': [('B', 5), ('C', 8), ('E', 2)],\n    'E': [('C', 10), ('D', 2)]\n}\n\nmst, total = prim_mst(graph, 'A')\nprint(f\"MST: {mst}\")\nprint(f\"Total: {total}\")  # 9",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "7",
   "q": "Program 7: Floyd-Warshall (All Pairs Shortest Path)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def floyd_warshall(graph, n):\n    INF = float('inf')\n    dist = [[INF] * n for _ in range(n)]\n    \n    for i in range(n):\n        dist[i][i] = 0\n    \n    for u, v, w in graph:\n        dist[u][v] = w\n    \n    for k in range(n):\n        for i in range(n):\n            for j in range(n):\n                if dist[i][k] + dist[k][j] < dist[i][j]:\n                    dist[i][j] = dist[i][k] + dist[k][j]\n    \n    return dist\n\nedges = [(0,1,3), (0,2,8), (0,4,-4), (1,3,1), (1,4,7),\n         (2,1,4), (3,0,2), (3,2,-5), (4,3,6)]\n\ndist = floyd_warshall(edges, 5)\nfor row in dist:\n    print([f\"{x:3}\" if x != float('inf') else \"INF\" for x in row])",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "8",
   "q": "Program 8: Counting Sort",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def counting_sort(arr):\n    if not arr:\n        return arr\n    max_val = max(arr)\n    min_val = min(arr)\n    range_size = max_val - min_val + 1\n    \n    count = [0] * range_size\n    output = [0] * len(arr)\n    \n    for num in arr:\n        count[num - min_val] += 1\n    \n    for i in range(1, len(count)):\n        count[i] += count[i - 1]\n    \n    for num in reversed(arr):\n        output[count[num - min_val] - 1] = num\n        count[num - min_val] -= 1\n    \n    return output\n\nprint(counting_sort([4, 2, 2, 8, 3, 3, 1]))\n# Output: [1, 2, 2, 3, 3, 4, 8]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "9",
   "q": "Program 9: Radix Sort",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def radix_sort(arr):\n    if not arr:\n        return arr\n    max_digits = len(str(max(arr)))\n    \n    for digit in range(max_digits):\n        buckets = [[] for _ in range(10)]\n        for num in arr:\n            d = (num // (10 ** digit)) % 10\n            buckets[d].append(num)\n        arr = [num for bucket in buckets for num in bucket]\n    \n    return arr\n\nprint(radix_sort([170, 45, 75, 90, 802, 24, 2, 66]))\n# Output: [2, 24, 45, 66, 75, 90, 170, 802]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "10",
   "q": "Program 10: Shell Sort",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def shell_sort(arr):\n    n = len(arr)\n    gap = n // 2\n    while gap > 0:\n        for i in range(gap, n):\n            temp = arr[i]\n            j = i\n            while j >= gap and arr[j - gap] > temp:\n                arr[j] = arr[j - gap]\n                j -= gap\n            arr[j] = temp\n        gap //= 2\n    return arr\n\nprint(shell_sort([12, 34, 54, 2, 3]))\n# Output: [2, 3, 12, 34, 54]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "11",
   "q": "Program 11: Tim Sort (Simplified)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def insertion_sort_range(arr, left, right):\n    for i in range(left + 1, right + 1):\n        key = arr[i]\n        j = i - 1\n        while j >= left and arr[j] > key:\n            arr[j + 1] = arr[j]\n            j -= 1\n        arr[j + 1] = key\n\ndef merge_sorted(arr, l, m, r):\n    left = arr[l:m+1]\n    right = arr[m+1:r+1]\n    i = j = 0\n    k = l\n    while i < len(left) and j < len(right):\n        if left[i] <= right[j]:\n            arr[k] = left[i]\n            i += 1\n        else:\n            arr[k] = right[j]\n            j += 1\n        k += 1\n    while i < len(left):\n        arr[k] = left[i]\n        i += 1; k += 1\n    while j < len(right):\n        arr[k] = right[j]\n        j += 1; k += 1\n\ndef tim_sort(arr):\n    RUN = 32\n    n = len(arr)\n    for start in range(0, n, RUN):\n        end = min(start + RUN - 1, n - 1)\n        insertion_sort_range(arr, start, end)\n    \n    size = RUN\n    while size < n:\n        for left in range(0, n, 2 * size):\n            mid = min(left + size - 1, n - 1)\n            right = min(left + 2 * size - 1, n - 1)\n            if mid < right:\n                merge_sorted(arr, left, mid, right)\n        size *= 2\n    return arr\n\nimport random\narr = random.sample(range(100), 20)\nprint(tim_sort(arr))",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "12",
   "q": "Program 12: Longest Increasing Subsequence",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from bisect import bisect_left\n\ndef lis_dp(nums):\n    \"\"\"O(n²) DP approach\"\"\"\n    if not nums:\n        return 0\n    dp = [1] * len(nums)\n    for i in range(1, len(nums)):\n        for j in range(i):\n            if nums[j] < nums[i]:\n                dp[i] = max(dp[i], dp[j] + 1)\n    return max(dp)\n\ndef lis_binary(nums):\n    \"\"\"O(n log n) approach with binary search\"\"\"\n    tails = []\n    for num in nums:\n        idx = bisect_left(tails, num)\n        if idx == len(tails):\n            tails.append(num)\n        else:\n            tails[idx] = num\n    return len(tails)\n\nnums = [10, 9, 2, 5, 3, 7, 101, 18]\nprint(f\"LIS length (DP): {lis_dp(nums)}\")        # 4\nprint(f\"LIS length (Binary): {lis_binary(nums)}\")  # 4",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "13",
   "q": "Program 13: Longest Palindromic Substring",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def longest_palindrome(s):\n    def expand(left, right):\n        while left >= 0 and right < len(s) and s[left] == s[right]:\n            left -= 1\n            right += 1\n        return s[left+1:right]\n    \n    result = \"\"\n    for i in range(len(s)):\n        # Odd length\n        p1 = expand(i, i)\n        # Even length\n        p2 = expand(i, i + 1)\n        \n        if len(p1) > len(result):\n            result = p1\n        if len(p2) > len(result):\n            result = p2\n    \n    return result\n\nprint(longest_palindrome(\"babad\"))    # \"bab\" or \"aba\"\nprint(longest_palindrome(\"cbbd\"))     # \"bb\"\nprint(longest_palindrome(\"racecar\"))  # \"racecar\"",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "14",
   "q": "Program 14: Word Break Problem",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def word_break(s, word_dict):\n    n = len(s)\n    dp = [False] * (n + 1)\n    dp[0] = True\n    \n    for i in range(1, n + 1):\n        for j in range(i):\n            if dp[j] and s[j:i] in word_dict:\n                dp[i] = True\n                break\n    \n    return dp[n]\n\ndef word_break_all(s, word_dict):\n    \"\"\"Return all possible segmentations\"\"\"\n    memo = {}\n    def backtrack(start):\n        if start in memo:\n            return memo[start]\n        if start == len(s):\n            return [[]]\n        results = []\n        for end in range(start + 1, len(s) + 1):\n            word = s[start:end]\n            if word in word_dict:\n                for rest in backtrack(end):\n                    results.append([word] + rest)\n        memo[start] = results\n        return results\n    \n    return [\" \".join(words) for words in backtrack(0)]\n\nword_dict = {\"leet\", \"code\", \"leetcode\"}\nprint(word_break(\"leetcode\", word_dict))  # True\n\nword_dict2 = {\"cats\", \"dog\", \"sand\", \"and\", \"cat\"}\nprint(word_break_all(\"catsanddog\", word_dict2))\n# ['cats and dog', 'cat sand dog']",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "15",
   "q": "Program 15: Matrix Chain Multiplication",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def matrix_chain_order(dims):\n    n = len(dims) - 1\n    dp = [[0] * n for _ in range(n)]\n    \n    for chain_len in range(2, n + 1):\n        for i in range(n - chain_len + 1):\n            j = i + chain_len - 1\n            dp[i][j] = float('inf')\n            for k in range(i, j):\n                cost = dp[i][k] + dp[k+1][j] + dims[i] * dims[k+1] * dims[j+1]\n                dp[i][j] = min(dp[i][j], cost)\n    \n    return dp[0][n-1]\n\ndims = [10, 30, 5, 60]  # 3 matrices: 10×30, 30×5, 5×60\nprint(f\"Minimum multiplications: {matrix_chain_order(dims)}\")\n# Output: 4500",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "16",
   "q": "Program 16: Rod Cutting Problem",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def rod_cutting(prices, n):\n    dp = [0] * (n + 1)\n    cuts = [0] * (n + 1)\n    \n    for i in range(1, n + 1):\n        for j in range(1, i + 1):\n            if j <= len(prices) and prices[j-1] + dp[i-j] > dp[i]:\n                dp[i] = prices[j-1] + dp[i-j]\n                cuts[i] = j\n    \n    # Reconstruct solution\n    pieces = []\n    remaining = n\n    while remaining > 0:\n        pieces.append(cuts[remaining])\n        remaining -= cuts[remaining]\n    \n    return dp[n], pieces\n\nprices = [1, 5, 8, 9, 10, 17, 17, 20]\nmax_revenue, pieces = rod_cutting(prices, 8)\nprint(f\"Max revenue: {max_revenue}\")  # 22\nprint(f\"Cut lengths: {pieces}\")       # [2, 6]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "17",
   "q": "Program 17: Egg Drop Problem",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def egg_drop(eggs, floors):\n    dp = [[0] * (floors + 1) for _ in range(eggs + 1)]\n    \n    for i in range(1, eggs + 1):\n        dp[i][0] = 0\n        dp[i][1] = 1\n    \n    for j in range(1, floors + 1):\n        dp[1][j] = j\n    \n    for i in range(2, eggs + 1):\n        for j in range(2, floors + 1):\n            dp[i][j] = float('inf')\n            for k in range(1, j + 1):\n                cost = 1 + max(dp[i-1][k-1], dp[i][j-k])\n                dp[i][j] = min(dp[i][j], cost)\n    \n    return dp[eggs][floors]\n\nprint(f\"2 eggs, 10 floors: {egg_drop(2, 10)} trials\")  # 4\nprint(f\"2 eggs, 100 floors: {egg_drop(2, 100)} trials\") # 14",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "18",
   "q": "Program 18: Trie — Word Search",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class TrieNode:\n    def __init__(self):\n        self.children = {}\n        self.word = None\n\ndef word_search_ii(board, words):\n    root = TrieNode()\n    for word in words:\n        node = root\n        for c in word:\n            node = node.children.setdefault(c, TrieNode())\n        node.word = word\n    \n    rows, cols = len(board), len(board[0])\n    found = []\n    \n    def dfs(r, c, node):\n        if r < 0 or r >= rows or c < 0 or c >= cols:\n            return\n        char = board[r][c]\n        if char not in node.children:\n            return\n        \n        next_node = node.children[char]\n        if next_node.word:\n            found.append(next_node.word)\n            next_node.word = None  # Avoid duplicates\n        \n        board[r][c] = '#'\n        for dr, dc in [(0,1),(0,-1),(1,0),(-1,0)]:\n            dfs(r+dr, c+dc, next_node)\n        board[r][c] = char\n    \n    for r in range(rows):\n        for c in range(cols):\n            dfs(r, c, root)\n    \n    return found\n\nboard = [['o','a','a','n'], ['e','t','a','e'], ['i','h','k','r'], ['i','f','l','v']]\nwords = [\"oath\", \"pea\", \"eat\", \"rain\"]\nprint(word_search_ii(board, words))  # ['oath', 'eat']",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "19",
   "q": "Program 19: Graph Coloring",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def graph_coloring(graph, num_colors):\n    n = len(graph)\n    colors = [-1] * n\n    \n    def is_safe(node, color):\n        for neighbor in graph[node]:\n            if colors[neighbor] == color:\n                return False\n        return True\n    \n    def solve(node):\n        if node == n:\n            return True\n        for color in range(num_colors):\n            if is_safe(node, color):\n                colors[node] = color\n                if solve(node + 1):\n                    return True\n                colors[node] = -1\n        return False\n    \n    if solve(0):\n        return colors\n    return None\n\ngraph = {\n    0: [1, 2, 3],\n    1: [0, 2],\n    2: [0, 1, 3],\n    3: [0, 2]\n}\n\nresult = graph_coloring(graph, 3)\ncolor_names = ['Red', 'Green', 'Blue']\nfor node, color in enumerate(result):\n    print(f\"Node {node}: {color_names[color]}\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "20",
   "q": "Program 20: Detect Cycle in Directed Graph",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def has_cycle_directed(graph, n):\n    WHITE, GRAY, BLACK = 0, 1, 2\n    color = [WHITE] * n\n    \n    def dfs(node):\n        color[node] = GRAY\n        for neighbor in graph.get(node, []):\n            if color[neighbor] == GRAY:\n                return True\n            if color[neighbor] == WHITE and dfs(neighbor):\n                return True\n        color[node] = BLACK\n        return False\n    \n    return any(color[i] == WHITE and dfs(i) for i in range(n))\n\n# Graph with cycle: 0→1→2→0\ngraph1 = {0: [1], 1: [2], 2: [0]}\nprint(f\"Has cycle: {has_cycle_directed(graph1, 3)}\")  # True\n\n# Graph without cycle: 0→1→2\ngraph2 = {0: [1], 1: [2], 2: []}\nprint(f\"Has cycle: {has_cycle_directed(graph2, 3)}\")  # False",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "21",
   "q": "Program 21: Strongly Connected Components (Tarjan's)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def tarjan_scc(graph, n):\n    index_counter = [0]\n    stack = []\n    lowlink = [0] * n\n    index = [0] * n\n    on_stack = [False] * n\n    visited = [False] * n\n    sccs = []\n    \n    def strongconnect(v):\n        index[v] = index_counter[0]\n        lowlink[v] = index_counter[0]\n        index_counter[0] += 1\n        visited[v] = True\n        stack.append(v)\n        on_stack[v] = True\n        \n        for w in graph.get(v, []):\n            if not visited[w]:\n                strongconnect(w)\n                lowlink[v] = min(lowlink[v], lowlink[w])\n            elif on_stack[w]:\n                lowlink[v] = min(lowlink[v], index[w])\n        \n        if lowlink[v] == index[v]:\n            scc = []\n            while True:\n                w = stack.pop()\n                on_stack[w] = False\n                scc.append(w)\n                if w == v:\n                    break\n            sccs.append(scc)\n    \n    for v in range(n):\n        if not visited[v]:\n            strongconnect(v)\n    \n    return sccs\n\ngraph = {0: [1], 1: [2], 2: [0, 3], 3: [4], 4: [5], 5: [3]}\nprint(tarjan_scc(graph, 6))\n# Output: [[4, 5, 3], [0, 2, 1]] — two SCCs",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "22",
   "q": "Program 22: Maximum Flow (Ford-Fulkerson)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from collections import defaultdict, deque\n\ndef max_flow(graph, source, sink, n):\n    # BFS to find augmenting path\n    def bfs(source, sink, parent):\n        visited = set([source])\n        queue = deque([source])\n        while queue:\n            u = queue.popleft()\n            for v in range(n):\n                if v not in visited and graph[u][v] > 0:\n                    visited.add(v)\n                    parent[v] = u\n                    if v == sink:\n                        return True\n                    queue.append(v)\n        return False\n    \n    total_flow = 0\n    parent = [-1] * n\n    \n    while bfs(source, sink, parent):\n        path_flow = float('inf')\n        v = sink\n        while v != source:\n            u = parent[v]\n            path_flow = min(path_flow, graph[u][v])\n            v = u\n        \n        v = sink\n        while v != source:\n            u = parent[v]\n            graph[u][v] -= path_flow\n            graph[v][u] += path_flow\n            v = u\n        \n        total_flow += path_flow\n        parent = [-1] * n\n    \n    return total_flow\n\n# Capacity graph\nn = 6\ngraph = [[0]*n for _ in range(n)]\nedges = [(0,1,16), (0,2,13), (1,2,10), (1,3,12), (2,1,4), (2,4,14), (3,2,9), (3,5,20), (4,3,7), (4,5,4)]\nfor u, v, c in edges:\n    graph[u][v] = c\n\nprint(f\"Max flow: {max_flow(graph, 0, 5, n)}\")  # 23",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "23",
   "q": "Program 23: Hungarian Algorithm (Simplified Assignment)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def hungarian(cost_matrix):\n    \"\"\"Simplified assignment problem using brute force for small matrices\"\"\"\n    from itertools import permutations\n    \n    n = len(cost_matrix)\n    min_cost = float('inf')\n    best_assignment = None\n    \n    for perm in permutations(range(n)):\n        cost = sum(cost_matrix[i][perm[i]] for i in range(n))\n        if cost < min_cost:\n            min_cost = cost\n            best_assignment = perm\n    \n    return min_cost, best_assignment\n\ncost = [\n    [9, 2, 7, 8],\n    [6, 4, 3, 7],\n    [5, 8, 1, 8],\n    [7, 6, 9, 4]\n]\n\nmin_cost, assignment = hungarian(cost)\nprint(f\"Min cost: {min_cost}\")  # 13\nfor i, j in enumerate(assignment):\n    print(f\"Worker {i} -> Job {j} (cost: {cost[i][j]})\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "24",
   "q": "Program 24: Suffix Array",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def build_suffix_array(text):\n    suffixes = [(text[i:], i) for i in range(len(text))]\n    suffixes.sort()\n    return [idx for _, idx in suffixes]\n\ndef search_pattern(text, pattern, sa):\n    n = len(text)\n    m = len(pattern)\n    lo, hi = 0, n - 1\n    \n    while lo <= hi:\n        mid = (lo + hi) // 2\n        suffix = text[sa[mid]:sa[mid]+m]\n        if suffix == pattern:\n            return sa[mid]\n        elif suffix < pattern:\n            lo = mid + 1\n        else:\n            hi = mid - 1\n    return -1\n\ntext = \"banana$\"\nsa = build_suffix_array(text)\nprint(f\"Suffix array: {sa}\")\n# Suffixes in order: $, a$, ana$, anana$, banana$, na$, nana$\n\nidx = search_pattern(text, \"ana\", sa)\nprint(f\"'ana' found at index: {idx}\")  # 1",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "25",
   "q": "Program 25: KMP String Matching",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def kmp_search(text, pattern):\n    def build_lps(pattern):\n        lps = [0] * len(pattern)\n        length = 0\n        i = 1\n        while i < len(pattern):\n            if pattern[i] == pattern[length]:\n                length += 1\n                lps[i] = length\n                i += 1\n            elif length:\n                length = lps[length - 1]\n            else:\n                lps[i] = 0\n                i += 1\n        return lps\n    \n    lps = build_lps(pattern)\n    matches = []\n    i = j = 0\n    \n    while i < len(text):\n        if text[i] == pattern[j]:\n            i += 1\n            j += 1\n        \n        if j == len(pattern):\n            matches.append(i - j)\n            j = lps[j - 1]\n        elif i < len(text) and text[i] != pattern[j]:\n            if j:\n                j = lps[j - 1]\n            else:\n                i += 1\n    \n    return matches\n\ntext = \"AABAACAADAABAABA\"\npattern = \"AABA\"\nprint(f\"Pattern found at: {kmp_search(text, pattern)}\")\n# Output: [0, 9, 12]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "26",
   "q": "Program 26: Rabin-Karp String Matching",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def rabin_karp(text, pattern, prime=101):\n    n, m = len(text), len(pattern)\n    d = 256  # Number of characters\n    \n    pattern_hash = 0\n    text_hash = 0\n    h = pow(d, m - 1, prime)\n    matches = []\n    \n    # Calculate hash of pattern and first window\n    for i in range(m):\n        pattern_hash = (d * pattern_hash + ord(pattern[i])) % prime\n        text_hash = (d * text_hash + ord(text[i])) % prime\n    \n    for i in range(n - m + 1):\n        if pattern_hash == text_hash:\n            if text[i:i+m] == pattern:  # Verify\n                matches.append(i)\n        \n        if i < n - m:\n            text_hash = (d * (text_hash - ord(text[i]) * h) + ord(text[i + m])) % prime\n            if text_hash < 0:\n                text_hash += prime\n    \n    return matches\n\nprint(rabin_karp(\"AABAACAADAABAABA\", \"AABA\"))\n# Output: [0, 9, 12]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "27",
   "q": "Program 27: Articulation Points (Cut Vertices)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def find_articulation_points(graph, n):\n    visited = [False] * n\n    disc = [0] * n\n    low = [0] * n\n    parent = [-1] * n\n    ap = set()\n    timer = [0]\n    \n    def dfs(u):\n        children = 0\n        visited[u] = True\n        disc[u] = low[u] = timer[0]\n        timer[0] += 1\n        \n        for v in graph.get(u, []):\n            if not visited[v]:\n                children += 1\n                parent[v] = u\n                dfs(v)\n                low[u] = min(low[u], low[v])\n                \n                if parent[u] == -1 and children > 1:\n                    ap.add(u)\n                if parent[u] != -1 and low[v] >= disc[u]:\n                    ap.add(u)\n            elif v != parent[u]:\n                low[u] = min(low[u], disc[v])\n    \n    for i in range(n):\n        if not visited[i]:\n            dfs(i)\n    \n    return ap\n\ngraph = {0: [1, 2], 1: [0, 2], 2: [0, 1, 3], 3: [2, 4], 4: [3]}\nprint(f\"Articulation points: {find_articulation_points(graph, 5)}\")\n# Output: {2, 3}",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "28",
   "q": "Program 28: Traveling Salesman (DP with Bitmask)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def tsp(dist):\n    n = len(dist)\n    INF = float('inf')\n    dp = [[INF] * n for _ in range(1 << n)]\n    dp[1][0] = 0\n    \n    for mask in range(1 << n):\n        for u in range(n):\n            if dp[mask][u] == INF:\n                continue\n            for v in range(n):\n                if mask & (1 << v):\n                    continue\n                new_mask = mask | (1 << v)\n                dp[new_mask][v] = min(dp[new_mask][v], dp[mask][u] + dist[u][v])\n    \n    full_mask = (1 << n) - 1\n    return min(dp[full_mask][i] + dist[i][0] for i in range(1, n))\n\ndist = [\n    [0, 10, 15, 20],\n    [10, 0, 35, 25],\n    [15, 35, 0, 30],\n    [20, 25, 30, 0]\n]\nprint(f\"Min tour cost: {tsp(dist)}\")  # 80",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "29",
   "q": "Program 29: Convex Hull (Graham Scan)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def convex_hull(points):\n    def cross(o, a, b):\n        return (a[0]-o[0])*(b[1]-o[1]) - (a[1]-o[1])*(b[0]-o[0])\n    \n    points = sorted(set(points))\n    if len(points) <= 1:\n        return points\n    \n    # Lower hull\n    lower = []\n    for p in points:\n        while len(lower) >= 2 and cross(lower[-2], lower[-1], p) <= 0:\n            lower.pop()\n        lower.append(p)\n    \n    # Upper hull\n    upper = []\n    for p in reversed(points):\n        while len(upper) >= 2 and cross(upper[-2], upper[-1], p) <= 0:\n            upper.pop()\n        upper.append(p)\n    \n    return lower[:-1] + upper[:-1]\n\npoints = [(0,0), (1,1), (2,2), (4,4), (0,4), (4,0), (2,1), (1,3)]\nhull = convex_hull(points)\nprint(f\"Convex hull: {hull}\")\n# Output: [(0, 0), (4, 0), (4, 4), (0, 4)]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "30",
   "q": "Program 30: Maximum Bipartite Matching",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def max_bipartite_matching(graph, n, m):\n    \"\"\"n = left nodes, m = right nodes, graph[i] = list of right nodes\"\"\"\n    match_right = [-1] * m\n    \n    def dfs(u, visited):\n        for v in graph[u]:\n            if not visited[v]:\n                visited[v] = True\n                if match_right[v] == -1 or dfs(match_right[v], visited):\n                    match_right[v] = u\n                    return True\n        return False\n    \n    result = 0\n    for u in range(n):\n        visited = [False] * m\n        if dfs(u, visited):\n            result += 1\n    \n    return result, match_right\n\n# 3 applicants, 3 jobs\n# Applicant 0: can do jobs 0, 1\n# Applicant 1: can do job 2\n# Applicant 2: can do jobs 1, 2\ngraph = {0: [0, 1], 1: [2], 2: [1, 2]}\nmatches, assignment = max_bipartite_matching(graph, 3, 3)\nprint(f\"Max matching: {matches}\")  # 3\nfor job, applicant in enumerate(assignment):\n    print(f\"Job {job} -> Applicant {applicant}\")",
     "numbered": false
    }
   ]
  }
 ],
 "takeaways": [],
 "_drills": 30,
 "_code": 30
});
