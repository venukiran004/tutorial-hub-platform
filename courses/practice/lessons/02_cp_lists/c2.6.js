/* ============================================================================
   PRACTICE C2.6 — Programs · 6
   ----------------------------------------------------------------------------
   Imported from the Coding Practice source set and rendered through the
   Tutorial Hub design system. Generated by .build/import-practice.py —
   edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "c2.6",
 "lede": "**30 problems** from the lists and arrays set. Each answer is hidden until you ask for it — attempt it first, because reading a solution feels like learning and is not.",
 "objectives": [
  "Work through 30 problems on programs without looking anything up",
  "Predict each answer before revealing it",
  "Recognise the pattern each problem belongs to",
  "Explain your solution out loud, including its complexity"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer before you reveal.** Even a wrong attempt makes the answer stick; reading first does not.",
      "**Say the complexity out loud** where the problem has one. Getting it wrong is the useful part.",
      "Use the bar above to open or close everything at once, and **Ctrl K** to jump to another set."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "Program 151: Smallest Missing Positive Integer",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def first_missing_positive(nums):\n    n = len(nums)\n    # Place each number at its correct index (1 at index 0, etc.)\n    for i in range(n):\n        while 1 <= nums[i] <= n and nums[nums[i]-1] != nums[i]:\n            nums[nums[i]-1], nums[i] = nums[i], nums[nums[i]-1]\n    for i in range(n):\n        if nums[i] != i + 1:\n            return i + 1\n    return n + 1\n\nprint(first_missing_positive([3, 4, -1, 1]))  # Output: 2\nprint(first_missing_positive([1, 2, 0]))       # Output: 3",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "2",
   "q": "Program 152: Longest Subarray with At Most K Distinct",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def longest_k_distinct(nums, k):\n    count = {}\n    left = max_len = 0\n    for right in range(len(nums)):\n        count[nums[right]] = count.get(nums[right], 0) + 1\n        while len(count) > k:\n            count[nums[left]] -= 1\n            if count[nums[left]] == 0:\n                del count[nums[left]]\n            left += 1\n        max_len = max(max_len, right - left + 1)\n    return max_len\n\nprint(longest_k_distinct([1,2,1,2,3], 2))    # Output: 4\nprint(longest_k_distinct([1,2,1,3,4], 3))    # Output: 4",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "3",
   "q": "Program 153: Minimum Size Subarray Sum",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def min_subarray(target, nums):\n    left = total = 0\n    min_len = float('inf')\n    for right in range(len(nums)):\n        total += nums[right]\n        while total >= target:\n            min_len = min(min_len, right - left + 1)\n            total -= nums[left]\n            left += 1\n    return min_len if min_len != float('inf') else 0\n\nprint(min_subarray(7, [2,3,1,2,4,3]))  # Output: 2 (4+3)\nprint(min_subarray(11, [1,1,1,1,1,1,1,1]))  # Output: 0",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "4",
   "q": "Program 154: Count Subarrays with Product < K",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def count_product_less(nums, k):\n    if k <= 1: return 0\n    count = 0\n    product = 1\n    left = 0\n    for right in range(len(nums)):\n        product *= nums[right]\n        while product >= k:\n            product //= nums[left]\n            left += 1\n        count += right - left + 1\n    return count\n\nprint(count_product_less([10, 5, 2, 6], 100))  # Output: 8",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "5",
   "q": "Program 155: Maximum Length of Repeated Subarray",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def find_length(nums1, nums2):\n    m, n = len(nums1), len(nums2)\n    dp = [[0]*(n+1) for _ in range(m+1)]\n    max_len = 0\n    for i in range(1, m+1):\n        for j in range(1, n+1):\n            if nums1[i-1] == nums2[j-1]:\n                dp[i][j] = dp[i-1][j-1] + 1\n                max_len = max(max_len, dp[i][j])\n    return max_len\n\nprint(find_length([1,2,3,2,1], [3,2,1,4,7]))  # Output: 3",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "6",
   "q": "Program 156: Shortest Unsorted Continuous Subarray",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def find_unsorted(nums):\n    n = len(nums)\n    left, right = n, 0\n    max_seen = float('-inf')\n    min_seen = float('inf')\n    \n    for i in range(n):\n        if nums[i] < max_seen:\n            right = i\n        max_seen = max(max_seen, nums[i])\n    \n    for i in range(n-1, -1, -1):\n        if nums[i] > min_seen:\n            left = i\n        min_seen = min(min_seen, nums[i])\n    \n    return right - left + 1 if right > left else 0\n\nprint(find_unsorted([2,6,4,8,10,9,15]))  # Output: 5\nprint(find_unsorted([1,2,3,4]))            # Output: 0",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "7",
   "q": "Program 157: Candy Distribution Problem",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def candy(ratings):\n    n = len(ratings)\n    candies = [1] * n\n    # Left to right\n    for i in range(1, n):\n        if ratings[i] > ratings[i-1]:\n            candies[i] = candies[i-1] + 1\n    # Right to left\n    for i in range(n-2, -1, -1):\n        if ratings[i] > ratings[i+1]:\n            candies[i] = max(candies[i], candies[i+1] + 1)\n    return sum(candies)\n\nprint(candy([1, 0, 2]))      # Output: 5\nprint(candy([1, 2, 2]))      # Output: 4\nprint(candy([1, 3, 2, 2, 1]))  # Output: 7",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "8",
   "q": "Program 158: Gas Station Problem",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def can_complete_circuit(gas, cost):\n    if sum(gas) < sum(cost):\n        return -1\n    start = tank = 0\n    for i in range(len(gas)):\n        tank += gas[i] - cost[i]\n        if tank < 0:\n            start = i + 1\n            tank = 0\n    return start\n\nprint(can_complete_circuit([1,2,3,4,5], [3,4,5,1,2]))  # Output: 3\nprint(can_complete_circuit([2,3,4], [3,4,3]))            # Output: -1",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "9",
   "q": "Program 159: Circular Array Loop Detection",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def circular_array_loop(nums):\n    n = len(nums)\n    for start in range(n):\n        slow = fast = start\n        forward = nums[start] > 0\n        while True:\n            slow = (slow + nums[slow]) % n\n            # Check direction consistency\n            if (nums[slow] > 0) != forward: break\n            # Move fast twice\n            fast = (fast + nums[fast]) % n\n            if (nums[fast] > 0) != forward: break\n            fast = (fast + nums[fast]) % n\n            if (nums[fast] > 0) != forward: break\n            if slow == fast:\n                if slow == (slow + nums[slow]) % n: break  # Single element\n                return True\n    return False\n\nprint(circular_array_loop([2, -1, 1, 2, 2]))  # True\nprint(circular_array_loop([-1, 2]))            # False",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "10",
   "q": "Program 160: Minimum Swaps to Sort",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def min_swaps(lst):\n    indexed = sorted(enumerate(lst), key=lambda x: x[1])\n    visited = [False] * len(lst)\n    swaps = 0\n    for i in range(len(lst)):\n        if visited[i] or indexed[i][0] == i:\n            continue\n        cycle_size = 0\n        j = i\n        while not visited[j]:\n            visited[j] = True\n            j = indexed[j][0]\n            cycle_size += 1\n        swaps += cycle_size - 1\n    return swaps\n\nprint(min_swaps([4, 3, 2, 1]))     # Output: 2\nprint(min_swaps([1, 5, 4, 3, 2]))  # Output: 2",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "11",
   "q": "Program 161: Maximum Circular Subarray Sum",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def max_circular_subarray(nums):\n    # Case 1: Max subarray is non-circular (Kadane's)\n    max_normal = kadane_max(nums)\n    # Case 2: Max subarray wraps around = total - min subarray\n    total = sum(nums)\n    min_subarray = kadane_min(nums)\n    max_wrap = total - min_subarray\n    # If all negative, max_wrap would be 0 (invalid)\n    if max_wrap == 0:\n        return max_normal\n    return max(max_normal, max_wrap)\n\ndef kadane_max(nums):\n    max_sum = current = nums[0]\n    for n in nums[1:]:\n        current = max(n, current + n)\n        max_sum = max(max_sum, current)\n    return max_sum\n\ndef kadane_min(nums):\n    min_sum = current = nums[0]\n    for n in nums[1:]:\n        current = min(n, current + n)\n        min_sum = min(min_sum, current)\n    return min_sum\n\nprint(max_circular_subarray([5, -3, 5]))         # Output: 10\nprint(max_circular_subarray([8, -1, 3, 4]))      # Output: 15\nprint(max_circular_subarray([-3, -2, -3]))        # Output: -2",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "12",
   "q": "Program 162: Find All Subarrays of Length K with Distinct Elements",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def subarrays_distinct_k(nums, k):\n    result = []\n    window = {}\n    for i in range(len(nums)):\n        window[nums[i]] = window.get(nums[i], 0) + 1\n        if i >= k:\n            old = nums[i - k]\n            window[old] -= 1\n            if window[old] == 0:\n                del window[old]\n        if i >= k - 1 and len(window) == k:\n            result.append(nums[i-k+1:i+1])\n    return result\n\nprint(subarrays_distinct_k([1,2,3,2,1,3], 3))\n# Output: [[1,2,3], [2,1,3]]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "13",
   "q": "Program 163: Maximum Gap Between Sorted Elements",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def maximum_gap(nums):\n    if len(nums) < 2:\n        return 0\n    nums.sort()\n    max_gap = 0\n    for i in range(1, len(nums)):\n        max_gap = max(max_gap, nums[i] - nums[i-1])\n    return max_gap\n\nprint(maximum_gap([3, 6, 9, 1]))  # Output: 3\nprint(maximum_gap([10]))           # Output: 0",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "14",
   "q": "Program 164: Find All Distinct Pairs with Difference K",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def pairs_with_diff(nums, k):\n    num_set = set(nums)\n    pairs = set()\n    for num in nums:\n        if num + k in num_set:\n            pairs.add((min(num, num+k), max(num, num+k)))\n        if num - k in num_set:\n            pairs.add((min(num, num-k), max(num, num-k)))\n    return sorted(pairs)\n\nprint(pairs_with_diff([1, 5, 3, 4, 2], 2))\n# Output: [(1, 3), (2, 4), (3, 5)]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "15",
   "q": "Program 165: Reduce Array to Half (Min Removals)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from collections import Counter\n\ndef min_set_size(arr):\n    freq = sorted(Counter(arr).values(), reverse=True)\n    removed = count = 0\n    target = len(arr) // 2\n    for f in freq:\n        removed += f\n        count += 1\n        if removed >= target:\n            return count\n    return count\n\nprint(min_set_size([3,3,3,3,5,5,5,2,2,7]))  # Output: 2",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "16",
   "q": "Program 166: Majority Element II (> n/3)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def majority_element_ii(nums):\n    \"\"\"Boyer-Moore for n/3 threshold — at most 2 candidates.\"\"\"\n    c1 = c2 = None\n    cnt1 = cnt2 = 0\n    for num in nums:\n        if num == c1: cnt1 += 1\n        elif num == c2: cnt2 += 1\n        elif cnt1 == 0: c1, cnt1 = num, 1\n        elif cnt2 == 0: c2, cnt2 = num, 1\n        else: cnt1 -= 1; cnt2 -= 1\n    # Verify\n    threshold = len(nums) // 3\n    result = []\n    if nums.count(c1) > threshold: result.append(c1)\n    if c2 is not None and nums.count(c2) > threshold: result.append(c2)\n    return result\n\nprint(majority_element_ii([3,2,3]))     # [3]\nprint(majority_element_ii([1,2,1,2,3]))  # [1, 2]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "17",
   "q": "Program 167: Trap Rain Water II (2D)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import heapq\n\ndef trap_rain_2d(height_map):\n    if not height_map or len(height_map) < 3: return 0\n    rows, cols = len(height_map), len(height_map[0])\n    visited = [[False]*cols for _ in range(rows)]\n    heap = []\n    \n    for r in range(rows):\n        for c in [0, cols-1]:\n            heapq.heappush(heap, (height_map[r][c], r, c))\n            visited[r][c] = True\n    for c in range(cols):\n        for r in [0, rows-1]:\n            if not visited[r][c]:\n                heapq.heappush(heap, (height_map[r][c], r, c))\n                visited[r][c] = True\n    \n    water = 0\n    while heap:\n        h, r, c = heapq.heappop(heap)\n        for dr, dc in [(0,1),(0,-1),(1,0),(-1,0)]:\n            nr, nc = r+dr, c+dc\n            if 0 <= nr < rows and 0 <= nc < cols and not visited[nr][nc]:\n                visited[nr][nc] = True\n                water += max(0, h - height_map[nr][nc])\n                heapq.heappush(heap, (max(h, height_map[nr][nc]), nr, nc))\n    return water\n\nm = [[1,4,3,1,3,2],[3,2,1,3,2,4],[2,3,3,2,3,1]]\nprint(trap_rain_2d(m))  # Output: 4",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "18",
   "q": "Program 168: Task Scheduler",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from collections import Counter\n\ndef least_interval(tasks, n):\n    counts = Counter(tasks)\n    max_count = max(counts.values())\n    max_tasks = sum(1 for c in counts.values() if c == max_count)\n    result = (max_count - 1) * (n + 1) + max_tasks\n    return max(result, len(tasks))\n\nprint(least_interval([\"A\",\"A\",\"A\",\"B\",\"B\",\"B\"], 2))  # Output: 8\nprint(least_interval([\"A\",\"A\",\"A\",\"B\",\"B\",\"B\"], 0))  # Output: 6",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "19",
   "q": "Program 169: Car Fleet Problem",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def car_fleet(target, position, speed):\n    pairs = sorted(zip(position, speed), reverse=True)\n    fleets = 0\n    last_time = 0\n    for pos, spd in pairs:\n        time = (target - pos) / spd\n        if time > last_time:\n            fleets += 1\n            last_time = time\n    return fleets\n\nprint(car_fleet(12, [10,8,0,5,3], [2,4,1,1,3]))  # Output: 3",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "20",
   "q": "Program 170: Daily Temperatures (Days Until Warmer)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def daily_temperatures(temps):\n    result = [0] * len(temps)\n    stack = []\n    for i, t in enumerate(temps):\n        while stack and temps[stack[-1]] < t:\n            idx = stack.pop()\n            result[idx] = i - idx\n        stack.append(i)\n    return result\n\nprint(daily_temperatures([73,74,75,71,69,72,76,73]))\n# Output: [1, 1, 4, 2, 1, 1, 0, 0]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "21",
   "q": "Program 171: Asteroid Collision",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def asteroid_collision(asteroids):\n    stack = []\n    for ast in asteroids:\n        alive = True\n        while alive and stack and ast < 0 < stack[-1]:\n            if stack[-1] < -ast:\n                stack.pop()\n            elif stack[-1] == -ast:\n                stack.pop()\n                alive = False\n            else:\n                alive = False\n        if alive:\n            stack.append(ast)\n    return stack\n\nprint(asteroid_collision([5, 10, -5]))     # [5, 10]\nprint(asteroid_collision([8, -8]))          # []\nprint(asteroid_collision([10, 2, -5]))      # [10]\nprint(asteroid_collision([-2, -1, 1, 2]))   # [-2, -1, 1, 2]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "22",
   "q": "Program 172: Implement LFU Cache Using Lists",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from collections import defaultdict, OrderedDict\n\nclass LFUCache:\n    def __init__(self, capacity):\n        self.cap = capacity\n        self.min_freq = 0\n        self.key_val = {}\n        self.key_freq = {}\n        self.freq_keys = defaultdict(OrderedDict)\n    \n    def get(self, key):\n        if key not in self.key_val: return -1\n        self._update_freq(key)\n        return self.key_val[key]\n    \n    def put(self, key, val):\n        if self.cap <= 0: return\n        if key in self.key_val:\n            self.key_val[key] = val\n            self._update_freq(key)\n            return\n        if len(self.key_val) >= self.cap:\n            evict_key, _ = self.freq_keys[self.min_freq].popitem(last=False)\n            del self.key_val[evict_key]\n            del self.key_freq[evict_key]\n        self.key_val[key] = val\n        self.key_freq[key] = 1\n        self.freq_keys[1][key] = None\n        self.min_freq = 1\n    \n    def _update_freq(self, key):\n        freq = self.key_freq[key]\n        del self.freq_keys[freq][key]\n        if not self.freq_keys[freq] and freq == self.min_freq:\n            self.min_freq += 1\n        self.key_freq[key] = freq + 1\n        self.freq_keys[freq + 1][key] = None\n\ncache = LFUCache(2)\ncache.put(1, 1); cache.put(2, 2)\nprint(cache.get(1))  # 1\ncache.put(3, 3)      # Evicts key 2\nprint(cache.get(2))  # -1",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "23",
   "q": "Program 173: Valid Parentheses Using Stack (List)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def is_valid_parens(s):\n    stack = []\n    pairs = {')': '(', '}': '{', ']': '['}\n    for char in s:\n        if char in '({[':\n            stack.append(char)\n        elif char in ')}]':\n            if not stack or stack[-1] != pairs[char]:\n                return False\n            stack.pop()\n    return len(stack) == 0\n\nprint(is_valid_parens(\"({[]})\"))  # True\nprint(is_valid_parens(\"([)]\"))    # False\nprint(is_valid_parens(\"\"))        # True",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "24",
   "q": "Program 174: Evaluate Expression Using Two Stacks",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def evaluate(expression):\n    def precedence(op):\n        if op in '+-': return 1\n        if op in '*/': return 2\n        return 0\n    \n    def apply_op(a, b, op):\n        if op == '+': return a + b\n        if op == '-': return a - b\n        if op == '*': return a * b\n        if op == '/': return int(a / b)\n    \n    values = []\n    ops = []\n    i = 0\n    while i < len(expression):\n        if expression[i] == ' ':\n            i += 1; continue\n        if expression[i] == '(':\n            ops.append(expression[i])\n        elif expression[i].isdigit():\n            num = 0\n            while i < len(expression) and expression[i].isdigit():\n                num = num * 10 + int(expression[i])\n                i += 1\n            values.append(num)\n            continue\n        elif expression[i] == ')':\n            while ops and ops[-1] != '(':\n                b, a = values.pop(), values.pop()\n                values.append(apply_op(a, b, ops.pop()))\n            ops.pop()\n        else:\n            while ops and precedence(ops[-1]) >= precedence(expression[i]):\n                b, a = values.pop(), values.pop()\n                values.append(apply_op(a, b, ops.pop()))\n            ops.append(expression[i])\n        i += 1\n    while ops:\n        b, a = values.pop(), values.pop()\n        values.append(apply_op(a, b, ops.pop()))\n    return values[0]\n\nprint(evaluate(\"3 + 5 * 2\"))        # 13\nprint(evaluate(\"(3 + 5) * 2\"))      # 16\nprint(evaluate(\"10 + 2 * 6\"))       # 22",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "25",
   "q": "Program 175: Skyline Problem",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import heapq\n\ndef get_skyline(buildings):\n    events = []\n    for left, right, height in buildings:\n        events.append((left, -height, right))   # Start\n        events.append((right, 0, 0))             # End\n    events.sort()\n    \n    result = []\n    heap = [(0, float('inf'))]  # (neg_height, end)\n    \n    for x, neg_h, end in events:\n        while heap[0][1] <= x:\n            heapq.heappop(heap)\n        if neg_h:\n            heapq.heappush(heap, (neg_h, end))\n        max_h = -heap[0][0]\n        if not result or result[-1][1] != max_h:\n            result.append([x, max_h])\n    \n    return result\n\nprint(get_skyline([[2,9,10],[3,7,15],[5,12,12],[15,20,10],[19,24,8]]))\n# Output: [[2,10],[3,15],[7,12],[12,0],[15,10],[20,8],[24,0]]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "26",
   "q": "Program 176: Range Sum Query (Prefix Sum)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class RangeSumQuery:\n    def __init__(self, nums):\n        self.prefix = [0]\n        for num in nums:\n            self.prefix.append(self.prefix[-1] + num)\n    \n    def query(self, left, right):\n        return self.prefix[right + 1] - self.prefix[left]\n\nrsq = RangeSumQuery([1, 3, 5, 7, 9, 11])\nprint(rsq.query(1, 3))  # 15 (3+5+7)\nprint(rsq.query(0, 5))  # 36 (1+3+5+7+9+11)\nprint(rsq.query(2, 4))  # 21 (5+7+9)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "27",
   "q": "Program 177: Range Sum Query 2D (Prefix Sum Matrix)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class RangeSum2D:\n    def __init__(self, matrix):\n        rows, cols = len(matrix), len(matrix[0])\n        self.prefix = [[0]*(cols+1) for _ in range(rows+1)]\n        for i in range(rows):\n            for j in range(cols):\n                self.prefix[i+1][j+1] = (matrix[i][j] + self.prefix[i][j+1] +\n                                          self.prefix[i+1][j] - self.prefix[i][j])\n    \n    def query(self, r1, c1, r2, c2):\n        return (self.prefix[r2+1][c2+1] - self.prefix[r1][c2+1] -\n                self.prefix[r2+1][c1] + self.prefix[r1][c1])\n\nm = [[3,0,1,4,2],[5,6,3,2,1],[1,2,0,1,5],[4,1,0,1,7],[1,0,3,0,5]]\nrsq = RangeSum2D(m)\nprint(rsq.query(2, 1, 4, 3))  # 8\nprint(rsq.query(1, 1, 2, 2))  # 11",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "28",
   "q": "Program 178: Difference Array (Range Update)",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class DifferenceArray:\n    def __init__(self, n):\n        self.diff = [0] * (n + 1)\n    \n    def update(self, left, right, val):\n        self.diff[left] += val\n        self.diff[right + 1] -= val\n    \n    def get_result(self):\n        result = []\n        current = 0\n        for i in range(len(self.diff) - 1):\n            current += self.diff[i]\n            result.append(current)\n        return result\n\nda = DifferenceArray(5)\nda.update(1, 3, 2)   # Add 2 to indices 1-3\nda.update(2, 4, 3)   # Add 3 to indices 2-4\nprint(da.get_result())  # [0, 2, 5, 5, 3]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "29",
   "q": "Program 179: Sparse Vector Dot Product",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class SparseVector:\n    def __init__(self, nums):\n        self.pairs = [(i, v) for i, v in enumerate(nums) if v != 0]\n    \n    def dot(self, other):\n        i = j = result = 0\n        while i < len(self.pairs) and j < len(other.pairs):\n            if self.pairs[i][0] == other.pairs[j][0]:\n                result += self.pairs[i][1] * other.pairs[j][1]\n                i += 1; j += 1\n            elif self.pairs[i][0] < other.pairs[j][0]:\n                i += 1\n            else:\n                j += 1\n        return result\n\nv1 = SparseVector([1, 0, 0, 2, 3])\nv2 = SparseVector([0, 3, 0, 4, 0])\nprint(v1.dot(v2))  # Output: 8 (0+0+0+8+0)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "30",
   "q": "Program 180: Implement Circular Buffer",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class CircularBuffer:\n    def __init__(self, capacity):\n        self.buf = [None] * capacity\n        self.cap = capacity\n        self.head = self.size = 0\n    \n    def push(self, val):\n        idx = (self.head + self.size) % self.cap\n        if self.size == self.cap:\n            self.head = (self.head + 1) % self.cap\n        else:\n            self.size += 1\n        self.buf[idx] = val\n    \n    def pop(self):\n        if self.size == 0: return None\n        val = self.buf[self.head]\n        self.head = (self.head + 1) % self.cap\n        self.size -= 1\n        return val\n    \n    def to_list(self):\n        return [self.buf[(self.head + i) % self.cap] for i in range(self.size)]\n\ncb = CircularBuffer(3)\ncb.push(1); cb.push(2); cb.push(3)\nprint(cb.to_list())  # [1, 2, 3]\ncb.push(4)           # Overwrites 1\nprint(cb.to_list())  # [2, 3, 4]\nprint(cb.pop())      # 2",
     "numbered": false
    }
   ]
  }
 ],
 "takeaways": [],
 "_drills": 30,
 "_code": 30
});
