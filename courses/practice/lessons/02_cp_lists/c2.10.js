/* ============================================================================
   PRACTICE C2.10 — More List Programs · 2
   ----------------------------------------------------------------------------
   Imported from the Coding Practice source set and rendered through the
   Tutorial Hub design system. Generated by .build/import-practice.py —
   edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "c2.10",
 "lede": "**7 problems** from the lists and arrays set. Each answer is hidden until you ask for it — attempt it first, because reading a solution feels like learning and is not.",
 "objectives": [
  "Work through 7 problems on more list programs without looking anything up",
  "Predict each answer before revealing it",
  "Recognise the pattern each problem belongs to",
  "Explain your solution out loud, including its complexity"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer before you reveal.** Even a wrong attempt makes the answer stick; reading first does not.",
      "**Say the complexity out loud** where the problem has one. Getting it wrong is the useful part.",
      "Use the bar above to open or close everything at once, and **Ctrl K** to jump to another set."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "Program 248: array.array holds one C type and pays for it in memory",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import array, sys\n\nxs = list(range(1000))\narr = array.array(\"i\", range(1000))      # 'i' = signed int, 4 bytes each\n\nprint(\"list  typecode: anything\")\nprint(\"array typecode:\", arr.typecode, \"itemsize\", arr.itemsize, \"bytes\")\nprint(\"list  bytes  :\", sys.getsizeof(xs), \"+ the int objects it points at\")\nprint(\"array bytes  :\", sys.getsizeof(arr))\n\narr.append(1000)\nprint(\"append works :\", arr[-1])\ntry:\n    arr.append(\"nope\")\nexcept TypeError as e:\n    print(\"wrong type   :\", e)\n\nprint(\"back to a list:\", arr.tolist()[:5], \"...\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "list  typecode: anything\narray typecode: i itemsize 4 bytes\nlist  bytes  : 8056 + the int objects it points at\narray bytes  : 4200\nappend works : 1000\nwrong type   : 'str' object cannot be interpreted as an integer\nback to a list: [0, 1, 2, 3, 4] ...",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "2",
   "q": "Program 249: A numpy array is not a list, and the operators prove it",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import numpy as np\n\nxs = [1, 2, 3]\na = np.array(xs)\n\nprint(\"list  + list :\", xs + xs, \"  <- concatenation\")\nprint(\"array + array:\", a + a, \"  <- elementwise addition\")\nprint(\"list  * 2    :\", xs * 2)\nprint(\"array * 2    :\", a * 2)\n\nprint(\"broadcasting :\", a + 10)\nprint(\"vectorised   :\", a ** 2)\n\n# a list comprehension is the list-world equivalent\nprint(\"comprehension:\", [v ** 2 for v in xs])\n\nprint(\"dtype is fixed:\", a.dtype)\nprint(\"mixed types coerce:\", np.array([1, 2.5, 3]).dtype)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "list  + list : [1, 2, 3, 1, 2, 3]   <- concatenation\narray + array: [2 4 6]   <- elementwise addition\nlist  * 2    : [1, 2, 3, 1, 2, 3]\narray * 2    : [2 4 6]\nbroadcasting : [11 12 13]\nvectorised   : [1 4 9]\ncomprehension: [1, 4, 9]\ndtype is fixed: int64\nmixed types coerce: float64",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "3",
   "q": "Program 250: itertools.product flattens nested loops over lists",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from itertools import product\n\nsizes = [\"S\", \"M\"]\ncolours = [\"red\", \"blue\"]\n\nnested = []\nfor s in sizes:\n    for c in colours:\n        nested.append((s, c))\nprint(\"nested loops:\", nested)\nprint(\"product     :\", list(product(sizes, colours)))\n\n# repeat= gives you the same list crossed with itself\nprint(\"repeat=2    :\", list(product([0, 1], repeat=2)))\n\n# the classic use: a parameter grid\ngrid = list(product([0.1, 0.5], [\"adam\", \"sgd\"], [16, 32]))\nprint(\"grid size   :\", len(grid))\nprint(\"first three :\", grid[:3])",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "nested loops: [('S', 'red'), ('S', 'blue'), ('M', 'red'), ('M', 'blue')]\nproduct     : [('S', 'red'), ('S', 'blue'), ('M', 'red'), ('M', 'blue')]\nrepeat=2    : [(0, 0), (0, 1), (1, 0), (1, 1)]\ngrid size   : 8\nfirst three : [(0.1, 'adam', 16), (0.1, 'adam', 32), (0.1, 'sgd', 16)]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "4",
   "q": "Program 251: Type hints for lists, and what they do not do",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from typing import Sequence, Iterable\n\ndef total(nums: list[int]) -> int:       # 3.9+: builtin generics, no typing import\n    return sum(nums)\n\ndef first(items: Sequence[str]) -> str:  # Sequence: indexable, len(), read-only intent\n    return items[0]\n\ndef count_all(items: Iterable[int]) -> int:   # Iterable: only promises \"you can loop it\"\n    return sum(1 for _ in items)\n\nprint(\"total       :\", total([1, 2, 3]))\nprint(\"first       :\", first([\"a\", \"b\"]), \"| tuples satisfy Sequence too:\", first((\"x\", \"y\")))\nprint(\"count_all   :\", count_all(range(5)), \"<- a range, not a list\")\nprint(\"annotations :\", total.__annotations__)\n\n# Nothing checks the hint at run time. Passing strings does not fail *because of*\n# list[int]; it fails later, inside sum(), for the ordinary reason.\ntry:\n    total([\"a\", \"b\"])\nexcept TypeError as e:\n    print(\"runtime error:\", e)\n    print(\"             ^ raised by sum(), not by the annotation\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "total       : 6\nfirst       : a | tuples satisfy Sequence too: x\ncount_all   : 5 <- a range, not a list\nannotations : {'nums': list[int], 'return': <class 'int'>}\nruntime error: unsupported operand type(s) for +: 'int' and 'str'\n             ^ raised by sum(), not by the annotation",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "5",
   "q": "Program 252: Sorting strings: case, and why sorted() is not alphabetical",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "words = [\"banana\", \"Apple\", \"cherry\", \"Zebra\", \"apple\"]\n\nprint(\"default     :\", sorted(words))            # every capital sorts before every lowercase\nprint(\"case-folded :\", sorted(words, key=str.lower))\n\n# ord() explains it: the whole uppercase block sits below the lowercase block\nprint(\"ord('A')\", ord(\"A\"), \" ord('Z')\", ord(\"Z\"), \" ord('a')\", ord(\"a\"))\nprint(\"'Zebra' < 'apple':\", \"Zebra\" < \"apple\")\n\n# str.lower is enough for ASCII; casefold also folds things like the German sharp s\nprint(\"lower vs casefold:\", \"STRASSE\".lower() == \"stra\\u00dfe\".lower(),\n      \"|\", \"STRASSE\".casefold() == \"stra\\u00dfe\".casefold())\n\n# stability means equal keys keep their input order\nprint(\"stable tie  :\", sorted([\"apple\", \"Apple\"], key=str.lower))\nprint(\"reversed tie:\", sorted([\"Apple\", \"apple\"], key=str.lower))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "default     : ['Apple', 'Zebra', 'apple', 'banana', 'cherry']\ncase-folded : ['Apple', 'apple', 'banana', 'cherry', 'Zebra']\nord('A') 65  ord('Z') 90  ord('a') 97\n'Zebra' < 'apple': True\nlower vs casefold: False | True\nstable tie  : ['apple', 'Apple']\nreversed tie: ['Apple', 'apple']",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "6",
   "q": "Program 253: Growing a list: append is amortised O(1), insert(0) is not",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import sys, timeit\n\nxs = []\nsizes = []\nfor i in range(20):\n    xs.append(i)\n    sizes.append(sys.getsizeof(xs))\nprint(\"capacity steps:\", sorted(set(sizes))[:8], \"...\")\nprint(\"len 20 costs  :\", sys.getsizeof(xs), \"bytes of pointer array\")\n\n# Timings vary by machine; the ratio is the point, not the absolute numbers.\nn = 5000\nappend_t = timeit.timeit(\"xs.append(1)\", setup=\"xs=[]\", number=n)\ninsert_t = timeit.timeit(\"xs.insert(0, 1)\", setup=\"xs=[]\", number=n)\n\nprint(\"append x%d   : %.4f s\" % (n, append_t))\nprint(\"insert(0) x%d: %.4f s\" % (n, insert_t))\nprint(\"insert(0) shifts every element, so it is slower:\", insert_t > append_t)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "capacity steps: [88, 120, 184, 248] ...\nlen 20 costs  : 248 bytes of pointer array\nappend x5000   : 0.0001 s\ninsert(0) x5000: 0.0033 s\ninsert(0) shifts every element, so it is slower: True",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "7",
   "q": "Program 254: Membership: a list scans, a set hashes",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import timeit\n\nn = 20000\nxs = list(range(n))\nst = set(xs)\n\ntarget = n - 1                       # worst case for the list: the last element\nlist_t = timeit.timeit(lambda: target in xs, number=200)\nset_t = timeit.timeit(lambda: target in st, number=200)\n\n# Timings vary by machine; what is stable is which one wins, and by how much.\nprint(\"value in list: %.6f s\" % list_t)\nprint(\"value in set : %.6f s\" % set_t)\nprint(\"set is faster:\", set_t < list_t)\n\n# but a set loses order and duplicates, and needs hashable items\nprint(\"order lost   :\", list(set([\"b\", \"a\", \"b\"])) != [\"b\", \"a\", \"b\"])\ntry:\n    {[1, 2]}\nexcept TypeError as e:\n    print(\"unhashable   :\", e)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "value in list: 0.017777 s\nvalue in set : 0.000012 s\nset is faster: True\norder lost   : True\nunhashable   : unhashable type: 'list'",
     "numbered": false
    }
   ]
  }
 ],
 "takeaways": [],
 "_drills": 7,
 "_code": 14
});
