/* ============================================================================
   PRACTICE C6.1 — Programs · 1
   ----------------------------------------------------------------------------
   Imported from the Coding Practice source set and rendered through the
   Tutorial Hub design system. Generated by .build/import-practice.py —
   edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "c7.1",
 "lede": "**30 problems** from the errors, i/o and concurrency set. Each answer is hidden until you ask for it — attempt it first, because reading a solution feels like learning and is not.",
 "objectives": [
  "Work through 30 problems on programs without looking anything up",
  "Predict each answer before revealing it",
  "Recognise the pattern each problem belongs to",
  "Explain your solution out loud, including its complexity"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "viz",
   "title": "The order of try, except, else and finally",
   "caption": "`else` runs only when nothing was raised; `finally` runs on every path, including a return from inside the try. Most questions here turn on that last clause.",
   "svg": "<svg viewBox='0 0 880 220' role='img' aria-label='Control flow through try, except, else and finally on both the success and failure paths'><g style='stroke-width:2'><rect x='40' y='44' width='170' height='44' rx='6' style='fill:var(--accent);fill-opacity:.13;stroke:var(--accent)'/><rect x='260' y='44' width='170' height='44' rx='6' style='fill:var(--good);fill-opacity:.14;stroke:var(--good)'/><rect x='260' y='104' width='170' height='44' rx='6' style='fill:var(--crit);fill-opacity:.12;stroke:var(--crit)'/><rect x='480' y='74' width='170' height='44' rx='6' style='fill:var(--warn);fill-opacity:.14;stroke:var(--warn)'/></g><text x='62' y='72' class='s-sub' style='fill:var(--ink-2)'>try</text><text x='282' y='72' class='s-sub' style='fill:var(--ink-2)'>else -- no exception</text><text x='282' y='132' class='s-sub' style='fill:var(--ink-2)'>except -- one was raised</text><text x='502' y='102' class='s-label' style='fill:var(--warn)'>finally</text><g style='stroke:var(--ink-3);stroke-width:1.5'><line x1='214' y1='58' x2='256' y2='58' marker-end='url(#ex-a)'/><line x1='214' y1='76' x2='256' y2='118' marker-end='url(#ex-a)'/><line x1='434' y1='66' x2='476' y2='88' marker-end='url(#ex-a)'/><line x1='434' y1='126' x2='476' y2='104' marker-end='url(#ex-a)'/></g><text x='670' y='96' class='s-sub' style='fill:var(--warn)'>always runs</text><text x='40' y='180' class='s-sub' style='fill:var(--crit)'>A return inside finally silently discards the exception and any earlier return -- a favourite of these question sets.</text><defs><marker id='ex-a' markerWidth='8' markerHeight='8' refX='6' refY='4' orient='auto'><path d='M0 0 L8 4 L0 8 z' style='fill:var(--ink-3)'/></marker></defs></svg>"
  },
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer before you reveal.** Even a wrong attempt makes the answer stick; reading first does not.",
      "**Say the complexity out loud** where the problem has one. Getting it wrong is the useful part.",
      "Use the bar above to open or close everything at once, and **Ctrl K** to jump to another set."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "Program 1: Read File Line by Line",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def read_lines(filepath):\n    lines = []\n    with open(filepath, 'r') as f:\n        for line in f:\n            lines.append(line.strip())\n    return lines\n\n# Write sample then read\nwith open(\"sample.txt\", \"w\") as f:\n    f.write(\"Hello\\nWorld\\nPython\")\n\nprint(read_lines(\"sample.txt\"))\n# Output: ['Hello', 'World', 'Python']",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "2",
   "q": "Program 2: CSV Reader/Writer",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import csv\n\n# Write CSV\ndata = [[\"Name\", \"Age\", \"City\"],\n        [\"Alice\", 30, \"NYC\"],\n        [\"Bob\", 25, \"LA\"]]\n\nwith open(\"people.csv\", \"w\", newline=\"\") as f:\n    writer = csv.writer(f)\n    writer.writerows(data)\n\n# Read CSV\nwith open(\"people.csv\", \"r\") as f:\n    reader = csv.DictReader(f)\n    for row in reader:\n        print(dict(row))\n\n# Output:\n# {'Name': 'Alice', 'Age': '30', 'City': 'NYC'}\n# {'Name': 'Bob', 'Age': '25', 'City': 'LA'}",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "3",
   "q": "Program 3: JSON Read/Write",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import json\n\ndata = {\n    \"users\": [\n        {\"name\": \"Alice\", \"age\": 30, \"skills\": [\"Python\", \"ML\"]},\n        {\"name\": \"Bob\", \"age\": 25, \"skills\": [\"Java\", \"DevOps\"]}\n    ]\n}\n\n# Write\nwith open(\"data.json\", \"w\") as f:\n    json.dump(data, f, indent=2)\n\n# Read\nwith open(\"data.json\", \"r\") as f:\n    loaded = json.load(f)\n\nprint(loaded[\"users\"][0][\"name\"])  # Output: Alice\nprint(json.dumps(loaded, indent=2)[:50])",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "4",
   "q": "Program 4: YAML Parser",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# pip install pyyaml\nimport yaml\n\nconfig_str = \"\"\"\ndatabase:\n  host: localhost\n  port: 5432\n  name: mydb\nlogging:\n  level: INFO\n  file: app.log\n\"\"\"\n\nconfig = yaml.safe_load(config_str)\nprint(config[\"database\"][\"host\"])  # Output: localhost\nprint(config[\"logging\"][\"level\"])  # Output: INFO\n\n# Dump back to YAML\noutput = yaml.dump(config, default_flow_style=False)\nprint(output)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "5",
   "q": "Program 5: Pickle Serialization",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import pickle\n\nclass Model:\n    def __init__(self, name, accuracy):\n        self.name = name\n        self.accuracy = accuracy\n    def __repr__(self):\n        return f\"Model({self.name}, acc={self.accuracy})\"\n\nmodel = Model(\"RandomForest\", 0.95)\n\n# Serialize\nwith open(\"model.pkl\", \"wb\") as f:\n    pickle.dump(model, f)\n\n# Deserialize\nwith open(\"model.pkl\", \"rb\") as f:\n    loaded = pickle.load(f)\n\nprint(loaded)  # Output: Model(RandomForest, acc=0.95)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "6",
   "q": "Program 6: INI Config Parser",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import configparser\n\nconfig = configparser.ConfigParser()\nconfig[\"DEFAULT\"] = {\"debug\": \"false\"}\nconfig[\"server\"] = {\"host\": \"0.0.0.0\", \"port\": \"8080\"}\nconfig[\"database\"] = {\"host\": \"localhost\", \"port\": \"5432\", \"name\": \"mydb\"}\n\n# Write\nwith open(\"config.ini\", \"w\") as f:\n    config.write(f)\n\n# Read\nconfig2 = configparser.ConfigParser()\nconfig2.read(\"config.ini\")\nprint(config2[\"server\"][\"host\"])     # Output: 0.0.0.0\nprint(config2[\"database\"][\"name\"])   # Output: mydb\nprint(config2[\"server\"][\"debug\"])    # Output: false (from DEFAULT)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "7",
   "q": "Program 7: Binary File Read/Write",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import struct\n\n# Write binary data\ndata = [(1, 3.14, b'Hi'), (2, 2.72, b'By')]\n\nwith open(\"data.bin\", \"wb\") as f:\n    for id_, val, msg in data:\n        packed = struct.pack(\"if2s\", id_, val, msg)\n        f.write(packed)\n\n# Read binary data\nwith open(\"data.bin\", \"rb\") as f:\n    size = struct.calcsize(\"if2s\")\n    while True:\n        chunk = f.read(size)\n        if not chunk:\n            break\n        id_, val, msg = struct.unpack(\"if2s\", chunk)\n        print(f\"ID={id_}, Val={val:.2f}, Msg={msg.decode()}\")\n\n# Output:\n# ID=1, Val=3.14, Msg=Hi\n# ID=2, Val=2.72, Msg=By",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "8",
   "q": "Program 8: Memory-Mapped File",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import mmap\nimport os\n\n# Create file\nwith open(\"mmap_test.txt\", \"wb\") as f:\n    f.write(b\"Hello, Memory Mapped File!\")\n\n# Read with mmap\nwith open(\"mmap_test.txt\", \"r+b\") as f:\n    mm = mmap.mmap(f.fileno(), 0)\n    print(mm[:5].decode())  # Output: Hello\n    \n    # Search\n    pos = mm.find(b\"Mapped\")\n    print(f\"'Mapped' found at position: {pos}\")  # Output: 15\n    \n    # Modify in place\n    mm[0:5] = b\"HELLO\"\n    mm.close()\n\nwith open(\"mmap_test.txt\", \"r\") as f:\n    print(f.read())  # Output: HELLO, Memory Mapped File!",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "9",
   "q": "Program 9: Temp File Handling",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import tempfile\nimport os\n\n# Named temp file\nwith tempfile.NamedTemporaryFile(mode='w', suffix='.txt', delete=False) as f:\n    f.write(\"Temporary data\\n\")\n    temp_path = f.name\n\nprint(f\"Temp file: {temp_path}\")\nwith open(temp_path, 'r') as f:\n    print(f.read())\n\nos.unlink(temp_path)\n\n# Temp directory\nwith tempfile.TemporaryDirectory() as tmpdir:\n    filepath = os.path.join(tmpdir, \"test.txt\")\n    with open(filepath, 'w') as f:\n        f.write(\"In temp dir\")\n    print(f\"Temp dir: {tmpdir}\")\n# Directory auto-deleted here",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "10",
   "q": "Program 10: File Watcher",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import os\nimport time\nimport hashlib\n\nclass FileWatcher:\n    def __init__(self, path):\n        self.path = path\n        self.file_hashes = {}\n        self._scan()\n    \n    def _hash_file(self, filepath):\n        with open(filepath, 'rb') as f:\n            return hashlib.md5(f.read()).hexdigest()\n    \n    def _scan(self):\n        for root, dirs, files in os.walk(self.path):\n            for name in files:\n                fp = os.path.join(root, name)\n                self.file_hashes[fp] = self._hash_file(fp)\n    \n    def check_changes(self):\n        changes = {\"modified\": [], \"added\": [], \"deleted\": []}\n        current = {}\n        for root, dirs, files in os.walk(self.path):\n            for name in files:\n                fp = os.path.join(root, name)\n                h = self._hash_file(fp)\n                current[fp] = h\n                if fp not in self.file_hashes:\n                    changes[\"added\"].append(fp)\n                elif self.file_hashes[fp] != h:\n                    changes[\"modified\"].append(fp)\n        for fp in self.file_hashes:\n            if fp not in current:\n                changes[\"deleted\"].append(fp)\n        self.file_hashes = current\n        return changes\n\nprint(\"FileWatcher class implemented\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "11",
   "q": "Program 11: Log File Parser",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import re\nfrom collections import Counter\n\nlog_data = \"\"\"\n2024-01-15 10:23:45 ERROR Database connection failed\n2024-01-15 10:24:00 INFO Server started on port 8080\n2024-01-15 10:24:15 WARNING High memory usage: 85%\n2024-01-15 10:25:00 ERROR Timeout on API call\n2024-01-15 10:25:30 INFO Request processed successfully\n\"\"\"\n\npattern = r'(\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}) (\\w+) (.+)'\n\nentries = []\nfor match in re.finditer(pattern, log_data):\n    entries.append({\n        \"timestamp\": match.group(1),\n        \"level\": match.group(2),\n        \"message\": match.group(3)\n    })\n\nlevel_counts = Counter(e[\"level\"] for e in entries)\nprint(f\"Log entries: {len(entries)}\")\nprint(f\"Level counts: {dict(level_counts)}\")\nerrors = [e for e in entries if e[\"level\"] == \"ERROR\"]\nprint(f\"Errors: {[e['message'] for e in errors]}\")\n# Output:\n# Log entries: 5\n# Level counts: {'ERROR': 2, 'INFO': 2, 'WARNING': 1}\n# Errors: ['Database connection failed', 'Timeout on API call']",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "12",
   "q": "Program 12: Gzip Compression",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import gzip\nimport json\n\ndata = {\"users\": [{\"name\": f\"User_{i}\", \"score\": i*10} for i in range(100)]}\njson_str = json.dumps(data)\n\n# Compress\nwith gzip.open(\"data.json.gz\", \"wt\") as f:\n    f.write(json_str)\n\n# Decompress\nwith gzip.open(\"data.json.gz\", \"rt\") as f:\n    loaded = json.loads(f.read())\n\nimport os\noriginal_size = len(json_str.encode())\ncompressed_size = os.path.getsize(\"data.json.gz\")\nprint(f\"Original: {original_size} bytes\")\nprint(f\"Compressed: {compressed_size} bytes\")\nprint(f\"Ratio: {compressed_size/original_size:.1%}\")\nprint(f\"Users loaded: {len(loaded['users'])}\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "13",
   "q": "Program 13: ZIP Archive Handler",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import zipfile\nimport os\n\n# Create files\nfor i in range(3):\n    with open(f\"file_{i}.txt\", \"w\") as f:\n        f.write(f\"Content of file {i}\\n\" * 10)\n\n# Create ZIP\nwith zipfile.ZipFile(\"archive.zip\", \"w\", zipfile.ZIP_DEFLATED) as zf:\n    for i in range(3):\n        zf.write(f\"file_{i}.txt\")\n\n# Read ZIP info\nwith zipfile.ZipFile(\"archive.zip\", \"r\") as zf:\n    for info in zf.infolist():\n        print(f\"{info.filename}: {info.file_size} → {info.compress_size} bytes\")\n    \n    # Extract specific file\n    content = zf.read(\"file_0.txt\").decode()\n    print(f\"First file content: {content[:20]}...\")\n\n# Cleanup\nfor i in range(3):\n    os.remove(f\"file_{i}.txt\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "14",
   "q": "Program 14: MessagePack Serialization",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# pip install msgpack\nimport msgpack\nimport json\nimport sys\n\ndata = {\n    \"name\": \"Alice\",\n    \"scores\": [95, 87, 92, 88],\n    \"metadata\": {\"level\": 5, \"active\": True}\n}\n\n# Serialize with msgpack\npacked = msgpack.packb(data, use_bin_type=True)\njson_str = json.dumps(data)\n\nprint(f\"MsgPack size: {len(packed)} bytes\")\nprint(f\"JSON size: {len(json_str)} bytes\")\nprint(f\"Savings: {1 - len(packed)/len(json_str):.1%}\")\n\n# Deserialize\nunpacked = msgpack.unpackb(packed, raw=False)\nprint(f\"Unpacked: {unpacked}\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "15",
   "q": "Program 15: SQLite Database Operations",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import sqlite3\n\n# Create and populate\nconn = sqlite3.connect(\":memory:\")\ncursor = conn.cursor()\n\ncursor.execute(\"\"\"\n    CREATE TABLE employees (\n        id INTEGER PRIMARY KEY,\n        name TEXT NOT NULL,\n        department TEXT,\n        salary REAL\n    )\n\"\"\")\n\nemployees = [\n    (\"Alice\", \"Engineering\", 95000),\n    (\"Bob\", \"Marketing\", 75000),\n    (\"Charlie\", \"Engineering\", 88000),\n    (\"Diana\", \"HR\", 70000),\n    (\"Eve\", \"Engineering\", 105000)\n]\n\ncursor.executemany(\"INSERT INTO employees (name, department, salary) VALUES (?, ?, ?)\", employees)\nconn.commit()\n\n# Query\ncursor.execute(\"\"\"\n    SELECT department, COUNT(*), AVG(salary) \n    FROM employees \n    GROUP BY department \n    ORDER BY AVG(salary) DESC\n\"\"\")\n\nfor dept, count, avg_sal in cursor.fetchall():\n    print(f\"{dept}: {count} employees, avg salary ${avg_sal:,.0f}\")\n\nconn.close()\n# Output:\n# Engineering: 3 employees, avg salary $96,000\n# Marketing: 1 employees, avg salary $75,000\n# HR: 1 employees, avg salary $70,000",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "16",
   "q": "Program 16: Path Operations with pathlib",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from pathlib import Path\n\n# Current directory\ncwd = Path.cwd()\nprint(f\"CWD: {cwd}\")\n\n# Path construction\nconfig = Path.home() / \".config\" / \"myapp\" / \"settings.json\"\nprint(f\"Config: {config}\")\nprint(f\"Parent: {config.parent}\")\nprint(f\"Stem: {config.stem}\")\nprint(f\"Suffix: {config.suffix}\")\n\n# Find files\npy_files = list(Path(\".\").glob(\"*.py\"))\nprint(f\"Python files: {len(py_files)}\")\n\n# File info\nfor p in Path(\".\").iterdir():\n    if p.is_file():\n        print(f\"  {p.name}: {p.stat().st_size} bytes\")\n        break",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "17",
   "q": "Program 17: Environment File Parser",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def parse_env_file(filepath):\n    env = {}\n    try:\n        with open(filepath, 'r') as f:\n            for line in f:\n                line = line.strip()\n                if not line or line.startswith('#'):\n                    continue\n                if '=' in line:\n                    key, value = line.split('=', 1)\n                    value = value.strip().strip('\"').strip(\"'\")\n                    env[key.strip()] = value\n    except FileNotFoundError:\n        pass\n    return env\n\n# Create sample .env\nwith open(\".env.sample\", \"w\") as f:\n    f.write(\"\"\"# Database config\nDB_HOST=localhost\nDB_PORT=5432\nDB_NAME=\"myapp_db\"\nAPI_KEY='sk-abc123'\nDEBUG=true\n\"\"\")\n\nconfig = parse_env_file(\".env.sample\")\nprint(config)\n# Output: {'DB_HOST': 'localhost', 'DB_PORT': '5432', 'DB_NAME': 'myapp_db', 'API_KEY': 'sk-abc123', 'DEBUG': 'true'}",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "18",
   "q": "Program 18: TOML Parser",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import tomllib  # Python 3.11+\n\ntoml_str = \"\"\"\n[project]\nname = \"myapp\"\nversion = \"1.0.0\"\ndescription = \"A sample project\"\n\n[project.dependencies]\npython = \">=3.9\"\nfastapi = \">=0.100\"\n\n[server]\nhost = \"0.0.0.0\"\nport = 8080\nworkers = 4\n\"\"\"\n\n# Write to file and parse\nwith open(\"config.toml\", \"w\") as f:\n    f.write(toml_str)\n\nwith open(\"config.toml\", \"rb\") as f:\n    config = tomllib.load(f)\n\nprint(config[\"project\"][\"name\"])     # Output: myapp\nprint(config[\"server\"][\"port\"])      # Output: 8080\nprint(config[\"project\"][\"dependencies\"])\n# Output: {'python': '>=3.9', 'fastapi': '>=0.100'}",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "19",
   "q": "Program 19: Excel File Handler",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "# pip install openpyxl\nfrom openpyxl import Workbook, load_workbook\n\n# Create workbook\nwb = Workbook()\nws = wb.active\nws.title = \"Sales\"\n\nheaders = [\"Product\", \"Q1\", \"Q2\", \"Q3\", \"Q4\"]\nws.append(headers)\nws.append([\"Widget A\", 100, 150, 200, 180])\nws.append([\"Widget B\", 200, 180, 220, 250])\nws.append([\"Widget C\", 50, 75, 60, 90])\n\n# Add formula\nws[\"G1\"] = \"Total\"\nfor row in range(2, 5):\n    ws[f\"G{row}\"] = f\"=SUM(B{row}:E{row})\"\n\nwb.save(\"sales.xlsx\")\n\n# Read back\nwb2 = load_workbook(\"sales.xlsx\")\nws2 = wb2.active\nfor row in ws2.iter_rows(min_row=1, max_row=4, values_only=True):\n    print(row)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "20",
   "q": "Program 20: Streaming Large File Processing",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def process_large_file(filepath, chunk_size=8192):\n    \"\"\"Process a large file in chunks without loading entirely into memory.\"\"\"\n    total_bytes = 0\n    line_count = 0\n    word_count = 0\n    \n    with open(filepath, 'r', buffering=chunk_size) as f:\n        for line in f:\n            total_bytes += len(line.encode())\n            line_count += 1\n            word_count += len(line.split())\n    \n    return {\n        \"bytes\": total_bytes,\n        \"lines\": line_count,\n        \"words\": word_count\n    }\n\n# Create sample file\nwith open(\"large_sample.txt\", \"w\") as f:\n    for i in range(1000):\n        f.write(f\"Line {i}: This is sample data for testing\\n\")\n\nstats = process_large_file(\"large_sample.txt\")\nprint(stats)\n# Output: {'bytes': ~45000, 'lines': 1000, 'words': 8000}",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "21",
   "q": "Program 21: JSON Lines (JSONL) Handler",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import json\n\ndef write_jsonl(filepath, records):\n    with open(filepath, 'w') as f:\n        for record in records:\n            f.write(json.dumps(record) + '\\n')\n\ndef read_jsonl(filepath):\n    records = []\n    with open(filepath, 'r') as f:\n        for line in f:\n            if line.strip():\n                records.append(json.loads(line))\n    return records\n\n# Usage\nrecords = [\n    {\"id\": 1, \"text\": \"Hello world\", \"label\": \"greeting\"},\n    {\"id\": 2, \"text\": \"Buy now!\", \"label\": \"spam\"},\n    {\"id\": 3, \"text\": \"Meeting at 3pm\", \"label\": \"event\"}\n]\n\nwrite_jsonl(\"data.jsonl\", records)\nloaded = read_jsonl(\"data.jsonl\")\nfor r in loaded:\n    print(r)\n# Output:\n# {'id': 1, 'text': 'Hello world', 'label': 'greeting'}\n# {'id': 2, 'text': 'Buy now!', 'label': 'spam'}\n# {'id': 3, 'text': 'Meeting at 3pm', 'label': 'event'}",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "22",
   "q": "Program 22: File Diff Tool",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import difflib\n\ntext1 = \"\"\"Line 1: Hello\nLine 2: World\nLine 3: Python\nLine 4: Programming\n\"\"\".splitlines(keepends=True)\n\ntext2 = \"\"\"Line 1: Hello\nLine 2: World!\nLine 3: Python 3\nLine 5: New Line\n\"\"\".splitlines(keepends=True)\n\n# Unified diff\ndiff = difflib.unified_diff(text1, text2, fromfile=\"original.txt\", tofile=\"modified.txt\")\nprint(\"\".join(diff))\n\n# Similarity ratio\nmatcher = difflib.SequenceMatcher(None, \"\".join(text1), \"\".join(text2))\nprint(f\"\\nSimilarity: {matcher.ratio():.2%}\")\n# Output: Shows diff with +/- lines, Similarity: ~75%",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "23",
   "q": "Program 23: Protobuf-Style Serialization",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import struct\n\nclass SimpleProto:\n    \"\"\"Minimal binary serialization format.\"\"\"\n    \n    @staticmethod\n    def encode(data: dict) -> bytes:\n        parts = []\n        for key, value in data.items():\n            key_bytes = key.encode()\n            parts.append(struct.pack(\"H\", len(key_bytes)))\n            parts.append(key_bytes)\n            if isinstance(value, int):\n                parts.append(b'\\x01')\n                parts.append(struct.pack(\"q\", value))\n            elif isinstance(value, str):\n                val_bytes = value.encode()\n                parts.append(b'\\x02')\n                parts.append(struct.pack(\"I\", len(val_bytes)))\n                parts.append(val_bytes)\n            elif isinstance(value, float):\n                parts.append(b'\\x03')\n                parts.append(struct.pack(\"d\", value))\n        return b''.join(parts)\n    \n    @staticmethod\n    def decode(data: bytes) -> dict:\n        result = {}\n        offset = 0\n        while offset < len(data):\n            key_len = struct.unpack_from(\"H\", data, offset)[0]; offset += 2\n            key = data[offset:offset+key_len].decode(); offset += key_len\n            type_byte = data[offset]; offset += 1\n            if type_byte == 1:\n                result[key] = struct.unpack_from(\"q\", data, offset)[0]; offset += 8\n            elif type_byte == 2:\n                val_len = struct.unpack_from(\"I\", data, offset)[0]; offset += 4\n                result[key] = data[offset:offset+val_len].decode(); offset += val_len\n            elif type_byte == 3:\n                result[key] = struct.unpack_from(\"d\", data, offset)[0]; offset += 8\n        return result\n\noriginal = {\"name\": \"Alice\", \"age\": 30, \"score\": 95.5}\nencoded = SimpleProto.encode(original)\ndecoded = SimpleProto.decode(encoded)\nprint(f\"Original: {original}\")\nprint(f\"Encoded: {len(encoded)} bytes\")\nprint(f\"Decoded: {decoded}\")",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "24",
   "q": "Program 24: Parquet-Like Column Store",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import json\n\nclass ColumnStore:\n    \"\"\"Simple columnar storage format.\"\"\"\n    \n    def __init__(self):\n        self.columns = {}\n        self.row_count = 0\n    \n    def from_rows(self, rows):\n        if not rows:\n            return\n        self.row_count = len(rows)\n        for key in rows[0]:\n            self.columns[key] = [row.get(key) for row in rows]\n    \n    def select(self, *cols):\n        result = []\n        for i in range(self.row_count):\n            row = {col: self.columns[col][i] for col in cols if col in self.columns}\n            result.append(row)\n        return result\n    \n    def where(self, col, predicate):\n        indices = [i for i, v in enumerate(self.columns[col]) if predicate(v)]\n        result = {}\n        for key in self.columns:\n            result[key] = [self.columns[key][i] for i in indices]\n        store = ColumnStore()\n        store.columns = result\n        store.row_count = len(indices)\n        return store\n    \n    def aggregate(self, col, func):\n        return func(self.columns[col])\n\nrows = [\n    {\"name\": \"Alice\", \"dept\": \"Eng\", \"salary\": 95000},\n    {\"name\": \"Bob\", \"dept\": \"Mkt\", \"salary\": 75000},\n    {\"name\": \"Charlie\", \"dept\": \"Eng\", \"salary\": 88000},\n]\n\ncs = ColumnStore()\ncs.from_rows(rows)\n\neng = cs.where(\"dept\", lambda x: x == \"Eng\")\nprint(eng.select(\"name\", \"salary\"))\nprint(f\"Avg Eng salary: {eng.aggregate('salary', lambda x: sum(x)/len(x))}\")\n# Output:\n# [{'name': 'Alice', 'salary': 95000}, {'name': 'Charlie', 'salary': 88000}]\n# Avg Eng salary: 91500.0",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "25",
   "q": "Program 25: File Encryption/Decryption",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import hashlib\nimport os\n\ndef xor_encrypt(data: bytes, key: bytes) -> bytes:\n    \"\"\"Simple XOR encryption (for learning — use AES in production).\"\"\"\n    key_repeated = (key * (len(data) // len(key) + 1))[:len(data)]\n    return bytes(a ^ b for a, b in zip(data, key_repeated))\n\ndef encrypt_file(input_path, output_path, password):\n    key = hashlib.sha256(password.encode()).digest()\n    salt = os.urandom(16)\n    derived_key = hashlib.pbkdf2_hmac('sha256', key, salt, 100000)\n    \n    with open(input_path, 'rb') as f:\n        plaintext = f.read()\n    \n    ciphertext = xor_encrypt(plaintext, derived_key)\n    \n    with open(output_path, 'wb') as f:\n        f.write(salt + ciphertext)\n\ndef decrypt_file(input_path, output_path, password):\n    key = hashlib.sha256(password.encode()).digest()\n    \n    with open(input_path, 'rb') as f:\n        salt = f.read(16)\n        ciphertext = f.read()\n    \n    derived_key = hashlib.pbkdf2_hmac('sha256', key, salt, 100000)\n    plaintext = xor_encrypt(ciphertext, derived_key)\n    \n    with open(output_path, 'wb') as f:\n        f.write(plaintext)\n\n# Demo\nwith open(\"secret.txt\", \"w\") as f:\n    f.write(\"Top secret message!\")\n\nencrypt_file(\"secret.txt\", \"secret.enc\", \"mypassword\")\ndecrypt_file(\"secret.enc\", \"secret_dec.txt\", \"mypassword\")\n\nwith open(\"secret_dec.txt\", \"r\") as f:\n    print(f.read())  # Output: Top secret message!",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "26",
   "q": "Program 26: Catch a Specific Exception, Not Everything",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def parse_age(text):\n    try:\n        return int(text)\n    except ValueError:          # only the error you know how to handle\n        return None\n\nprint(parse_age(\"42\"))          # Output: 42\nprint(parse_age(\"forty\"))       # Output: None\n\n# A bare \"except:\" would also swallow KeyboardInterrupt and SystemExit,\n# which is how a script becomes impossible to stop.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "27",
   "q": "Program 27: try / except / else / finally in Order",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def run(divisor):\n    try:\n        result = 10 / divisor\n    except ZeroDivisionError:\n        print(\"except: cannot divide by zero\")\n    else:\n        print(f\"else: got {result}\")     # runs ONLY if no exception\n    finally:\n        print(\"finally: always runs\")\n\nrun(2)\nrun(0)\n# Output: else: got 5.0\n# Output: finally: always runs\n# Output: except: cannot divide by zero",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "28",
   "q": "Program 28: Raising Your Own Exception",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "def withdraw(balance, amount):\n    if amount > balance:\n        raise ValueError(f\"cannot withdraw {amount} from {balance}\")\n    return balance - amount\n\nprint(withdraw(100, 30))        # Output: 70\ntry:\n    withdraw(100, 500)\nexcept ValueError as e:\n    print(\"refused:\", e)        # Output: refused: cannot withdraw 500 from 100",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "29",
   "q": "Program 29: Custom Exception Class Hierarchy",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class AppError(Exception):\n    \"\"\"Base for everything this application raises.\"\"\"\n\nclass NotFound(AppError):\n    pass\n\nclass Forbidden(AppError):\n    pass\n\ndef handle(exc):\n    try:\n        raise exc\n    except NotFound:\n        return 404\n    except Forbidden:\n        return 403\n    except AppError:\n        return 500\n\nprint(handle(NotFound(\"no such user\")))     # Output: 404\nprint(handle(Forbidden(\"read only\")))       # Output: 403\nprint(handle(AppError(\"something else\")))   # Output: 500\n\n# One base class means callers can catch everything you raise with one except.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "30",
   "q": "Program 30: Exception Chaining with raise ... from",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class ConfigError(Exception):\n    pass\n\ndef load_port(raw):\n    try:\n        return int(raw)\n    except ValueError as e:\n        raise ConfigError(\"PORT must be a number\") from e\n\ntry:\n    load_port(\"abc\")\nexcept ConfigError as e:\n    print(type(e).__name__, \"-\", e)          # Output: ConfigError - PORT must be a number\n    print(\"caused by:\", type(e.__cause__).__name__)   # Output: caused by: ValueError\n\n# \"from e\" keeps the original in __cause__, so the traceback shows both.",
     "numbered": false
    }
   ]
  }
 ],
 "takeaways": [],
 "_drills": 30,
 "_code": 30
});
