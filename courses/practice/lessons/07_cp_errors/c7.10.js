/* ============================================================================
   PRACTICE C6.10 — Predict the Output — Scenario Questions · 1
   ----------------------------------------------------------------------------
   Imported from the Coding Practice source set and rendered through the
   Tutorial Hub design system. Generated by .build/import-practice.py —
   edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "c7.10",
 "lede": "**30 problems** from the errors, i/o and concurrency set. Each answer is hidden until you ask for it — attempt it first, because reading a solution feels like learning and is not.",
 "objectives": [
  "Work through 30 problems on predict the output — scenario questions without looking anything up",
  "Predict each answer before revealing it",
  "Recognise the pattern each problem belongs to",
  "Explain your solution out loud, including its complexity"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer before you reveal.** Even a wrong attempt makes the answer stick; reading first does not.",
      "**Say the complexity out loud** where the problem has one. Getting it wrong is the useful part.",
      "Use the bar above to open or close everything at once, and **Ctrl K** to jump to another set."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "Threads — output of `sorted(results)`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\n\nresults = []\n\ndef worker(n):\n    results.append(n * n)\n\nthreads = [threading.Thread(target=worker, args=(i,)) for i in range(5)]\nfor t in threads:\n    t.start()\nfor t in threads:\n    t.join()\n\nprint(sorted(results))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `[0, 1, 4, 9, 16]` **Explanation:** Threads run concurrently. `join()` waits for all to finish. Results sorted for determinism."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "2",
   "q": "Threads — output of `threads = [threading.Thread(target=increment,…`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\n\ncounter = 0\nlock = threading.Lock()\n\ndef increment(n):\n    global counter\n    for _ in range(n):\n        with lock:\n            counter += 1\n\nthreads = [threading.Thread(target=increment, args=(10000,)) for _ in range(5)]\nfor t in threads:\n    t.start()\nfor t in threads:\n    t.join()\n\nprint(counter)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `50000` **Explanation:** Lock ensures thread-safe increment. Without lock, counter could be less than 50000."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "3",
   "q": "Threads — output of `f\"Thread {name}: {local.name}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\n\nlocal = threading.local()\n\ndef worker(name):\n    local.name = name\n    print(f\"Thread {name}: {local.name}\")\n\nt1 = threading.Thread(target=worker, args=(\"A\",))\nt2 = threading.Thread(target=worker, args=(\"B\",))\nt1.start()\nt1.join()\nt2.start()\nt2.join()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Thread A: A`, `Thread B: B` **Explanation:** `threading.local()` provides thread-specific storage. Each thread has its own `name`."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "4",
   "q": "Threads — output of `results = [limiter.acquire() for _ in range(5…`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\nimport time\n\nclass RateLimiter:\n    def __init__(self, max_calls, period):\n        self.max_calls = max_calls\n        self.period = period\n        self.calls = 0\n        self.lock = threading.Lock()\n    \n    def acquire(self):\n        with self.lock:\n            if self.calls < self.max_calls:\n                self.calls += 1\n                return True\n            return False\n\nlimiter = RateLimiter(3, 1.0)\nresults = [limiter.acquire() for _ in range(5)]\nprint(results)\nprint(f\"Calls made: {limiter.calls}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `[True, True, True, False, False]`, `Calls made: 3` **Explanation:** Rate limiter allows max_calls within period. Lock ensures thread safety."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "5",
   "q": "Threads — output of `t1 = threading.Thread(target=waiter, args=(\"T…`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\n\nevent = threading.Event()\nlog = []\n\ndef waiter(name):\n    log.append(f\"{name} waiting\")\n    event.wait()\n    log.append(f\"{name} proceeding\")\n\nt1 = threading.Thread(target=waiter, args=(\"T1\",))\nt2 = threading.Thread(target=waiter, args=(\"T2\",))\nt1.start()\nt2.start()\n\nimport time\ntime.sleep(0.1)\nlog.append(\"Signal!\")\nevent.set()\n\nt1.join()\nt2.join()\nprint(log)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `['T1 waiting', 'T2 waiting', 'Signal!', 'T1 proceeding', 'T2 proceeding']` (order of last two may vary) **Explanation:** Event blocks threads until `set()` is called. Both threads resume after signal."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "6",
   "q": "Threads — output of `f\"Max concurrent: {max_concurrent}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\n\nsemaphore = threading.Semaphore(2)\nactive = []\nlock = threading.Lock()\nlog = []\n\ndef worker(name):\n    with semaphore:\n        with lock:\n            active.append(name)\n            log.append(f\"{name}: active={len(active)}\")\n        import time\n        time.sleep(0.05)\n        with lock:\n            active.remove(name)\n\nthreads = [threading.Thread(target=worker, args=(f\"T{i}\",)) for i in range(4)]\nfor t in threads:\n    t.start()\nfor t in threads:\n    t.join()\n\nmax_concurrent = max(int(entry.split('=')[1]) for entry in log)\nprint(f\"Max concurrent: {max_concurrent}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Max concurrent: 2` **Explanation:** Semaphore(2) limits concurrent access to 2 threads at a time."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "7",
   "q": "Threads — output of `sorted(results)`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\nfrom queue import Queue\n\ndef producer(q, items):\n    for item in items:\n        q.put(item)\n    q.put(None)  # sentinel\n\ndef consumer(q, results):\n    while True:\n        item = q.get()\n        if item is None:\n            break\n        results.append(item * 2)\n\nq = Queue()\nresults = []\n\np = threading.Thread(target=producer, args=(q, [1, 2, 3, 4, 5]))\nc = threading.Thread(target=consumer, args=(q, results))\n\np.start()\nc.start()\np.join()\nc.join()\n\nprint(sorted(results))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `[2, 4, 6, 8, 10]` **Explanation:** Producer-consumer pattern using thread-safe Queue. None sentinel signals completion."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "8",
   "q": "A property — output of `counter.value`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\n\nclass SafeCounter:\n    def __init__(self):\n        self._value = 0\n        self._lock = threading.Lock()\n    \n    def increment(self):\n        with self._lock:\n            self._value += 1\n    \n    def decrement(self):\n        with self._lock:\n            self._value -= 1\n    \n    @property\n    def value(self):\n        with self._lock:\n            return self._value\n\ncounter = SafeCounter()\nthreads = []\nfor _ in range(100):\n    threads.append(threading.Thread(target=counter.increment))\nfor _ in range(30):\n    threads.append(threading.Thread(target=counter.decrement))\n\nfor t in threads:\n    t.start()\nfor t in threads:\n    t.join()\n\nprint(counter.value)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `70` **Explanation:** Thread-safe counter with lock. 100 increments - 30 decrements = 70."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "9",
   "q": "Threads — output of `f\"All arrived: {len(arrived) == 3}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\n\nbarrier = threading.Barrier(3)\norder = []\nlock = threading.Lock()\n\ndef worker(name):\n    with lock:\n        order.append(f\"{name} arrived\")\n    barrier.wait()\n    with lock:\n        order.append(f\"{name} passed\")\n\nthreads = [threading.Thread(target=worker, args=(f\"T{i}\",)) for i in range(3)]\nfor t in threads:\n    t.start()\nfor t in threads:\n    t.join()\n\narrived = [x for x in order if 'arrived' in x]\npassed = [x for x in order if 'passed' in x]\nprint(f\"All arrived: {len(arrived) == 3}\")\nprint(f\"All passed: {len(passed) == 3}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `All arrived: 3`, `All passed: 3` **Explanation:** Barrier syncs 3 threads — all must arrive before any can pass."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "10",
   "q": "A pool executor — output of `len(results)`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from concurrent.futures import ThreadPoolExecutor, as_completed\n\ndef fetch(url):\n    import time\n    time.sleep(0.01)\n    return f\"Data from {url}\"\n\nurls = ['site1.com', 'site2.com', 'site3.com']\n\nwith ThreadPoolExecutor(max_workers=3) as executor:\n    futures = {executor.submit(fetch, url): url for url in urls}\n    results = {}\n    for future in as_completed(futures):\n        url = futures[future]\n        results[url] = future.result()\n\nprint(len(results))\nprint(all('Data from' in v for v in results.values()))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `3`, `True` **Explanation:** ThreadPoolExecutor manages thread pool. `as_completed` yields futures as they finish."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "11",
   "q": "A pool executor — output of `from concurrent.futures import ThreadPoolExec…`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from concurrent.futures import ThreadPoolExecutor\n\ndef square(n):\n    return n * n\n\nwith ThreadPoolExecutor() as executor:\n    results = list(executor.map(square, range(5)))\n\nprint(results)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `[0, 1, 4, 9, 16]` **Explanation:** `executor.map` applies function to iterable, returns results in order."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "12",
   "q": "__new__ versus __init__ — output of `len(results)`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\n\nclass Singleton:\n    _instance = None\n    _lock = threading.Lock()\n    \n    def __new__(cls):\n        if cls._instance is None:\n            with cls._lock:\n                if cls._instance is None:\n                    cls._instance = super().__new__(cls)\n        return cls._instance\n\nresults = set()\ndef create():\n    obj = Singleton()\n    results.add(id(obj))\n\nthreads = [threading.Thread(target=create) for _ in range(10)]\nfor t in threads:\n    t.start()\nfor t in threads:\n    t.join()\n\nprint(len(results))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `1` **Explanation:** Double-checked locking ensures thread-safe singleton — only one instance created."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "13",
   "q": "A try / except block — output of `q.full()`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from queue import Queue, Empty\n\nq = Queue(maxsize=3)\nq.put(1)\nq.put(2)\nq.put(3)\n\nprint(q.full())\nprint(q.qsize())\n\ntry:\n    q.put(4, timeout=0.01)\nexcept Exception as e:\n    print(f\"Queue full: {type(e).__name__}\")\n\nprint(q.get())\nprint(q.get())\nprint(q.empty())",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `True`, `3`, `Queue full: Full`, `1`, `2`, `False` **Explanation:** Bounded queue. `put` with timeout raises `Full`. FIFO ordering."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "14",
   "q": "A while loop — output of `f\"[{priority}] {task}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from queue import PriorityQueue\n\npq = PriorityQueue()\npq.put((3, \"low priority\"))\npq.put((1, \"high priority\"))\npq.put((2, \"medium priority\"))\n\nwhile not pq.empty():\n    priority, task = pq.get()\n    print(f\"[{priority}] {task}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "[1] high priority\n[2] medium priority\n[3] low priority",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Explanation:** PriorityQueue processes lowest number first (min-heap)."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "15",
   "q": "Threads — output of `self._thread = threading.Thread(target=self._…`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\nimport time\n\nclass Timer:\n    def __init__(self, interval, callback):\n        self.interval = interval\n        self.callback = callback\n        self.running = False\n        self._thread = None\n    \n    def start(self):\n        self.running = True\n        self._thread = threading.Thread(target=self._run, daemon=True)\n        self._thread.start()\n    \n    def _run(self):\n        count = 0\n        while self.running and count < 3:\n            time.sleep(self.interval)\n            self.callback(count)\n            count += 1\n    \n    def stop(self):\n        self.running = False\n\nresults = []\ntimer = Timer(0.01, lambda c: results.append(c))\ntimer.start()\ntime.sleep(0.1)\ntimer.stop()\nprint(results)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `[0, 1, 2]` **Explanation:** Timer calls callback at intervals. Daemon thread auto-exits when main thread ends."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "16",
   "q": "Threads — output of `f\"Readers: {rwl.readers}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\n\nclass ReadWriteLock:\n    def __init__(self):\n        self.readers = 0\n        self.lock = threading.Lock()\n        self.write_lock = threading.Lock()\n    \n    def acquire_read(self):\n        with self.lock:\n            self.readers += 1\n            if self.readers == 1:\n                self.write_lock.acquire()\n    \n    def release_read(self):\n        with self.lock:\n            self.readers -= 1\n            if self.readers == 0:\n                self.write_lock.release()\n    \n    def acquire_write(self):\n        self.write_lock.acquire()\n    \n    def release_write(self):\n        self.write_lock.release()\n\nrwl = ReadWriteLock()\nrwl.acquire_read()\nprint(f\"Readers: {rwl.readers}\")\nrwl.acquire_read()\nprint(f\"Readers: {rwl.readers}\")\nrwl.release_read()\nrwl.release_read()\nprint(f\"Readers: {rwl.readers}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Readers: 1`, `Readers: 2`, `Readers: 0` **Explanation:** Multiple readers allowed simultaneously. Writers get exclusive access."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "17",
   "q": "A pool executor — output of `f\"First completed: {first_result}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from concurrent.futures import ThreadPoolExecutor, wait, FIRST_COMPLETED\nimport time\n\ndef task(n):\n    time.sleep(n * 0.01)\n    return n\n\nwith ThreadPoolExecutor(max_workers=3) as executor:\n    futures = [executor.submit(task, i) for i in [3, 1, 2]]\n    done, not_done = wait(futures, return_when=FIRST_COMPLETED)\n    \n    first_result = list(done)[0].result()\n    print(f\"First completed: {first_result}\")\n    print(f\"Still running: {len(not_done)}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `First completed: 1`, `Still running: 2` **Explanation:** `FIRST_COMPLETED` returns as soon as any future completes. Task 1 finishes first."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "18",
   "q": "Threads — output of `bb.get()`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\nfrom collections import deque\n\nclass BoundedBuffer:\n    def __init__(self, capacity):\n        self.buffer = deque()\n        self.capacity = capacity\n        self.lock = threading.Lock()\n        self.not_full = threading.Condition(self.lock)\n        self.not_empty = threading.Condition(self.lock)\n    \n    def put(self, item):\n        with self.not_full:\n            while len(self.buffer) >= self.capacity:\n                self.not_full.wait()\n            self.buffer.append(item)\n            self.not_empty.notify()\n    \n    def get(self):\n        with self.not_empty:\n            while len(self.buffer) == 0:\n                self.not_empty.wait()\n            item = self.buffer.popleft()\n            self.not_full.notify()\n            return item\n\nbb = BoundedBuffer(2)\nbb.put(\"a\")\nbb.put(\"b\")\nprint(bb.get())\nbb.put(\"c\")\nprint(bb.get())\nprint(bb.get())",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `a`, `b`, `c` **Explanation:** Condition variables coordinate producer-consumer with bounded buffer."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "19",
   "q": "Threads — output of `f\"First setters: {first_count}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\n\nclass AtomicFlag:\n    def __init__(self):\n        self._flag = False\n        self._lock = threading.Lock()\n    \n    def set(self):\n        with self._lock:\n            old = self._flag\n            self._flag = True\n            return not old  # True if we were first to set\n    \n    def is_set(self):\n        with self._lock:\n            return self._flag\n\nflag = AtomicFlag()\nresults = []\n\ndef try_set(name):\n    if flag.set():\n        results.append(f\"{name} was first\")\n    else:\n        results.append(f\"{name} was not first\")\n\nthreads = [threading.Thread(target=try_set, args=(f\"T{i}\",)) for i in range(3)]\nfor t in threads:\n    t.start()\nfor t in threads:\n    t.join()\n\nfirst_count = sum(1 for r in results if 'was first' in r)\nprint(f\"First setters: {first_count}\")\nprint(f\"Total: {len(results)}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `First setters: 1`, `Total: 3` **Explanation:** Atomic test-and-set. Exactly one thread wins the race to set the flag first."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "20",
   "q": "A pool executor — output of `future.done()`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from concurrent.futures import ThreadPoolExecutor\n\ndef divide(a, b):\n    if b == 0:\n        raise ValueError(\"Cannot divide by zero\")\n    return a / b\n\nwith ThreadPoolExecutor() as executor:\n    future = executor.submit(divide, 10, 0)\n    \n    print(future.done())\n    \n    import time\n    time.sleep(0.1)\n    \n    print(future.done())\n    \n    try:\n        result = future.result()\n    except ValueError as e:\n        print(f\"Error: {e}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** (first `done()` may be True/False), `True`, `Error: Cannot divide by zero` **Explanation:** Exceptions in thread pool are captured and re-raised when `.result()` is called."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "21",
   "q": "Threads — output of `log[-1]`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\n\nclass CountDownLatch:\n    def __init__(self, count):\n        self.count = count\n        self.lock = threading.Lock()\n        self.event = threading.Event()\n    \n    def count_down(self):\n        with self.lock:\n            self.count -= 1\n            if self.count <= 0:\n                self.event.set()\n    \n    def wait(self):\n        self.event.wait()\n\nlatch = CountDownLatch(3)\nlog = []\n\ndef worker(n):\n    log.append(f\"T{n} done\")\n    latch.count_down()\n\nthreads = [threading.Thread(target=worker, args=(i,)) for i in range(3)]\nfor t in threads:\n    t.start()\n\nlatch.wait()\nlog.append(\"All complete\")\n\nfor t in threads:\n    t.join()\n\nprint(log[-1])",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `All complete` **Explanation:** CountDownLatch waits until count reaches 0. All threads done before \"All complete\"."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "22",
   "q": "A pool executor — output of `f\"Doubles: {doubles}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from concurrent.futures import ThreadPoolExecutor\nfrom functools import partial\n\ndef process(multiplier, value):\n    return value * multiplier\n\nwith ThreadPoolExecutor() as executor:\n    double = partial(process, 2)\n    triple = partial(process, 3)\n    \n    doubles = list(executor.map(double, [1, 2, 3, 4]))\n    triples = list(executor.map(triple, [1, 2, 3, 4]))\n\nprint(f\"Doubles: {doubles}\")\nprint(f\"Triples: {triples}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Doubles: [2, 4, 6, 8]`, `Triples: [3, 6, 9, 12]` **Explanation:** `partial` creates specialized functions. `executor.map` parallelizes execution."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "23",
   "q": "Threads — output of `f\"Call count: {call_count}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\n\nclass Once:\n    def __init__(self):\n        self._done = False\n        self._lock = threading.Lock()\n        self._result = None\n    \n    def do(self, func):\n        if self._done:\n            return self._result\n        with self._lock:\n            if not self._done:\n                self._result = func()\n                self._done = True\n        return self._result\n\nonce = Once()\ncall_count = 0\n\ndef expensive():\n    global call_count\n    call_count += 1\n    return \"initialized\"\n\nresults = []\nthreads = [threading.Thread(target=lambda: results.append(once.do(expensive))) for _ in range(5)]\nfor t in threads:\n    t.start()\nfor t in threads:\n    t.join()\n\nprint(f\"Call count: {call_count}\")\nprint(f\"All same: {len(set(results)) == 1}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Call count: 1`, `All same: True` **Explanation:** `Once` ensures function executes exactly once. All threads get same result."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "24",
   "q": "Threads — output of `pipeline.process([1, 2, 3])`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\nfrom queue import Queue\n\nclass Pipeline:\n    def __init__(self, *stages):\n        self.stages = stages\n    \n    def process(self, data):\n        queues = [Queue() for _ in range(len(self.stages) + 1)]\n        \n        for item in data:\n            queues[0].put(item)\n        queues[0].put(None)\n        \n        def stage_worker(func, in_q, out_q):\n            while True:\n                item = in_q.get()\n                if item is None:\n                    out_q.put(None)\n                    break\n                out_q.put(func(item))\n        \n        threads = []\n        for i, stage in enumerate(self.stages):\n            t = threading.Thread(target=stage_worker, args=(stage, queues[i], queues[i+1]))\n            t.start()\n            threads.append(t)\n        \n        results = []\n        while True:\n            item = queues[-1].get()\n            if item is None:\n                break\n            results.append(item)\n        \n        for t in threads:\n            t.join()\n        \n        return results\n\npipeline = Pipeline(\n    lambda x: x * 2,\n    lambda x: x + 1,\n    lambda x: x ** 2\n)\n\nprint(pipeline.process([1, 2, 3]))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `[9, 25, 49]` **Explanation:** Pipeline: 1→2→3→9, 2→4→5→25, 3→6→7→49. Each stage runs in its own thread."
    }
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "25",
   "q": "Threads — output of `f\"Remaining: {remaining}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\n\nclass ThreadSafeList:\n    def __init__(self):\n        self._list = []\n        self._lock = threading.RLock()\n    \n    def append(self, item):\n        with self._lock:\n            self._list.append(item)\n    \n    def remove_if(self, predicate):\n        with self._lock:\n            self._list = [x for x in self._list if not predicate(x)]\n            return len(self._list)\n    \n    def snapshot(self):\n        with self._lock:\n            return list(self._list)\n\ntsl = ThreadSafeList()\nfor i in range(10):\n    tsl.append(i)\n\nremaining = tsl.remove_if(lambda x: x % 2 == 0)\nprint(f\"Remaining: {remaining}\")\nprint(f\"Items: {tsl.snapshot()}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Remaining: 5`, `Items: [1, 3, 5, 7, 9]` **Explanation:** Thread-safe list using RLock (reentrant lock). `remove_if` filters atomically."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "26",
   "q": "A pool executor — output of `f\"Batches: {len(batches)}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from concurrent.futures import ThreadPoolExecutor\nimport time\n\ndef batch_process(batch):\n    return [x ** 2 for x in batch]\n\ndata = list(range(20))\nbatch_size = 5\nbatches = [data[i:i+batch_size] for i in range(0, len(data), batch_size)]\n\nwith ThreadPoolExecutor(max_workers=4) as executor:\n    results = list(executor.map(batch_process, batches))\n    flat_results = [item for batch in results for item in batch]\n\nprint(f\"Batches: {len(batches)}\")\nprint(f\"Total results: {len(flat_results)}\")\nprint(flat_results[:5])",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Batches: 4`, `Total results: 20`, `[0, 1, 4, 9, 16]` **Explanation:** Data split into batches, processed in parallel, results flattened."
    }
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "27",
   "q": "Threads — output of `self.timer = threading.Timer(self.delay, func…`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\n\nclass Debouncer:\n    def __init__(self, delay):\n        self.delay = delay\n        self.timer = None\n        self.lock = threading.Lock()\n    \n    def call(self, func, *args):\n        with self.lock:\n            if self.timer:\n                self.timer.cancel()\n            self.timer = threading.Timer(self.delay, func, args)\n            self.timer.start()\n\nresults = []\ndebouncer = Debouncer(0.05)\n\nfor i in range(5):\n    debouncer.call(lambda x: results.append(x), i)\n\nimport time\ntime.sleep(0.2)\nprint(results)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `[4]` **Explanation:** Debouncer cancels previous timer on each call. Only last call (i=4) executes."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "28",
   "q": "Threads — output of `f\"Consumed count: {len(results[0]) if res…`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\n\ncondition = threading.Condition()\nitems = []\nMAX = 5\n\ndef producer():\n    for i in range(7):\n        with condition:\n            while len(items) >= MAX:\n                condition.wait()\n            items.append(i)\n            condition.notify_all()\n\ndef consumer():\n    consumed = []\n    for _ in range(7):\n        with condition:\n            while not items:\n                condition.wait()\n            consumed.append(items.pop(0))\n            condition.notify_all()\n    return consumed\n\nt1 = threading.Thread(target=producer)\nresults = [None]\nt2 = threading.Thread(target=lambda: results.__setitem__(0, consumer()))\n\nt1.start()\nt2.start()\nt1.join()\nt2.join()\n\n# Consumer thread modifies results\nprint(f\"Consumed count: {len(results[0]) if results[0] else 0}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Consumed count: 7` **Explanation:** Condition variable coordinates bounded producer-consumer."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "29",
   "q": "Threads — output of `f\"Ready: {result.is_ready()}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from concurrent.futures import ThreadPoolExecutor, Future\nimport threading\n\nclass AsyncResult:\n    def __init__(self):\n        self._future = Future()\n    \n    def set(self, value):\n        self._future.set_result(value)\n    \n    def get(self, timeout=None):\n        return self._future.result(timeout=timeout)\n    \n    def is_ready(self):\n        return self._future.done()\n\nresult = AsyncResult()\nprint(f\"Ready: {result.is_ready()}\")\n\nthreading.Timer(0.01, lambda: result.set(42)).start()\nprint(f\"Value: {result.get(timeout=1)}\")\nprint(f\"Ready: {result.is_ready()}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Ready: False`, `Value: 42`, `Ready: True` **Explanation:** Future-based async result. `get()` blocks until value is set."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "30",
   "q": "Threads — output of `sorted(pool.results)`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import threading\nfrom queue import Queue\n\nclass WorkerPool:\n    def __init__(self, num_workers):\n        self.queue = Queue()\n        self.results = []\n        self.lock = threading.Lock()\n        self.workers = []\n        \n        for _ in range(num_workers):\n            t = threading.Thread(target=self._worker, daemon=True)\n            t.start()\n            self.workers.append(t)\n    \n    def _worker(self):\n        while True:\n            func, args = self.queue.get()\n            if func is None:\n                break\n            result = func(*args)\n            with self.lock:\n                self.results.append(result)\n            self.queue.task_done()\n    \n    def submit(self, func, *args):\n        self.queue.put((func, args))\n    \n    def wait(self):\n        self.queue.join()\n    \n    def shutdown(self):\n        for _ in self.workers:\n            self.queue.put((None, None))\n\npool = WorkerPool(3)\nfor i in range(6):\n    pool.submit(lambda x: x ** 2, i)\n\npool.wait()\npool.shutdown()\nprint(sorted(pool.results))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `[0, 1, 4, 9, 16, 25]` **Explanation:** Custom thread pool with task queue. Workers process tasks, results collected with lock."
    }
   ],
   "kind": "predict"
  }
 ],
 "takeaways": [],
 "_drills": 30,
 "_code": 31
});
