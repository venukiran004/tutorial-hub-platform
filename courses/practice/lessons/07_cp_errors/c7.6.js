/* ============================================================================
   PRACTICE C6.6 — Programs · 6
   ----------------------------------------------------------------------------
   Imported from the Coding Practice source set and rendered through the
   Tutorial Hub design system. Generated by .build/import-practice.py —
   edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "c7.6",
 "lede": "**19 problems** from the errors, i/o and concurrency set. Each answer is hidden until you ask for it — attempt it first, because reading a solution feels like learning and is not.",
 "objectives": [
  "Work through 19 problems on programs without looking anything up",
  "Predict each answer before revealing it",
  "Recognise the pattern each problem belongs to",
  "Explain your solution out loud, including its complexity"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer before you reveal.** Even a wrong attempt makes the answer stick; reading first does not.",
      "**Say the complexity out loud** where the problem has one. Getting it wrong is the useful part.",
      "Use the bar above to open or close everything at once, and **Ctrl K** to jump to another set."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "A context manager — output of `s.read()`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class MultiContextManager:\n    def __init__(self, *managers):\n        self.managers = managers\n        self.results = []\n    \n    def __enter__(self):\n        for m in self.managers:\n            self.results.append(m.__enter__())\n        return self.results\n    \n    def __exit__(self, *args):\n        for m in reversed(self.managers):\n            m.__exit__(*args)\n\nimport io\ns1 = io.StringIO(\"stream1\")\ns2 = io.StringIO(\"stream2\")\n\nwith MultiContextManager(s1, s2) as streams:\n    for s in streams:\n        print(s.read())",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `stream1`, `stream2` **Explanation:** Combined context manager handles multiple resources, exits in reverse order."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "2",
   "q": "A for loop — output of `f\"{item.get('id')}: {item.text}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import xml.etree.ElementTree as ET\n\nxml_str = \"<root><item id='1'>Apple</item><item id='2'>Banana</item></root>\"\nroot = ET.fromstring(xml_str)\n\nfor item in root.findall('item'):\n    print(f\"{item.get('id')}: {item.text}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `1: Apple`, `2: Banana` **Explanation:** ElementTree parses XML. `findall` finds matching elements, `get` reads attributes."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "3",
   "q": "A class definition — output of `rs.operations`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import io\n\nclass RecordingStream:\n    def __init__(self):\n        self.operations = []\n        self.buffer = io.StringIO()\n    \n    def write(self, data):\n        self.operations.append(('write', len(data)))\n        return self.buffer.write(data)\n    \n    def read(self, n=-1):\n        self.operations.append(('read', n))\n        return self.buffer.read(n)\n    \n    def seek(self, pos):\n        self.operations.append(('seek', pos))\n        return self.buffer.seek(pos)\n\nrs = RecordingStream()\nrs.write(\"hello\")\nrs.seek(0)\nrs.read(3)\nprint(rs.operations)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `[('write', 5), ('seek', 0), ('read', 3)]` **Explanation:** Recording stream pattern — logs all I/O operations for debugging."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "4",
   "q": "Stripping whitespace — output of `wrapped = textwrap.fill(textwrap.dedent(text)…`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import io\nimport textwrap\n\ntext = \"\"\"\n    This is a long paragraph that needs to be wrapped\n    to fit within a certain width for display purposes.\n    It contains multiple sentences.\n\"\"\"\n\nwrapped = textwrap.fill(textwrap.dedent(text).strip(), width=30)\nprint(wrapped)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "This is a long paragraph that\nneeds to be wrapped to fit\nwithin a certain width for\ndisplay purposes. It contains\nmultiple sentences.",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Explanation:** `textwrap.fill` wraps text to specified width. `dedent` removes common indentation."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "5",
   "q": "yield from — output of `result = [line.strip() for line in merge_stre…`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import io\n\ndef merge_streams(*streams):\n    for stream in streams:\n        yield from stream\n\ns1 = io.StringIO(\"a\\nb\\n\")\ns2 = io.StringIO(\"c\\nd\\n\")\ns3 = io.StringIO(\"e\\n\")\n\nresult = [line.strip() for line in merge_streams(s1, s2, s3)]\nprint(result)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `['a', 'b', 'c', 'd', 'e']` **Explanation:** Generator that yields from multiple streams sequentially."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "6",
   "q": "A raised exception — output of `lw.write(\"Hello\")`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import io\n\nclass LimitedWriter:\n    def __init__(self, limit):\n        self.limit = limit\n        self.written = 0\n        self.buffer = io.StringIO()\n    \n    def write(self, data):\n        remaining = self.limit - self.written\n        if remaining <= 0:\n            raise IOError(\"Write limit exceeded\")\n        truncated = data[:remaining]\n        self.buffer.write(truncated)\n        self.written += len(truncated)\n        return len(truncated)\n\nlw = LimitedWriter(10)\nprint(lw.write(\"Hello\"))\nprint(lw.write(\"World!!!\"))\nprint(lw.buffer.getvalue())",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `5`, `5`, `HelloWorld` **Explanation:** Limited writer truncates data to fit within byte limit."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "7",
   "q": "JSON encoding or decoding — output of `f\"Compact length: {len(compact)}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import json\n\ndata = {\"key\": \"value\", \"number\": 42, \"list\": [1, 2, 3]}\ncompact = json.dumps(data, separators=(',', ':'))\npretty = json.dumps(data, indent=2)\nprint(f\"Compact length: {len(compact)}\")\nprint(f\"Pretty length: {len(pretty)}\")\nsorted_json = json.dumps(data, sort_keys=True)\nprint(sorted_json)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Compact length: 33`, `Pretty length: 56`, `{\"key\": \"value\", \"list\": [1, 2, 3], \"number\": 42}` **Explanation:** JSON formatting options: compact removes whitespace, sort_keys orders keys."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "8",
   "q": "Filesystem paths — output of `p.suffixes`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from pathlib import PurePath\n\np = PurePath(\"archive.tar.gz\")\nprint(p.suffixes)\nprint(p.stem)\nprint(''.join(p.suffixes))\n\np2 = PurePath(\"no_extension\")\nprint(p2.suffix)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `['.tar', '.gz']`, `archive.tar`, `.tar.gz`, `` **Explanation:**`suffixes`returns list of all extensions.`suffix` returns only last one."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "9",
   "q": "A class definition — output of `cr.read()`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import io\n\nclass CachingReader:\n    def __init__(self, stream):\n        self.stream = stream\n        self._cache = None\n    \n    def read(self):\n        if self._cache is None:\n            self._cache = self.stream.read()\n        return self._cache\n    \n    def invalidate(self):\n        self._cache = None\n        self.stream.seek(0)\n\ncr = CachingReader(io.StringIO(\"expensive data\"))\nprint(cr.read())\nprint(cr.read())  # from cache\nprint(cr._cache is not None)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `expensive data`, `expensive data`, `True` **Explanation:** Caching reader reads once and serves from cache on subsequent calls."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "10",
   "q": "A class definition — output of `buf.getvalue()`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import io\n\nclass LineNumberWriter:\n    def __init__(self, stream):\n        self.stream = stream\n        self.line_no = 1\n    \n    def write(self, text):\n        lines = text.split('\\n')\n        for i, line in enumerate(lines):\n            if i > 0:\n                self.stream.write('\\n')\n                if line:  # don't number empty final line\n                    self.line_no += 1\n            if line:\n                self.stream.write(f\"{self.line_no:3d}| {line}\")\n\nbuf = io.StringIO()\nlnw = LineNumberWriter(buf)\nlnw.write(\"Hello\\nWorld\\nPython\")\nprint(buf.getvalue())",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "  1| Hello\n  2| World\n  3| Python",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Explanation:** Writer that prepends line numbers to each line."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "11",
   "q": "A generator expression — output of `head(f, 3)`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import io\n\ndef head(stream, n=5):\n    result = []\n    for i, line in enumerate(stream):\n        if i >= n:\n            break\n        result.append(line.strip())\n    return result\n\ntext = \"\\n\".join(f\"Line {i}\" for i in range(10))\nf = io.StringIO(text)\nprint(head(f, 3))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `['Line 0', 'Line 1', 'Line 2']` **Explanation:** Head utility reads first N lines — doesn't need to read entire file."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "12",
   "q": "The iterator protocol — output of `list(fr)`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import io\nimport re\n\nclass FilteredReader:\n    def __init__(self, stream, pattern):\n        self.stream = stream\n        self.pattern = re.compile(pattern)\n    \n    def __iter__(self):\n        for line in self.stream:\n            if self.pattern.search(line):\n                yield line.strip()\n\ntext = \"INFO: Started\\nERROR: Failed\\nINFO: Running\\nERROR: Timeout\\nINFO: Done\"\nfr = FilteredReader(io.StringIO(text), r\"ERROR\")\nprint(list(fr))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `['ERROR: Failed', 'ERROR: Timeout']` **Explanation:** Filtered reader yields only lines matching a regex pattern."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "13",
   "q": "A class definition — output of `f\"Parts: {len(sw.parts)}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import io\n\nclass SplitWriter:\n    def __init__(self, max_lines=3):\n        self.max_lines = max_lines\n        self.parts = [io.StringIO()]\n        self.current_lines = 0\n    \n    def writeln(self, text):\n        if self.current_lines >= self.max_lines:\n            self.parts.append(io.StringIO())\n            self.current_lines = 0\n        self.parts[-1].write(text + \"\\n\")\n        self.current_lines += 1\n\nsw = SplitWriter(max_lines=2)\nfor i in range(5):\n    sw.writeln(f\"line {i}\")\n\nprint(f\"Parts: {len(sw.parts)}\")\nfor i, part in enumerate(sw.parts):\n    print(f\"Part {i}: {part.getvalue().strip()}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Parts: 3\nPart 0: line 0\nline 1\nPart 1: line 2\nline 3\nPart 2: line 4",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Explanation:** Split writer creates new parts when max lines reached."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "14",
   "q": "An itertools recipe — output of `list(interleave_streams(s1, s2))`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import io\nimport itertools\n\ndef interleave_streams(*streams):\n    iterators = [iter(s) for s in streams]\n    for lines in itertools.zip_longest(*iterators, fillvalue=None):\n        for line in lines:\n            if line is not None:\n                yield line.strip()\n\ns1 = io.StringIO(\"a\\nb\\n\")\ns2 = io.StringIO(\"1\\n2\\n3\\n\")\n\nprint(list(interleave_streams(s1, s2)))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `['a', '1', 'b', '2', '3']` **Explanation:** Interleaves lines from multiple streams using `zip_longest`."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "15",
   "q": "A class definition — output of `f\"Data: {cw.getvalue()}\"`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import io\n\nclass ChecksumWriter:\n    def __init__(self):\n        self.buffer = io.StringIO()\n        self.checksum = 0\n    \n    def write(self, data):\n        for ch in data:\n            self.checksum = (self.checksum + ord(ch)) % 256\n        return self.buffer.write(data)\n    \n    def getvalue(self):\n        return self.buffer.getvalue()\n\ncw = ChecksumWriter()\ncw.write(\"ABC\")\nprint(f\"Data: {cw.getvalue()}\")\nprint(f\"Checksum: {cw.checksum}\")\nprint(f\"Expected: {(65+66+67) % 256}\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Data: ABC`, `Checksum: 198`, `Expected: 198` **Explanation:** Writer that computes running checksum of written data."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ]
  },
  {
   "t": "drill",
   "n": "16",
   "q": "A @contextmanager generator — output of `a.read()`",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from contextlib import contextmanager\n\n@contextmanager\ndef nested_contexts(*managers):\n    results = []\n    try:\n        for m in managers:\n            results.append(m.__enter__())\n        yield results\n    finally:\n        for m in reversed(managers):\n            m.__exit__(None, None, None)\n\nimport io\ns1 = io.StringIO(\"first\")\ns2 = io.StringIO(\"second\")\n\nwith nested_contexts(s1, s2) as [a, b]:\n    print(a.read())\n    print(b.read())",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `first`, `second` **Explanation:** Dynamically nesting multiple context managers — similar to `contextlib.ExitStack`."
    },
    {
     "t": "hr"
    },
    {
     "t": "p",
     "text": "**From the runnable 650-program set.** These were the `file1`…`file13_questions_*.py` files; they now live here, grouped by topic. Every program below was executed and the **Output** shown under it is that run's real stdout — nothing is hand-written."
    }
   ],
   "terms": [
    "Answer",
    "Explanation",
    "From the runnable 650-program set",
    "Output"
   ]
  },
  {
   "t": "h2",
   "n": "",
   "text": "Context Managers & Custom Exceptions"
  },
  {
   "t": "drill",
   "n": "17",
   "q": "Program 67: Implement a custom context manager",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nContext managers implement __enter__ and __exit__ methods.\nUsed with 'with' statement for resource management.\n__exit__ receives exception info and can suppress exceptions by returning True.\n\"\"\"\n\n\nclass FileManager:\n    \"\"\"Custom context manager for file operations.\"\"\"\n\n    def __init__(self, filename, mode):\n        self.filename = filename\n        self.mode = mode\n        self.file = None\n\n    def __enter__(self):\n        self.file = open(self.filename, self.mode)\n        return self.file\n\n    def __exit__(self, exc_type, exc_val, exc_tb):\n        if self.file:\n            self.file.close()\n        # Return False to propagate exceptions (default)\n        return False\n\n\nclass Timer:\n    \"\"\"Context manager that times a block of code.\"\"\"\n\n    def __enter__(self):\n        self.start = time.time()\n        return self\n\n    def __exit__(self, *args):\n        self.elapsed = time.time() - self.start\n        print(f\"Elapsed: {self.elapsed:.4f}s\")\n        return False\n\n\n# Examples\nprint(\"Q93: Custom Context Manager\")\nwith Timer() as t:\n    total = sum(range(1000000))\nprint(f\"Sum: {total}\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q93: Custom Context Manager\nElapsed: 0.0245s\nSum: 499999500000",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "18",
   "q": "Program 68: Implement a context manager using contextlib",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\ncontextlib.contextmanager decorator converts a generator function into\na context manager. Code before yield = __enter__, code after yield = __exit__.\n\"\"\"\nfrom contextlib import contextmanager\n\n\n@contextmanager\ndef managed_resource(name):\n    \"\"\"Context manager using contextlib.\"\"\"\n    print(f\"Acquiring resource: {name}\")\n    try:\n        yield name  # This is what 'as' receives\n    finally:\n        print(f\"Releasing resource: {name}\")\n\n\n@contextmanager\ndef suppress_errors(*exceptions):\n    \"\"\"Context manager that suppresses specified exceptions.\"\"\"\n    try:\n        yield\n    except exceptions as e:\n        print(f\"Suppressed: {type(e).__name__}: {e}\")\n\n\n# Examples\nprint(\"Q94: Context Manager with contextlib\")\nwith managed_resource(\"Database\") as r:\n    print(f\"Using {r}\")\n\nwith suppress_errors(ValueError, TypeError):\n    int(\"not a number\")\nprint(\"Continued after suppressed error\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q94: Context Manager with contextlib\nAcquiring resource: Database\nUsing Database\nReleasing resource: Database\nSuppressed: ValueError: invalid literal for int() with base 10: 'not a number'\nContinued after suppressed error",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "19",
   "q": "Program 69: Implement a custom exception hierarchy",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nCustom exceptions should inherit from Exception or a specific built-in exception.\nCreate a hierarchy for your application's error handling.\n\"\"\"\n\n\nclass AppError(Exception):\n    \"\"\"Base exception for the application.\"\"\"\n\n    def __init__(self, message, code=None):\n        super().__init__(message)\n        self.code = code\n        self.message = message\n\n\nclass ValidationError(AppError):\n    \"\"\"Raised when input validation fails.\"\"\"\n\n    def __init__(self, field, message):\n        super().__init__(f\"Validation error on '{field}': {message}\", code=400)\n        self.field = field\n\n\nclass NotFoundError(AppError):\n    \"\"\"Raised when a resource is not found.\"\"\"\n\n    def __init__(self, resource, identifier):\n        super().__init__(f\"{resource} with id '{identifier}' not found\", code=404)\n        self.resource = resource\n        self.identifier = identifier\n\n\nclass AuthenticationError(AppError):\n    \"\"\"Raised when authentication fails.\"\"\"\n\n    def __init__(self, message=\"Authentication failed\"):\n        super().__init__(message, code=401)\n\n\nclass AuthorizationError(AppError):\n    \"\"\"Raised when user lacks permission.\"\"\"\n\n    def __init__(self, action, resource):\n        super().__init__(f\"Not authorized to {action} on {resource}\", code=403)\n        self.action = action\n        self.resource = resource\n\n\n# Examples\nprint(\"Q99: Custom Exception Hierarchy\")\ntry:\n    raise ValidationError(\"email\", \"Invalid email format\")\nexcept ValidationError as e:\n    print(f\"Caught: {e} (code: {e.code}, field: {e.field})\")\n\ntry:\n    raise NotFoundError(\"User\", \"12345\")\nexcept AppError as e:  # Catches all app errors\n    print(f\"Caught: {e} (code: {e.code})\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q99: Custom Exception Hierarchy\nCaught: Validation error on 'email': Invalid email format (code: 400, field: email)\nCaught: User with id '12345' not found (code: 404)",
     "numbered": false
    }
   ]
  }
 ],
 "takeaways": [],
 "_drills": 19,
 "_code": 25
});
