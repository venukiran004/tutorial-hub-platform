/* ============================================================================
   PRACTICE C10.1 — Databases, ORM & Caching
   ----------------------------------------------------------------------------
   Imported from the Coding Practice source set and rendered through the
   Tutorial Hub design system. Generated by .build/import-practice.py —
   edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "c10.1",
 "lede": "**22 problems** from the backend, databases and production set. Each answer is hidden until you ask for it — attempt it first, because reading a solution feels like learning and is not.",
 "objectives": [
  "Work through 22 problems on databases, orm & caching without looking anything up",
  "Predict each answer before revealing it",
  "Recognise the pattern each problem belongs to",
  "Explain your solution out loud, including its complexity"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "viz",
   "title": "Where backend latency actually goes",
   "caption": "Application code is rarely the bottleneck. Almost every slow endpoint is waiting on something across a network, and the fix is usually fewer round trips rather than faster code.",
   "svg": "<svg viewBox='0 0 880 210' role='img' aria-label='A latency budget dominated by database and network waits rather than application code'><text x='30' y='40' class='s-label' style='fill:var(--ink-3)'>a typical 200 ms request</text><rect x='30' y='56' width='60' height='34' style='fill:var(--good);fill-opacity:.5;stroke:var(--good)' stroke-width='2'/><rect x='94' y='56' width='330' height='34' style='fill:var(--crit);fill-opacity:.45;stroke:var(--crit)' stroke-width='2'/><rect x='428' y='56' width='200' height='34' style='fill:var(--warn);fill-opacity:.45;stroke:var(--warn)' stroke-width='2'/><rect x='632' y='56' width='90' height='34' style='fill:var(--accent);fill-opacity:.4;stroke:var(--accent)' stroke-width='2'/><text x='30' y='112' class='s-sub' style='fill:var(--good)'>app code</text><text x='150' y='112' class='s-sub' style='fill:var(--crit)'>database queries</text><text x='450' y='112' class='s-sub' style='fill:var(--warn)'>external API calls</text><text x='632' y='112' class='s-sub' style='fill:var(--accent)'>serialise</text><text x='30' y='154' class='s-sub' style='fill:var(--ink-3)'>Optimising the green block cannot help. Cutting one round trip from the red one halves the request.</text><text x='30' y='182' class='s-sub' style='fill:var(--crit)'>Measure before optimising -- and count queries per request, which is the number that usually explains the shape.</text></svg>"
  },
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer before you reveal.** Even a wrong attempt makes the answer stick; reading first does not.",
      "**Say the complexity out loud** where the problem has one. Getting it wrong is the useful part.",
      "Use the bar above to open or close everything at once, and **Ctrl K** to jump to another set."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "Program 1: ORM vs raw SQL",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q441: ORM vs raw SQL\")\nprint(\"  ORM (Object-Relational Mapping):\")\nprint(\"    + Pythonic API, less SQL, migration support\")\nprint(\"    - Performance overhead, complex queries harder\")\nprint(\"  Raw SQL:\")\nprint(\"    + Full control, optimized queries\")\nprint(\"    - More code, SQL injection risk, no migrations\")\nprint(\"  Best: ORM for CRUD, raw SQL for complex analytics\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q441: ORM vs raw SQL\n  ORM (Object-Relational Mapping):\n    + Pythonic API, less SQL, migration support\n    - Performance overhead, complex queries harder\n  Raw SQL:\n    + Full control, optimized queries\n    - More code, SQL injection risk, no migrations\n  Best: ORM for CRUD, raw SQL for complex analytics",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "2",
   "q": "Program 2: SQLAlchemy internals",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q442: SQLAlchemy internals\")\nprint(\"  Core: SQL Expression Language (tables, selects, joins)\")\nprint(\"  ORM: maps classes to tables (Unit of Work pattern)\")\nprint(\"  Session: identity map + unit of work + transaction\")\nprint(\"  Engine: connection pool + dialect\")\nprint(\"  Identity map: ensures one object per DB row per session\")\nprint(\"  Lazy loading: load related objects on access\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q442: SQLAlchemy internals\n  Core: SQL Expression Language (tables, selects, joins)\n  ORM: maps classes to tables (Unit of Work pattern)\n  Session: identity map + unit of work + transaction\n  Engine: connection pool + dialect\n  Identity map: ensures one object per DB row per session\n  Lazy loading: load related objects on access",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "3",
   "q": "Program 3: Django ORM internals",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q443: Django ORM internals\")\nprint(\"  QuerySet: lazy, chainable query builder\")\nprint(\"  Manager: interface for DB queries (objects)\")\nprint(\"  Migrations: schema versioning (makemigrations, migrate)\")\nprint(\"  Lazy evaluation: QuerySet evaluated only when needed\")\nprint(\"  Caching: QuerySet results cached after evaluation\")\nprint(\"  select_related (JOIN) vs prefetch_related (2 queries)\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q443: Django ORM internals\n  QuerySet: lazy, chainable query builder\n  Manager: interface for DB queries (objects)\n  Migrations: schema versioning (makemigrations, migrate)\n  Lazy evaluation: QuerySet evaluated only when needed\n  Caching: QuerySet results cached after evaluation\n  select_related (JOIN) vs prefetch_related (2 queries)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "4",
   "q": "Program 4: Connection pooling",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q444: Connection pooling\")\nprint(\"  Reuse DB connections instead of creating new ones.\")\nprint(\"  SQLAlchemy: create_engine(url, pool_size=5, max_overflow=10)\")\nprint(\"  Pool types: QueuePool (default), NullPool, StaticPool\")\nprint(\"  Settings: pool_size, max_overflow, pool_recycle, pool_timeout\")\nprint(\"  Async: create_async_engine with async pool\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q444: Connection pooling\n  Reuse DB connections instead of creating new ones.\n  SQLAlchemy: create_engine(url, pool_size=5, max_overflow=10)\n  Pool types: QueuePool (default), NullPool, StaticPool\n  Settings: pool_size, max_overflow, pool_recycle, pool_timeout\n  Async: create_async_engine with async pool",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "5",
   "q": "Program 5: Transactions",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "class TransactionDemo:\n    \"\"\"Simulates database transactions.\"\"\"\n    def __init__(self):\n        self.data = {}\n        self._backup = None\n\n    def begin(self):\n        self._backup = self.data.copy()\n\n    def commit(self):\n        self._backup = None\n\n    def rollback(self):\n        if self._backup is not None:\n            self.data = self._backup\n            self._backup = None\n\nprint(\"Q445: Transactions\")\ndb = TransactionDemo()\ndb.begin()\ndb.data[\"user\"] = \"Alice\"\ndb.data[\"order\"] = 123\nprint(f\"  Before rollback: {db.data}\")\ndb.rollback()\nprint(f\"  After rollback: {db.data}\")\n\ndb.begin()\ndb.data[\"user\"] = \"Bob\"\ndb.commit()\nprint(f\"  After commit: {db.data}\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q445: Transactions\n  Before rollback: {'user': 'Alice', 'order': 123}\n  After rollback: {}\n  After commit: {'user': 'Bob'}",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "6",
   "q": "Program 6: ACID properties",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q446: ACID properties\")\nprint(\"  Atomicity: all-or-nothing (transaction fully completes or rolls back)\")\nprint(\"  Consistency: DB moves from one valid state to another\")\nprint(\"  Isolation: concurrent transactions don't interfere\")\nprint(\"  Durability: committed data persists (survives crashes)\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q446: ACID properties\n  Atomicity: all-or-nothing (transaction fully completes or rolls back)\n  Consistency: DB moves from one valid state to another\n  Isolation: concurrent transactions don't interfere\n  Durability: committed data persists (survives crashes)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "7",
   "q": "Program 7: Isolation levels",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q447: Isolation levels (from weakest to strongest)\")\nprint(\"  READ UNCOMMITTED: see uncommitted changes (dirty reads)\")\nprint(\"  READ COMMITTED: only see committed data (default in PG)\")\nprint(\"  REPEATABLE READ: consistent reads within transaction\")\nprint(\"  SERIALIZABLE: full isolation (as if sequential)\")\nprint(\"  Trade-off: higher isolation = less concurrency\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q447: Isolation levels (from weakest to strongest)\n  READ UNCOMMITTED: see uncommitted changes (dirty reads)\n  READ COMMITTED: only see committed data (default in PG)\n  REPEATABLE READ: consistent reads within transaction\n  SERIALIZABLE: full isolation (as if sequential)\n  Trade-off: higher isolation = less concurrency",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "8",
   "q": "Program 8: Optimistic locking",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q448: Optimistic locking\")\nprint(\"  Assumes conflicts are rare. Uses version column.\")\nprint(\"  UPDATE users SET name='Bob', version=version+1\")\nprint(\"  WHERE id=1 AND version=5\")\nprint(\"  If version changed → conflict → retry or fail\")\nprint(\"  Best for: low-contention, read-heavy workloads\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q448: Optimistic locking\n  Assumes conflicts are rare. Uses version column.\n  UPDATE users SET name='Bob', version=version+1\n  WHERE id=1 AND version=5\n  If version changed → conflict → retry or fail\n  Best for: low-contention, read-heavy workloads",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "9",
   "q": "Program 9: Pessimistic locking",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q449: Pessimistic locking\")\nprint(\"  Locks rows before modifying. Prevents concurrent access.\")\nprint(\"  SELECT * FROM users WHERE id=1 FOR UPDATE\")\nprint(\"  Other transactions wait until lock is released.\")\nprint(\"  Best for: high-contention, write-heavy workloads\")\nprint(\"  Risk: deadlocks if lock ordering is inconsistent\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q449: Pessimistic locking\n  Locks rows before modifying. Prevents concurrent access.\n  SELECT * FROM users WHERE id=1 FOR UPDATE\n  Other transactions wait until lock is released.\n  Best for: high-contention, write-heavy workloads\n  Risk: deadlocks if lock ordering is inconsistent",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "10",
   "q": "Program 10: N+1 query problem",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q450: N+1 query problem\")\nprint(\"  Fetching N related objects causes N+1 queries:\")\nprint(\"  1 query for parent objects + N queries for each child\")\nprint(\"  Solution: eager loading\")\nprint(\"  SQLAlchemy: joinedload(), subqueryload(), selectinload()\")\nprint(\"  Django: select_related() (JOIN), prefetch_related() (2 queries)\")\n\nclass Author:\n    def __init__(self, name, books):\n        self.name = name\n        self.books = books\n\nauthors = [Author(\"Alice\", [\"Book1\"]), Author(\"Bob\", [\"Book2\", \"Book3\"])]\n# N+1: for a in authors: fetch_books(a.id)  # 1 + N queries\n# Fixed: fetch_authors_with_books()  # 1-2 queries with JOIN/prefetch\nprint(f\"  Example: {len(authors)} authors, eager load all books in 1-2 queries\")\nprint()\n\nprint(\"=\" * 70)\nprint(\"FILE 8 COMPLETE: Questions 401-450\")\nprint(\"=\" * 70)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q450: N+1 query problem\n  Fetching N related objects causes N+1 queries:\n  1 query for parent objects + N queries for each child\n  Solution: eager loading\n  SQLAlchemy: joinedload(), subqueryload(), selectinload()\n  Django: select_related() (JOIN), prefetch_related() (2 queries)\n  Example: 2 authors, eager load all books in 1-2 queries\n\n======================================================================\nFILE 8 COMPLETE: Questions 401-450\n======================================================================",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "11",
   "q": "Program 11: Query optimization",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nQuery optimization reduces database query execution time.\n\nTechniques:\n1. Use EXPLAIN ANALYZE to understand query plans\n2. Add indexes on frequently queried columns\n3. Avoid SELECT * (fetch only needed columns)\n4. Use JOINs instead of subqueries when possible\n5. Batch inserts/updates\n6. Use database-side filtering (WHERE) not application-side\n7. Use pagination (LIMIT/OFFSET or cursor-based)\n8. Cache frequently accessed data\n\"\"\"\n\nprint(\"Q451: Query optimization\")\nprint(\"  EXPLAIN ANALYZE SELECT * FROM users WHERE email = 'a@b.com';\")\nprint(\"  Before index: Seq Scan (O(n))\")\nprint(\"  After index:  Index Scan (O(log n))\")\nprint(\"  Key rules:\")\nprint(\"    - Index columns in WHERE, JOIN, ORDER BY\")\nprint(\"    - Avoid functions on indexed columns\")\nprint(\"    - Use covering indexes for read-heavy queries\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q451: Query optimization\n  EXPLAIN ANALYZE SELECT * FROM users WHERE email = 'a@b.com';\n  Before index: Seq Scan (O(n))\n  After index:  Index Scan (O(log n))\n  Key rules:\n    - Index columns in WHERE, JOIN, ORDER BY\n    - Avoid functions on indexed columns\n    - Use covering indexes for read-heavy queries",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "12",
   "q": "Program 12: Database indexing",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nIndexes speed up data retrieval at the cost of slower writes and extra storage.\n\nTypes:\n- B-tree: default, good for equality and range queries\n- Hash: equality only (fast)\n- GIN: full-text search, arrays, JSONB\n- GiST: geometric data, full-text search\n- BRIN: large sequential datasets\n\nWhen to index: columns in WHERE, JOIN, ORDER BY, frequently queried.\nWhen NOT to: small tables, rarely queried columns, high-write tables.\n\"\"\"\n\nprint(\"Q452: Database indexing\")\nprint(\"  CREATE INDEX idx_users_email ON users(email);\")\nprint(\"  CREATE INDEX idx_orders_user_date ON orders(user_id, created_at);\")\nprint(\"  CREATE UNIQUE INDEX idx_users_email_unique ON users(email);\")\nprint(\"  # Partial index:\")\nprint(\"  CREATE INDEX idx_active_users ON users(id) WHERE active = true;\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q452: Database indexing\n  CREATE INDEX idx_users_email ON users(email);\n  CREATE INDEX idx_orders_user_date ON orders(user_id, created_at);\n  CREATE UNIQUE INDEX idx_users_email_unique ON users(email);\n  # Partial index:\n  CREATE INDEX idx_active_users ON users(id) WHERE active = true;",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "13",
   "q": "Program 13: Composite indexes",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nComposite indexes cover multiple columns. Column order matters!\n\nRule: Put most selective column first, or follow query pattern.\nA composite index on (a, b, c) can satisfy:\n- WHERE a = 1\n- WHERE a = 1 AND b = 2\n- WHERE a = 1 AND b = 2 AND c = 3\nBut NOT: WHERE b = 2 (needs leftmost prefix)\n\nThis is called the \"leftmost prefix\" rule.\n\"\"\"\n\nprint(\"Q453: Composite indexes\")\nprint(\"  CREATE INDEX idx_name ON orders(user_id, status, created_at);\")\nprint(\"  Satisfies: WHERE user_id=1\")\nprint(\"  Satisfies: WHERE user_id=1 AND status='paid'\")\nprint(\"  Satisfies: WHERE user_id=1 AND status='paid' AND created_at > '2024-01-01'\")\nprint(\"  NOT:       WHERE status='paid'  (skips leftmost column)\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q453: Composite indexes\n  CREATE INDEX idx_name ON orders(user_id, status, created_at);\n  Satisfies: WHERE user_id=1\n  Satisfies: WHERE user_id=1 AND status='paid'\n  Satisfies: WHERE user_id=1 AND status='paid' AND created_at > '2024-01-01'\n  NOT:       WHERE status='paid'  (skips leftmost column)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "14",
   "q": "Program 14: Unique indexes",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nUnique indexes enforce uniqueness constraint while also speeding up lookups.\nThey reject duplicate values in the indexed columns.\n\nUse for: email addresses, usernames, natural keys, composite unique constraints.\nNULL handling varies by database (PostgreSQL allows multiple NULLs).\n\"\"\"\n\nprint(\"Q454: Unique indexes\")\nprint(\"  CREATE UNIQUE INDEX idx_email ON users(email);\")\nprint(\"  -- Composite unique:\")\nprint(\"  CREATE UNIQUE INDEX idx_user_role ON user_roles(user_id, role_id);\")\nprint(\"  -- Partial unique (only active):\")\nprint(\"  CREATE UNIQUE INDEX idx_active_email ON users(email) WHERE deleted_at IS NULL;\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q454: Unique indexes\n  CREATE UNIQUE INDEX idx_email ON users(email);\n  -- Composite unique:\n  CREATE UNIQUE INDEX idx_user_role ON user_roles(user_id, role_id);\n  -- Partial unique (only active):\n  CREATE UNIQUE INDEX idx_active_email ON users(email) WHERE deleted_at IS NULL;",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "15",
   "q": "Program 15: Migration strategies",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nDatabase migrations manage schema changes over time.\n\nTools:\n- Alembic (SQLAlchemy): alembic revision --autogenerate\n- Django migrations: python manage.py makemigrations\n- Flyway: SQL-based migrations\n- Liquibase: XML/YAML-based\n\nBest practices:\n- Always backward compatible (expand-then-contract)\n- Test migrations on production-like data\n- Never modify existing migrations\n- Use transactional migrations when possible\n\"\"\"\n\nprint(\"Q455: Migration strategies\")\nprint(\"  Expand-contract pattern:\")\nprint(\"  1. Add new column (nullable)\")\nprint(\"  2. Deploy code that writes to both old and new\")\nprint(\"  3. Backfill data\")\nprint(\"  4. Deploy code that reads from new\")\nprint(\"  5. Drop old column\")\nprint(\"  Alembic: alembic revision --autogenerate -m 'add email'\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q455: Migration strategies\n  Expand-contract pattern:\n  1. Add new column (nullable)\n  2. Deploy code that writes to both old and new\n  3. Backfill data\n  4. Deploy code that reads from new\n  5. Drop old column\n  Alembic: alembic revision --autogenerate -m 'add email'",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "16",
   "q": "Program 16: Database sharding",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q456: Database sharding\")\nprint(\"  Split data across multiple databases for horizontal scaling.\")\nprint(\"  Strategies: hash-based, range-based, geographic\")\nprint(\"  Challenges: cross-shard queries, rebalancing, transactions\")\nprint(\"  Example: user_id % num_shards → shard assignment\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q456: Database sharding\n  Split data across multiple databases for horizontal scaling.\n  Strategies: hash-based, range-based, geographic\n  Challenges: cross-shard queries, rebalancing, transactions\n  Example: user_id % num_shards → shard assignment",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "17",
   "q": "Program 17: Replication",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q457: Replication\")\nprint(\"  Copy data across multiple DB servers for availability + reads.\")\nprint(\"  Types: synchronous (strong consistency) vs async (eventual)\")\nprint(\"  Primary-replica: one writer, multiple readers\")\nprint(\"  Multi-primary: multiple writers (conflict resolution needed)\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q457: Replication\n  Copy data across multiple DB servers for availability + reads.\n  Types: synchronous (strong consistency) vs async (eventual)\n  Primary-replica: one writer, multiple readers\n  Multi-primary: multiple writers (conflict resolution needed)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "18",
   "q": "Program 18: Read replicas",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q458: Read replicas\")\nprint(\"  Route read queries to replicas, writes to primary.\")\nprint(\"  SQLAlchemy: use different engine binds for read/write\")\nprint(\"  Replication lag: reads may be slightly stale\")\nprint(\"  Use for: reporting, search, analytics queries\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q458: Read replicas\n  Route read queries to replicas, writes to primary.\n  SQLAlchemy: use different engine binds for read/write\n  Replication lag: reads may be slightly stale\n  Use for: reporting, search, analytics queries",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "19",
   "q": "Program 19: Redis basics",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q459: Redis basics\")\nprint(\"  In-memory data store: key-value, lists, sets, hashes, sorted sets.\")\nprint(\"  import redis\")\nprint(\"  r = redis.Redis(host='localhost', port=6379)\")\nprint(\"  r.set('key', 'value', ex=3600)  # 1 hour TTL\")\nprint(\"  r.get('key')  # b'value'\")\nprint(\"  r.hset('user:1', mapping={'name': 'Alice', 'age': 30})\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q459: Redis basics\n  In-memory data store: key-value, lists, sets, hashes, sorted sets.\n  import redis\n  r = redis.Redis(host='localhost', port=6379)\n  r.set('key', 'value', ex=3600)  # 1 hour TTL\n  r.get('key')  # b'value'\n  r.hset('user:1', mapping={'name': 'Alice', 'age': 30})",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "20",
   "q": "Program 20: Redis caching",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q460: Redis caching\")\nprint(\"  Cache-aside pattern: check cache → miss → query DB → store in cache\")\nprint(\"  Write-through: write to cache + DB simultaneously\")\nprint(\"  Write-behind: write to cache, async write to DB\")\nprint(\"  TTL: set expiration to prevent stale data\")\nprint(\"  Invalidation: delete cache on data change\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q460: Redis caching\n  Cache-aside pattern: check cache → miss → query DB → store in cache\n  Write-through: write to cache + DB simultaneously\n  Write-behind: write to cache, async write to DB\n  TTL: set expiration to prevent stale data\n  Invalidation: delete cache on data change",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "21",
   "q": "Program 21: Cache invalidation",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q461: Cache invalidation\")\nprint(\"  'Two hard things: cache invalidation and naming things.'\")\nprint(\"  Strategies:\")\nprint(\"    TTL: auto-expire after time period\")\nprint(\"    Event-based: invalidate on data change\")\nprint(\"    Version keys: cache_key:v2 (bump version)\")\nprint(\"    Tag-based: invalidate all keys with a tag\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q461: Cache invalidation\n  'Two hard things: cache invalidation and naming things.'\n  Strategies:\n    TTL: auto-expire after time period\n    Event-based: invalidate on data change\n    Version keys: cache_key:v2 (bump version)\n    Tag-based: invalidate all keys with a tag",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "22",
   "q": "Program 22: Distributed caching",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "print(\"Q462: Distributed caching\")\nprint(\"  Spread cache across multiple nodes for scalability.\")\nprint(\"  Redis Cluster: automatic sharding across nodes\")\nprint(\"  Memcached: simple distributed caching\")\nprint(\"  Consistent hashing: minimize redistribution on node changes\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q462: Distributed caching\n  Spread cache across multiple nodes for scalability.\n  Redis Cluster: automatic sharding across nodes\n  Memcached: simple distributed caching\n  Consistent hashing: minimize redistribution on node changes",
     "numbered": false
    }
   ]
  }
 ],
 "takeaways": [],
 "_drills": 22,
 "_code": 44
});
