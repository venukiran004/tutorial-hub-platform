/* ============================================================================
   PRACTICE C6.14 — Async Iterators & Context Managers · 3
   ----------------------------------------------------------------------------
   Imported from the Coding Practice source set and rendered through the
   Tutorial Hub design system. Generated by .build/import-practice.py —
   edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "c6.14",
 "lede": "**23 problems** from the errors, i/o and concurrency set. Each answer is hidden until you ask for it — attempt it first, because reading a solution feels like learning and is not.",
 "objectives": [
  "Work through 23 problems on async iterators & context managers without looking anything up",
  "Predict each answer before revealing it",
  "Recognise the pattern each problem belongs to",
  "Explain your solution out loud, including its complexity"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer before you reveal.** Even a wrong attempt makes the answer stick; reading first does not.",
      "**Say the complexity out loud** where the problem has one. Getting it wrong is the useful part.",
      "Use the bar above to open or close everything at once, and **Ctrl K** to jump to another set."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "Program 214: RabbitMQ basics",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nRabbitMQ is an open-source message broker implementing AMQP protocol.\n\nCore concepts:\n- Producer: sends messages to an exchange\n- Exchange: routes messages to queues (direct, fanout, topic, headers)\n- Queue: stores messages until consumed\n- Consumer: receives messages from queues\n- Binding: rules connecting exchanges to queues\n\nPython library: pika (sync), aio-pika (async)\n\"\"\"\n\nprint(\"Q328: RabbitMQ basics\")\nprint(\"  # pika example:\")\nprint(\"  connection = pika.BlockingConnection(params)\")\nprint(\"  channel = connection.channel()\")\nprint(\"  channel.queue_declare(queue='tasks')\")\nprint(\"  channel.basic_publish(exchange='', routing_key='tasks', body='Hello')\")\nprint(\"  # Consumer:\")\nprint(\"  channel.basic_consume(queue='tasks', on_message_callback=callback)\")\nprint(\"  channel.start_consuming()\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q328: RabbitMQ basics\n  # pika example:\n  connection = pika.BlockingConnection(params)\n  channel = connection.channel()\n  channel.queue_declare(queue='tasks')\n  channel.basic_publish(exchange='', routing_key='tasks', body='Hello')\n  # Consumer:\n  channel.basic_consume(queue='tasks', on_message_callback=callback)\n  channel.start_consuming()",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "2",
   "q": "Program 215: Kafka basics",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nApache Kafka is a distributed event streaming platform for high-throughput,\nfault-tolerant messaging.\n\nCore concepts:\n- Topics: categories/feeds of messages\n- Partitions: topics split for parallelism\n- Producers: publish messages to topics\n- Consumers: read from topics\n- Consumer groups: parallel consumption\n- Brokers: Kafka servers in a cluster\n- Offsets: position of consumer in a partition\n\nPython libraries: kafka-python, confluent-kafka, aiokafka\n\"\"\"\n\nprint(\"Q329: Kafka basics\")\nprint(\"  # kafka-python example:\")\nprint(\"  producer = KafkaProducer(bootstrap_servers='localhost:9092')\")\nprint(\"  producer.send('my-topic', b'message')\")\nprint(\"  # Consumer:\")\nprint(\"  consumer = KafkaConsumer('my-topic', group_id='my-group')\")\nprint(\"  for msg in consumer: process(msg)\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q329: Kafka basics\n  # kafka-python example:\n  producer = KafkaProducer(bootstrap_servers='localhost:9092')\n  producer.send('my-topic', b'message')\n  # Consumer:\n  consumer = KafkaConsumer('my-topic', group_id='my-group')\n  for msg in consumer: process(msg)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "3",
   "q": "Program 216: Celery architecture",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nCelery is a distributed task queue for Python. Architecture:\n\n1. Client/Producer: submits tasks (.delay() or .apply_async())\n2. Broker: message transport (Redis, RabbitMQ)\n3. Worker: executes tasks\n4. Result Backend: stores results (Redis, DB)\n\nFlow: Client -> Broker -> Worker -> Result Backend\nWorkers can run on multiple machines for horizontal scaling.\n\"\"\"\n\nprint(\"Q330: Celery architecture\")\nprint(\"  app = Celery('myapp', broker='redis://localhost:6379')\")\nprint(\"  @app.task\")\nprint(\"  def add(x, y): return x + y\")\nprint(\"  # Call: result = add.delay(4, 6)\")\nprint(\"  # Get result: result.get(timeout=10)\")\nprint(\"  # Run worker: celery -A myapp worker --loglevel=info\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q330: Celery architecture\n  app = Celery('myapp', broker='redis://localhost:6379')\n  @app.task\n  def add(x, y): return x + y\n  # Call: result = add.delay(4, 6)\n  # Get result: result.get(timeout=10)\n  # Run worker: celery -A myapp worker --loglevel=info",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "4",
   "q": "Program 217: Celery tasks",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nCelery tasks are units of work defined as decorated functions. Types:\n- Simple tasks: @app.task decorated functions\n- Bound tasks: @app.task(bind=True) - access self (task instance)\n- Shared tasks: @shared_task - no app dependency\n- Task chains: chain(task1.s(arg), task2.s())\n- Task groups: group(task.s(i) for i in range(10))\n- Task chords: chord(group, callback)\n\nTask options: retry, countdown, expires, queue, priority\n\"\"\"\n\nprint(\"Q331: Celery tasks\")\nprint(\"  @app.task(bind=True, max_retries=3)\")\nprint(\"  def process_order(self, order_id):\")\nprint(\"      try:\")\nprint(\"          # process order\")\nprint(\"          pass\")\nprint(\"      except Exception as exc:\")\nprint(\"          self.retry(exc=exc, countdown=60)\")\nprint(\"  # Chain: chain(validate.s(data), process.s(), notify.s())()\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q331: Celery tasks\n  @app.task(bind=True, max_retries=3)\n  def process_order(self, order_id):\n      try:\n          # process order\n          pass\n      except Exception as exc:\n          self.retry(exc=exc, countdown=60)\n  # Chain: chain(validate.s(data), process.s(), notify.s())()",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "5",
   "q": "Program 218: Celery retries",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nCelery provides built-in retry mechanisms for failed tasks:\n\n1. Manual retry: self.retry(exc=exc, countdown=60, max_retries=3)\n2. Autoretry: @app.task(autoretry_for=(Exception,), retry_backoff=True)\n3. Retry backoff: exponential delay between retries\n4. Retry jitter: randomize delay to prevent thundering herd\n\nretry_backoff=True: 1s, 2s, 4s, 8s...\nretry_backoff_max=600: cap at 10 minutes\nretry_jitter=True: add randomness\n\"\"\"\n\nprint(\"Q332: Celery retries\")\nprint(\"  @app.task(bind=True, autoretry_for=(ConnectionError,),\")\nprint(\"           retry_backoff=True, retry_backoff_max=300,\")\nprint(\"           retry_jitter=True, max_retries=5)\")\nprint(\"  def fetch_data(self, url):\")\nprint(\"      response = requests.get(url)\")\nprint(\"      return response.json()\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q332: Celery retries\n  @app.task(bind=True, autoretry_for=(ConnectionError,),\n           retry_backoff=True, retry_backoff_max=300,\n           retry_jitter=True, max_retries=5)\n  def fetch_data(self, url):\n      response = requests.get(url)\n      return response.json()",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "6",
   "q": "Program 219: Celery scheduling",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nCelery Beat is a scheduler that kicks off tasks at regular intervals.\n\nSetup:\n1. Define schedule in app.conf.beat_schedule\n2. Run beat: celery -A myapp beat\n3. Run worker: celery -A myapp worker\n\nSchedule types:\n- crontab(minute, hour, day_of_week): cron-style\n- timedelta(seconds=30): periodic interval\n- solar(event, lat, lon): solar events\n\"\"\"\n\nprint(\"Q333: Celery scheduling\")\nprint(\"  app.conf.beat_schedule = {\")\nprint(\"      'cleanup-every-hour': {\")\nprint(\"          'task': 'myapp.tasks.cleanup',\")\nprint(\"          'schedule': crontab(minute=0, hour='*/1'),\")\nprint(\"      },\")\nprint(\"      'report-every-morning': {\")\nprint(\"          'task': 'myapp.tasks.daily_report',\")\nprint(\"          'schedule': crontab(hour=9, minute=0),\")\nprint(\"      },\")\nprint(\"  }\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q333: Celery scheduling\n  app.conf.beat_schedule = {\n      'cleanup-every-hour': {\n          'task': 'myapp.tasks.cleanup',\n          'schedule': crontab(minute=0, hour='*/1'),\n      },\n      'report-every-morning': {\n          'task': 'myapp.tasks.daily_report',\n          'schedule': crontab(hour=9, minute=0),\n      },\n  }",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "7",
   "q": "Program 220: Distributed locks",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nDistributed locks coordinate access to shared resources across multiple\nprocesses or machines. Prevents duplicate processing in distributed systems.\n\nImplementations:\n1. Redis-based (SET NX EX): most common, simple\n2. ZooKeeper: strong consistency\n3. Database: SELECT FOR UPDATE\n4. etcd: distributed key-value store\n\nPython: redis-lock, python-redis-lock, sherlock\n\"\"\"\n\nclass SimpleRedisLock:\n    \"\"\"Simulated Redis-based distributed lock.\"\"\"\n    _locks = {}  # Simulating Redis\n\n    def __init__(self, name, timeout=10):\n        self.name = name\n        self.timeout = timeout\n\n    def acquire(self):\n        if self.name not in self._locks:\n            self._locks[self.name] = True\n            return True\n        return False\n\n    def release(self):\n        self._locks.pop(self.name, None)\n\n    def __enter__(self):\n        if not self.acquire():\n            raise RuntimeError(\"Could not acquire lock\")\n        return self\n\n    def __exit__(self, *args):\n        self.release()\n\nprint(\"Q334: Distributed locks\")\nwith SimpleRedisLock(\"process-orders\"):\n    print(\"  Lock acquired, processing orders...\")\nprint(\"  Lock released\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q334: Distributed locks\n  Lock acquired, processing orders...\n  Lock released",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "8",
   "q": "Program 221: Distributed workers",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nDistributed workers process tasks across multiple machines. Each worker\npulls tasks from a shared queue (broker) and processes them independently.\n\nKey considerations:\n- Task serialization (JSON, pickle, msgpack)\n- Result storage and retrieval\n- Worker health monitoring\n- Task routing (specific workers for specific tasks)\n- Autoscaling based on queue depth\n- Graceful shutdown and warm shutdown\n\"\"\"\n\nprint(\"Q335: Distributed workers\")\nprint(\"  # Celery multi-machine setup:\")\nprint(\"  # Machine 1: celery -A app worker -Q default -c 4\")\nprint(\"  # Machine 2: celery -A app worker -Q heavy -c 2\")\nprint(\"  # Route tasks: app.conf.task_routes = {\")\nprint(\"  #     'app.heavy_task': {'queue': 'heavy'},\")\nprint(\"  # }\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q335: Distributed workers\n  # Celery multi-machine setup:\n  # Machine 1: celery -A app worker -Q default -c 4\n  # Machine 2: celery -A app worker -Q heavy -c 2\n  # Route tasks: app.conf.task_routes = {\n  #     'app.heavy_task': {'queue': 'heavy'},\n  # }",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "9",
   "q": "Program 222: Worker pools",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nWorker pools manage a collection of workers that process tasks concurrently.\n\nCelery pool types:\n- prefork: multiprocessing (default), best for CPU-bound\n- eventlet: greenlet-based, best for I/O-bound\n- gevent: greenlet-based, similar to eventlet\n- solo: single-threaded, for debugging\n- threads: threading pool\n\nProcessPoolExecutor and ThreadPoolExecutor are stdlib alternatives.\n\"\"\"\nfrom concurrent.futures import ProcessPoolExecutor, ThreadPoolExecutor\n\ndef square(n):\n    return n ** 2\n\nprint(\"Q336: Worker pools\")\nwith ThreadPoolExecutor(max_workers=4) as pool:\n    results = list(pool.map(square, range(5)))\n    print(f\"  ThreadPool results: {results}\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q336: Worker pools\n  ThreadPool results: [0, 1, 4, 9, 16]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "10",
   "q": "Program 223: Task orchestration",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nTask orchestration coordinates complex workflows of dependent tasks.\n\nPatterns:\n1. Chain: task1 -> task2 -> task3 (sequential)\n2. Group: [task1, task2, task3] (parallel)\n3. Chord: group + callback (parallel then aggregate)\n4. Map/Starmap: apply task to list of args\n5. DAG: directed acyclic graph of dependencies\n\nTools: Celery canvas, Airflow, Prefect, Dagster\n\"\"\"\n\nprint(\"Q337: Task orchestration\")\nprint(\"  # Celery canvas example:\")\nprint(\"  workflow = chain(\")\nprint(\"      fetch_data.s(url),\")\nprint(\"      parse_data.s(),\")\nprint(\"      chord(\")\nprint(\"          group(process_chunk.s(i) for i in range(4)),\")\nprint(\"          aggregate_results.s()\")\nprint(\"      )\")\nprint(\"  )\")\nprint(\"  workflow.apply_async()\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q337: Task orchestration\n  # Celery canvas example:\n  workflow = chain(\n      fetch_data.s(url),\n      parse_data.s(),\n      chord(\n          group(process_chunk.s(i) for i in range(4)),\n          aggregate_results.s()\n      )\n  )\n  workflow.apply_async()",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "11",
   "q": "Program 224: APScheduler",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nAPScheduler (Advanced Python Scheduler) is a lightweight, in-process task\nscheduler. Unlike Celery Beat, it doesn't require a broker.\n\nSchedulers:\n- BlockingScheduler: for standalone scripts\n- BackgroundScheduler: for running alongside other code\n- AsyncIOScheduler: for asyncio applications\n\nTrigger types:\n- date: one-time execution\n- interval: periodic execution\n- cron: cron-style scheduling\n\"\"\"\n\nprint(\"Q338: APScheduler\")\nprint(\"  from apscheduler.schedulers.background import BackgroundScheduler\")\nprint(\"  scheduler = BackgroundScheduler()\")\nprint(\"  @scheduler.scheduled_job('interval', seconds=30)\")\nprint(\"  def cleanup(): ...\")\nprint(\"  @scheduler.scheduled_job('cron', hour=9)\")\nprint(\"  def morning_report(): ...\")\nprint(\"  scheduler.start()\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q338: APScheduler\n  from apscheduler.schedulers.background import BackgroundScheduler\n  scheduler = BackgroundScheduler()\n  @scheduler.scheduled_job('interval', seconds=30)\n  def cleanup(): ...\n  @scheduler.scheduled_job('cron', hour=9)\n  def morning_report(): ...\n  scheduler.start()",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "12",
   "q": "Program 225: Background jobs",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nBackground jobs run tasks outside the request-response cycle in web apps.\nThis prevents long-running operations from blocking user responses.\n\nCommon approaches:\n1. Celery tasks (distributed)\n2. Threading (in-process)\n3. asyncio.create_task() (async apps)\n4. Background threads in FastAPI/Flask\n5. System cron jobs\n6. APScheduler (in-process)\n\"\"\"\n\ndef demo_background_job():\n    import threading\n\n    def background_work(data):\n        time.sleep(0.05)\n        print(f\"  Background job completed: {data}\")\n\n    print(\"  Starting background job...\")\n    thread = threading.Thread(target=background_work, args=(\"process_report\",))\n    thread.start()\n    print(\"  Main thread continues immediately\")\n    thread.join()\n\nprint(\"Q339: Background jobs\")\ndemo_background_job()\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q339: Background jobs\n  Starting background job...\n  Main thread continues immediately\n  Background job completed: process_report",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "13",
   "q": "Program 226: Job queues",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nJob queues manage and execute background tasks. They provide:\n- Priority ordering\n- Retry logic\n- Dead letter queues (for failed jobs)\n- Rate limiting\n- Scheduled execution\n- Job dependencies\n\nPython options: Celery, RQ (Redis Queue), Huey, Dramatiq, arq\n\"\"\"\n\nclass SimpleJobQueue:\n    \"\"\"A basic in-memory job queue.\"\"\"\n    def __init__(self):\n        self._queue = queue.PriorityQueue()\n        self._job_id = 0\n\n    def enqueue(self, func, *args, priority=5):\n        self._job_id += 1\n        self._queue.put((priority, self._job_id, func, args))\n        return self._job_id\n\n    def process_all(self):\n        while not self._queue.empty():\n            priority, job_id, func, args = self._queue.get()\n            result = func(*args)\n            print(f\"  Job {job_id} (priority={priority}): {result}\")\n\nprint(\"Q340: Job queues\")\njq = SimpleJobQueue()\njq.enqueue(lambda x: x * 2, 5, priority=3)\njq.enqueue(lambda x: x + 10, 5, priority=1)  # Higher priority\njq.enqueue(lambda x: x ** 2, 5, priority=2)\njq.process_all()\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q340: Job queues\n  Job 2 (priority=1): 15\n  Job 3 (priority=2): 25\n  Job 1 (priority=3): 10",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "14",
   "q": "Program 227: Python concurrency debugging",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nDebugging concurrent Python code is challenging. Tools and techniques:\n\n1. logging module (thread-safe) instead of print\n2. threading.enumerate() - list active threads\n3. faulthandler module - dump tracebacks on crash\n4. sys._current_frames() - get all thread stacks\n5. py-spy - sampling profiler, no code changes needed\n6. tracemalloc - track memory allocations per thread\n7. Use thread names for identification\n\"\"\"\n\nprint(\"Q341: Python concurrency debugging\")\nprint(f\"  Active threads: {threading.active_count()}\")\nfor t in threading.enumerate():\n    print(f\"    Thread: {t.name}, Daemon: {t.daemon}\")\n\nimport faulthandler\nfaulthandler.enable()\nprint(\"  faulthandler enabled (Ctrl+Break for traceback on Windows)\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q341: Python concurrency debugging\n  Active threads: 1\n    Thread: MainThread, Daemon: False\n  faulthandler enabled (Ctrl+Break for traceback on Windows)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "15",
   "q": "Program 228: Thread dumps",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nThread dumps show the call stack of every thread, essential for diagnosing\ndeadlocks and hanging threads.\n\nHow to get thread dumps:\n1. faulthandler.dump_traceback() - dump all thread stacks\n2. sys._current_frames() - programmatic access\n3. signal handler (SIGUSR1 on Unix)\n4. py-spy dump --pid <PID>\n5. jstack-like tools\n\"\"\"\nimport sys\nimport traceback\n\ndef dump_threads():\n    \"\"\"Dump all thread stacks.\"\"\"\n    frames = sys._current_frames()\n    for thread_id, frame in frames.items():\n        print(f\"  Thread {thread_id}:\")\n        for line in traceback.format_stack(frame)[-2:]:\n            print(f\"    {line.strip()}\")\n\nprint(\"Q342: Thread dumps\")\ndump_threads()\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q342: Thread dumps\n  Thread 26584:\n    File \"c:\\Users\\venuk\\Desktop\\CLAUDE LEARNING\\tutorial-hub\\13_Coding_Practice\\file6_questions_301_to_350.py\", line 1400, in <module>\n    dump_threads()\n    File \"c:\\Users\\venuk\\Desktop\\CLAUDE LEARNING\\tutorial-hub\\13_Coding_Practice\\file6_questions_301_to_350.py\", line 1396, in dump_threads\n    for line in traceback.format_stack(frame)[-2:]:",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "16",
   "q": "Program 229: Profiling concurrent code",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nProfiling concurrent code requires specialized tools:\n\n1. cProfile: per-thread profiling (wrap each thread's target)\n2. py-spy: sampling profiler, shows all threads\n3. yappi: thread-aware profiler\n4. scalene: CPU + memory + GPU profiler\n5. time.perf_counter() for wall-clock timing\n\nKey: Profile wall-clock time (not CPU time) for I/O-bound code\n\"\"\"\n\nimport cProfile\n\ndef profiled_work():\n    total = sum(range(100000))\n    return total\n\nprint(\"Q343: Profiling concurrent code\")\nprint(\"  # Per-thread profiling:\")\nprint(\"  profiler = cProfile.Profile()\")\nprint(\"  profiler.enable()\")\nprint(\"  do_work()\")\nprint(\"  profiler.disable()\")\nprint(\"  profiler.print_stats('cumulative')\")\nresult = profiled_work()\nprint(f\"  Sample result: {result}\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q343: Profiling concurrent code\n  # Per-thread profiling:\n  profiler = cProfile.Profile()\n  profiler.enable()\n  do_work()\n  profiler.disable()\n  profiler.print_stats('cumulative')\n  Sample result: 4999950000",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "17",
   "q": "Program 230: Lock contention",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nLock contention occurs when multiple threads compete for the same lock,\ncausing them to wait and reducing parallelism.\n\nSymptoms: High CPU with low throughput, threads spending time waiting.\n\nSolutions:\n1. Reduce lock scope (hold lock for minimum time)\n2. Use finer-grained locks\n3. Use lock-free data structures\n4. Use Read-Write locks (many readers, one writer)\n5. Use atomic operations\n6. Partition data to reduce contention\n\"\"\"\n\nprint(\"Q344: Lock contention\")\nimport threading\n\nlock = threading.Lock()\ncounter = 0\n\ndef increment_with_lock(n):\n    global counter\n    for _ in range(n):\n        with lock:  # Lock held briefly = less contention\n            counter += 1\n\nthreads = [threading.Thread(target=increment_with_lock, args=(1000,)) for _ in range(4)]\nfor t in threads:\n    t.start()\nfor t in threads:\n    t.join()\nprint(f\"  Counter: {counter} (expected 4000)\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q344: Lock contention\n  Counter: 4000 (expected 4000)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "18",
   "q": "Program 231: Async debugging",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nDebugging asyncio code:\n\n1. asyncio.get_event_loop().set_debug(True) - enable debug mode\n2. PYTHONASYNCIODEBUG=1 environment variable\n3. Slow callback warnings (>100ms)\n4. asyncio.all_tasks() - list running tasks\n5. Task.print_stack() - print task's stack\n6. aiomonitor - remote monitoring of asyncio apps\n\nDebug mode catches: unawaited coroutines, slow callbacks,\nunclosed resources.\n\"\"\"\n\nasync def demo_async_debug():\n    loop = asyncio.get_event_loop()\n    # loop.set_debug(True)  # Enable in development\n    tasks = asyncio.all_tasks()\n    print(f\"  Running tasks: {len(tasks)}\")\n    for task in tasks:\n        print(f\"    Task: {task.get_name()}\")\n\nprint(\"Q345: Async debugging\")\nasyncio.run(demo_async_debug())\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q345: Async debugging\n  Running tasks: 1\n    Task: Task-79",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "19",
   "q": "Program 232: Async exception handling",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nException handling in asyncio requires special attention:\n\n1. Exceptions in tasks don't propagate until awaited\n2. Unhandled exceptions in tasks are logged as warnings\n3. Use try/except in coroutines\n4. asyncio.gather(return_exceptions=True) captures exceptions\n5. Set exception handler: loop.set_exception_handler(handler)\n6. TaskGroup (3.11+) propagates exceptions properly\n\"\"\"\n\nasync def failing_task():\n    raise ValueError(\"Something went wrong\")\n\nasync def demo_async_exceptions():\n    # gather with return_exceptions\n    results = await asyncio.gather(\n        asyncio.sleep(0.01),\n        failing_task(),\n        return_exceptions=True\n    )\n    for r in results:\n        if isinstance(r, Exception):\n            print(f\"  Caught exception: {r}\")\n        else:\n            print(f\"  Success: {r}\")\n\nprint(\"Q346: Async exception handling\")\nasyncio.run(demo_async_exceptions())\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q346: Async exception handling\n  Success: None\n  Caught exception: Something went wrong",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "20",
   "q": "Program 233: Multiprocessing pitfalls",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nCommon multiprocessing pitfalls:\n\n1. Pickling: Arguments and returns must be picklable\n2. Shared state: Processes don't share memory by default\n3. Resource cleanup: Child processes may not clean up properly\n4. Import guard: Always use if __name__ == '__main__'\n5. Large data transfer: Slow due to serialization\n6. Fork issues: Forking multi-threaded programs is dangerous\n7. Global variables: Each process gets a copy\n8. Deadlocks: Lock held during fork\n\"\"\"\n\nprint(\"Q347: Multiprocessing pitfalls\")\nprint(\"  1. Always use: if __name__ == '__main__'\")\nprint(\"  2. Use Manager() for shared state\")\nprint(\"  3. Prefer 'spawn' over 'fork' for safety\")\nprint(\"  4. Keep data transfer minimal (use shared memory)\")\nprint(\"  5. Ensure all args/returns are picklable\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q347: Multiprocessing pitfalls\n  1. Always use: if __name__ == '__main__'\n  2. Use Manager() for shared state\n  3. Prefer 'spawn' over 'fork' for safety\n  4. Keep data transfer minimal (use shared memory)\n  5. Ensure all args/returns are picklable",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "21",
   "q": "Program 234: Fork safety",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nos.fork() copies the entire process, including locks, file descriptors,\nand thread state. This can cause:\n\n1. Deadlocks: If a lock was held by another thread during fork\n2. Corrupted state: Half-modified shared resources\n3. Resource leaks: Duplicated file descriptors\n\nSolutions:\n- Use 'spawn' start method: multiprocessing.set_start_method('spawn')\n- os.register_at_fork() handlers\n- Don't fork after creating threads\n- Close connections before forking\n\"\"\"\nimport multiprocessing\n\nprint(\"Q348: Fork safety\")\nprint(f\"  Current start method: {multiprocessing.get_start_method()}\")\nprint(\"  Best practice: multiprocessing.set_start_method('spawn')\")\nprint(\"  'spawn' creates fresh process (no fork issues)\")\nprint(\"  'fork' copies parent process (fast but risky)\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q348: Fork safety\n  Current start method: spawn\n  Best practice: multiprocessing.set_start_method('spawn')\n  'spawn' creates fresh process (no fork issues)\n  'fork' copies parent process (fast but risky)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "22",
   "q": "Program 235: Async performance tuning",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nTips for optimizing asyncio performance:\n\n1. Use uvloop as event loop (2-4x faster than default)\n2. Minimize await points in hot paths\n3. Use connection pooling (aiohttp, asyncpg)\n4. Batch operations (gather multiple small tasks)\n5. Avoid blocking calls (use asyncio.to_thread())\n6. Profile with py-spy or yappi\n7. Set appropriate concurrency limits (semaphores)\n8. Use asyncio.Queue for backpressure\n\"\"\"\n\nprint(\"Q349: Async performance tuning\")\nprint(\"  # Use uvloop for speed:\")\nprint(\"  import uvloop\")\nprint(\"  asyncio.set_event_loop_policy(uvloop.EventLoopPolicy())\")\nprint(\"  # Or: uvloop.install()\")\nprint(\"  # Batch operations:\")\nprint(\"  results = await asyncio.gather(*[fetch(url) for url in urls])\")\nprint(\"  # Limit concurrency:\")\nprint(\"  sem = asyncio.Semaphore(100)\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q349: Async performance tuning\n  # Use uvloop for speed:\n  import uvloop\n  asyncio.set_event_loop_policy(uvloop.EventLoopPolicy())\n  # Or: uvloop.install()\n  # Batch operations:\n  results = await asyncio.gather(*[fetch(url) for url in urls])\n  # Limit concurrency:\n  sem = asyncio.Semaphore(100)",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "23",
   "q": "Program 236: Concurrency best practices",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nPython concurrency best practices:\n\n1. Choose the right model:\n   - asyncio for I/O-bound (preferred for new code)\n   - threading for I/O-bound with sync libraries\n   - multiprocessing for CPU-bound\n\n2. Avoid shared mutable state when possible\n3. Use thread-safe data structures (queue.Queue)\n4. Prefer higher-level APIs (Executor, asyncio.gather)\n5. Always handle cleanup (context managers)\n6. Use timeouts everywhere\n7. Log thread/task names for debugging\n8. Test concurrent code thoroughly\n9. Use structured concurrency (TaskGroup)\n10. Monitor and instrument production systems\n\"\"\"\n\nprint(\"Q350: Concurrency best practices\")\nprint(\"  1. I/O-bound → asyncio or threading\")\nprint(\"  2. CPU-bound → multiprocessing\")\nprint(\"  3. Avoid shared mutable state\")\nprint(\"  4. Use timeouts on all I/O operations\")\nprint(\"  5. Handle cancellation and cleanup\")\nprint(\"  6. Use structured concurrency (TaskGroup)\")\nprint(\"  7. Monitor with metrics and tracing\")\nprint(\"  8. Test with race condition detectors\")\nprint()\n\nprint(\"=\" * 70)\nprint(\"FILE 6 COMPLETE: Questions 301-350 (Concurrency & AsyncIO continued)\")\nprint(\"=\" * 70)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q350: Concurrency best practices\n  1. I/O-bound → asyncio or threading\n  2. CPU-bound → multiprocessing\n  3. Avoid shared mutable state\n  4. Use timeouts on all I/O operations\n  5. Handle cancellation and cleanup\n  6. Use structured concurrency (TaskGroup)\n  7. Monitor with metrics and tracing\n  8. Test with race condition detectors\n\n======================================================================\nFILE 6 COMPLETE: Questions 301-350 (Concurrency & AsyncIO continued)\n======================================================================",
     "numbered": false
    }
   ]
  }
 ],
 "takeaways": [],
 "_drills": 23,
 "_code": 46
});
