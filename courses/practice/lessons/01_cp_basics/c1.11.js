/* ============================================================================
   PRACTICE C1.11 — Programs · 11
   ----------------------------------------------------------------------------
   Imported from the Coding Practice source set and rendered through the
   Tutorial Hub design system. Generated by .build/import-practice.py —
   edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "c1.11",
 "lede": "**20 problems** from the basics and data types set. Each answer is hidden until you ask for it — attempt it first, because reading a solution feels like learning and is not.",
 "objectives": [
  "Work through 20 problems on programs without looking anything up",
  "Predict each answer before revealing it",
  "Recognise the pattern each problem belongs to",
  "Explain your solution out loud, including its complexity"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer before you reveal.** Even a wrong attempt makes the answer stick; reading first does not.",
      "**Say the complexity out loud** where the problem has one. Getting it wrong is the useful part.",
      "Use the bar above to open or close everything at once, and **Ctrl K** to jump to another set."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "Program 150: Find the longest word in a sentence",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nSplit by spaces and find the word with maximum length.\nTime Complexity: O(n)\n\"\"\"\n\n\ndef longest_word(sentence):\n    \"\"\"Find the longest word in a sentence.\"\"\"\n    words = sentence.split()\n    if not words:\n        return \"\"\n    return max(words, key=len)\n\n\n# Examples\nprint(\"Q28: Longest Word\")\nprint(longest_word(\"The quick brown fox jumped over the lazy dog\"))  # jumped\nprint(longest_word(\"I love programming\"))  # programming\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q28: Longest Word\njumped\nprogramming",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "2",
   "q": "Program 151: Check whether two strings differ by exactly one character",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nTwo strings differ by exactly one character if they have the same length\nand differ at exactly one position.\nTime Complexity: O(n)\n\"\"\"\n\n\ndef differ_by_one(s1, s2):\n    \"\"\"Check if strings differ at exactly one position.\"\"\"\n    if len(s1) != len(s2):\n        return False\n    diff_count = 0\n    for c1, c2 in zip(s1, s2):\n        if c1 != c2:\n            diff_count += 1\n            if diff_count > 1:\n                return False\n    return diff_count == 1\n\n\n# Examples\nprint(\"Q29: Differ By One Character\")\nprint(differ_by_one(\"abc\", \"adc\"))  # True\nprint(differ_by_one(\"abc\", \"abc\"))  # False (same)\nprint(differ_by_one(\"abc\", \"axx\"))  # False (differ by 2)\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q29: Differ By One Character\nTrue\nFalse\nFalse",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "3",
   "q": "Program 152: Replace spaces with %20",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nURL encoding of spaces. Similar to what encodeURI does.\nTime Complexity: O(n)\nSpace Complexity: O(n)\n\"\"\"\n\n\ndef replace_spaces(s):\n    \"\"\"Replace all spaces with %20.\"\"\"\n    result = []\n    for char in s:\n        if char == ' ':\n            result.append('%20')\n        else:\n            result.append(char)\n    return \"\".join(result)\n\n\ndef replace_spaces_pythonic(s):\n    \"\"\"Using replace method.\"\"\"\n    return s.replace(' ', '%20')\n\n\n# Examples\nprint(\"Q30: Replace Spaces with %20\")\nprint(replace_spaces(\"Hello World Python\"))  # Hello%20World%20Python\nprint(replace_spaces(\"Mr John Smith\"))  # Mr%20John%20Smith\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q30: Replace Spaces with %20\nHello%20World%20Python\nMr%20John%20Smith",
     "numbered": false
    }
   ]
  },
  {
   "t": "h2",
   "n": "",
   "text": "Programs"
  },
  {
   "t": "drill",
   "n": "4",
   "q": "Program 153: Dicts Keep Insertion Order",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "d = {}\nfor key in [\"z\", \"a\", \"m\"]:\n    d[key] = key.upper()\nprint(list(d))                    # Output: ['z', 'a', 'm']\n\nd[\"a\"] = \"changed\"                # updating does NOT move it\nprint(list(d))                    # Output: ['z', 'a', 'm']\n\ndel d[\"a\"]\nd[\"a\"] = \"re-added\"               # deleting then re-adding DOES\nprint(list(d))                    # Output: ['z', 'm', 'a']\n\n# Guaranteed since 3.7. Ordering is by first insertion of each key.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "5",
   "q": "Program 154: Merging Dicts Three Ways",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "a = {\"x\": 1, \"y\": 2}\nb = {\"y\": 20, \"z\": 3}\n\nprint({**a, **b})            # Output: {'x': 1, 'y': 20, 'z': 3}\nprint(a | b)                 # Output: {'x': 1, 'y': 20, 'z': 3}\nprint(b | a)                 # Output: {'y': 2, 'z': 3, 'x': 1}\n\nmerged = dict(a)\nmerged.update(b)\nprint(merged)                # Output: {'x': 1, 'y': 20, 'z': 3}\nprint(a)                     # Output: {'x': 1, 'y': 2}\n\n# The RIGHT-hand dict wins on conflicts. | leaves both originals alone;\n# .update() mutates the receiver.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "6",
   "q": "Program 155: Dict Keys Must Be Hashable",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "d = {}\nd[(1, 2)] = \"tuple works\"\nd[frozenset([1, 2])] = \"frozenset works\"\nprint(len(d))                    # Output: 2\n\ntry:\n    d[[1, 2]] = \"list fails\"\nexcept TypeError as e:\n    print(\"unhashable:\", \"unhashable\" in str(e))     # Output: unhashable: True\n\nprint(hash(1) == hash(1.0) == hash(True))            # Output: True\nd2 = {1: \"int\", 1.0: \"float\", True: \"bool\"}\nprint(d2)                                            # Output: {1: 'bool'}\n\n# 1, 1.0 and True hash equal AND compare equal, so they are the same key. The\n# first insertion keeps the key object; later ones only replace the value.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "7",
   "q": "Program 156: Sets Are Unordered and Deduplicating",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "s = {3, 1, 2, 3, 1}\nprint(sorted(s))              # Output: [1, 2, 3]\nprint(len(s))                 # Output: 3\n\na = {1, 2, 3, 4}\nb = {3, 4, 5}\nprint(sorted(a | b))          # Output: [1, 2, 3, 4, 5]\nprint(sorted(a & b))          # Output: [3, 4]\nprint(sorted(a - b))          # Output: [1, 2]\nprint(sorted(a ^ b))          # Output: [1, 2, 5]\nprint(a.isdisjoint({9}))      # Output: True\nprint({1, 2} <= a)            # Output: True",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "8",
   "q": "Program 157: Membership Testing: list Versus set",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "import time\n\ndata_list = list(range(50_000))\ndata_set = set(data_list)\ntarget = 49_999\n\nstart = time.perf_counter()\nfor _ in range(200):\n    target in data_list\nlist_time = time.perf_counter() - start\n\nstart = time.perf_counter()\nfor _ in range(200):\n    target in data_set\nset_time = time.perf_counter() - start\n\nprint(\"set is far faster:\", set_time < list_time / 10)\n# Output: set is far faster: True\n\n# \"in\" on a list is O(n) - it walks every element. On a set or dict it is O(1).",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "9",
   "q": "Program 158: Counter Does the Counting for You",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from collections import Counter\n\nwords = \"the cat sat on the mat the end\".split()\nc = Counter(words)\nprint(c[\"the\"])                       # Output: 3\nprint(c[\"missing\"])                   # Output: 0\nprint(c.most_common(2))               # Output: [('the', 3), ('cat', 1)]\n\nprint(sorted((Counter(\"aab\") + Counter(\"abc\")).items()))\n# Output: [('a', 3), ('b', 2), ('c', 1)]\nprint(sorted((Counter(\"aaab\") - Counter(\"ab\")).items()))\n# Output: [('a', 2)]\n\n# A missing key gives 0 instead of raising, and subtraction drops counts\n# that reach zero or below.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "10",
   "q": "Program 159: defaultdict Versus setdefault",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from collections import defaultdict\n\npairs = [(\"fruit\", \"apple\"), (\"veg\", \"leek\"), (\"fruit\", \"pear\")]\n\ngrouped = defaultdict(list)\nfor key, value in pairs:\n    grouped[key].append(value)\nprint(dict(grouped))          # Output: {'fruit': ['apple', 'pear'], 'veg': ['leek']}\n\nmanual = {}\nfor key, value in pairs:\n    manual.setdefault(key, []).append(value)\nprint(manual)                 # Output: {'fruit': ['apple', 'pear'], 'veg': ['leek']}\n\nprint(grouped[\"new\"])         # Output: []\nprint(\"new\" in grouped)       # Output: True\n\n# Reading a missing key from a defaultdict CREATES it. setdefault does not\n# have that side effect on plain reads.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "11",
   "q": "Program 160: deque: Fast at Both Ends",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from collections import deque\nimport time\n\nd = deque([1, 2, 3])\nd.appendleft(0)\nd.append(4)\nprint(list(d))                # Output: [0, 1, 2, 3, 4]\nprint(d.popleft(), d.pop())   # Output: 0 4\n\nd.rotate(1)\nprint(list(d))                # Output: [3, 1, 2]\n\nwindow = deque(maxlen=3)\nfor i in range(6):\n    window.append(i)\nprint(list(window))           # Output: [3, 4, 5]\n\n# list.pop(0) is O(n) because everything shifts; deque.popleft() is O(1).",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "12",
   "q": "Program 161: namedtuple: a Tuple With Names",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from collections import namedtuple\n\nPoint = namedtuple(\"Point\", [\"x\", \"y\"])\np = Point(3, 4)\n\nprint(p.x, p[0])                 # Output: 3 3\nprint(p)                         # Output: Point(x=3, y=4)\nprint(p._asdict())               # Output: {'x': 3, 'y': 4}\nprint(p._replace(y=10))          # Output: Point(x=3, y=10)\nprint(p == (3, 4))               # Output: True\n\nx, y = p\nprint(x + y)                     # Output: 7\n\n# It IS a tuple: immutable, unpackable, and comparable to plain tuples.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "13",
   "q": "Program 162: Nested Structures and the Shared-Reference Trap",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "grid = [[0] * 3] * 3          # THREE references to ONE row\ngrid[0][0] = 1\nprint(grid)                   # Output: [[1, 0, 0], [1, 0, 0], [1, 0, 0]]\n\ngood = [[0] * 3 for _ in range(3)]     # a new row each iteration\ngood[0][0] = 1\nprint(good)                   # Output: [[1, 0, 0], [0, 0, 0], [0, 0, 0]]\n\nprint(grid[0] is grid[1])     # Output: True\nprint(good[0] is good[1])     # Output: False",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "14",
   "q": "Program 163: Sorting Records by Several Keys",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "people = [\n    {\"name\": \"ada\", \"dept\": \"eng\", \"age\": 36},\n    {\"name\": \"bob\", \"dept\": \"ops\", \"age\": 25},\n    {\"name\": \"cal\", \"dept\": \"eng\", \"age\": 25},\n]\n\nprint([p[\"name\"] for p in sorted(people, key=lambda p: (p[\"dept\"], p[\"age\"]))])\n# Output: ['cal', 'ada', 'bob']\n\nprint([p[\"name\"] for p in sorted(people, key=lambda p: (p[\"dept\"], -p[\"age\"]))])\n# Output: ['ada', 'cal', 'bob']\n\nfrom operator import itemgetter\nprint([p[\"name\"] for p in sorted(people, key=itemgetter(\"age\", \"name\"))])\n# Output: ['bob', 'cal', 'ada']\n\n# Python's sort is STABLE, so you can also sort by the minor key first and the\n# major key second and get the same answer.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "15",
   "q": "Program 164: Grouping with itertools.groupby",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from itertools import groupby\n\nrows = [(\"eng\", \"ada\"), (\"eng\", \"cal\"), (\"ops\", \"bob\"), (\"eng\", \"dan\")]\n\n# groupby only groups ADJACENT equal keys - so sort first\nrows.sort(key=lambda r: r[0])\nfor key, group in groupby(rows, key=lambda r: r[0]):\n    print(key, [name for _, name in group])\n# Output: eng ['ada', 'cal', 'dan']\n# Output: ops ['bob']\n\nunsorted = [(\"a\", 1), (\"b\", 2), (\"a\", 3)]\nprint([(k, len(list(g))) for k, g in groupby(unsorted, key=lambda r: r[0])])\n# Output: [('a', 1), ('b', 1), ('a', 1)]\n\n# Forgetting to sort gives three groups instead of two, silently.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "16",
   "q": "Program 165: zip Stops at the Shortest",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "a = [1, 2, 3]\nb = \"xy\"\n\nprint(list(zip(a, b)))                 # Output: [(1, 'x'), (2, 'y')]\n\nfrom itertools import zip_longest\nprint(list(zip_longest(a, b, fillvalue=\"-\")))\n# Output: [(1, 'x'), (2, 'y'), (3, '-')]\n\ntry:\n    list(zip(a, b, strict=True))\nexcept ValueError as e:\n    print(\"strict caught the mismatch\")     # Output: strict caught the mismatch\n\npairs = [(1, \"a\"), (2, \"b\")]\nnums, letters = zip(*pairs)\nprint(nums, letters)                   # Output: (1, 2) ('a', 'b')",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "17",
   "q": "Program 166: Flattening and Chaining",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from itertools import chain\n\nnested = [[1, 2], [3], [], [4, 5]]\nprint(list(chain.from_iterable(nested)))     # Output: [1, 2, 3, 4, 5]\nprint([x for sub in nested for x in sub])    # Output: [1, 2, 3, 4, 5]\nprint(sum(nested, []))                       # Output: [1, 2, 3, 4, 5]\n\ndeep = [1, [2, [3, [4]]]]\ndef flatten(items):\n    for item in items:\n        if isinstance(item, list):\n            yield from flatten(item)\n        else:\n            yield item\nprint(list(flatten(deep)))                   # Output: [1, 2, 3, 4]\n\n# sum(nested, []) is quadratic - it builds a new list per step. Fine for four\n# items, wrong for four thousand.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "18",
   "q": "Program 167: Removing Items While Iterating",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "nums = [1, 2, 3, 4, 5, 6]\n\nwrong = nums[:]\nfor n in wrong:\n    if n % 2 == 0:\n        wrong.remove(n)          # mutating the list being iterated\nprint(wrong)                     # Output: [1, 3, 5]\n\nskipped = [1, 2, 2, 3]\nbad = skipped[:]\nfor n in bad:\n    if n == 2:\n        bad.remove(n)\nprint(bad)                       # Output: [1, 2, 3]\n\nright = [n for n in nums if n % 2]\nprint(right)                     # Output: [1, 3, 5]\n\n# The second case shows the real danger: the index advances past an element\n# that shifted down, so one of the 2s survives.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "19",
   "q": "Program 168: Dict and Set Comprehensions",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "words = [\"apple\", \"banana\", \"fig\", \"kiwi\"]\n\nprint({w: len(w) for w in words})\n# Output: {'apple': 5, 'banana': 6, 'fig': 3, 'kiwi': 4}\nprint({len(w) for w in words})\n# Output: {3, 4, 5, 6}\nprint({w: len(w) for w in words if len(w) > 3})\n# Output: {'apple': 5, 'banana': 6, 'kiwi': 4}\n\noriginal = {\"a\": 1, \"b\": 2}\nprint({v: k for k, v in original.items()})     # Output: {1: 'a', 2: 'b'}\n\nprint({w[0]: w for w in words})\n# Output: {'a': 'apple', 'b': 'banana', 'f': 'fig', 'k': 'kiwi'}\n\n# Duplicate keys keep the LAST value, quietly.",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "20",
   "q": "Program 169: Views Are Live, Copies Are Not",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "d = {\"a\": 1, \"b\": 2}\nkeys = d.keys()\nsnapshot = list(d.keys())\n\nd[\"c\"] = 3\nprint(sorted(keys))          # Output: ['a', 'b', 'c']\nprint(snapshot)              # Output: ['a', 'b']\n\nprint(d.keys() & {\"a\", \"z\"}) # Output: {'a'}\n\ntry:\n    for k in d.keys():\n        d[\"d\"] = 4\nexcept RuntimeError as e:\n    print(\"changed size during iteration\")\n    # Output: changed size during iteration",
     "numbered": false
    }
   ]
  }
 ],
 "takeaways": [],
 "_drills": 20,
 "_code": 23
});
