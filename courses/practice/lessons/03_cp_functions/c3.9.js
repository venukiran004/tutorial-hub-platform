/* ============================================================================
   PRACTICE C3.9 — Predict the Output — Scenario Questions · 4
   ----------------------------------------------------------------------------
   Imported from the Coding Practice source set and rendered through the
   Tutorial Hub design system. Generated by .build/import-practice.py —
   edit the importer, not this file.
   ========================================================================= */
EC.receiveLesson({
 "id": "c3.9",
 "lede": "**18 problems** from the functions and functional set. Each answer is hidden until you ask for it — attempt it first, because reading a solution feels like learning and is not.",
 "objectives": [
  "Work through 18 problems on predict the output — scenario questions without looking anything up",
  "Predict each answer before revealing it",
  "Recognise the pattern each problem belongs to",
  "Explain your solution out loud, including its complexity"
 ],
 "prerequisites": [],
 "blocks": [
  {
   "t": "callout",
   "kind": "note",
   "title": "How to use this set",
   "body": [
    {
     "t": "ul",
     "items": [
      "**Answer before you reveal.** Even a wrong attempt makes the answer stick; reading first does not.",
      "**Say the complexity out loud** where the problem has one. Getting it wrong is the useful part.",
      "Use the bar above to open or close everything at once, and **Ctrl K** to jump to another set."
     ]
    }
   ]
  },
  {
   "t": "drill",
   "n": "1",
   "q": "Q91. A lambda — what is the output?",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "data = [('Alice', 85), ('Bob', 92), ('Charlie', 78)]\nresult = dict(map(lambda x: (x[0], 'Pass' if x[1] >= 80 else 'Fail'), data))\nprint(result)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `{'Alice': 'Pass', 'Bob': 'Pass', 'Charlie': 'Fail'}` **Explanation:** Map transforms tuples, dict constructor builds the dictionary."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "2",
   "q": "Q92. A lambda — what is the output?",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "nums = [1, 2, 3, 4, 5]\ng = map(lambda x: x ** 2, nums)\nprint(type(g))\nprint(list(g))\nprint(list(g))  # second time",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `<class 'map'>`, `[1, 4, 9, 16, 25]`, `[]` **Explanation:** `map()` returns an iterator — exhausted after first `list()` call."
    }
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "3",
   "q": "Q93. An itertools recipe — what is the output?",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from itertools import takewhile, dropwhile\nnums = [1, 3, 5, 2, 4, 6]\ntaken = list(takewhile(lambda x: x < 5, nums))\ndropped = list(dropwhile(lambda x: x < 5, nums))\nprint(taken)\nprint(dropped)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `[1, 3]`, `[5, 2, 4, 6]` **Explanation:** `takewhile` takes until condition fails. `dropwhile` drops until condition fails."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "4",
   "q": "Q94. map() and filter() — what is the output?",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "nums = [1, 2, 3, 4, 5]\nmapped = map(str, nums)\nresult = '-'.join(mapped)\nprint(result)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `1-2-3-4-5` **Explanation:** Convert to strings with `map(str, ...)`, then join."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "5",
   "q": "Q95. A lambda — what is the output?",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from functools import reduce\n\ndef factorial(n):\n    return reduce(lambda a, b: a * b, range(1, n+1), 1)\n\nprint(factorial(5))\nprint(factorial(0))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `120`, `1` **Explanation:** Reduce with initial value 1. For 0, `range(1, 1)` is empty, returns initial value."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "6",
   "q": "Q96. A lambda — what is the output?",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "sorter = lambda x: sorted(x, key=lambda item: item[-1])\ndata = ['banana', 'apple', 'cherry']\nprint(sorter(data))",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `['banana', 'apple', 'cherry']` **Explanation:** Sorting by last character: 'a', 'e', 'y'. banana < apple < cherry by last char."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "7",
   "q": "Q97. map() and filter() — what is the output?",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "nested = [[1, 2, 3], [4, 5], [6]]\nlengths = list(map(len, nested))\nprint(lengths)\ntotal = sum(map(len, nested))\nprint(total)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `[3, 2, 1]`, `6` **Explanation:** `map(len, ...)` gets length of each sublist. Sum gives total element count."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "8",
   "q": "Q98. A lambda — what is the output?",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from functools import reduce\nwords = ['Python', 'is', 'awesome']\nresult = reduce(lambda a, b: f\"{a} {b}\", words)\nprint(result)",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `Python is awesome` **Explanation:** Reduce joins words with spaces. Equivalent to `' '.join(words)`."
    }
   ],
   "terms": [
    "Answer",
    "Explanation"
   ],
   "kind": "predict"
  },
  {
   "t": "drill",
   "n": "9",
   "q": "Q99. A generator expression — what is the output?",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "from operator import itemgetter\ndata = [{'name': 'Alice', 'age': 30}, {'name': 'Bob', 'age': 25}, {'name': 'Charlie', 'age': 35}]\nby_age = sorted(data, key=itemgetter('age'))\nprint([d['name'] for d in by_age])",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Answer:** `['Bob', 'Alice', 'Charlie']` **Explanation:** `itemgetter('age')` is more efficient than `lambda x: x['age']` for sorting."
    },
    {
     "t": "hr"
    },
    {
     "t": "p",
     "text": "**From the runnable 650-program set.** These were the `file1`…`file13_questions_*.py` files; they now live here, grouped by topic. Every program below was executed and the **Output** shown under it is that run's real stdout — nothing is hand-written."
    }
   ],
   "terms": [
    "Answer",
    "Explanation",
    "From the runnable 650-program set",
    "Output"
   ],
   "kind": "predict"
  },
  {
   "t": "h2",
   "n": "",
   "text": "Rebuilding the Builtins"
  },
  {
   "t": "drill",
   "n": "10",
   "q": "Program 140: Build your own enumerate()",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nenumerate() returns tuples of (index, element) from an iterable.\nOur version should support a start parameter.\n\"\"\"\n\n\ndef my_enumerate(iterable, start=0):\n    \"\"\"Custom enumerate implementation.\"\"\"\n    index = start\n    for item in iterable:\n        yield (index, item)\n        index += 1\n\n\n# Examples\nprint(\"Q83: Custom enumerate()\")\nfor i, char in my_enumerate(\"abc\"):\n    print(f\"{i}: {char}\", end=\"  \")  # 0: a  1: b  2: c\nprint()\nfor i, val in my_enumerate([10, 20, 30], start=1):\n    print(f\"{i}: {val}\", end=\"  \")  # 1: 10  2: 20  3: 30\nprint(\"\\n\")",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q83: Custom enumerate()\n0: a  1: b  2: c  \n1: 10  2: 20  3: 30",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "11",
   "q": "Program 141: Build your own zip()",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nzip() combines multiple iterables element-wise, stopping at the shortest.\n\"\"\"\n\n\ndef my_zip(*iterables):\n    \"\"\"Custom zip implementation.\"\"\"\n    iterators = [iter(it) for it in iterables]\n    while True:\n        result = []\n        for it in iterators:\n            try:\n                result.append(next(it))\n            except StopIteration:\n                return\n        yield tuple(result)\n\n\ndef my_zip_longest(*iterables, fillvalue=None):\n    \"\"\"Custom zip_longest - continues until longest iterable is exhausted.\"\"\"\n    iterators = [iter(it) for it in iterables]\n    active = len(iterators)\n    while active:\n        result = []\n        active_check = active\n        for i, it in enumerate(iterators):\n            try:\n                result.append(next(it))\n            except StopIteration:\n                active_check -= 1\n                result.append(fillvalue)\n        if active_check == 0:\n            return\n        yield tuple(result)\n        active = active_check\n\n\n# Examples\nprint(\"Q84: Custom zip()\")\nprint(list(my_zip([1, 2, 3], ['a', 'b', 'c'])))  # [(1, 'a'), (2, 'b'), (3, 'c')]\nprint(list(my_zip([1, 2], ['a', 'b', 'c'])))  # [(1, 'a'), (2, 'b')]\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q84: Custom zip()\n[(1, 'a'), (2, 'b'), (3, 'c')]\n[(1, 'a'), (2, 'b')]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "12",
   "q": "Program 142: Build your own map()",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nmap() applies a function to each element of an iterable.\n\"\"\"\n\n\ndef my_map(func, *iterables):\n    \"\"\"Custom map implementation supporting multiple iterables.\"\"\"\n    iterators = [iter(it) for it in iterables]\n    while True:\n        try:\n            args = [next(it) for it in iterators]\n            yield func(*args)\n        except StopIteration:\n            return\n\n\n# Examples\nprint(\"Q85: Custom map()\")\nprint(list(my_map(lambda x: x ** 2, [1, 2, 3, 4])))  # [1, 4, 9, 16]\nprint(list(my_map(lambda x, y: x + y, [1, 2, 3], [10, 20, 30])))  # [11, 22, 33]\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q85: Custom map()\n[1, 4, 9, 16]\n[11, 22, 33]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "13",
   "q": "Program 143: Build your own filter()",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nfilter() returns elements for which a function returns True.\n\"\"\"\n\n\ndef my_filter(func, iterable):\n    \"\"\"Custom filter implementation.\"\"\"\n    for item in iterable:\n        if func is None:\n            if item:  # filter(None, ...) filters falsy values\n                yield item\n        elif func(item):\n            yield item\n\n\n# Examples\nprint(\"Q86: Custom filter()\")\nprint(list(my_filter(lambda x: x > 3, [1, 2, 3, 4, 5, 6])))  # [4, 5, 6]\nprint(list(my_filter(lambda x: x % 2 == 0, range(10))))  # [0, 2, 4, 6, 8]\nprint(list(my_filter(None, [0, 1, '', 'hello', None, 42])))  # [1, 'hello', 42]\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q86: Custom filter()\n[4, 5, 6]\n[0, 2, 4, 6, 8]\n[1, 'hello', 42]",
     "numbered": false
    }
   ]
  },
  {
   "t": "h2",
   "n": "",
   "text": "The Functional Toolkit"
  },
  {
   "t": "drill",
   "n": "14",
   "q": "Program 144: Partial functions",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nA partial function is created using functools.partial(). It allows you to\n\"freeze\" some portion of a function's arguments and/or keywords, producing a\nnew callable with a simplified signature. This is extremely useful when you\nhave a general-purpose function but frequently call it with certain arguments\nfixed.\n\nfunctools.partial(func, *args, **kwargs) returns a new partial object that,\nwhen called, behaves like func called with the positional arguments args and\nkeyword arguments kwargs. Any additional arguments supplied to the partial\nobject are appended to args, and any additional keyword arguments merge into\nkwargs (overriding if duplicated).\n\nPartial functions improve code readability by giving meaningful names to\nspecialized versions of generic functions. They are widely used in callback\nregistrations, GUI programming, and functional pipelines where you need to\nadapt function signatures.\n\"\"\"\nfrom functools import partial\n\ndef power(base, exponent):\n    \"\"\"Raise base to the given exponent.\"\"\"\n    return base ** exponent\n\n# Create specialized versions using partial\nsquare = partial(power, exponent=2)\ncube = partial(power, exponent=3)\n\ndef multiply(a, b, c):\n    return a * b * c\n\ndouble = partial(multiply, 2)       # a=2 frozen\ntriple_and_double = partial(multiply, 2, 3)  # a=2, b=3 frozen\n\nprint(\"Q251: Partial functions\")\nprint(f\"  square(5)            = {square(5)}\")\nprint(f\"  cube(3)              = {cube(3)}\")\nprint(f\"  double(3, 4)         = {double(3, 4)}\")       # 2*3*4\nprint(f\"  triple_and_double(5) = {triple_and_double(5)}\")  # 2*3*5\nprint(f\"  square.func          = {square.func.__name__}\")\nprint(f\"  square.keywords      = {square.keywords}\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q251: Partial functions\n  square(5)            = 25\n  cube(3)              = 27\n  double(3, 4)         = 24\n  triple_and_double(5) = 30\n  square.func          = power\n  square.keywords      = {'exponent': 2}",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "15",
   "q": "Program 145: Higher-order functions",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nA higher-order function is a function that does at least one of the following:\n  1. Takes one or more functions as arguments.\n  2. Returns a function as its result.\n\nPython supports higher-order functions natively because functions are first-\nclass objects — they can be assigned to variables, stored in data structures,\npassed as arguments, and returned from other functions.\n\nBuilt-in higher-order functions include map(), filter(), sorted() (with key),\nand functools.reduce(). Writing your own higher-order functions is a powerful\ntechnique for abstraction. It allows you to parameterize behavior, implement\nstrategy patterns, and build flexible APIs.\n\nHigher-order functions are a cornerstone of functional programming, enabling\ncomposition, currying, and declarative coding styles.\n\"\"\"\n\ndef apply_operation(func, x, y):\n    \"\"\"Higher-order function: takes a function and applies it.\"\"\"\n    return func(x, y)\n\ndef add(a, b):\n    return a + b\n\ndef subtract(a, b):\n    return a - b\n\ndef make_multiplier(factor):\n    \"\"\"Higher-order function: returns a function.\"\"\"\n    def multiplier(x):\n        return x * factor\n    return multiplier\n\ntimes_five = make_multiplier(5)\n\n# Using built-in higher-order functions\nnumbers = [1, 2, 3, 4, 5, 6]\nevens = list(filter(lambda x: x % 2 == 0, numbers))\nsquared = list(map(lambda x: x ** 2, numbers))\n\nprint(\"Q252: Higher-order functions\")\nprint(f\"  apply_operation(add, 10, 3)      = {apply_operation(add, 10, 3)}\")\nprint(f\"  apply_operation(subtract, 10, 3) = {apply_operation(subtract, 10, 3)}\")\nprint(f\"  times_five(7)                    = {times_five(7)}\")\nprint(f\"  filter evens from {numbers}      = {evens}\")\nprint(f\"  map square on {numbers}          = {squared}\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q252: Higher-order functions\n  apply_operation(add, 10, 3)      = 13\n  apply_operation(subtract, 10, 3) = 7\n  times_five(7)                    = 35\n  filter evens from [1, 2, 3, 4, 5, 6]      = [2, 4, 6]\n  map square on [1, 2, 3, 4, 5, 6]          = [1, 4, 9, 16, 25, 36]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "16",
   "q": "Program 146: Reduce",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nfunctools.reduce(function, iterable[, initializer]) applies a two-argument\nfunction cumulatively to the items of an iterable, from left to right, to\nreduce the sequence to a single value.\n\nFor example, reduce(f, [a, b, c, d]) computes f(f(f(a, b), c), d). If an\ninitializer is provided, it is placed before the items of the iterable in\nthe reduction, serving as a default when the iterable is empty.\n\nreduce() was a built-in in Python 2 but was moved to functools in Python 3\nbecause Guido van Rossum argued that explicit loops are often clearer. Still,\nreduce() is invaluable for aggregation tasks like computing products,\nflattening lists, or composing functions.\n\nCommon pitfall: reduce on an empty iterable without an initializer raises a\nTypeError. Always provide an initializer for safety when the iterable might\nbe empty.\n\"\"\"\nfrom functools import reduce\n\nnumbers = [1, 2, 3, 4, 5]\n\n# Sum using reduce\ntotal = reduce(lambda acc, x: acc + x, numbers)\n\n# Product using reduce\nproduct = reduce(lambda acc, x: acc * x, numbers)\n\n# Flatten nested lists\nnested = [[1, 2], [3, 4], [5, 6]]\nflat = reduce(lambda acc, lst: acc + lst, nested, [])\n\n# Find maximum using reduce\nmaximum = reduce(lambda a, b: a if a > b else b, numbers)\n\n# String concatenation\nwords = [\"Hello\", \" \", \"World\", \"!\"]\nsentence = reduce(lambda acc, w: acc + w, words)\n\nprint(\"Q253: Reduce\")\nprint(f\"  Sum of {numbers}     = {total}\")\nprint(f\"  Product of {numbers} = {product}\")\nprint(f\"  Flatten {nested}     = {flat}\")\nprint(f\"  Max of {numbers}     = {maximum}\")\nprint(f\"  Concatenate {words}  = '{sentence}'\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q253: Reduce\n  Sum of [1, 2, 3, 4, 5]     = 15\n  Product of [1, 2, 3, 4, 5] = 120\n  Flatten [[1, 2], [3, 4], [5, 6]]     = [1, 2, 3, 4, 5, 6]\n  Max of [1, 2, 3, 4, 5]     = 5\n  Concatenate ['Hello', ' ', 'World', '!']  = 'Hello World!'",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "17",
   "q": "Program 147: Functional programming",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nFunctional programming (FP) is a paradigm that treats computation as the\nevaluation of mathematical functions and avoids changing state and mutable\ndata. Python is multi-paradigm but supports many FP concepts:\n\nKey FP principles in Python:\n  1. Pure functions — same input always gives same output, no side effects.\n  2. Immutability — prefer tuples and frozensets over lists and sets.\n  3. First-class functions — functions as values.\n  4. Higher-order functions — map, filter, reduce, sorted with key.\n  5. Closures and lambdas — anonymous and nested functions.\n  6. Comprehensions — declarative transformations.\n  7. itertools and functools — FP utility libraries.\n\nWhile Python doesn't enforce FP, adopting its principles leads to code that\nis easier to test, debug, and reason about. The key is to minimize side\neffects and prefer expressions over statements where practical.\n\"\"\"\n\nfrom functools import reduce\nfrom itertools import chain, starmap\n\n# Pure function — no side effects\ndef pure_add(a, b):\n    return a + b\n\n# Function composition\ndef compose(*funcs):\n    \"\"\"Compose functions right to left: compose(f, g)(x) == f(g(x)).\"\"\"\n    return reduce(lambda f, g: lambda *a, **kw: f(g(*a, **kw)), funcs)\n\ndouble_fn = lambda x: x * 2\nincrement = lambda x: x + 1\n\ndouble_then_increment = compose(increment, double_fn)\nincrement_then_double = compose(double_fn, increment)\n\n# Pipeline using map and filter (declarative style)\ndata = range(1, 11)\nresult = list(map(lambda x: x ** 2, filter(lambda x: x % 2 == 0, data)))\n\nprint(\"Q254: Functional programming\")\nprint(f\"  pure_add(3, 4)               = {pure_add(3, 4)}\")\nprint(f\"  double_then_increment(5)     = {double_then_increment(5)}\")  # (5*2)+1=11\nprint(f\"  increment_then_double(5)     = {increment_then_double(5)}\")  # (5+1)*2=12\nprint(f\"  Squares of evens in 1..10    = {result}\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q254: Functional programming\n  pure_add(3, 4)               = 7\n  double_then_increment(5)     = 11\n  increment_then_double(5)     = 12\n  Squares of evens in 1..10    = [4, 16, 36, 64, 100]",
     "numbered": false
    }
   ]
  },
  {
   "t": "drill",
   "n": "18",
   "q": "Program 148: Lambda functions",
   "body": [
    {
     "t": "code",
     "lang": "python",
     "code": "\"\"\"\nA lambda function is a small anonymous function defined with the 'lambda'\nkeyword. Its syntax is: lambda arguments: expression. The expression is\nevaluated and returned automatically — no 'return' statement is needed.\n\nLambda functions can take any number of arguments but can only contain a\nsingle expression. They cannot include statements (assignments, loops, etc.)\nor annotations. They are most useful as short throwaway functions passed to\nhigher-order functions like sorted(), map(), filter(), and reduce().\n\nLimitations:\n  - No multi-line logic or statements.\n  - No docstrings.\n  - Harder to debug (show as <lambda> in tracebacks).\n  - Overuse reduces readability.\n\nBest practice: use lambdas only for simple, single-expression callbacks.\nFor anything more complex, define a named function with def.\n\"\"\"\n\n# Basic lambda\nadd_lambda = lambda a, b: a + b\n\n# Lambda with default arguments\ngreet = lambda name, greeting=\"Hello\": f\"{greeting}, {name}!\"\n\n# Sorting with lambda key\nstudents = [(\"Alice\", 85), (\"Bob\", 92), (\"Charlie\", 78)]\nby_grade = sorted(students, key=lambda s: s[1], reverse=True)\n\n# Lambda in data transformation\npairs = [(1, 'one'), (2, 'two'), (3, 'three')]\nnumbers_only = list(map(lambda p: p[0], pairs))\n\n# Conditional lambda\nclassify = lambda x: \"even\" if x % 2 == 0 else \"odd\"\n\nprint(\"Q255: Lambda functions\")\nprint(f\"  add_lambda(3, 7)     = {add_lambda(3, 7)}\")\nprint(f\"  greet('Alice')       = {greet('Alice')}\")\nprint(f\"  greet('Bob', 'Hi')   = {greet('Bob', 'Hi')}\")\nprint(f\"  Sorted by grade desc = {by_grade}\")\nprint(f\"  Numbers only         = {numbers_only}\")\nprint(f\"  classify(4)          = {classify(4)}\")\nprint(f\"  classify(7)          = {classify(7)}\")\nprint()",
     "numbered": false
    },
    {
     "t": "p",
     "text": "**Output**"
    },
    {
     "t": "code",
     "lang": "text",
     "code": "Q255: Lambda functions\n  add_lambda(3, 7)     = 10\n  greet('Alice')       = Hello, Alice!\n  greet('Bob', 'Hi')   = Hi, Bob!\n  Sorted by grade desc = [('Bob', 92), ('Alice', 85), ('Charlie', 78)]\n  Numbers only         = [1, 2, 3]\n  classify(4)          = even\n  classify(7)          = odd",
     "numbered": false
    }
   ]
  }
 ],
 "takeaways": [],
 "_drills": 18,
 "_code": 27
});
